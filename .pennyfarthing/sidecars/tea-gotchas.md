## TEA Gotchas

<!-- migrated from Claude auto-memory store, 2026-06-24 -->

### WN kill-turn crash (158-48): the story's "A2 gated on in_combat" hypothesis is INCOMPLETE — the real root cause is the DORMANT [ENCOUNTER RESOLVED] zone; probe before pinning (2026-06-28, 158-48)
- **The story narrative blamed "the A2 de-nativized WN zone (gated on context.in_combat) doesn't fire on the victory turn."** A 30-min code probe disproved the mechanism and found the REAL one — documented in the code itself at `orchestrator.py` TurnContext `pending_resolution_signal` field (~line 909): the `[ENCOUNTER RESOLVED]` zone is **dormant in production**. `snapshot.pending_resolution_signal` IS stamped on a WN kill (`dispatch/dice.py` `_emit_player_beat_resolution_close`, player_victory — confirmed by a throwaway probe: `dispatch_dice_throw` on a solo heavy_metal kill stamps `outcome='player_victory'`), but `_build_turn_context` / `refresh_turn_context_post_dispatch` NEVER copy it into `TurnContext.pending_resolution_signal`. The only code that ever threaded it (the module-level `run_narration_turn` wrapper) was deleted in story 49-5; the 49-5 follow-up (re-thread + clear-after-consume) was never done. So the field is None EVERY turn → the zone (orchestrator.py:2229, fires on `pending_resolution_signal is not None`) never renders AND the start-a-confrontation menu (orchestrator.py:2190, gated on `... is None`, always true) DOES → narrator flails past max_turns. **Lesson: when a story states a root-cause mechanism, verify it against live code with a probe before writing tests against the stated seam — the symptom was right, the mechanism was wrong, and the actual fix is genre-agnostic (revive the dormant bridge), not WN-specific.**
- **Direct precedent = `tests/server/test_pacing_hint_turn_context_wiring.py` (story 81-3).** SAME bug shape: a TurnContext field declared + consumed but never populated by `_build_turn_context`. Mirror that file's harness EXACTLY for the dice-path seam: `_make_sd(loader, ...)` builds a real `_SessionData` with a loaded pack (caverns_and_claudes = WWN) + `MagicMock` repos; `_build_turn_context(sd)`; assert `ctx.<field>`; then `Orchestrator(client=FakeAnthropicSdkClient(responses=[]))` + `await build_narrator_prompt(...)` for the rendered-prompt wiring. **Use a NON-procedural world (`sunken_keep`, NOT `beneath_sunden`)** or `_project_current_region` logs a loud `dungeon.region_projection ... corrupt dungeon` ERROR against the mock dungeon_repo (same rationale as #1086's `test_nondungeon`).
- **TWO threading seams, pin BOTH.** The DICE path (the kill-turn scenario) builds the victory-turn context via `_build_turn_context` (dice_throw.py:485, AFTER `dispatch_dice_throw` already stamped the snapshot) — pinned in `test_resolution_signal_turn_context_wiring.py`. The free-nav narrator-beat path stamps DURING dispatch (post the pre-dispatch `_build_turn_context`), so `refresh_turn_context_post_dispatch` must ALSO thread it — pinned in `test_wwn_combat_denativization.py` (AC3 literally says "real refresh"; mirror the #1086 cold-seat `_FakeRefreshSessionData` tests). A refresh-only fix leaves the dice path (the story) broken.
- **The prompt assertions are non-vacuous — verified by probe.** With the signal threaded into the context, `build_narrator_prompt` yields `[ENCOUNTER RESOLVED]` + "Do NOT emit beat_selections" (narrator.py:345 resolution short-circuit) and DROPS "AVAILABLE ENCOUNTER TYPES" — the start-menu discriminator only works if the pack offers confrontations (caverns does: combat/chase/negotiation), so assert `ctx.available_confrontations` as a fixture-sanity precondition.
- **OTEL twin contract: op=`resolution_context_refreshed`, component="encounter"** (twin of #1086's `cold_seat_context_refreshed`). Capture via `monkeypatch.setattr("sidequest.telemetry.watcher_hub.publish_event", spy)` (the function imports it locally at call time). Assert it fires ONCE on a resolution turn and NOT on a no-signal turn (no false positive).
- **Two companions I did NOT test (flagged as Delivery Findings, not pinned):** (1) **one-shot clearing** — the `ResolutionSignal` docstring says "reads this slot on the next turn and clears it"; without clearing, the stale snapshot signal re-threads EVERY subsequent turn → the zone re-fires forever (violates AC4). I did NOT write a test because the seam is Dev's choice (49-5 says "clear at the session-handler call site after the orchestrator returns", NOT read-and-clear in `_build_turn_context`) — a "call _build_turn_context twice" test would wrongly dictate read-and-clear. BLOCKING finding; Dev owns the clearing test. (2) **`yield_side=None`** — `_build_resolution_signal` for player_victory derives `yield_side=None`, which flows through `**attrs` into `Span.open` → `start_as_current_span(attributes={"yield_side": None})` → OTEL's "Invalid type NoneType" warning (`Span.open`, span.py, sets attrs VERBATIM with no None filter). Benign warning, non-AC; flagged for Dev's glance (centralized fix = None-filter in `Span.open`).
- **The 158-41 graceful-degrade catch is a BACKSTOP, not this fix.** `AnthropicSdkLoopExceeded` is now caught on the free-nav path (websocket_session_handler.py:1078, ADR-006) so the socket no longer tears down — but the degraded path returns an error message, NOT the victory NARRATION, so the card is STILL lost. 158-48 is the ROOT-cause fix (give the narrator the right directive so it converges); don't conflate them.

### Dogfight force-dispatch RED (ADR-153 §7 / 158-29): the Plan-2 stubs ghost a model, and a FORCE-seat dispatch must clear the bank's 0.6 confidence gate or it seats nothing (2026-06-27, 158-29)
- **The Plan-2 doc (`2026-06-26-dogfight-rebuild-plan-2`) ships RED test code that does NOT match the live protocol.** It builds `DispatchPackage(per_player=[PerPlayerDispatch(player_id=..., dispatch=[])], cross_player=[])` — but **`PerPlayerDispatch` does not exist** (the class is `PlayerDispatch`, `protocol/dispatch.py:200`), `PlayerDispatch` REQUIRES `raw_action`, `DispatchPackage` REQUIRES `confidence_global`, and **`SubsystemDispatch` REQUIRES `idempotency_key` + `confidence`** (no defaults, `dispatch.py:106`). Pasting the stub → `ValidationError`/`ImportError` for the WRONG reason. Model the package on the live `tests/agents/subsystems/test_dogfight_dispatch_wiring.py` helpers (`_package_with` / `_dogfight_dispatch`), not the plan. (Nth instance of "a plan that ships complete RED test code still has harness bugs against the LIVE API" — see the 159-3 entry.)
- **THE non-obvious trap for Dev's GREEN: a force-injected dogfight dispatch must carry `confidence ≥ 0.6` or `run_dispatch_bank` degrades it to a narrator hint and SEATS NOTHING** (the existing `test_low_confidence_dogfight_degrades_and_does_not_engage` proves a 0.2 dispatch seats nothing). The §7 force-dispatch is a deliberate force-seat, so the injector must set a gate-clearing confidence (~1.0). A wiring test asserting a real SEAT fails silently-plausibly if the injected confidence is sub-threshold — flagged as a blocking Delivery Finding so Dev doesn't copy the plan's confidence-less snippet.
- **Driving the REAL pre-pass hermetically:** `execute_intent_router_pre_narrator_pass(intent_router=..., snapshot=, pack=, action=, player_name=)` takes the router as a REQUIRED kwarg — pass a `MagicMock()` whose `.decompose = AsyncMock(return_value=<package>)` (NOT a monkeypatch of a class method). For the 158-29 "router miss" the return is an EMPTY package (no confrontation dispatch); the injector (once wired ~`intent_router_pass.py:915`, after the `confrontation_verb_unrouted` block, before the unregistered-gate) force-dispatches. No LLM runs. `otel_capture` IS available in `tests/server/conftest.py:1111` (global-tracer install) — assert spans with `[s for s in otel_capture.get_finished_spans() if s.name == X]`, run `-n0` (OTEL span-count deadlock rule).
- **The over-fire gate is the real RED surface, not the happy path.** The dogfight def's `intent_verbs` include generic singles (`lock`/`gun`/`engage`); `_confrontation_verb_hits` is word-level + suffix-stripped (`tokenize`, `verb_tokens <= action_tokens`) so "guns"→"gun" but "lock the door"→1 generic hit. Test BOTH directions: strong (`intercept`/`pursue`/`missile`/`dogfight` OR ≥2 distinct) fires; a single generic verb on a mundane action must NOT phantom-seat. The fixture pack `swn_test_pack` carries the SAME `[dogfight, intercept, pursue, engage, lock, missile, gun]` as live space_opera, so the repro action "bring guns online, lock a firing solution" = 2 distinct hits works against the fixture.
- **Degrade-loud fixture:** a frameless dogfight (`make_frameless_dogfight_pack()` — nulls `opponent_default_stats` on the loaded pack; `ConfrontationDef` is `extra:forbid` but NOT frozen, so the mutation is clean on a freshly-loaded, unshared pack) cannot seat → `instantiate_encounter_from_trigger` raises `SealedLetterArityError` → `run_dogfight_dispatch` catches → `dogfight.dispatch.rejected`. This is the executable "never wedge the narrator even when it truly can't seat" guarantee. The GENERAL max_turns-degrade is 158-41, NOT this story.

### Dogfight RED (ADR-153 §6 firewall/seating): §6 supersedes the live 59-17 contract; an UNCAUGHT arity-raise leaks cross-test state under -n0 (2026-06-27, 158-31)
- **§6 is a real behavior inversion of currently-GREEN tests, not new ground.** `test_dogfight_instantiation_production_path.py` (Story 59-17) passes on develop: a co-located hostile pilot ("Vulture") IS seated via the sealed-letter *location fallback*, and a dogfight with no adversary REFUSES (encounter None). ADR-153 §6 deletes that: the dogfight Other comes from `opponent_default_stats` (default-from-frame) or a router-named contact, NEVER a co-located NPC — and since the def always carries a frame, a dogfight ALWAYS seats a frame-default ship rather than refusing. The location fallback seated BOTH the 59-17 pilot and the 158-34 ground creature via the SAME `_npc_fallback_at_location(... adversary_only=True)` path (encounter_lifecycle.py ~1719) — §6 removes it wholesale. So Plan-1's §6 seater change (a §6 story) breaks 3 dispatch tests that nominally belong to §7/Plan-2; updating them is unavoidable (Plan-1 can't be green otherwise). Keith ruled 2026-06-27: update them to the §6 contract + log a deviation. The explicit-`npcs_present` path is untouched (stays green).
- **VERIFY EACH DOGFIGHT TEST IN ISOLATION — the RED-phase combined run lies.** `test_dogfight_default_opponent` (empty snapshot → currently raises `SealedLetterArityError`, the RED state) leaks global state that makes a LATER `run_confrontation_dispatch` arity-reject a co-located opponent it would otherwise seat. Mechanism is the UNCAUGHT raise: a CAUGHT arity raise (existing `pytest.raises` test `test_dogfight_instantiation_rejects_zero_npcs`) does NOT leak; a clean return (`test_dogfight_seating_scale`, seats a creature) does NOT leak. So testing-runner's combined-suite summary CONFLATED failure reasons — it reported test 1 failing on `out.data=={}`/`arity_rejected` when, run alone, it actually fails on `assert blue.name != "Vulture"` (Vulture seated). Both are RED, but the message misattributes. The leak VANISHES post-green (Plan 1 makes the dogfight seat-from-frame, so no test hits the arity-raise). `instantiate_encounter_from_trigger` sets `snapshot.encounter = enc` BEFORE the arity gate (~line 1625 vs raise ~1833) — likely-related exception-path residue; flagged for Dev to confirm the serial (-n0) suite is clean after green.
- **Dogfight HP has TWO homes — pick per assertion.** Shot resolution + `check_hp_depletion` read frame HP from `per_actor_state["frame_hp"]` via `frame_hp_resolver(encounter)` (sidequest.game.dogfight_shot — NOT narration_apply as ADR-153 Plan 1's draft claimed). But the seater ALSO mints an `Npc` core from `opponent_default_stats` (find_creature_core). The Task-3 default-opponent test asserts the seated opponent's durability via `snapshot.find_creature_core(name).hp.max == 8` (the minted core); the Task-4 wiring test resolves shots against a `lambda n: cores.get(n)` CreatureCore resolver (mirrors `tests/game/test_resolve_dogfight_shots.py`). `check_hp_depletion` itself emits `encounter.resolved` with `source="hp_depletion"` and sets `enc.resolved`/`enc.outcome` — so the firewall OTEL assertion belongs on that span, and the wiring test PASSES today (regression lock), it is not RED. The live pack's dogfight-def shape is validator territory (Task 5 `validate_rules_in_pack`), never pytest (`feedback_no_content_in_unit_tests`).

### In-tree multi-package relocation → test files MUST be namespaced in a subdir, or pytest basename-collides (2026-06-26, 159-4)
- 159-4 (companion package core) was re-scoped by an owner ruling: the companion is NOT a standalone `../sidequest-companion` repo (Plan C's assumption, now VOID) — it's a SIBLING import package inside `sidequest-understudy`, alongside `src/seat_core` and `src/understudy`, all built into one wheel (`[tool.hatch.build.targets.wheel] packages = ["src/understudy", "src/seat_core"]` → Dev adds `"src/companion"`). Companion imports `seat_core` directly, never `understudy`; the "no test-harness dependency" constraint holds at the PACKAGE boundary, not the repo boundary.
- **The trap:** Plan C names test files `test_manifest.py`, `test_persona.py`, `test_actuation.py`. The understudy repo ALREADY has `tests/test_manifest.py`, `tests/test_persona.py`, `tests/test_actuation.py` (understudy's own modules). Dropping companion tests at `tests/` root → pytest package-import-mode basename collision (two `tests.test_manifest`), collection error for the WRONG reason. Fix: put them in `tests/companion/` with an empty `tests/companion/__init__.py` — the repo already does exactly this for `tests/seat_core/`. Distinct dotted paths (`tests.companion.test_manifest` vs `tests.test_manifest`) → no collision.
- **seat_core API matched Plan C verbatim** (verified against live source, no harness traps this time): `seat_core.core` exports `Message(role,content)` frozen dc, `FakeStructuredModel(script, default)`, `StructuredModel` Protocol with `async decide(system, transcript) -> DecideResult(value,input_tokens,output_tokens)`, `ModelError`; `seat_core.llm.factory.make_model(spec, output_model, *, default=None)` (the `"fake"` case REQUIRES `default`, returns `FakeStructuredModel([], default)`); `seat_core.persona.axis.{Role(StrEnum pet/peer/hireling), SeatAxes(narrative_vs_mechanical: float[0,1], verbosity/decisiveness/reading_tolerance: Literal["low","medium","high"], extra="forbid")}`. RED was clean: every failure was exactly `No module named 'companion'`.
- **Scope split:** 159-4 = Plan C Tasks 2–8 (intent, manifest, persona, dice, protocol, brain, actuation). 159-5 = Tasks 9–11 (run loop, ws_transport, CLI, full-loop wiring). 159-5's tests ALSO belong in `tests/companion/`, and the `companion = "companion.cli:app"` script entry goes in understudy's pyproject, not a new package's.
- **Added beyond Plan C (all green-able against the plan's impl):** manifest `safe_load`/python-tag rejection + malformed-YAML loud-fail (lang-review #8), brain non-CompanionIntent-value→YIELD branch (#1: net degrades but never fabricates), `extra="forbid"` rejection on intent, hireling-vs-pet bond divergence, mirror clears stale prompt on NARRATION_END, and `test_packaging.py` — the ruling-guard wiring test that scans `src/companion/*.py` for any `understudy` import and asserts `src/companion` is in the wheel packages.

### 159-5 RED (run loop + ws_transport + CLI): `websockets` is an UNDECLARED dep, and `seat_frame` is built-but-unused (2026-06-26, 159-5)
- **`websockets` is NOT in understudy's pyproject** (deps were `playwright/httpx/pydantic/typer/pyyaml/anthropic`). Task 10's `ws_transport.py` + `cli.py` both `import websockets`, so `test_ws_transport.py` and `test_cli.py` RED via `ModuleNotFoundError: No module named 'websockets'` — they fail on the TRANSITIVE dep, not the target module. Clean RED, but Dev's GREEN must add `websockets>=12` to `[project.dependencies]` AND `uv sync` BEFORE those files even collect. I added `test_cli_packaging.py` (pure `tomllib`, no import) so the dep+script requirement fails on a clean ASSERTION regardless of install state — the reliable RED signal for the packaging surface. The `companion = "companion.cli:app"` script entry is likewise still absent (only `understudy` is registered).
- **`websockets.ConnectionClosed(None, None)`** is the version-robust ctor for the close-path test on `websockets>=12` (recv → None on close; a non-close error must PROPAGATE — lang-review #1, don't broad-swallow). Can't run that test until Dev installs the dep; accept the ModuleNotFoundError as the RED until then.
- **OPEN DESIGN QUESTION — `seat_frame` (PLAYER_SEAT) is built but the planned run loop never sends it.** 159-4 shipped `protocol.seat_frame()`, and design-spec Section 3 lists a Seat handshake (PLAYER_SEAT → SEAT_CONFIRMED), but Plan C Task 9's loop goes connect → react-to-frames with NO explicit seat step (the mirror picks up `self_player_id` from `SESSION_EVENT{connected/ready}`). I did NOT write a test requiring PLAYER_SEAT (spec-authority: Plan C > spec) and flagged it as a Delivery Finding: Dev confirms server auto-seats on connect, else adds the seat step + a test — otherwise `seat_frame` is dead code (No Stubbing). Same caution for chargen: Plan C's `_chargen_choice` maps a non-ACT/empty decision to `"0"` (first option), bordering on a silent fallback — left untested deliberately, flagged for Reviewer.
- **The slow-brain test locks the BOUNDED decide.** The run loop must call `companion.brain.decide(brain, system, ctx, defn.decide_timeout_s)` (the `asyncio.wait_for` wrapper), NOT `brain.decide` directly. `test_run::test_slow_brain_yields_and_does_not_stall` (decide_timeout_s=0.05, a 10s Slow brain) fails if Dev skips the wrapper — that's the "never stall the table" guard made executable.
- **REWORK (round 1): the HIGH `connect_frame` game_slug bug + the all-fixtures ripple.** Reviewer rejected on a real defect 159-4 had foreshadowed: `connect_frame` put `defn.session_url` (a full ws:// URL) into the payload field `game_slug`, but the `/ws` route has NO path slug (`sidequest-server/app.py:322`) and the real client sends `game_slug` = a SHORT room slug (`sidequest-ui/src/App.tsx:2310`) → the companion can't join the human's room. Fix drove a NEW REQUIRED `CompanionDef.game_slug` field + `connect_frame` using it. **The ripple lesson:** making a model field REQUIRED breaks EVERY fixture that builds it — under `extra="forbid"`, even ADDING `game_slug=...` to the fixtures fails until the field lands, AND a YAML fixture that now carries `game_slug:` fails extra-forbid too. So update ALL six `CompanionDef` fixtures (kwargs: test_run/test_protocol/test_full_loop `_defn`/`_donut`; YAML: test_manifest `_VALID`, test_persona `_DEF`, test_cli `_DEF`) in the same RED commit → coherent "field missing everywhere" red (50 pass / 22 fail here), clean green when Dev adds it. The contract test must assert BOTH `game_slug == defn.game_slug` AND `!= defn.session_url` (proves the bug is fixed, not just that a field exists). Chose REQUIRED over optional+guard: a companion that can't name its room is meaningless, and fail-loud-at-load honors No-Silent-Fallbacks.

### A plan that ships complete RED test code still has harness bugs against the LIVE API — verify every ctor/import before pasting (2026-06-25, 159-3)
- Plan B (companion bond + perception seam) shipped full test code for all 5 tasks. Pasting it verbatim would have produced RED for the WRONG reasons in 4 of 5 files, and worse, tests that stay broken after Dev's GREEN. Three concrete traps, all caught by reading the real source first:
  - **`SessionRoom()` needs args.** The dataclass has two leading no-default fields `slug: str` and `mode: GameMode` (`session_room.py:147-148`), so `SessionRoom()` → `TypeError`, not the feature-missing ImportError you want. Correct construction (every existing test): `SessionRoom(slug="x", mode=GameMode.SOLO)`, `from sidequest.game.persistence import GameMode`.
  - **`GameStateView()` is un-instantiable.** `sidequest/game/projection/view.py` `GameStateView` is a `typing.Protocol` → `TypeError: Protocols cannot be instantiated`. The concrete impl is `SessionGameStateView` (a `@dataclass`, constructs no-arg). Projection tests pass `view=SessionGameStateView()`; the firewall only reads `view` for genre rules, which `ComposedFilter.with_no_genre_rules()` disables.
  - **`emitters._watcher_publish` is not a module attribute.** `emitters.py` imports `_watcher_publish` FUNCTION-LOCALLY (`from sidequest.server.session_handler import _watcher_publish`, lines 65/468) to dodge an emitters↔session_handler import cycle. So `monkeypatch.setattr("sidequest.server.emitters._watcher_publish", ...)` (the plan's target) raises AttributeError. `session_handler.py:51` re-exports it specifically "consumed by emitters.py." Robust capture: patch the alias on BOTH `sidequest.server.emitters` AND `sidequest.server.session_handler` with `raising=False` — covers either module-level or function-local resolution. (Contrast: `connect.py:69` DOES import it at module level, so the plan's `connect._watcher_publish` patch target is fine.)
- **publish_event is NOT an OTEL tracer span by default.** `watcher_hub.publish_event` only mints an OTLP span when `SIDEQUEST_WATCHER_AS_SPANS=1`; otherwise it's an in-process hub broadcast. So `InMemorySpanExporter` (the `test_projection_otel.py` pattern) will NOT capture companion `companion.bond_resolved`/`companion.routed_as_pet` events — and the hub `publish()` drops events when no event loop is bound, so a sync unit test can't subscribe either. For sync unit-level watcher assertions, the project convention is `monkeypatch.setattr(<consuming-module>, "_watcher_publish", capture)` — patch where USED.
- **The reusable move:** when a plan ships test code, treat it as a draft. Before pasting, grep/read the actual signature of every external symbol the test constructs or imports (`SessionRoom`, the view, the envelope, the payload's model_config, the monkeypatch target). The RED must fail ONLY on the missing feature symbol; any other error (TypeError on a ctor, AttributeError on a patch target, ValidationError from extra-forbid you didn't expect) is a harness bug that will also break GREEN. Capture the corrected-ctor/import facts as Delivery Findings so Dev doesn't re-introduce them.

### From-scratch-package RED: TEA scaffolds the harness, ModuleNotFoundError IS the red, commit the whole tree (2026-06-25, 159-1)
- An EXTRACTION/new-package story (159-1 created `sidequest-seat-core` from nothing) has no RED state until pytest can collect and run. There is no "source" to import-fail against yet. TEA must lay down the minimal harness so `uv run pytest` executes: `pyproject.toml`, empty package markers (`src/<pkg>/__init__.py` etc.), `tests/__init__.py`, `.gitignore` — but NO feature logic. The implementation modules (`src/<pkg>/*.py`) are Dev's GREEN. This crosses the usual "TEA writes no source" line only for infrastructure; log it as a `minor` deviation (spec source = plan's Task-1 scaffold) and state the harness boundary explicitly in the assessment so Dev doesn't re-create pyproject/markers.
- Keep the top-level `__init__.py` docstring-LESS if a smoke test asserts `pkg.__doc__` — that keeps Task-1's smoke red until Dev adds the docstring in GREEN. An empty marker file (or comment-only) has `__doc__ == None`.
- The canonical RED for not-yet-written modules is `ModuleNotFoundError` at COLLECTION — pytest prints "Interrupted: N errors during collection" and runs nothing else. The plan itself calls this the expected failure ("Step 2: Expected FAIL with ModuleNotFoundError"). To ALSO show an assertion-level red (e.g. the smoke test), run that one file in isolation (`uv run pytest tests/test_smoke.py`) since collection errors abort the full-suite run before any test executes.
- `uv sync` for `requires-python = ">=3.12"` resolves the NEWEST available interpreter (got 3.14 here, not 3.12). `StrEnum`/`match`/`X | None`/frozen dataclass all fine on 3.14. If a specific interpreter matters, pin `.python-version`; otherwise it's in-spec — flag as non-blocking, don't "fix."
- The `pf` branch-protection PreToolUse hook did NOT block a commit on the new repo's `main` — an unregistered repo (absent from `.pennyfarthing/repos.yaml`) isn't in the protected set, so `git commit` on its `main` succeeds. (Contrast: orchestrator `main` IS protected.) Commit the FULL red tree (harness + tests); `.venv`/`uv.lock` handled by `.gitignore` (commit `uv.lock`, ignore `.venv`). Per the dirty-tree gotcha below, leave `git status --short` empty after the RED commit.

### Green suite is meaningless against a dirty/untracked tree (2026-06-10, 102-7)
- A passing suite proves nothing if it ran against an uncommitted/untracked working tree. 102-7's Reviewer caught the same masking twice: B1 — a retirement test passed only because `magic.yaml` was deleted in the working tree but the deletion was never committed (still present at HEAD); B2 — a production module `sidequest/mutation/context_builder.py` imported unguarded in the live per-turn path passed because the file existed on disk but was never `git add`ed → `ModuleNotFoundError` on any clean checkout/CI/merge.
- Why: untracked files import fine and `D`-deletions vanish from disk locally, so pytest sees the intended state — a fresh checkout sees HEAD, which differs.
- How to apply: before claiming "passes on clean HEAD," gate on `git status --short` being EMPTY across changed paths (no `??`, no ` D`, no ` M`), THEN run. Definitive check: `git worktree add <tmp> HEAD`, confirm empty status, run with `PYTHONPATH=<worktree>` so imports resolve from the checkout (not the main tree's editable install), then `git worktree remove`. Also `git diff develop...HEAD --diff-filter=A` should list every NEW production module a feature needs — missing = never committed.

### Gate on the FULL suite with content, not a scoped subset (2026-05-27, from dogfight×SWN build)
- Subagents verifying with scoped subsets (`pytest tests/server/` or `tests/genre/`) mislabel real regressions as "pre-existing." `tests/server/` EXCLUDES `tests/integration/`, and content-gated calibration tests (read YAML via `SIDEQUEST_GENRE_PACKS`) SKIP when the pack path isn't set — so a scoped run shows green while the full run fails.
- Two regressions slipped through this way: `tests/integration/test_dogfight_playtest_smoke` (stale `per_actor_state == {}` assertion broken by correct frame-HP seeding) and `tests/genre/test_confrontation_calibration[space_opera]` (ADR-093 ceiling: opponent ability scores must be ≤10, authored Reflex 12 broke it).
- How: capture the FULL-suite baseline failure list at the start. Run subagents with `SIDEQUEST_GENRE_PACKS=../sidequest-content/genre_packs uv run pytest -q` and diff against that exact baseline — any failure not in it is a regression to fix, never "pre-existing." Never accept a "pre-existing" claim whose test name isn't in the recorded baseline; verify on current HEAD. Stale-assertion regressions (a test pinning `== {}` that new seeding invalidates) are the common shape.

### Don't re-run the full server suite in background subagents (2026-06-17, Fate Contest merge)
- A background Dev subagent re-running the full server suite (~13,451 tests, xdist) multiple times (rebase-verify → rework-verify → "load every pack") burned ~300K credits in one session. Full-suite output × large-file reads (e.g. `narration_apply.py` ~6700 lines) × heavy background resumes compound fast.
- Verify with the documented baseline + targeted tests, not whole-suite reruns. After a loader/`rules.py`-validation change the cheap canary is `tests/genre/test_pack_load.py` + new guardrail tests (~4s) — covers "did I break all pack loading."
- Run the full suite at most ONCE per change, foreground; never re-run "to be sure" in a background loop. Don't re-derive baselines by checking out develop (banned).

### testing-runner clobbers .session/{STORY_ID}-session.md — use a throwaway id (2026-06-09, 88-3)
- The `testing-runner` subagent writes a results "cache" to `.session/{STORY_ID}-session.md` and overwrites any existing session file wholesale (a ~100KB approved session → a 32-line stub). The CURRENT story's own id clobbers too, not just another story's. Four confirmed offenses (24-10, 60-4, 61-followup-C, 88-3 which clobbered twice in one run); the no-write-to-.session instruction alone is NOT load-bearing — only a throwaway STORY_ID reliably prevents it.
- `.session/*-session.md` is gitignored and unrecoverable from git/pf; only recovery is the conversation transcript JSONL under `~/.claude/projects/<proj>/*.jsonl` (`grep -l "<unique session string>" *.jsonl`). The session is NOT expendable mid-phase — gates, Reviewer, and SM finish all read it; finish archives it as the permanent audit record.
- Apply: pass a throwaway STORY_ID/RUN_ID mapping to no live session (`STORY_ID: "_verify"`, `RUN_ID: "verify-scratch"`) — the agent template's `STORY_ID: "{STORY_ID}"` is a tripwire, not the live id. Belt-and-suspenders: `cp .session/{id}-session.md /tmp/{id}-session.backup.md` before any testing-runner/reviewer-preflight spawn; restore before finish. testing-runner may also silently edit test files (ruff fixes) — re-check `git status` after.

### glenross Fate drive seed is a CONTENT fix + the auto-narrate handler-seam hermeticity trap (2026-06-22, 153-13)
- 153-13 degenerate turn-0 quest (`seed_drive.title == .objective == "Episcopal Rector"`) is a CONTENT bug, not server. glenross chargen leaves `character.drive == ""` because its `char_creation.yaml` ships NO drive scene; `quest_seed.py` then falls back to `calling_label`. The board's "drive set to the vocation" root cause is wrong — there is no such assignment. Fix = add a drive surface to glenross (a `drive` scene à la space_opera, or per-calling `drives:`); sibling `tea_and_murder/worlds/blackthorn_moor` has the same gap. Server (`builder.py` 1591-1610, `quest_seed.py`) is correct + out of scope.
- HANDLER-SEAM HERMETICITY GOTCHA (reusable): glenross's `_chargen_confirmation` AUTO-NARRATES its opening on commit (caverns/flickering_reach does NOT), which runs the post-narration sidecar extractor — the one turn collaborator NOT covered by `tests/server/conftest.py`'s `build_async_anthropic` stub. It reaches `llm_factory.query` (`from claude_agent_sdk import query`, llm_factory.py:24). To stay hermetic: install `FakeQuery(structured_output_stream({}))` (empty `SidecarExtraction` validates) on BOTH `sidequest.agents.llm_factory.query` AND `anthropic_sdk_client.query`, FROM THE TEST BODY not a fixture — the autouse `_refuse` guard (conftest.py:517) re-binds `query` after requested fixtures, so a fixture-level fake loses the ordering race.
- RED tests: `sidequest-server/tests/server/test_153_13_glenross_fate_drive_seed.py`.

### STALE — ADR-112 cache-promotion no longer breaks test_60_6 (both tests deleted in 119-3) (2026-06-18, 151-1)
- STALE: `tests/agents/test_60_6_stable_prefix_live_drift.py` AND `tests/agents/test_cache_ttl_prefix_and_otel.py` were BOTH deleted in story 119-3 (commit f970091e, the claude-agent-sdk port). There is no longer a cache-prefix invariant test to break. The 151-1 spec's "promoting narrator_output_only breaks test_60_6, update it" was un-followable, and no currently-passing test broke. Always grep for the named test before assuming a landmine.
- Post-119-3 the per-block `cache=True/False` markers are DEAD for the narrator path — `AnthropicSdkClient.complete_with_tools` flattens every `system_blocks` entry into ONE plain `system_prompt` (`anthropic_sdk_client.py:434`) and the `claude` CLI owns caching. The promotion still wins tokens but via a DIFFERENT axis: System bucket → stable `system_prompt` (CLI-cached cross-turn) vs User bucket → per-turn user message (never cross-turn cached). The rebate is a LIVE/playtest measurement (`narration.turn.cached_input_*` spans), not a fixture assertion.
- Historical (pre-119-3) trap kept for context: test_60_6 drove 5 turns with progressively mutating fields and asserted byte-identical prefix digest; promoting a `_mutating_contexts` field (npc_pool/state_summary/tropes/world_context) into the prefix correctly drifted it → those tests encoded the pre-promotion invariant, NOT pre-existing failures. Fix was to make the promoted field session-static in the test; only the FULL suite surfaced it.

### The "12 poker/auction table failures" are a stale-content-branch artifact, not develop debt (2026-05-30)
- The poker/auction `table_resolution` migration is already complete on develop: server #522 (`aab25c69`, N-seat model + `tests/genre/test_poker_table_content.py`/`test_auction_table_content.py`) and content #294 (`cb36694`, migrated spaghetti_western poker + tea_and_murder auction: resolution_mode=table_resolution, win_condition=table_showdown, table_game, max_decision_points=3, dial metrics dropped, beats accuse/raise_bid renamed). All 12 pass on current content develop.
- Why it looked red: WWN Plan 3 ran the content tree on `feat/wwn-elemental-harmony-binding`, cut BEFORE #294 — so the server suite (GENRE_PACKS → that stale tree) saw the old dial schema and reported 12 failures. The failures track the content checkout, not develop.
- Apply: if those 12 fail, your content tree predates #294 — `git -C sidequest-content checkout develop && pull`, don't "migrate". Story 64-13 was filed to redo this then cancelled on discovering #294. Read the file / run the cited test BEFORE setup.

### Two ways a reference-projection keeper-absence firewall test goes vacuous (2026-06-09)
- Epic 100 (reference pages → React SPA, server-side `reference_visibility.classify()` firewall). Trap 1 — untested carve path (100-5): a `classify()` keeper carve can be logically correct yet have ZERO guarding tests. The `{legends:[...]}` map form needed a separate carve (`('legends','*','related_tropes')`) from the flat-Vec form; removing it broke 0 tests = vacuous. Loaders accept multiple shapes (`genre/loader.py:_load_legends_flexible`) and the generic-YAML path reads raw — so EVERY shape reaching a keeper field needs its own carve AND its own mutation-failing test.
- Trap 2 — `json.dumps` ensure_ascii escaping (100-6): keeper-absence tests doing `assert KEEPER not in json.dumps(projection)` are VACUOUS when the keeper string contains non-ASCII (em-dash U+2014 lifted from real content): `json.dumps` defaults `ensure_ascii=True`, escaping `—`→—, so the substring never appears whether or not the field leaked. Fix: `json.dumps(obj, ensure_ascii=False)` (shared `_blob()` helper on ALL asserts), or assert structurally over leaf values.
- Rules: every `classify()` carve gets an independent mutation test that FAILS when the carve is removed (carve breaking 0 tests = REJECT); prefer structural leaf-walk over substring-matching serialized JSON (else `ensure_ascii=False`); RED fixtures from live packs carry non-ASCII — assume it; add a positive-control test (keeper IS in raw YAML, scrubbed in projection). The firewall is automatic IF every section routes through `build_generic_yaml_section`; a raw `yaml.safe_load`→`JSONResponse` splat leaks everything.

### Cold-open / chargen-complete test harness selection + the test_genre fixture-pack leak (2026-06-21, 153-2)
- Use the `test_genre/flickering_reach` fixture-pack harness for opening/cold-open/chargen-complete server tests: handler over `tests/fixtures/packs` (`_FIXTURE_PACKS_DIR`), `seed_slug_for_test(genre="test_genre", world="flickering_reach")`, minimal `_walk_and_confirm`, copied `_pg_isolation` (needs `SIDEQUEST_TEST_DATABASE_URL`). Canonical: `tests/server/test_lore_seeding_dispatch.py`; hermetic and mutable (`pack.worlds.get("flickering_reach").openings = [...]`).
- Do NOT use `caverns_sunden` (removed → `beneath_sunden`; module-skip-listed in `conftest.py::_CAVERNS_SUNDEN_DEPRECATED_TESTS`) or `caverns_and_claudes/beneath_sunden` (ADR-106 procedural megadungeon → chargen-complete triggers real claude-agent-sdk `query()`, non-hermetic). `caverns_and_claudes/flickering_reach` is a ghost-world slug (no world object).
- Recurring leak: a dungeon materializer writes `worlds/beneath_sunden/rooms/exp001.r*.yaml` (untracked) into the SHARED `tests/fixtures/packs/test_genre/` tree → missing-`world.yaml` dir makes `test_genre` fail to load → breaks every test_genre harness test. Fix: `rm -rf tests/fixtures/packs/test_genre/worlds/beneath_sunden/`. Recurs on any full-suite run that hits that dungeon test; real fix = materializer output to session/tmp.
- Cold-open mechanics: `_run_opening_turn_narration` emits `cold_open_messages` (establishing_narration verbatim THEN opening_seed=first_turn_invitation); watcher events `cold_open_emitted` + `establishing_narration_emitted` publish via `websocket_session_handler._watcher_publish` (patch there to spy).

### Binding a pack to WN hp_depletion breaks two calibration tests — migrate per space_opera precedent (2026-05-29)
- Rewriting a pack's combat confrontation from `opposed_check`/dual-dial to `beat_selection` + `win_condition: hp_depletion` regresses two `sidequest-server` calibration tests that pinned the old schema. They will NOT be in your Task-0 baseline so they read as fresh regressions — but mirror the space_opera→SWN fix, don't delete.
- (1) `tests/genre/test_pack_load.py::test_<pack>_pack_loads_with_dual_dial_schema` — rewritten combat has no player_metric/opponent_metric so the dual-dial assertion NPEs; rewrite like `test_space_opera_...`: filter to `win_condition == "dial_threshold"` confrontations, assert ≥1 remains (non-vacuous), apply the invariant only to those.
- (2) `tests/genre/test_confrontation_calibration.py` `COMBAT_PACKS` — drop the pack (no longer has an opposed_check confrontation); the module comment already documents space_opera was dropped for the same reason.
- Also: a hp_depletion combat's `opponent_default_stats` must author ALL SIX ability scores — a damage/control spell's save resolves vs attrs like Insight/Spirit via attribute_map and `swn.save_params` fails loud (KeyError) on a missing stat.

### Full server suite has a ~258-269 hermeticity-guard baseline (2026-06-15)
- Running the full `sidequest-server` suite (`uv run pytest -q`) yields ~258–269 PRE-EXISTING failures even with `SIDEQUEST_DATABASE_URL` set. Dominant cause (522 line-mentions) is the LLM-hermeticity guard in `tests/server/conftest.py:539`: `LlmClientError: Test attempted to construct the REAL Anthropic SDK via build_async_anthropic()`.
- Many pre-existing tests drive a narration turn through `_execute_narration_turn` but don't stub the pre-narrator objective classifier (`build_unseeded_objective_classifier_llm` → `build_async_anthropic()` at `websocket_session_handler.py:1197`), dying there BEFORE reaching the subsystem under test. Remaining failures are known content/loader baselines (EH/HM/CAC class-filter + bestiary) and missing corpus fixtures.
- Verify your story didn't regress: grep the FAILED list for files in your change's blast radius; read the traceback for any hit — if it dies at the hermeticity guard or an unrelated loader, it's baseline, not yours. Don't panic at the raw count.

### Server suite needs SIDEQUEST_DATABASE_URL + two more env traps (2026-05-27..06-04)
- Post-ADR-115 (SQLite→Postgres), tests touching the persistence substrate raise `MissingDatabaseUrlError`. The `testing-runner` subagent does NOT set it — a full-suite run reports ~33 phantom failures that are pure env. Fix: `SIDEQUEST_DATABASE_URL=postgresql://$USER@localhost:5432/sidequest_test`.
- SECOND var — `SIDEQUEST_TEST_DATABASE_URL` (the TEST variant): some handler/integration tests (e.g. `tests/handlers/test_aside_channel_wiring.py`) gate on this, NOT `SIDEQUEST_DATABASE_URL`. Without it they SKIP (silently masking pass/fail) — can make an already-fixed test look unverified and a story premise look live. Set BOTH to the test DB. Burned an SM premise-read on 76-8.
- THIRD trap — `uv run pytest` can run a BROKEN GLOBAL pytest. pytest is in the server `dev` optional-extra, not base deps; `uv sync` alone falls through to `~/.local/bin/pytest` which fails collection with `ModuleNotFoundError: No module named 'opentelemetry'`. Tell: an existing known-good test reproduces the same collection error. Fix: `uv sync --extra dev`; verify `uv run which pytest` → `.venv/bin/pytest`.

### tests/server autouse fixture repoints loader to frozen fixture packs (2026-06, 76-7)
- `tests/server/conftest.py::_fixture_pack_search_paths` is an AUTOUSE fixture that monkeypatches `sidequest.genre.loader.DEFAULT_GENRE_PACK_SEARCH_PATHS` to `[tests/fixtures/packs]`. So under `tests/server/` (incl. anything using `session_handler_factory`), `load`/`GenreLoader` resolves the FROZEN fixture packs, NOT live `sidequest-content` — even with `SIDEQUEST_GENRE_PACKS` set.
- Implication: a "real-content" server test that loads e.g. `elemental_harmony/burning_peace` FAILS (fixture pack lacks that world); a standalone probe with the real loader misleads. Use a fixture world: `caverns_and_claudes/flickering_reach` loads with 5 authored factions (most fixture packs ship a flickering_reach world with a factions block).
- Related: factions for the ADR-118 entity index source from `World.lore.factions` (world-tier flavor, ADR-120) via `dispatch/entity_sync._collect_world_factions` — never genre/global.

### Minimal WN-family RulesConfig test fixture = `wwn` + `WwnConfig(attribute_map=...)`; both default-spread constants are length-6 and unvalidated (153-34, 2026-06-21)
- All four WN siblings (`swn`/`cwn`/`wwn`/`awn`) have a `model_validator` (`rules.py` `_validate_swn`/`_cwn`/`_wwn`/`_awn`) that REQUIRES a complete 6-key `attribute_map` (STRENGTH/CONSTITUTION/DEXTERITY/INTELLIGENCE/WISDOM/CHARISMA → flavor names that must all appear in `ability_score_names`). So you can't construct a bare WN `RulesConfig` — the lightest valid fixture is `RulesConfig.model_validate({"ruleset":"wwn","ability_score_names":[...6...],"wwn":{"attribute_map":AMAP}})`, mirroring `tests/game/ruleset/test_loader_binding.py::test_wwn_ruleset_binds`. `WwnConfig()` defaults (system_strain/trauma/magic) are self-consistent, so only `attribute_map` need be passed.
- **Adding a 7th ability to trigger the ≠6 path:** append an UNMAPPED flavor name (e.g. `"Psyche"`) to `ability_score_names`. The validator only checks that all 6 required keys are present and all `attribute_map` *values* ⊆ `ability_score_names` — it does NOT require the reverse — so a declared-but-unmapped 7th ability passes validation cleanly (this is the reviewer's literal "psionics-heavy SWN adds a Psyche stat" scenario).
- **Two length-6 default-spread constants, both unvalidated against ability count:** `_WN_STANDARD_ARRAY=[14,12,11,10,9,7]` (`without_number.py`, used by the `point_buy` override) and `_DEFAULT_STANDARD_ARRAY=[15,14,13,12,10,8]` (`base.py`, used by the `standard_array` method when no array authored). For a 7-ability config with `standard_array=None`, BOTH `point_buy` and `standard_array` methods reach a 6-value pool and then `IndexError: pop from empty list` in `assign_attributes` (`rest.pop(0)` on the 7th name). Reproduce via `get_ruleset_module("wwn").generate_attributes(method=..., ability_names=<7>, standard_array=None, ...)` before writing RED — confirmed both paths crash. `RulesConfig._validate_standard_array` guards only the *authored*-array case (`standard_array is not None`); the default case is the gap. A single `standard_array is None && len(ability_score_names) > default_len` check at the model-validation seam greens both method-path RED tests. See [[story_153_34_wn_pointbuy_dead_config]].

### Read the TARGET SOURCE before writing any RED test — an already-present implementation citing the story number means it already shipped via a parallel clone (126-10, 2026-06-18)
- 126-10 (trim `build_fate_projection` for the Fate router prompt) came to RED looking like normal p2 work. But the *first* thing I did was read the injection site (`sidequest/server/intent_router_pass.py`) and the projection module (`sidequest/game/ruleset/fate_projection.py`) — and BOTH already contained the fix: `trim_fate_projection_for_router()` + `_ROUTER_DROP_KEYS = ("character_aspects","scene_aspects")`, wired into the `ruleset=="fate"` branch (lines 376–392) with a dedicated `intent_router_fate_vocabulary_span` OTEL emit. The docstrings literally said *"Story 126-10."* That is the loudest possible tell that the work is done.
- **Confirmed it wasn't my own stale state:** `git log --all | grep 126-10` → one commit `2a704e38 feat(126-10): … (#946)`; `gh pr view 946 --json state,mergedAt` → **MERGED 10:17Z** (this clone's SM picked the story from `backlog` ~12:30Z); `git rev-list --count origin/develop..HEAD` → **0** (branch had no commits beyond develop, fix is an ancestor of HEAD). A parallel oq clone shipped it; only the sprint tracker on this clone still said `backlog`. Second occurrence of this exact pattern (118-8 was 2026-06-15) — it is systemic, not a fluke. See [[project_backlog_story_already_shipped]].
- **What to do instead of authoring RED tests you can't make fail:** verify the *existing* tests are GREEN and that they include the coverage a paranoid RED phase would have demanded, then close via SM finish. Here testing-runner returned 18/18 GREEN across `tests/game/ruleset/test_fate_projection.py` (trim contract + narrator-untouched invariant) and `tests/server/test_fate_classifier_enrichment.py` — the latter carrying the **wiring** test (`test_pre_narrator_pass_ships_trimmed_fate_block`), the **OTEL evidence** test (`test_fate_vocabulary_span_fires_with_trim_evidence`), and the **non-Fate isolation** test (`test_router_omits_fate_block_for_non_fate_pack`). Writing redundant tests, or worse hand-routing to Dev for a no-op GREEN, both manufacture work that doesn't exist.
- **Don't auto-progress the TDD phase machine on an already-shipped story.** The normal red→exit hands to Dev (green); that destination is wrong here. Record the finding (Delivery Finding: tracker drift, non-blocking) + a "Tests Required: No — already shipped" TEA Assessment, then escalate to SM for the finish ceremony (`pf sprint story finish` handles `backlog`→`done` and prints "No PR to merge"; the empty feature branch gets deleted after `git checkout develop`).

### Bind synthetic ruleset packs to the REAL production ruleset, not vestigial `dial` — `dial` is bound by no live pack and silently masks binding-specific validation (126-2, 2026-06-18)
- Re-verifying router-driven **table_resolution** poker seating, I copied the existing `tests/server/test_table_resolution_wiring.py` fixture, which builds `RulesConfig(ruleset="dial", ...)`. All 4 tests passed. But **no live pack binds `dial`** (CLAUDE.md: it's the vestigial schema default; every one of the 11 packs binds a Without-Number or Fate ruleset). The live poker table ships in `spaghetti_western`, which binds **`fate`**. Testing under `dial` verified a path production never takes.
- **The tell + the fidelity check:** when a contract runs "under a ruleset," grep `genre_packs/*/rules.yaml` for `ruleset:` to find which binding the real feature uses, and bind the synthetic pack to THAT. Here: poker/auction → `fate` (spaghetti_western, tea_and_murder); war_rig_crew → `cwn` (road_warrior).
- **What it exposed:** flipping the fixture to `ruleset="fate"` first *failed* — but only on `RulesConfig` validation (`ruleset 'fate' requires rules.fate (FateConfig …); none authored — no silent default`, ADR-144 F4a), NOT on the engine. Supplying `fate=FateConfig()` (all fields default; the `_validate_fate` model-validator only checks presence-not-None) made all 4 pass. So **`dial` masked a fail-loud binding requirement** the production path enforces. Lesson: a green test under `dial` is not evidence the path works under the real binding — the binding can have its own required-content validators (`_validate_fate`, the swn/cwn/wwn `attribute_map` validators) that `dial` doesn't.
- **Why it was still green under fate (don't over-claim a break):** table resolution is ruleset-agnostic by design — `FateRulesetModule` does NOT override `deal_table`/`resolve_table`; they're concrete on the base `RulesetModule` and delegate to `game/table/engine.py` ("orthogonal to combat resolution — every ruleset inherits it"). So the transient "fails under fate" was a *fixture* gap (missing FateConfig), not an engine regression. Read the ruleset module for an override before concluding a binding changes behavior.
- **Re-verification bugs come back GREEN — that's legitimate, don't manufacture a RED.** 126-2's "card game broken" was downstream of the intent-router blocker fixed in 126-9; the seating engine never regressed. The RED-phase deliverable is then a **regression lock** (passing by design), and the story closes "verified, no fix" off the `tests_fail` gate. Per playtesting cadence (Keith): playtest-fix stories routinely fix the root upstream, so a sibling re-verify lands green — write the lock, document the upstream fix, don't fake a failure.

### Ground a story's ACs against the migration that ACTUALLY SHIPPED, not the title's premise — a "depends on whichever lands" story can be half-void on arrival (119-4 RED, 2026-06-16)
- 119-4's title said "depends on whichever migration lands (119-2 or 119-3)" and named three ACs: (1) swap the static `ANTHROPIC_AUTH_TOKEN` env token for an auto-refreshing `ant auth login` profile + fail loud on expiry; (2) OTEL span tagging credit-vs-PAYG billing pool; (3) monitor the "200 monthly credit + 50.52 prepaid overflow buffer before it silently bills PAYG." If you write tests straight off that, you test **fiction**: 119-2 (static token) never shipped — **119-3 (claude-agent-sdk) did**, and its transport already resolves the host's auto-refreshing OAuth login (no static token exists in that path; `assert_subscription_auth()` *requires* the token UNSET). AND the epic was reframed 2026-06-15: the "Agent SDK credit" was CANCELLED, so there is no $200 credit and no $50.52 buffer, and 119-3 fails loud (no silent PAYG to monitor "before"). ~2 of 3 headline ACs referenced a dead premise.
- **The tell:** the story title predates the epic reframe / the sibling migration that won. Before designing RED, read (a) the epic context's `## Overview` for any "REFRAMED <date>" / "CANCELLED" / "VOID" language, (b) `git log` of the dep to see which mutually-exclusive sibling actually merged (PR #908 = 119-3 here), and (c) the dep's design spec for an explicit "119-N owns X" boundary (the 119-2 spec §6 literally scoped 119-4 as the token-swap — but only *if 119-2 had shipped*). Grep the live code for the AC's named mechanism (`grep auth_path|ratelimit|credit`) — if the thing the AC says to "swap/monitor" isn't there, the premise moved.
- **Don't silently reinterpret — surface the fork to the user, THEN log deviations.** When a money/infra story's ACs are half-void, that's a scope decision the user owns (I used AskUserQuestion: "build the real subset" vs "minimal" vs "re-spec"). After they pick, log each descope/reframe as a 6-field Design Deviation (severity major for a dropped headline AC) and capture the residuals as Delivery Findings. Writing tests against the void premise, OR quietly swapping the AC without telling them, both violate No Silent Fallbacks — applied to your own communication.
- **Watch your OWN option text for unverified claims:** I pitched AC3' as "monitor from the anthropic-ratelimit headers we already capture" — then grep proved we DON'T capture them (`llm_request_span` documents `llm.ratelimit_input_tokens_remaining` but no code populates it; the agent-SDK is a subprocess returning a `ResultMessage`, not HTTP headers). The honest available signal was `ResultMessage.total_cost_usd` (surface as `sdk_reported_cost_usd`). Verify the mechanism exists in code before you offer it as an option.

### A dependency that looks "missing" is usually a STALE LOCAL CHECKOUT — `git fetch` + check `origin/<base>` before you declare it absent (121-2 RED, 2026-06-15)
- 121-2 (F4b) depended on 121-1 (F4a engine spine). It looked entirely absent on the local `sidequest-server` working tree: no `FateConfig` in `genre/models/rules.py`, no `seed_chargen_resources` on `FateRulesetModule`, no `ChargenResources.fate_sheet`, no F4a test file. I (TEA) checked `git log --oneline -8`, local branches, and stash — all came up empty — and wrongly concluded F4a never merged (blamed the `merge_pr` no-op gotcha). **That was wrong.** SM then checked the remote: PR #884 was `MERGED` into `origin/develop` (commit `f527ddb7`); local `develop` was simply **3 commits behind** (0 ahead, 3 behind). A `git pull --ff-only` brought every F4a symbol in. The dependency was satisfied the whole time.
- **The miss:** `git log --all` and `git branch -a` only see refs you've already fetched. A story can merge on GitHub minutes before you look; your local mirror won't show it until you `git fetch`. Checking the working tree proves what YOU have, not what `origin` has.
- **RED precondition check for any story with `depends_on`/"dep live" ACs — do this FIRST, before grepping symbols:** `git fetch origin && git rev-list --left-right --count <base>...origin/<base>` (nonzero "behind" ⇒ pull before judging), then if still unsure `gh pr list --repo <repo> --state all --search "<dep keywords>"` to see if the dep PR merged. Only after you're current should "symbol absent in working tree" mean anything. The fast tell I should have used: `git grep -l "class FateConfig" origin/develop -- <path>` checks the REMOTE ref directly.
- **The real escalation rule still holds, but gate it on being current:** only after confirming you're synced with origin AND the dep symbols are genuinely absent do you record a *blocking* finding and hand back to SM (and even then, don't rebuild the dep from the downstream lane). Don't stop the line on a stale checkout.

### Creation-seed lore links are NOT 1:1 with the PC — filter by choice_label (93-4 RED, 2026-06-10)
- `seed_lore_from_char_creation` (sidequest-server/sidequest/game/lore_seeding.py:206) seeds **one fragment per choice in EVERY scene — including choices the player did NOT pick**, all into one shared per-session `LoreStore` (`_SessionData.lore_store`, default_factory). Fragment id is `lore_char_creation_<scene_id>_<choice_index>` — **not player-scoped**. So "this character's lore" is NOT "all CharacterCreation fragments in the store."
- The only correct per-PC discriminator is the answer the PC actually gave: `fragment.metadata['choice_label'] == Character.creation_answers[].value` (for `kind=="choice"`). That value-match IS the cross-character firewall (AC6 / ADR-104-105 intent) — two PCs answering the same scene differently must not see each other's pick. Build the firewall test by seeding ONE shared store and asserting each PC's surfaced titles ⊆ its own answers.
- `CreationAnswer` (protocol/models.py:403) records the chosen LABEL in `.value`, not the choice_index — so you can't reconstruct the fragment id directly; you match on `choice_label` metadata. Freeform answers have NO seed fragment (seeder only iterates `scene.choices`) — a freeform miss must NOT warn; only an unresolvable CHOICE answer is the loud-skip case.
- OTEL emit pattern for sheet-build: chargen already does `_watcher_publish("lore_retrieval", {...}, component="rag")` at chargen_mixin.py:1467. For a sync emit captured in an async test, use the `tests/integration/test_lore_wiring.py` harness: `watcher_hub.bind_loop(get_running_loop())` + clear `_subscribers` + `subscribe(_Sock())`, drive the sync `party_member_from_character`, `await asyncio.sleep(0.05)`, then filter `captured`.

### Orbital intent error seam is incomplete server-side (found in 98-3 RED, 2026-06-09)
- `OrbitalIntentHandler.handle` (sidequest-server/sidequest/handlers/orbital_intent.py) catches only `OrbitalContentUnavailableError` (→ ERROR code `orbital_unavailable`). The 98-2 fail-loud `OrbitalContentMissingError` (orbital/loader.py — unauthored `systems/<region>.yaml`) is NOT caught there, so a drill into an unauthored system likely surfaces as an unhandled exception, not a typed ERROR the UI can render. Logged as a Delivery Finding on 98-3; AC5's UI test pins the widget seam (`lastOrbitalError` prop) regardless.
- When RED-testing a UI seam fed by server errors, read the server handler's actual `except` clauses before assuming an error code exists on the wire — the loader raising loudly does not mean the handler forwards it.

### Confrontation resolution has MULTIPLE overlapping resolvers — find the incumbent before writing outcome tests (59-31)
- The per-turn sweep at the bottom of `_apply_narration_result_to_snapshot` runs resolvers IN ORDER, each short-circuiting on `enc.resolved`: `_resolve_if_no_opponent_remains` (4350, → `opponent_withdrew`) then `_resolve_dial_threshold_and_phase` (4359). The location-change handler (~2790) resolves EARLIER in the same call (→ `abandoned_on_location_change` / `player_victory` via `dial_threshold_outcome()`). So an "all opponents withdrawn" state already has an incumbent label (`opponent_withdrew`) — a new outcome story must RELABEL/REPLACE the incumbent, not add a resolver after it (the incumbent wins by running first).
- When writing outcome-label tests, drive the real entry `_apply_narration_result_to_snapshot(snap, NarrationTurnResult(...), player_name=, room=room_for(snap))` (no pack needed for the sweep), not the internal helper — survives whichever helper the Dev edits, and proves wiring.
- Confrontation OTEL is emitted via `_watcher_publish(event, fields, *, component="confrontation")` (sibling events: `confrontation_resolved_on_location_change`, `confrontation_deactivated_on_location_change`). Capture in tests by `monkeypatch.setattr(narration_apply, "_watcher_publish", _capture)` — the `component=` kwarg is how you filter confrontation events. `encounter_resolved_span` (the dial sweep's emit) is a separate OTEL-span channel that does NOT take `component=`; don't conflate the two when picking an assertion target.
- `dial_threshold_outcome()` (encounter.py:243) is a METHOD on StructuredEncounter, gated to `win_condition=="dial_threshold"`. An "alongside" helper the Architect asks for should also be a method; decide deliberately whether it inherits the win_condition gate (opponent-yield should NOT — a surrender is a win in combat too).

### The wiring failure class (the dominant bug category in this project)
- **Don't reinvent — wire up what exists.** Before building anything new, grep the codebase. Many systems are fully implemented but not wired into the server or UI. CLAUDE.md rule #3.
- **No half-wired features.** Connect the full pipeline or don't start. If something needs 5 connections, make 5 connections. Shipping 3 and calling it done is the exact failure mode Epic 15 was created to clean up.
- **Wire it, don't just define it.** Adding fields to a struct is not wiring. Adding props to a component is not wiring. Data must actually flow from source → consumer. If no code populates and transmits the field, the feature isn't done.
- **Never rationalize unwired code.** When `/sq-wire-it check` reports FAIL (no non-test consumers), the answer is **wire it**. Do not write paragraphs explaining why this time is different. If I catch myself typing "this is acceptable because" — STOP. That sentence is the bug. The wiring usually takes 5 minutes; the rationalization takes longer.
- **"Wiring means dashboard."** Internal data flow without OTEL visibility is not wired. A missing `WatcherEventBuilder::new(...)` call on a subsystem's dispatch path is a blocking wiring bug, never a "non-blocking improvement." Labeling an OTEL gap as non-blocking in my own audit is the exact self-compounded failure I hit in the 2026-04-09 Map fix.
- **No agent rationalization.** Dev/Architect/TEA/Reviewer are all Claude — they don't get to split blame across personas. "The Architect said it was fine" is not a defense. If a wiring gap is identified in any phase, the only valid response is "fix the code now," never "clarify the spec" or "file a follow-up story."
- **Verify wiring before claiming done.** `grep` for non-test consumers on the merged branch before marking a fix as complete. Don't wait for Keith to ask. A playtest with the GM panel open is the acceptance test.
- **Verify end-to-end before claiming fixed.** Code review is not verification. Trace the full wire path: source → channel → writer task → WebSocket → client handler → DOM. Session 7 had six consecutive "fixed" claims on the turn-lock bug, each found a real issue, none verified the message actually arrived at the browser.
- **Wiring gaps require action, not acknowledgment.** Finding a wiring gap and saying "yep that's a wiring gap" is worthless. Fix it. Log it. Check for siblings (if one compute-then-ignore exists, grep for the pattern). Verify OTEL. Don't move on.
- **Kitchen-sink gate failure is real** (story 16-1): nine agents approved unwired code because every phase rationalized it as "scope boundary" and "wiring comes in subsequent stories." The wiring-check gate exists in `.pennyfarthing/gates/` — every agent must enforce it, not just the reviewer as a backstop.

### No stubs, no fallbacks
- **No stubs. Ever.** When I hit a scope error or type mismatch, solve the actual problem. Don't substitute `Default::default()`, placeholder values, or restructure the design to avoid the issue. CLAUDE.md line 124: "Never say 'the right fix is X' and then do Y." Do X. If `snapshot` isn't in scope, move the code to where it is.
- **No silent fallbacks.** If a path/config/resource isn't where it should be, fail loudly. No `if not exists: try_other_thing`, no `unwrap_or_default()` that papers over missing data, no `Option::None` degradation on required fields.
- **No layered fallback design.** If the LLM is down, the system is down. Keyword intent classification "working" while narration is broken is not meaningful resilience — it's dead code with delusions of usefulness.
- **No reading `~/.pennyfarthing`.** Stay within the project directory tree. Global pennyfarthing config pulls in work-project settings that don't apply to SideQuest.

### Deferral is not an option
- **Never defer fixes.** When auditing and fixing gaps, fix every one in the current session. No "deferred to separate session," no "future work," no "needs porting first." There is no future session; there is only now.
- **Never suggest deferring work to Keith.** Never say "park it," "post-X problem," "follow-up story." He decides priorities, not me. If something's broken, fix it; if I can't, say what I've tried and what I'd try next.
- **Just execute.** Don't deprioritize or editorialize by task type ("this is a feature gap," "this needs X fixed first"). Route tasks for execution without commentary.
- **No "pre-existing" excuse.** Never dismiss broken tests as pre-existing and move on. If the suite isn't fully green at handoff, I failed. Don't check "does develop have the same bug" as a way to excuse not fixing it.
- **No baseline as insight.** "Most bugs are wiring bugs" is the central thesis of sq-wire-it, CLAUDE.md, and nine feedback memories — it's the assumed operating environment, not a discovery. Don't write retrospective bullets that restate documented fundamentals. Save insight slots for things that are actually surprising *given* the baseline.
- **Fix what you see, in this story.** If I find a test that lies about what it covers, a vacuous assertion, or a missing edge case in code I'm writing tests for — fix it NOW. Every "defer to follow-up" is debt compounding on Keith. Don't catalog problems; fix them.
- **No dressed-up scope shields — the 2026-04-14 incident.** Forbidden phrases: "out of this branch's scope," "days of forensic rewrite work," "honest green via #[ignore]," "real fix is X but for now Y," "incremental retirement path," "future work." Marking 39 broken integration tests `#[ignore = "tech-debt"]` is not an "honest green" — it's silencing failures with prettier framing. Disabling a wiring test disables the wiring detector for every subsystem it covered.
- **Two-step check before any `#[ignore]`:** (a) grep for the assertion substring — if it exists in current source, the fix is a one-path update, not "days of work"; (b) if it exists nowhere, the choice is implement-or-delete, never ignore.

### No weasel words in test design
- **"Cleanest / simplest / proper test approach" are weasel words.** State (1) WHAT the test covers, (2) WHY this shape is correct (cite the behavior or boundary). A test that "checks the happy path cleanly" isn't justified — specify exactly what the assertion proves.

### Trust Keith's instincts on timing
- **When Keith says a test is slow or a suite is misbehaving, he's right.** Investigate first, explain second. Dismissing his reads is gaslighting.

### Git gotchas
- **Never use `git stash`.** Ever. Pop causes conflicts, leaves orphans, loses visibility. Use temp branches for context switches, or re-apply manually.
- **Never run destructive git ops without explicit permission:** `reset --hard`, `reset --soft`, `checkout -- <file>`, `clean -f`, `branch -D`, `push --force`, `rebase -i`. Each of these has destroyed work in a past incident.
- **Never checkout `main` in a subrepo.** All subrepos use gitflow with `develop` as base. Checking out main cascades into pushing to the wrong branch and divergent history.
- **Never edit files in `/Users/keithavery/Projects/oq-1`.** OQ-1 is a parallel workspace with in-progress uncommitted changes. Treat it like another developer's machine. Remote branch deletes are fine; local state is hands-off.
- **Push before reorganizing.** Get work on remote FIRST. Then if you want pretty commits, do it on a new branch.
- **"Actually simpler" means you're about to improvise.** Stop. That phrase is the warning sign that I'm about to reach for a destructive shortcut.
- **When git is messy, one command at a time.** State the command, state what it does, wait. Don't chain recoveries.
- **If I fucked up, STOP.** Don't reach for more commands to fix it. State what happened and ask Keith what to do.

### Build / test gotchas
- **Build verification happens on OQ-2, not OQ-1.** All edits live in OQ-1/sidequest-api; after merge, pull on OQ-2 and `cargo build -p sidequest-server` there. The workspace-root build is a placeholder.
- **Test compile cascade:** Never spawn two cargo processes on the same workspace at once. Cargo's build lock queues them and a 2-minute timeout + new spawn creates zombie-compile cascades (4 competing cargos, 10+ minutes, zero results). Use `timeout: 300000` (5 min), `cargo build` first, `cargo test` second.
- **No duplicate test runs while testing-runner is active.** After spawning testing-runner, do not touch cargo on the same workspace with any build/test command. "Let me also check this other test file" = another full recompile. Wait for completion.
- **Don't rerun full test suites on every loop tick.** Full `cargo test` takes 90-300+ seconds. For playtest bugfix branches, `cargo build` is the gate; full test runs once at the end. Targeted `cargo test -p <crate> -- <filter>` for anything touching test-adjacent code.
- **No live LLM calls in the default suite.** Tests in sidequest-agents that call real `claude -p` burn tokens and cost 90+ seconds per run. Mock `ClaudeClient`. Live-LLM integration tests belong in `--ignored`.
- **0.02s cargo build means nothing changed.** If the build is suspiciously fast, your fix isn't in the binary. Look for "Compiling sidequest-server" in the output as proof the change landed.
- **Server logs exist and are informative.** Check `/tmp/sq-api.log` before speculating about causes.

### Playtest gotchas
- **Playtest mode is debugging, not building.** When Keith reports a bug, the infrastructure exists. Find the small break; don't rebuild pipelines from scratch. Prompt subagents with "diagnose and fix" not "build and wire."
- **Playtest focus is systems, not narration quality.** The prose has been solid for a long time — don't comment on writing quality, only log it when it indicates a system bug.
- **Don't stop at partial success during a trace.** "There it is!" followed by "but wait" is confusing. Complete the trace to the end, then report.
- **Don't restart the daemon.** It warms up ML models — restart is expensive. Leave it running across sessions.
- **Context discipline: thoroughness over speed.** Context pressure is not my problem — the system manages it via TirePump, relay, `/clear`. Don't rush an assessment, skip subagent results, or abbreviate a handoff to save tokens. Cutting corners to save context costs more context than doing it right the first time.

### Environment / process gotchas
- **Session files live in `.session/{story-id}-session.md`**, never in `sprint/` directly. Sprint is for archives after `pf sprint story finish`.
- **Skip architect + spec checks for SideQuest.** Personal project — streamlined RED → GREEN → VERIFY → REVIEW → FINISH. No architect spec validation, no spec-check/spec-reconcile.
- **No AI self-judgment.** Don't design automated Claude-judges-Claude validators for game decisions. Surface rich telemetry for human inspection instead.
- **Theme list tags are benchmark grades**, not storage locations. `[S] [A] [B] [C] [D] [U]` = superior/A/B/C/D/unknown tier. `[U]` ≠ "user-level," `[B]` ≠ "built-in."
- **`claude -p` fully supports tool use**, including `--allowedTools Bash(...)`. Never claim pipe mode doesn't support tools — the bug is somewhere else (prompt construction, binary paths, env vars).

### sm-setup can hand off to TEA without the story/epic context docs (2026-06-03, story 80-1)
- **What happened:** On TEA activation for 80-1, `pf validate context-story 80-1` failed (exit 2 — file not found). sm-setup had created `.session/80-1-session.md` (with technical approach + ACs inline) but **not** `sprint/context/context-story-80-1.md` or `context-epic-80.md`. Every sibling story (81-x, 82-x) had its context doc; 80-1 did not. The SM `resolve-gate` had reported `ready` during setup anyway, and the `gate-recovery` context-creation cascade never fired.
- **How to apply:** TEA's on-activation context gate (`pf validate context-story {id}`) is load-bearing — honor the "STOP on exit 1/2, do not auto-create" rule, but the clean recovery is to run `/pf:context create epic {N}` then `create story {N-N}` (cascade: epic first; if the story has no epic, the epic still needs a context doc because the story-context skill validates the parent exists). The story-context skill expects fixtures the plan may mis-name — read the real test files for fixture helper names before writing tests (80-1: `makePack`/`makeWorld`, `renderConnect`/`GENRES`, not the plan's `mockPack`/`renderConnectScreen`). Log the gap as a non-blocking Delivery Finding so the setup gate gets hardened.

### Historical incident log (things I've personally gotten wrong in this project)
- **2026-03-30 OTEL stub incident.** Said the right fix, then hacked `GameSnapshot::default()` when `snapshot` was out of scope. Re-read feedback_no_stubs_ever before ever reaching for a default value.
- **2026-03-31 git catastrophe.** Used `reset --soft`, `checkout -- .`, and a direct merge-to-develop in one session. Each "oh I'll just..." made things worse. Re-read feedback_git_discipline before any destructive op.
- **Story 19-8 Automapper rationalization.** Wiring check said FAIL, I wrote paragraphs explaining why it was fine. The wiring took 5 minutes. Re-read feedback_never_rationalize_unwired.
- **Story 16-1 kitchen-sink rationalization.** All 9 agents approved unwired code by calling it a "scope boundary." Re-read feedback_wiring_gate_failure.
- **2026-04-08 TurnRecord field-add incident.** Added struct fields, declared fixed — nobody was constructing TurnRecords. Re-read feedback_wire_not_define.
- **2026-04-09 Map OTEL self-correction.** Ran sq-wire-it on my own Map fix, found no OTEL spans, labeled it "non-blocking" and declared wiring PASS. Keith corrected: "this means it is not wired." Re-read feedback_wiring_means_dashboard.
- **2026-04-09 baseline-as-insight.** Wrote a retrospective bullet saying "every fix was a wiring bug" as if it were a synthesis. It's the project's central thesis. Re-read feedback_no_baseline_as_insight.

### testing-runner can clobber the session file (2026-05-30)
- **What happened:** During 72-3 verify, a `testing-runner` subagent invoked for a full-suite regression run wrote a "Test Result Cache" markdown over `.session/72-3-session.md`, destroying the entire session (all assessments, deviations, findings, ACs). `.session/` is gitignored → no git recovery; reconstructed from the in-session conversation record.
- **Why it bit:** The RUN_ID was `72-3-tea-verify` and the runner apparently derives a cache path from story id under `.session/`, colliding with `.session/{story}-session.md`.
- **How to apply:** When spawning `testing-runner`, (1) explicitly instruct it to ONLY run tests and report — "do not write any files, do not cache results to disk, do not touch `.session/`"; and (2) after it returns, `ls -la .session/{story}-session.md` to confirm size/mtime are sane before continuing. If clobbered, reconstruct from context immediately (the conversation holds every read+edit) and add a transparency recovery-note header. Consider snapshotting the session file to `/tmp` before a full-suite run as a cheap backup.

### Verify simplify-agent claims before applying — high-confidence ≠ correct (2026-06-01, story 65-8 verify)
- **What happened:** The three haiku simplify teammates returned two **high-confidence** findings that were both factually wrong, and rated the one genuinely valuable finding only *medium*.
  - reuse (high): "import `scripts/r2_manifest.py::load_manifest` instead of reimplementing the loader." Impossible — `scripts/` is in the **orchestrator** repo (`../scripts/`), not the `sidequest-server` package, so it isn't importable; and its contract differs (`list[dict]` vs `frozenset[str]`). A 2-command check (`ls scripts/r2_manifest.py` → not found; `grep ':func:' sidequest/` → 105 hits) debunked it.
  - quality (medium→dismiss): "`:func:` Sphinx role is inconsistent with peers." False — `:func:` is used 105× in the package; it IS the house style. Applying the "fix" would have introduced inconsistency.
  - efficiency (high): "cache `world_key_count` instead of recomputing per render." Technically a redundant walk, but the lore page is a cold path and the suggested fix *adds* a cache — anti-simplify. Declined.
  - The actually-valuable one was rated **medium** by reuse: the POI image key was built as two independent f-strings (gate vs presenter `<img src>`) — a silent drift hazard. Upgraded and applied (one shared `poi_image_key`).
- **How to apply:** Treat simplify-agent confidence as a *prompt to investigate*, not a verdict. Before applying ANY finding: (1) verify the factual premise with a quick grep/ls (is the import real? is the "inconsistency" actually inconsistent?); (2) reject "simplify" suggestions that ADD state/caching on cold paths — they betray the pass's purpose; (3) re-rank by *correctness impact*, not the agent's confidence — a medium "two strings must stay in lockstep" finding outranks a high "micro-optimize a cold path." The leader's judgment is the product, not the agents' raw output.

### simplify-reuse rates PRE-EXISTING / out-of-diff duplication "high confidence" — verify it's actually in THIS story's diff before applying (2026-06-03, story 80-1 verify)
- **What happened:** On a UI story (lobby grouped picker) that rewrote `OptionList.tsx`, simplify-reuse returned two *high*-confidence findings: "radio-button class string duplicates ModePicker — extract a shared util" and (medium) "localStorage try-catch boilerplate duplicated across App.tsx/ConnectScreen." Both real *duplication*, but **neither was introduced by the story.** `git diff develop...HEAD` showed the OptionList radio `className` ternary byte-identical on `+`/`-` (only re-indented in the rewrite) — carried over unchanged; `ModePicker.tsx` wasn't in the diff at all; the storage logic wasn't touched. Applying them would have edited out-of-scope files (`ModePicker`) for zero in-scope benefit and real regression risk.
- **How to apply:** The simplify pass is scoped to **the diff**, not the whole file. For every reuse finding, run `git diff <base>...HEAD -- <file>` and confirm the flagged duplication is *introduced or modified by this story* — a `+` line whose identical `-` twin exists (a move/re-indent) is pre-existing, decline it. Cross-component "extract a shared util" suggestions that pull in files outside the diff are scope creep; decline and note as pre-existing. Same root cause as the 65-8 entry (confidence ≠ correctness), different tell: **in-diff vs pre-existing.**

### context-story line numbers drift — confirm seams before testing (73-4)
- context-story-73-4 cited `narration_apply.py:~3199` as the "beat_applied" emit; that line is actually **inventory consume** code now. The real `beat_applied` watcher emits are TWO sites: `~4070` (beat_selection path) and `~5792` (opposed_check path — the one the 73-4 repro hit). Always `grep -n "beat_applied\|apply_beat"` to locate the live seam; the context doc's line numbers are advisory and drift.
- **Player-bound confrontation payload SSoT:** `build_confrontation_payload` (`sidequest/server/dispatch/confrontation.py`), called from `websocket_session_handler.py:~1632`. The `beat_applied`/`beat_no_op` emits in narration_apply are **dev/OTEL watcher** events, NOT player-facing. To get a per-beat descriptor to the UI, the carrier is the **encounter** (shared snapshot state between narration_apply's apply_beat and the handler's payload build) — `build_confrontation_payload` already does this for the `win_condition`/`player_hp` legibility keys (the precedent to mirror).
- **opposed_check applies a beat per side** (player then opponent). A singular `enc.last_beat_*` is clobbered by the opponent's beat UNLESS the player's beat resolved the encounter (then opponent's apply_beat early-returns `skipped_reason="encounter_resolved"`). For player-facing per-beat readouts, store **per-side** (`dict[side, ...]`) so the player's beat survives a later opponent beat in the same turn.

### Confrontation actor SEATING — traps when testing who's-in-the-fight (59-35)
- **`withdrawn` is an `EncounterActor` field, NOT an `Npc` field** (`game/encounter.py:125` vs `game/session.py` `Npc`). Specs that say "seat scene-present, not-withdrawn NPCs" reference a field a `snapshot.npcs` roster entry doesn't have. Don't write a test pinning `npc.withdrawn` — it's vacuous. Flag the spec ambiguity as a finding.
- **The opponent location-fallback conscripts EVERY same-location NPC as `side="opponent"`** with no disposition filter (`_npc_fallback_at_location`, `adversarial=True`, `encounter_lifecycle.py:466`). It fires ONLY when `npcs_present` is empty. So an empty-`npcs_present` combat with a co-located friendly ally seats that ally as the *enemy*. To isolate a friendly/ally-seating test from the opponent path, pass an EXPLICIT opponent in `npcs_present` (non-empty ⇒ opponent fallback skipped). The empty-`npcs_present`-with-mixed-dispositions case is its own RED collision test — assert the friendly ends `side="player"` and the hostile `side="opponent"`.
- **`participant.joined` hardcodes `source="seat"` for ALL `side=="player"` actors** (the participant-join loop in `instantiate_encounter_from_trigger`); opponents use `seating_source` (`router_named`/`location_fallback`/`materialized`). Any NEW player-side seating source (e.g. `friendly_fallback`) must be distinguished from PC seats — assert the new `source` value explicitly; today it can only ever read `"seat"`.
- **Disposition fixtures:** `Npc(disposition=N)` coerces an int. `> 10` ⇒ FRIENDLY, `< -10` ⇒ HOSTILE, else NEUTRAL (defaults). Use `disposition=25` for FRIENDLY. `armor_class` defaults to 10 on `CreatureCore` — an NPC built without one still has a resolvable AC.
- **No hp_depletion confrontation in `test_genre`** — its `combat` is `dial_threshold`. For an hp_depletion path, load real `space_opera` via `tests/_helpers/genre_paths.find_pack_path` + `@pytest.mark.skipif(not GENRE_PACKS_DIR.is_dir())`, and give the PC a full SWN stat block (`Physique/Reflex/...`) so the seam can roll 1d8+DEX initiative. Pattern: `test_72_8_presence_last_seen_stamp.py`.

### Dice-path / WWN cast seam testing (102-2 RED)
- **`monkeypatch.setattr("random.randint", ...)` pins EVERY module's rolls at once** — `narration_apply`, `dispatch/damage_roll`, downed seam, reprisal all do `import random` and share the one module object. Use the global pin when the implementation might move which module rolls (e.g. a spine extraction story); use the module-path pin (`sidequest.server.dispatch.damage_roll.random.randint`) only when isolating one roll site from another in the same test.
- **`Stat` (protocol/types.py) is an open RootModel[str], not a closed enum** — any non-blank string uppercases and validates. Synthetic packs with flavor stat names (Lore/Might) pass `Stat(beat.stat_check)` in `dispatch_dice_throw`; don't waste time mapping to STR/DEX.
- **`build_confrontation_payload` has NO pack/catalog param** — it derives the recipient's WWN `SpellcastingState` (Task 5 gate) but cannot resolve spell display names. A spellcasting projection contract can only carry what the state has: prepared spell-id strings + casts. If a story needs names on the wire, that's a signature change — flag it, don't silently spec it into a RED test.
- **`session_handler_factory(genre=...)` loads any real pack by slug** (real search paths at call time) — heavy_metal works for handler-level wiring tests; guard with `GENRE_PACKS_DIR.is_dir()` skipif. The factory's char is "Rux"/Fighter with EMPTY stats — hydrate `char.stats` AND `core.spellcasting` yourself before driving a WWN seam.
- **MagicMock packs break `dispatch_dice_throw`** (it iterates `pack.classes` for the WWN warrior gate; a MagicMock attr isn't iterable). The synthetic-pack rig from `test_wwn_cast_dispatch.py` only works for direct spine calls (`_resolve_wwn_cast_for_beat`); for the dispatch seam use a REAL pack.
- **UI App-level beat-commit harness:** `combat-player-echo-wiring.test.tsx` is the canonical copy source (GameBoard stubbed to a vi.hoisted prop trap + jest-websocket-mock + CHARACTER_CREATION/SESSION_EVENT boot). No R3F mocks needed as long as the stub keeps App off the canvas. Assert outbound frames via `server.messages.filter(type === MessageType.DICE_THROW)`.
- **Wire-shape regression direction that matters:** old-client frame WITHOUT the new optional field must validate (server-side `model_validate` on the keyless dict), and new non-cast frames must NOT grow the key (`"spell_id" in payload === false` client-side). Pin both ends; pydantic `extra_forbidden` makes the missing-field RED loud.
- **RED tests can pass by kill-coincidence — check pins against TODAY's behavior, not just target behavior.** 102-4: "no opponent reprisal on a sealed commit" pinned rng MAX so the player's 2d6=12 killed the 10-HP opponent in today's immediate-resolution flow → reprisal skipped → test vacuously GREEN in RED. Pin MIN instead: the player misses, today's reprisal still emits `encounter.opponent_attack_resolved` (it records miss math), and the span assertion becomes the genuine RED driver. Rule: for every new RED test, trace what TODAY's code does under the pinned rng before trusting the failure list — testing-runner reports tell you WHICH tests failed, you must ask why one passed.

### Fixtures that dispatch during setup must DEPEND on otel_capture (102-4, 2026-06-10)
- pytest instantiates fixtures in signature order: `(my_fixture, otel_capture)` runs the dispatch before the in-memory exporter installs, making span assertions structurally unsatisfiable while a sibling test asserting the same span inline passes. Put `otel_capture` in the fixture's own parameter list. Symptom: `finished_spans: []` for one consumer, spans present for another.
- Rework rounds can mix ONE true RED driver with green-by-design regression locks (ledger reset, double-commit rejection, MP wire proof) — document which is which in the docstrings so the tests-fail gate and Dev both know the contract.

### Chargen handler-walk wire tests: beneath_sunden connect reaches the real SDK — use flickering_reach (106-1, 2026-06-13)
- Driving `_chargen_confirmation` through `handle_message` (45-2 shape) for `caverns_and_claudes`: the WWN megadungeon world `beneath_sunden` runs ADR-106 connect-time init that constructs the real Anthropic SDK → the autouse `conftest.py::_no_real_anthropic_sdk` guard raises `LlmClientError` on `handle_message(connect_msg)` BEFORE chargen even starts. Use `flickering_reach` (the proven-hermetic world `test_45_2_chargen_to_playing_wire` walks). Safe to swap when the behavior under test is GENRE-tier (chargen loadout, kit-roll, inventory armor_class all live in the genre pack, not the world) — note the world is incidental in the test docstring.
- Hermetic chargen walk recipe: seed an empty MP save via `_build_pg_repos_for_slug`, `RoomRegistry()` + `attach_room_context`, SESSION_EVENT connect (assert `payload.event=='connected'`), PLAYER_SEAT, then loop CharacterCreationMessage choosing `"1"` on choice scenes / a name on freeform / `phase="continue"` on auto-advance until `builder.is_confirmation()`, then `phase="confirmation"`.

### "Already-delivered" stories: write a characterization test FIRST, then reframe as coverage (107-1, 2026-06-13)
- 107-1 ("dungeon scene/location advance: discovered_rooms/scene_id/region_transitions frozen, render under-fires") turned out to be ALREADY FIXED. Its forensics were dated the SAME DAY as `be4f7464` (#835, "in-dungeon movement for region-mode worlds") — the fix that made the procedural navigator reachable in-dungeon, which unfroze `current_region` → the render gate fires → the descent stops reading as one scene. **Always diff the story's forensics date against recent merges in the touched subsystem before writing RED tests.**
- The story named the WRONG fields. The procedural ADR-106 dungeon is REGION-based: a move advances `discovered_regions` / `current_region` / `region_transitions` (via `apply_world_patch` consensus-sync `session.py:1546` + `frontier_hook.notify_region_transition`). `discovered_rooms` is a separate room-graph field only filled at chargen, and **`GameSnapshot` has NO `scene_id` field at all** (`list_npcs_in_scene.py`/`query_scene_state.py` say so). Don't pin RED tests to a field name from the story title without grepping that the field exists and is the one the mechanism uses.
- The render chain for a PROCEDURAL room: materializer `dungeon/room_yaml_emit.py::write_room_yaml` writes `<world>/rooms/<region_id>.yaml` (region ids like `exp001.r1`) → `_maybe_emit_location_description` Path 1 (`load_room_payload`) sources it → fresh LOCATION_DESCRIPTION. The helper's two sources are static room YAML + `cartography.regions`; procedural rooms ride the FIRST via the materializer's YAML emit (NOT cartography — procedural ids aren't authored regions).
- When verification comes back GREEN, the genuine gap is usually TEST COVERAGE, not behavior — the deliverable becomes the regression tests themselves. Drive real components (real `run_movement_dispatch`; real `write_room_yaml` → real `load_room_payload` → real `_maybe_emit_location_description`), assert DISTINCT per-room output (region_id + prose differ) so the test actually catches "one frozen scene reused". Ask the user the disposition (cancel vs ship-as-coverage) — don't unilaterally cancel a sprint story.
- Content-free movement fixture: synthetic `RegionGraph` (add_node/add_edge) + a fake store exposing `load_map(entrance_id=)`/`load_frontier()` + a fake palette whose `.get(theme_id)` returns `display_name`+`narrator.{register,flavor,motifs}`. Mirrors `tests/agents/subsystems/test_movement_dispatch.py`. `run_movement_dispatch` success applies the per-PC patch so discovered_regions/region_transitions/current_region all advance as a side effect — assert them (the old tests only asserted `pc_regions`).

### Coverage-test docstrings must not overclaim — the Reviewer treats it as a lying-docstring (107-1 rework, 2026-06-13)
- The 107-1 integration test's docstring said it "verifies the full per-room render chain **end-to-end**" and "the per-room render **is wired**". Avasarala REJECTED: the test calls `_maybe_emit_location_description` directly with `room_id_override`, bypassing the production turn-handler gate (`_is_region_mode_world` + `_region_changed` at websocket_session_handler.py:2257-2287). Green only proved the helper can SOURCE a procedural-room YAML. In a lie-detector-culture project the comment-analyzer flags overclaiming docstrings with high confidence and the Reviewer cannot dismiss them. **Write docstrings that state what GREEN proves AND what is out of scope/deferred** (here: the gate firing per room → epic live playtest). A direct-helper-call test is fine; calling it "end-to-end" is not.
- **`assert <list>` (truthy) violates the meaningful-assertion rule (lang-review #6 / CLAUDE.md A4).** Use `assert len(x) == N` to lock the count. The Reviewer cannot dismiss a rule-matching finding (only downgrade severity), so a truthy-list assert WILL bounce the review. Always count.
- **A `MagicMock` world fakes a cartography region and masks no-source tests.** `_maybe_emit_location_description`'s fallback does `world.cartography.regions.get(room_id)` — on a `MagicMock` world that returns a truthy MagicMock, so `sourced=True` and it EMITS even for a room that doesn't exist. For a no-source/negative test set `world.cartography = None` (mirror `test_location_description_emit.py::test_emit_fires_no_source_when_neither_path_resolves`). The POSITIVE test is safe with a default MagicMock world only because `load_room_payload` succeeds and the fallback is never reached.
- **Split-party negative for current_region:** two seats, one moves, one stays → `region_for()` (no perspective) returns None (no consensus) → the apply_world_patch consensus-sync (session.py:1546) does NOT advance `current_region`; but the per-PC move STILL appends `discovered_regions` + logs `region_transitions` (shared fog-of-war, frontier_hook line 152 is unconditional). Assert both halves — it's the negative contract a single-PC test structurally cannot show.

## 108-6 WWN dying window (2026-06-14)

- **Weak-test trap on incapacitation gates.** A "downed soloist can still act" wiring
  test can pass *today* for the wrong reason: the current WWN dying-window status is
  minted NON-incapacitating, so `find_incapacitating_status` returns None and the gate
  trivially lets it through. The real solo-halt only exists once the window is
  *incapacitating*. Fix: assert `window.incapacitating is True` as the test premise, so
  the test actually exercises the gate carve instead of a degenerate fall-through.
- **`apply_post_resolution_lethality` early-returns** unless `encounter` is resolved with
  a PC-down outcome (`post_resolution_lethality.py:208-211`). Any per-turn clock/expiry
  homed there will NOT fire on a normal player-action turn (a stall) — so "clock can't be
  paused" must be wired on the player-action path. The handler has cfg via
  `session._session_data.genre_pack` (`player_action.py:429`), so the bound
  `cfg.trauma.*` is reachable at the gate. Test the OUTCOME via `PlayerActionHandler.handle`,
  not the function, to avoid coupling and to force the requirement onto the right path.
- **Grounded harnesses for WN lethality tests:** span capture = local `InMemorySpanExporter`
  + `_tracer=` (test_142_wn_lethality_spans.py); `resolve_downed` = `WwnRulesetModule()` +
  `WwnConfig(attribute_map=...)` + `CreatureCore` (test_wwn_lethality.py); downed seam =
  reuse `_make_reprisal_pack`/`_make_snapshot_and_encounter` from
  test_reprisal_wn_downed_seam.py (ablate the opponent core to 0 HP for the no-live-hostile
  case); gate = `_playing_session`/`_action_msg`/`_ReachedNarrationPath` sentinel from
  test_player_action_incapacitated_gate.py; stabilize tool = `pg_store_with` +
  `default_registry._tools[...]` direct-handler call.
## Verify pack disk-state before trusting a design's "the packs are migrated" claim (114-10)

The 114-9 design said "apply the Fate gear model to the four packs" and its migration
section assumed all four were already `ruleset: fate`. On disk only **pulp_noir** was —
spaghetti_western / tea_and_murder / wry_whimsy were still native d20 (point-buy,
`stat_ranges`, `typical_classes`), and their fate migrations (121-3/4/5) + the
Fate-archetype shape (121-7) were all backlog. Always `grep ^ruleset:` the real
`rules.yaml` (and check `archetypes.yaml` shape) before writing migration tests; a
"5-point apply" story can hide a ~30-point absorbed migration. Surface it as a blocking
Delivery Finding + ask the Operator the scope question — don't silently pick.

## Pin validator invariants as PURE functions when the model they validate doesn't exist yet

114-10's refresh-invariant / dangling-id checks validate a Fate-archetype shape that is
121-7 territory and undefined on disk. Writing `check_refresh_invariant(*, authored_refresh,
base_refresh, free_stunts, total_stunts) -> str|None` over primitives (not over an archetype
object) kept the tests refactor-stable and let Dev choose field names. Prove the wiring
separately via an integration test that runs the real validator on real content.

## "Closes the F2b deferral" means the deferred work IS in scope — don't let an Explore scout's reading of the EARLIER story's "stateless" docstring shrink it (118-5 RED, 2026-06-16)

118-5 ("F3e — compel accept/refuse round-trip; *closes the F2b deferral*") flagged that
F2b's `propose_fate_compel`/`offer_compel` fires `fate.compel.offered` but **persists
nothing** (the flow is stateless). A server Explore scout read F2b's docstring ("no economy
change happens here … acceptance lands with the F3 UI round-trip") and concluded **118-5 is
also stateless — no PendingCompel needed.** That was wrong: the *whole point* of "closes the
deferral" is that the deferred PendingCompel persistence lands in THIS story. The session
AC (highest spec authority) + the epic's "reactive FATE_STATE projection" mandate both
require it (the UI can't show a compel the server doesn't remember). **Tell:** when a title
says "closes the X deferral / picks up the F-something follow-up," the earlier story's
"stateless"/"deferred" docstring describes the EARLIER story, not yours — the deferred work
is your deliverable. Trust the session ACs over a sub-agent's scope inference.
- **Concrete anchors for the compel round-trip (what already existed vs the real gaps):**
  `FateRulesetModule.accept_compel` ALREADY existed (earn +1 FP + `fate.compel.accepted`
  span, `ruleset/fate.py:314`) — "wire up what exists," don't re-author it. The genuine
  gaps were: `refuse_compel` (spend 1 FP via the existing fail-loud `spend_fate_point` +
  a new `fate.compel.refused` span), `offer_compel` persistence (it takes no `encounter`
  today), `FateConflictEntry.pending_compels` on the projection (`fate_projection.py`
  `build_fate_state_payload`), the `compel_accept`/`compel_refuse` members on the
  `FateActionPayload.action` Literal (`protocol/fate.py:45`), a `fate_point_delta` field on
  the frozen `FateDispatchResult`, and the dispatch routing (route early like `concede`).
- **Refuse-at-0 is the load-bearing fail-loud test:** `spend_fate_point` already raises
  `FateEconomyError` at 0 (validate→mutate→emit), so `refuse_compel` must reuse it and the
  `fate.compel.refused` span must NOT fire on a rejected refusal. Pin both (raises + no span).
- **The UI compel control inherits the surface's existing `ruleset!=="fate"` / no-active-
  conflict empty-DOM gate** (`FateConflictSurface.tsx:98-100`) — so the epic's required
  "never co-renders with ConfrontationOverlay" paired negative is satisfied by the gate
  already; the new negatives to add are "no control when `pending_compels` empty." The
  board-level production-path wiring test reuses the `renderBoard` harness from
  `GameBoard-fate-tab.test.tsx` (no R3F mocks needed if you don't pass a `fateRoll`).

## When a Reviewer offers an either/or fix for a dishonest error path, pin the STRONG option by ERROR ORIGIN, not exception type (119-4 RED rework, 2026-06-16)

119-4's broad `except Exception` in `complete_with_tools` re-raised ANY in-loop error
(a parse `IndexError`, a `CLINotFoundError`, a timeout) as
`AgentSdkAuthUnavailable("subscription login absent or expired")` + a
`narrator.auth_unavailable` event — a false "login expired" on the very GM panel the
story exists to make honest. The Reviewer offered an **OR**: narrow the catch *or* keep
it broad with a non-committal message. Two RED-design lessons:

- **Forbid the weak option deliberately.** "Keep it broad + soften the *message*" still
  leaves the event NAME (`narrator.auth_unavailable`) and the exception TYPE
  (`AgentSdkAuthUnavailable`) asserting auth — the panel still shows an auth event for a
  parse bug. So the honest contract is: a non-auth/internal error must emit **no auth
  event and no `AgentSdkAuthUnavailable` at all.** Pin that (assert NOT isinstance auth +
  assert the event never fired). It forces the strong fix and rules out the cosmetic one.
- **Discriminate by ORIGIN (transport vs our loop body), not by exception TYPE.** The
  existing fakes raise a bare `RuntimeError` to mean "the `query()` transport failed = an
  absent login (OQ-5 → maps to auth)." A *type*-narrowed catch (`except ClaudeSDKError`)
  would stop catching that `RuntimeError` and break the AC1' query-raised test. The
  reconcilable contract: **transport boundary raises → auth+event (AC1'); our own
  message-processing raises → propagates raw, no auth signal.** To pin "our body raised,"
  monkeypatch the first per-message call (`_is_agent_result_message`) to raise a sentinel
  `ValueError` — an unambiguously INTERNAL fault the transport-catch must let escape. This
  also tells Dev the fix is a **loop restructure** (separate `__anext__` from body
  processing), not a type-narrow.
- **Comment-honesty fixes are NOT test-pinnable** (no source-text grep — house rule). Flag
  them as a Delivery Finding so Dev applies the Reviewer's [LOW] DOC cluster by hand; a
  green suite will not catch a stale comment.
- **Assert cause survival robustly:** `isinstance(raised, ValueError) or
  isinstance(raised.__cause__, ValueError)` accepts either a raw propagate or an honest
  `raise NonAuthError(...) from exc` wrap — don't over-pin the exact wrapper type.

### A fully-scripted plan's test code carries APPARITION SIGNATURES — ground every call against the canonical harness before trusting it (126-8 RED, 2026-06-18)
126-8 arrived with an 8-task server plan that wrote out *complete* test code per task. Convenient, but ~4 of its API shapes did not match the real codebase, and copying them verbatim would have produced tests that fail at fixture construction (wrong reason), not on missing production:
- `ThrowParams(position=.., velocity=.., angular=.., spin=0.0)` — **no `spin` field**; real shape is `velocity:(f,f,f)`, `angular:(f,f,f)`, `position:(f,f)` in `sidequest.protocol.dice` (plan said `protocol.models`). `ProtocolBase` is `extra="forbid"` so the bogus `spin` would have ValidationError'd.
- `StructuredEncounter(encounter_type="conflict")` alone — **`player_metric`/`opponent_metric` are REQUIRED** (no defaults). The ledger tests would have failed on validation, masking the real "no `pending_defenses` field" RED.
- Handler/wiring tests calling `session.handle_message(throw_msg)` — the canonical Fate harness (`tests/server/test_fate_throw_handler_wiring.py`) drives `HANDLER.handle(session, msg)` directly on a **`SimpleNamespace` session double** (`_state=_State.Playing`, `_session_data=sd`, `_room=SessionRoom`) and drains the room's `asyncio.Queue`; the registry wiring is a SEPARATE reflection assertion (`WebSocketSessionHandler._message_handler_for("FATE_THROW") is HANDLER`). A SimpleNamespace has no `handle_message`.
- `_build_turn_context(session, sd)` — real signature is `_build_turn_context(sd, *, room=...)` and it reads `sd.genre_pack.rules.confrontations` (so the SimpleNamespace `sd.genre_pack.rules` needs `confrontations=[]`, not just `ruleset="fate"`).
- **Tell:** the plan author writes the test against the API they *intend* to build, not the one on disk. For every constructor/call in a handed-down plan, run one of (a) `grep` the real model/signature, (b) read the nearest sibling test that already drives that seam. The canonical sibling for Fate dice-path handler tests is `test_fate_throw_handler_wiring.py`; for dispatch-layer, `test_fate_conflict.py` + `tests/game/ruleset/_dispatch_fixture.py`.

### Fate opponent targeting is DETERMINISTIC — a 1-PC/1-healthy-NPC fixture reliably parks (126-8)
`decide_opponent_action` (`game/fate_opponent.py:129`) picks the highest-threat live PC (finish-the-wounded → retaliate → highest-threat, seating-order tiebreak) and returns an attack — it never random-targets. So at REVEAL `_seat_opponent_commits` seats the NPC attacking the PC every time, and the DEFEND barrier parks deterministically with NO rng pin needed. To build the *immediate-resolve* (no-park) variant, seat NO live opponent and have the PC `overcome` a passive difficulty — nothing targets a PC, so `_build_pending_defenses` returns `[]`. Healthy vs depleted opponent is the park/no-park switch: a fresh `FateSheet()` survives REVEAL and attacks; the `_depleted_thug` (all stress checked + consequences filled) gets taken out and the conflict resolves instead.

### `sanitize_player_text(None)` returns "" — the optional-field sanitize landmine (118-9)
`protocol/sanitize.py::sanitize_player_text` starts `if not text: return ""`. So `sanitize_player_text(None)` returns `""`, NOT `None`, with no crash. When the field being sanitized is `Optional[str]` (e.g. Fate `payload.target`, where `None` means "passive action, no opponent"), a naive `sanitize_player_text(payload.target)` silently coerces `None → ""`, flipping a passive action into a broken active one downstream (`_opposition_total` treats a non-None target as a real defender → `find_creature_core("") → raises`). When a sanitize fix lands on an optional field, ALWAYS write a TRIPWIRE test asserting the `None` input is preserved as `None` (`test_passive_action_preserves_none_target`). It's green today and goes red the instant Dev does the naive thing. Don't trust the happy-path injection test alone.

### The standalone "Fate" dock tab hosts TWO surfaces — never "just remove" it (126-19 PART B)
A playtest finding said "remove the duplicate Fate dock tab; the sheet is already under Character→Stats." TRAP: the dock `fate` widget (`widgets/FateWidget.tsx` → `FatePanel`) renders the duplicate sheet AND story 118-7's **non-conflict 4dF roll tray** (`FateDiceTray`, testid `fate-dice-tray`, gated on `latestRoll`). The Character→Stats surface uses `FateCharacterSheet`, which takes NO `latestRoll` → has no tray. So removing the tab wholesale silently loses the out-of-conflict roll surface and reds `GameBoard-fate-roll.test.tsx`. (The Fate Conflict tab has its own tray, so IN-conflict rolls survive — it's the OUT-of-conflict roll that dies.) When a "remove a duplicate tab" story lands, grep the widget for every gated child (`grep -nE "latestRoll|FateDiceTray|conflict"`), not just the obvious one. Outcome: split into PART A (chrome suppression) + a PART B follow-up that re-homes the roll tray.

### The UI's bound-ruleset signal is `fateData != null` at GameBoard — and it does NOT reach HpPipScale (126-19 PART A)
There is NO `getRuleset()`/`isFateRuleset()` helper in the UI. The only Fate-vs-WN signal is `GameBoard.fateData` (the FATE_STATE projection; server emits it only on a `ruleset=='fate'` pack). Established suppression pattern: `showCurrency={fateData == null}` on the inventory widget (`GameBoard.renderWidgetContent`). CharacterPanel already receives a name-matched derivative (`fateSheet = fateData.characters.find(c => c.name === sheet.name)`) — but `HpPipScale` (the input-bar HP readout) gets NOTHING ruleset-related, so any "suppress native chrome on Fate" story must thread the signal there as green-phase WIRING, not CSS. For the branch test, drive `fateData` at the GameBoard seam (real prop, proves wiring, no prop-widening needed) and ALWAYS pair Fate-suppress with a WN-keep assertion — a one-sided test green-lights a global delete that breaks all 7 WN packs (SOUL "Bind the Ruleset, Don't Balance It"). Add a name-mismatch case to forbid keying off `fateSheet` alone (fragile → leaks). Surface→testid map: header `character-level-badge`+`character-edge-badge`; body pill+pips `character-edge-ticks`; party row `party-member-edge-{id}` + `{class} Lv.{level}` text; action bar `input-hp-scale`.

### `pf handoff` arg-inference breaks when the session has bold field-like labels — pass explicit args
`pf handoff resolve-gate / complete-phase / marker` infer `story/workflow/phase` from the active session file. A bold label in an Assessment section that LOOKS like a field key (e.g. `**Workflow choice:**`, and apparently any cluster of `**X:**` lines) makes inference fail with "Active session ... is missing story/workflow/phase fields — cannot infer arguments." Renaming the one offending label was NOT enough. FIX: pass args explicitly — `pf handoff resolve-gate 126-19 tdd <phase>`, `pf handoff complete-phase 126-19 tdd <from> <to> <gate_type>` (gate_type comes from resolve-gate's RESOLVE_RESULT), then `pf handoff marker <next_agent>`. Faster than hunting the offending line.

### A spec/plan's "this field is DEAD / no consumer" claim must be grep-verified before writing removal tests (153-1)
The IntentRouter output-slim spec (`2026-06-20-intent-router-output-slim-design.md`) and its plan both classified `action_rewrite.you` as **DEAD — "Read by: nothing"** in the consumer table, and the plan's self-review repeated it ("`.you` had no consumer"). It had TWO live readers: `intent_router.py:543` (`ar_emitted = bool(ar and (ar.you or ar.named or ar.intent))`, runs on every successful `decompose`) and `orchestrator.py:3725` (`ActionRewrite(you=pre_pass.you, ...)`, the pre-pass→NarrationTurnResult copy). Removing the field as written would `AttributeError` on every acting turn — the spine-goes-dark failure the whole story exists to prevent. ALWAYS `grep -rn "\.<field>\b" sidequest/` for every field a slim/refactor calls dead, BEFORE trusting the table; the RED test that catches it is one that drives the REAL `decompose` with the new shape and asserts `not hasattr(pkg.action_rewrite, "you")` (the field-removal AND the read-site fix both have to land or the test errors). Secondary trap: there were TWO `ActionRewrite` classes — `protocol/dispatch.py:253` (the pydantic schema field, in scope) and `agents/orchestrator.py:456` (a separate dataclass with `from_dict`, guarded LIVE by `test_orchestrator.py:822`, OUT of scope). Resolve which class each consumer touches before scoping the cut — `pre_pass.you` reads the protocol one, `ActionRewrite(you=...)` constructs the orchestrator one.

### Removing a model field orphans its tests AND its straggler path — handle BOTH in RED (153-1)
When a story removes a pydantic field that the model has `extra="forbid"`, two things break that the implementation plan often forgets: (1) existing tests that CONSTRUCT the old shape (`PlayerDispatch(lethality=[...])`) error at validation, not just at assertion — and tests for behavior being DELETED (the arbiter merge loop) can't be "made green," they must be removed (delete the test + its now-unused helper + import). (2) Haiku will still emit the removed key out of habit, so `extra="forbid"` would reject the WHOLE package unless a `@model_validator(mode="before")` strips+logs it (mirror `_coerce_stringified_lists`). Write the straggler-strip test in RED (`PlayerDispatch.model_validate({...,"resolved":[...],"lethality":[...]})` validates, drops the keys, logs "stripped_deprecated") so Dev can't ship the cut without the tolerance. The field-order assertion (`list(Model.model_fields)[0] == "..."`) is the unit-level proxy for a "generation-order is latency-order" reorder — it's the only part of a latency AC that IS unit-testable.

### Invented-NPC routing tests MUST use a name-shaped stranger — the epithet guard eats article-led mentions (126-38)
`_is_descriptive_epithet` (narration_apply.py:2147) returns True for any mention whose name is article-led ("A ...", "An ...", "The ..." — `_EPITHET_ARTICLES`). At the `_apply_npc_mentions` call site, such mentions are PRESERVED VERBATIM via `npc_epithet_preserved_span` and are NEVER routed through `_resolve_invented_naming_context` — so **zero** `npc.invented_name_routed` spans fire. My first 126-38 behavioral tests used `_mention("A dusty stranger")` and failed on "exactly one ... span must fire; got 0" — which looks like a missing-feature RED but is actually the test never entering the mint path. Fix: use a name-shaped, two-token proper name that matches NO bound culture by name/alias (e.g. `"Cutter McGraw"`) so it clears the epithet guard AND misses the 83-2 self-match, landing in the shuffle/region path under test. NB the guard is at the CALL SITE only — unit tests that call `_resolve_invented_naming_context` directly bypass it, so the article-led-vs-name-shaped distinction only bites behavioral/`_apply_npc_mentions`/wiring tests. Verify behavioral span tests RED on the *feature* assertion (strategy/region attr), not on "got 0".

### Additive-field RED differs by the model's `extra` policy — `extra="allow"` content models absorb the not-yet-declared kwarg, so a round-trip test passes SPURIOUSLY (157-2)
Epic-157 adds `factions: list[str]` to three content models, but they carry DIFFERENT `model_config` extra policies, which changes what a RED test can prove:
- `ManualEncounter` / `ManualNpc` are `extra="forbid"` → `ManualEncounter(..., factions=[...])` raises `extra_forbidden` TODAY → clean construction RED (and `enc.factions` AttributeErrors when not passed). This is the loud, reliable RED.
- `BestiaryEntry` is `extra="allow"` (it carries SRD color via extras) → `BestiaryEntry(..., factions=[x])` does NOT raise; pydantic stashes `factions` as an EXTRA and `.factions` returns `[x]`. So an additive-field **round-trip** test (`entry(factions=[x]).factions == [x]`) is GREEN before the field is ever declared — a false RED that proves nothing.
- **Tell / fix:** before trusting an additive-field RED, `grep "model_config" <model file>` for the target. For an `extra="allow"` model, anchor the RED on the assertions extras CAN'T fake: (1) the **default-when-not-passed** test (`entry().factions == []` — today an AttributeError because no extra is set, RED for the right reason; post-field it's `[]`), and (2) the **per-instance isolation** test (lang-review #2, mutable default leak). The round-trip test still belongs in the suite as a post-green correctness lock, just don't count it as your RED driver. (Companion to the `extra_forbidden`/`model_copy` notes above — same family, the `extra="allow"` direction is the one that bites silently.)

### Design-gated RED: pin the provisional binding in ONE helper, keep assertions binding-agnostic (126-38)
When a story is DESIGN-GATED (the data home isn't decided — here region→culture: per-culture `regions` field vs cartography `Region.cultures` vs world-default), don't refuse to write RED and don't silently pick for the Architect. Split the suite: (1) binding-AGNOSTIC tests that hold regardless of the choice (signature accepts the new context kwarg; the no-context legacy path is preserved; the production path threads the value to the OTEL span — assert only the span *attribute*, not the storage) and (2) design-gated positive-routing tests that pin a *provisional* binding isolated in a single helper (`_bind_region` here, via `Culture.model_copy(update={"regions":[...]})`) with a loud docstring + a BLOCKING delivery finding naming the relocation point. Precedent: 83-2 added `Culture.aliases` and read it off the same `effective_cultures(world)` list — mirroring that (option "field on Culture") minimises new read-paths, but flag the ADR-140 counter-argument (cartography is world-owned; a region naturally declares its inhabitants) so Architect actually rules. Gotcha confirmed: pydantic v2 `model_copy(update={...})` does NOT re-validate, so it sets a not-yet-declared field on an `extra="forbid"` model without raising — good (binding tests reach their real assertion) but it means the *construction* RED has to come from a direct `Model(..., newfield=...)` call, which DOES raise `extra_forbidden`.

### Movement-resolver RED: parallel edges to the SAME node are FALSE ambiguity, and bearing words in a phrase pre-empt the ordinal/descriptor path (153-22, 2026-06-21)
- The dungeon movement bug (`movement.py::_resolve` + `region_projection.py`) was two defects: (a) `assign_bearings` keys its bearing dict by `to_region_id`, so three parallel `a→b` edges collapse onto ONE bearing and `project_region` emits three identical `RegionExit(to="b", bearing=same)`; (b) the descriptor token-overlap path then sees three tied `b`-candidates and returns `ambiguous_descriptor` — even though all three lead to the SAME node. **The design-agnostic RED contract: a descriptor tie whose tied candidates all share one `to_region_id` is FALSE ambiguity and must resolve; a tie spanning ≥2 distinct nodes is GENUINE and must still fail loud.** Always write BOTH — the resolve test AND the distinct-node guard — or the dev "fixes" it by collapsing everything and silently picks a fork (violates AC "genuine ambiguity still fails loud").
- **The phrasing trap that makes an ordinal RED test lie:** `requested_bearing()` runs FIRST in `_resolve` and matches any compass/vertical token (`north/east/south/west/up/down/descend/...`). The verbatim playtest phrase was "the leftmost of the three **west** passages" — but "west" routes through the bearing path, NOT the ordinal bridge you're trying to test, and resolves coincidentally (or fails as `no_candidate_edges`) depending on the blake2b bearing assignment → non-deterministic RED. **Strip bearing words from ordinal-bridge test phrases:** use "the leftmost passage" / "the first passage" / "the second passage". Also note `_tokens` is exact-match, so plural "passages" does NOT match the `corridor` synonym "passage" (singular) — pick the singular if you want the token to score.
- **Deterministic RED levers that don't depend on depth:** drive `run_movement_dispatch` with `exit_descriptor` ONLY (direction="") — the descriptor path is pure token-overlap, so node `depth_score` is irrelevant and the test is reproducible. To force the descriptor to match only your target edges and not the entrance edge, give the entrance edge a different kind (e.g. reach `a` via a `shaft`) so "the corridor"/"the passage" scores 0 on it. `RegionGraph.add_edge` permits parallel multi-edges (rejects only self-loops + unknown endpoints), so `_graph_with` happily builds three `("a","b","corridor",False)` rows.
- **Design-agnostic AC-1 at the projection level:** assert `len({(e.to_region_id, e.bearing) for e in proj.exits}) == len(proj.exits)` — passes whether the dev fixes via distinct-label (3 distinct bearings) OR dedup (1 exit). But flag as a Design Deviation that this pins the fix to TOUCH `region_projection.py` (a materializer-only dedup leaves `project_region` collapsing a hand-built parallel-edge graph) — the dedup-vs-distinct-label call is a Dev/Architect decision per ADR-106 (parallel edges may be legit Jaquaysed loop geometry). See [[story_153_22_movement_parallel_edges]].

### The 108-2 roster reconciliation conscripts a co-located adversary under FATE combat — it is category-gated, NOT ruleset-gated (153-9 RED, 2026-06-21)
- `_resolve_opponent_from_roster` (`encounter_lifecycle.py:874`) is called from `instantiate_encounter_from_trigger` at **line 1389**, which is BEFORE `is_fate` is computed (line 1671). Its only decline gate is `if confrontation_category != "combat": return None` (line 939, the 150-2 non-combat fix). So for a **Fate combat** confrontation (category `combat`) it conscripts ANY co-located, `creature_id`-statted, hostile NPC over the router-named scene-active antagonist — the [FATE-OTHER-SEATING] bug. The 108-2 reconciliation exists to preserve a BOUND bestiary creature's COMBAT hp (ADR-059), which is meaningless under Fate (resolves on FateSheet stress, not hp_depletion). The fix is the **Fate sibling of the 150-2 decline**: decline conscription under a Fate binding regardless of category. Pin observable behavior through the public seam — the gate may go in the helper OR at the call site, so don't couple the RED test to the helper signature.
- **There is NO `ruleset: fate` fixture pack** anywhere under `tests/fixtures/` (the `spaghetti_western` *fixture* is `ruleset: dial`; the live spaghetti_western/pulp_noir *content* packs are `fate`). To drive `instantiate_encounter_from_trigger` end-to-end under Fate, load the **live `pulp_noir`** content pack via `tests/_helpers/genre_paths.find_pack_path` + `@pytest.mark.skipif(not _has_pack)` — the test_121_2 pattern. Confirmed: the full Fate-seeding path (`_seed_fate_opponents` at line ~1820) COMPLETES with the live pack (no fate-config crash), so an instantiate-level Fate seating test is feasible without authoring a fixture. Pick the combat type dynamically (`next(c for c in pack.rules.confrontations if c.category=="combat").confrontation_type`) so a content rename can't break it; assertions stay structural (named-Other vs conscripted-Other), so content drift can't redden them.
- **OTEL contract for the decline:** reuse the existing `encounter.roster_resolution_skipped` span (with `declined_name`) the 150-2 non-combat decline already emits, and assert the 108-2 `encounter.opponent_resolved_from_roster` span does NOT fire. The "resolve span absent" half is name-agnostic and robust; the "skip span present" half couples to the span name — note it for Dev so a Fate-specific span name gets the test updated. See [[story_153_9_fate_other_seating]].

### Chargen background/foci content stories: the test lives in sidequest-server, and the 153-4 spread helper SILENTLY skips the catalog seam (153-33, 2026-06-21)
- A content-authoring chargen story (author `backgrounds.yaml`/`foci.yaml`/`skills.yaml`) has its repo set to `content`, but **sidequest-content has no Python harness** (no `pyproject.toml`/`tests/`). The behavioral wiring test that proves the content flows through `CharacterBuilder.build()` MUST live in `sidequest-server/tests/integration/` on a *matching* `feat/...` branch cut from server `develop`. So the story is two-repo: content YAML + server test. Log it as a deviation; warn SM-finish + Reviewer that **both** branches merge. (Precedent: 153-4 spread test, heavy_metal chargen test — both server-side, both test content.)
- **The trap:** the obvious template, `test_153_4_swn_chargen_spread_wiring.py::_build_first_choice_character`, builds WITHOUT `.with_chargen_defs(...)`. Backgrounds/foci are only loaded into the builder via that fluent call. Copy that helper verbatim and your background/foci assertions fail for the WRONG reason (catalog never attached, not "content missing") — and worse, would pass spuriously if the catalog were attached elsewhere. **Mirror production `connect.py:968-973`:** `with_chargen_defs(backgrounds=resolve_backgrounds(pack, world), foci=resolve_foci(pack, world))`. Imports: `resolve_backgrounds`/`resolve_foci` from `sidequest.server.dispatch.chargen_defs_resolve`, `resolve_classes` from `...class_resolve`, `resolve_char_creation_scenes` from `...char_creation_resolve`.
- **Clean RED, even pre-content:** the contribution methods are designed to ALWAYS emit their OTEL span (lie-detector). With no content, `swn.chargen.background_skills` fires with `skills="{}"` + `reason="no_matching_background_def"`, and `swn.chargen.foci_applied` fires with `foci="[]"`. So anchor the RED on the **span payload being non-empty** (`json.loads(span.attributes["skills"])` truthy) + that payload ⊆ `char.skills`/`char.foci` — not on the span's mere presence (it's always present). Span attrs are JSON strings (`_json.dumps`), parse them.
- **`focus_id` is the foci seam, and it's per-scene-choice:** `MechanicalEffects.focus_id` (singular) on a `char_creation.yaml` choice is what populates `acc.foci`. Authoring `foci.yaml` alone grants nothing — Dev must ALSO wire `focus_id` into a choice-bearing scene per world (heavy_metal attaches it alongside `background:` on `origins` choices). A foci RED test forces this; without it the foci AC is a half-wire.
- **Per-world `background:` tags vs "genre-tier only":** space_opera's three worlds reference 11 DISTINCT `background:` ids in their scenes (only "Void-born" shared). A single genre-tier `backgrounds.yaml` must cover all 11, or each world ships a world-tier file (scope often defers this). Parametrize the wiring test over every live world (`@pytest.mark.parametrize` worlds) so a one-world fix can't pass — and file the count discrepancy as a Conflict finding for Architect. The validator's CG1/CG2/CG3 (`validate_pack_structure`) is a **no-op until catalogs ship**, so it can't be your RED driver, but `test_pack_validator_crossref.py::test_all_live_packs_pass_cross_reference_lint` auto-engages on content landing and enforces every tag/skill resolves — tell Dev to run full `just server-test`. See [[story_153_33_swn_chargen_backgrounds_foci]].

### Dispatch-wiring RED (sibling of 153-5 course): the dogfight is a confrontation TYPE, and its `confrontation_started` span fires at RESOLUTION not SEATING (153-6, 2026-06-22)
- **The ADR-077 dogfight is NOT a separate engine — it's a `ConfrontationDef` with `resolution_mode: sealed_letter_lookup`, seated by the SAME `instantiate_encounter_from_trigger(encounter_type="dogfight", …)` that `run_confrontation_dispatch` already calls.** So a new `dogfight` subsystem is a THIN wrapper (resolve dogfight type → materialize `params["opponent"]` as the Other per ADR-116, exactly like `confrontation.py` → call the primitive → emit a dispatch span). The story's "implemented-but-unreachable" gap is ONLY the IntentRouter route + a dispatch-level span + a watcher witness. Write the RED to assert the OUTCOME (a live `dogfight` encounter on `snapshot.encounter` with the red+blue pairing), NOT the internal call — robust to whether Dev wraps confrontation or calls instantiate directly. File a Don't-Reinvent delivery finding so Dev doesn't reimplement seating.
- **The span-timing trap that makes a seat-only RED a false-RED-that-stays-red:** `dogfight.confrontation_started` (and `maneuver_committed`/`cell_resolved`) is emitted in `server/dispatch/sealed_letter.py`, i.e. during a sealed-letter RESOLUTION turn — NOT at instantiation. **Seating** (`instantiate_encounter_from_trigger`) emits the generic `encounter.confrontation_initiated` (`telemetry/spans/encounter.py`). A dispatch test that only SEATS the dogfight (no maneuver turn) must assert `encounter.confrontation_initiated` + the structural encounter, and the NEW handler-level `dogfight.dispatch` span — assert `dogfight.confrontation_started` there and it stays red forever even after a correct GREEN. Grep `grep -rn confrontation_started sidequest/` and confirm WHICH file emits it before choosing your "engine engaged" span.
- **Pre-pass anti-trap differs from 153-5:** course needed a NET-NEW `orbital_content` bank-context key, so its pre-pass test asserted the signature param. Dogfight needs ONLY `snapshot/pack/player_name/npcs_present` — context the pre-pass ALREADY threads for `confrontation` — so there is no new param to assert. The pre-pass test's real value is proving the dispatch survives `run_unregistered_subsystem_gate` (drops unregistered subsystems BEFORE the bank) and seats through the real pass. Registration is the load-bearing reachability point; a hand-built bank context alone would mask a missing registry entry.
- **2 tests PASS in RED by design (matches 153-5):** the confidence-gate-degrade test (`run_dispatch_bank` checks `confidence < threshold` BEFORE the registry lookup, so a low-confidence dispatch degrades to a hint whether or not the handler exists) and the watcher-silent-on-success test (`detect_dispatch_engagement_mismatch` skips subsystems not in `_WITNESSES`, so an absent witness can't false-flag). Both stay meaningful post-GREEN. Don't try to force them red — note them in the self-check.
- **Router-prompt classification is NOT unit-tested (stub the router):** same precedent as `course` — construct the `SubsystemDispatch` directly and drive the real bank/pre-pass with `router.decompose = AsyncMock(...)`. Asserting the system-prompt names "dogfight" would be a forbidden source-text wiring test; the prompt edit is a playtest gate. Flag it as a deviation + a forward-impact note so Dev actually edits `intent_router.py`. See [[story_153_6_dogfight_dispatch_unreachable]].

### Reproduce-first overturns "wire the X" stories; SideQuest router classification is stubbed-by-design (2026-06-25, 158-28)
- 158-28 was framed "opponent seater is broken — wire it." Reproduction (driving the REAL `run_confrontation_dispatch`/`instantiate_encounter_from_trigger` against the real `heavy_metal/barsoom` pack) proved the **seater works**. The real bug was upstream: the IntentRouter emits `subsystem="combat"` for blunt verbs ("I attack X"), but `combat` is NOT a registered subsystem (the engager is `confrontation`; "combat" is only a confrontation *type*). `run_dispatch_bank` silently drops the unknown subsystem → nothing seats → narrator confabulates a kill. Lesson: for a "wire/fix the engine" bug, REPRODUCE at the production seam before writing RED — the seater may be innocent and the defect a vocabulary/routing mismatch one layer up.
- **The deterministic suite stubs the IntentRouter by design** (`tests/server/conftest.py` `_stub_intent_router_factory` + `_no_real_anthropic_sdk`). You CANNOT unit-test "router classifies combat verbs" deterministically — only the opt-in LIVE path does (`SIDEQUEST_VERIFY_*` + subscription pool, e.g. `test_102_3_live_router_classification.py`). Anchor the deterministic RED at the **dispatch-bank** level by feeding the EXACT live-router output (`SubsystemDispatch(subsystem="combat", ...)`) through `run_dispatch_bank` — no LLM. Pair it with a router-prompt fix validated by live eval + playtest. To make a real router probe: build via `build_intent_router_for_session(session_id=None)`, `await router.decompose(action=..., state_summary=_build_state_summary(snap, pack=pack))`. Do NOT set `ANTHROPIC_API_KEY` (a set key re-routes to PAYG — the 119-1 NO-GO); the narrator/router run over the Max subscription pool already authenticated in-session.
- **Before writing OTEL-span RED tests, grep existing span constants** (`grep -rhn '= "' sidequest/telemetry/spans/`). Story contexts routinely propose "required" spans that duplicate existing ones — 158-28's context proposed 5; 4 were redundant (`encounter.confrontation_initiated`, `encounter.beat_applied`, `state_patch.hp`, `narrator.tool_loop`+`AnthropicSdkLoopExceeded`). `one mechanism per problem` is load-bearing (enforced by `test_dispatch_engagement_spans.py`); reuse the existing span, log the redundancy as a Conflict finding, don't author a parallel span.

### A flavor-keyed FIXTURE reproduces a "fixed-by-canonicalization" crash deterministically (2026-06-27, 158-49)
- 158-49's crash (`KeyError stat 'STR'` committing a dogfight "attack" beat) was "fixed" on LIVE content by #510 canonicalizing space_opera to STR/DEX. But `tests/fixtures/packs/swn_test_pack` was DELIBERATELY left flavor-keyed (Physique/Reflex/Intellect/Cunning/Resolve — 158-51 deferred note). So driving `dispatch_dice_throw(beat_id="attack", character_stats=<flavor block>)` against the fixture reproduces the EXACT production traceback (`without_number._stat` KeyError) deterministically, with NO dependency on live content or env. Lesson: when a same-day canonicalization PR masks a crash on live content, check whether a synthetic fixture preserves the pre-canonical shape — it's the right RED vehicle, and it proves the STRUCTURAL bug (wrong menu) survives independent of the stat-name fix.
- **Root cause is a missing gate dimension, not the stat name:** `beat_filter.beats_available_for` synthesizes the WN personal-combat action set (`attack/total_defense/fighting_withdrawal/run`, beat_filter.py:368) gating on `is_wn_binding AND win_condition=="hp_depletion"` — it IGNORES `resolution_mode`. Since #508 made the sealed-letter dogfight resolve via `hp_depletion`, the dogfight inherits the ground-combat menu. Post-#510 the discriminator is beat IDENTITY (the personal-combat action set is wrong for a ship duel) NOT stat NAME (canonical SWN now has STR/DEX too) — so assert on `b.id in {attack,...}`, not on the stat key. Pair the unit assertion (`beats_available_for` excludes the set for a sealed_letter_lookup cdef) with a `build_confrontation_payload` integration assertion (the real client-facing menu surface) so a menu-layer-only fix is provably wired through. The `combat` (beat_selection) cdef is the regression-guard twin — it MUST keep synthesizing the WN attack (don't let the fix rip out the synthesis wholesale).
- **When a fix SWAPS the beat set, assert beat ATTRIBUTES, not just IDs (review-rework catch):** my AC2 payload test asserted only `b["id"]` (no WN personal-combat ids) and PASSED — but it never inspected `b["difficulty"]`. The new no-roll sealed-letter maneuver beats then silently got the ship's AC stamped as a d20 `difficulty` (via `build_confrontation_payload`'s `_offer_dc` loop, which only skipped item-use beats), polluting the `confrontation.beat_dc_authored` lie-detector span. Granny caught it. Lesson: a beat-set swap changes more than IDs — when the new beats are a different RESOLUTION CATEGORY (no-roll maneuver vs d20 strike), assert the category-specific attributes too: `"difficulty" not in beat_dict` for no-roll tiles (the item-use no-DC contract), and that the `*_authored` OTEL spans don't name the new beats. The rework regression tests live in `test_dogfight_swn_beat_set_158_49.py` (`test_dogfight_maneuver_tiles_carry_no_d20_difficulty` + the span twin).
