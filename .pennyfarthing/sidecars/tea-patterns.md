## TEA Patterns

### How to work with Keith
- **Senior architect, 30-year dev, learning Rust via the SideQuest port.** Talk at the architectural abstraction level — pattern names (trait objects, actor model, CQRS, composition), trade-offs, system decomposition. Don't over-explain general programming concepts.
- **Teaching mode on by default.** Explain Rust idioms as they come up, framed against the Python equivalent first (the port is Python→Rust), then Go when Python can't express the concept (traits → Go interfaces, ownership, compile-time guarantees). `#[derive(Serialize)]` ≈ `@dataclass`; traits ≈ Go interfaces, not Python ABCs.
- **Dictation artifacts expected.** "axiom" → "axum", "SERG" → "serde", "playlist" → "playtest", etc. Parse for intent before correcting.
- **Procedural world generation is a 30+ year domain for Keith** — MUSHcode, populinator, conlang markov, townomatic, gotown. SideQuest content systems are mature patterns he's rebuilt before, not novel experiments. Never treat conlang/NPC registry/tropes/POI as new.
- **Velocity calibration: ~20x human dev speed, sustained since Nov 2025.** Divide "human dev" estimates by 5-10x for parallelizable work. There is no "too much for one session."

### Core wiring patterns
- **Wiring means non-test consumers.** Before declaring GREEN, grep for the function/struct in production code paths. Library functions with no consumers are stubs, not features.
- **Wiring means the full pipeline.** "Wire X into Y" = source → intermediate layers → rendered output, not "component accepts props." ACs and tests must cover the full data flow.
- **Wiring means visible in the GM panel.** Every subsystem emit site gets an OTEL watcher event with content the lie detector needs to refute a Claude hallucination (e.g. `room_count`, `hp_delta`, `item_added`). Internal data flow without OTEL is not wired.
- **Combat wiring trace (reference):** IntentRouter → Orchestrator → lib.rs → Protocol → UI CombatOverlay. This is the template for verifying any intent-to-UI pipeline.
- **Debug, don't rebuild.** The code is ~98% done — find the small break. Trace the existing pipeline front-to-back before writing anything new. Dev agents repeatedly rebuild pipelines that already work.
- **Source-level wiring guard tests.** When a runtime integration test is impractical (dispatch_connect has 55 params, dockview needs real DOM sizing, bool CLI defaults), read the source file from the test and assert the wire exists as a substring/pattern within N lines of the emit site. Pattern reference: `map_telemetry_wiring_tests.rs` in sidequest-server, `gameboard-wiring.test.tsx` in sidequest-ui. These catch "someone deleted the wire in a future refactor" at `cargo test` / `vitest run` time, not at playtest time.

### Architectural patterns
- **Rust vs Python split.** If it doesn't operate an LLM, it goes in Rust (game engine, state, protocol, markov chains, name gen). If it runs model inference (Flux, Kokoro, ACE-Step — *not* Claude), it's in the Python daemon. Claude CLI calls go through Rust subprocess.
- **Sidecar tool pattern** (Epic 20): Narrator calls tool → parser validates → `assemble_turn` consumes. Established by `item_acquire`, `merchant_transact`, `lore_mark`. All future narrator-exposed tools follow this pattern.
- **Script tool prompt pattern** (ADR-056): Expose Rust generator binaries to the narrator via skill-style checklists (carrot), not threatening gate language (stick). Clear command table, "when to call" guidance, post-call checklist. Never "You MUST" or "Do NOT" — LLMs bash against hard gates.
- **No server-side re-invocation of the LLM to judge the LLM.** Post-hoc validation lives in OTEL spans ("was the tool used? did output match state?"), not a second LLM round-trip. The user calls this the "God lifting rocks" problem.
- **React `key` as layout reset tool.** Changing `key` on a stateful component forces React to unmount and remount it fresh — cleanest way to reset dockview/three.js/etc. without imperative reset code. Used in `GameBoard key={currentGenre}` to fix the Dockview drift bug.

### Content-mechanics stories span two repos (71-24)
- **A genre-pack mechanics story scoped `repos: sidequest-content` still needs a `sidequest-server` test branch.** The YAML bank lives in content, but the TDD tests are Python and exercise the server seams: the loader (`sidequest.genre.loader.load_genre_pack`), `RulesConfig.intent_verbs_by_type`, `confrontation_intent_validator.validate` (the real intent→`matched_type` path), and the production seating+resolution path (`instantiate_encounter_from_trigger` + `dispatch_dice_throw`). Create a matching `feat/<id>-…` branch on sidequest-server, commit the tests there, and log a Delivery Finding so SM-finish handles PRs in BOTH repos.
- **Canonical real-pack confrontation test to mirror:** `sidequest-server/tests/server/test_space_opera_swn_combat_e2e.py`. Helpers `_seated_combat` (seats via `instantiate_encounter_from_trigger` with `NpcMention(side="opponent")`) + `_throw` (`face=[20]` winning d20 through `dispatch_dice_throw`) + the `otel_capture` fixture (`tests/server/conftest.py`). Combat resolves on `enc.outcome == "player_victory"` after opponent `hp.current <= 0`; assert the `encounter.resolved` span `source == "hp_depletion"`. This is the sanctioned exception to "tests don't point at content."
- **A new SWN `combat`+`hp_depletion` confrontation MUST seed `opponent_default_stats` hp/armor_class/dexterity** or the loader's cross-field validator (`rules.py` ~528-565) raises at load — fail-loud by design so the content author finds it before a player triggers the encounter.
- **Orbital/orrery content is the same two-repo pattern, different seam (98-1).** A `repos: content` story that authors a `systems/<id>.yaml` or `orbits.yaml` orrery file tests against the server's orbital seams: `OrbitsConfig.model_validate` (the loader's parse path, loader.py:52 — `extra="forbid"` + `_validate_parent_refs` reject malformed/dangling content loudly), `load_orbital_content` (stage the file as `<tmp>/orbits.yaml` since the loader still hard-codes that name until ADR-141 S1 lands), and `_resolve_scope_center(orbits, Scope.system_root())` (render.py:826 — enforces *exactly one* parent-less body and returns the chart-center root). Canonical real-content orbital test to mirror: `tests/orbital/test_render_coyote_star.py` (uses the `content_dir` conftest fixture, `parents[2].parent / "sidequest-content"`). Pin exact `period_days`/`epoch_phase_deg`/`semi_major_au` against source to catch transcription drift.
- **Intent reroute is verb-set membership, not a code change.** Moving a verb between confrontation types = moving it in the YAML `intent_verbs` list. `validate()` scores by token overlap (`tokenize` strips light suffixes; `-ss` and ≤5-char words like "swing" survive), most-overlap wins, ties by pack-declaration order. To prove a reroute: assert the verb is in `intent_verbs_by_type[newtype]` and ABSENT from the old type, then drive a real action string through `validate()` and assert `matched_type`.

### RED-testing an intentionally-open ("or equivalent") storage AC (126-18)
- **When an AC leaves the store open** ("persists to room_states *or equivalent honored-scene-truth store*"), keep the BEHAVIORAL assertions storage-agnostic so a different store than the named one still passes: assert the value appears in `json.dumps(snapshot.model_dump(mode="json"))` (the exact forensic check — the bug was "string absent from the entire snapshot") and in the router's `_build_state_summary(snapshot)`. Pin ONLY the names you must (the authoring field, the helper, the span) and log a Design Deviation saying Dev may rename them.
- **Mirror the nearest working STRUCTURED path, not a fuzzy one.** 126-18 (opening props persist) had two candidate designs: prose-extraction from `establishing_narration` vs a structured `present_props` field. The story named opening-scene NPC persistence as the working parallel → mirror `preload_authored_npcs` (`game/world_materialization.py`): structured `OpeningSetting.present_props` (twin of `present_npcs`) + a `persist_opening_props` helper + a flat-only `opening.props_persisted` span (twin of `npc.authored_loaded`). Structured is deterministic + OTEL-verifiable; prose-extraction is the "winging it" the lie-detector exists to catch. "Don't Reinvent — Wire Up What Exists."
- **Import the not-yet-existent helper INSIDE each test, not at module top.** `from ...opening_helpers import persist_opening_props` at module scope makes the missing symbol a whole-file COLLECTION error that masks every test (incl. the schema tripwire that doesn't need it). Importing inside each test localizes the ImportError-RED to that test. Mirror `test_71_7_authored_crew_fresh_gate.py`'s string-literal span name (`PROPS_PERSISTED_SPAN = "opening.props_persisted"`) so the span assertion is RED before the constant exists.
- **An LLM guard (`must_not_narrate`, ADR-113) can't be unit-asserted — test its deterministic precondition.** The guard is a Haiku inference over the slimmed snapshot; you can't assert "the LLM won't say it." Assert the falsifiable precondition instead: the prop is present in `_build_state_summary` (what the router classifies against). If the prop is visible, the guard has no basis. Log a deviation noting the end-to-end "guard never fires" is a playtest/verify concern. `_build_state_summary` calls `snapshot.party_location()` → `apply_snapshot_slimming` projects `room_states` to ONLY the current room, so the test snapshot must seat a PC (`character_locations[name]=room_id`) at the prop's room or the projection drops it.
- **Light, real-content wiring test without PG:** `GenreLoader([CONTENT_ROOT]).load(pack)` + `_world_history_value` + `materialize_from_genre_pack(..., CampaignMaturity.Fresh, ...)` builds a real snapshot with no Postgres; drive the real seam (`_populate_opening_directive_on_chargen_complete`) with a `SimpleNamespace` session_data (it only `getattr`s a few attrs) and `patch.object(opening_helpers, "_resolve_opening_post_chargen", return_value=<custom Opening>)`. This is the no-PG alternative to the heavy `handle_message` chargen-walk e2e in `test_opening_emit_event_71_13.py`.

### Git / branch patterns
- **Gitflow on every subrepo.** Subrepos (sidequest-server, sidequest-ui, sidequest-content, sidequest-daemon) target `develop`. Only the orchestrator targets `main`. Always check `repos.yaml` before any git op.
- **Branch before editing.** `git checkout -b feat/description` BEFORE touching files, not after. Avoids the "hook-blocked commit → panic → stash scramble" failure mode.
- **Dirty work comparison pattern.** When local uncommitted changes conflict with a pull: commit dirty work to a temp branch, pull clean on the default branch, diff each file local-vs-remote side by side, categorize (identical / remote better / local has extra value / no remote change), then cherry-pick deliberately. Never stash.
- **Additive git ops need no permission** (commit, push non-force, pull --ff-only, checkout existing, checkout -b, fetch, add). **Destructive git ops always ask first** (reset, clean, force-push, branch -D, rebase -i, stash — banned outright).

### Playtest patterns
- **Pingpong cadence: every 2-3 minutes during active playtest.** Read `/Users/keithavery/Projects/sq-playtest-pingpong.md` frequently so new `open` items at the top don't pile up unnoticed. Update to `fixed` immediately when a fix lands — don't batch.
- **Always pull and test on new commits.** When `git log HEAD..origin` shows new commits during playtest: pull → rebuild → restart → test. No "want me to?" prompt.
- **Always rebuild on restart.** Every service restart during playtest is a full rebuild. Compile time is negligible vs. debugging a stale binary.
- **Keep going.** Work through the checklist autonomously during playtest. Don't pause to ask "want me to continue" — Keith wants momentum.
- **Restart in existing tmux panes.** Send Ctrl+C and re-run the command. Don't close/reopen panes — churn annoys Keith and wastes time.
- **Source-grep tests > integration tests for playtest fixes.** Faster to write, faster to run, catches the exact failure class (someone unwired it in a refactor) without needing a full stack harness.

### Spec hierarchy
- **Spec authority order:** Story scope (session file, highest) → story context → epic context → architecture docs / SOUL / rules (lowest). When sources conflict, the session scope wins. Log deviations **at the moment of the decision**, not at phase exit.

### Verify the session's "current state" against actual green tests before writing RED (2026-06-01, story 65-8)
- **What happened:** 65-8's session file described its core problems as "POI images never appear" and "POI slug manifest unused" — and proposed a 7-AC build. Reconnaissance (Explore agent) + running the *existing* `test_reference_poi_images.py` showed story **63-8** had already shipped that wiring (12 tests green). The session file was describing a *pre-63-8 world*. Only ~1 of the 7 ACs (the R2-manifest existence gate) was genuinely net-new; AC2 was done, AC5's type-chip was already in the live card markup, AC4 was ambiguous.
- **Why it matters:** Writing RED tests for the full session frame would have produced several green-on-arrival tests (duplicating 63-8) — not true RED, and vacuous per python-review check #6. A story's "Background/Problem" prose is a *snapshot at authoring time*; sibling stories can land between setup and RED and silently invalidate it.
- **How to apply:** Before writing RED, (1) dispatch an Explore pass to map the REAL symbols/signatures (don't trust session-file names — though here they were exact); (2) **run the existing test files for the subsystem** and read which behaviors are already green; (3) diff session ACs against green reality; (4) if material overlap, surface it to the boss with a crisp table + recommendation (re-scope/full/escalate) via AskUserQuestion rather than silently narrowing or silently bloating; (5) log the chosen scope as a `major` Design Deviation so Reviewer doesn't expect the dropped ACs' code in the PR. The net-new RED here: a loud-fail manifest loader, an existence gate at the real route (gated fixture world with one in-manifest + one authored-but-absent POI), and a `manifest_loaded` OTEL span — all failing for honest reasons, with the gate test literally capturing the broken `<img src=.../sunken-vault.png>` the old authorship-gate emits.
- **Setup-gap note:** sm-setup wrote the session file but NOT `sprint/context/context-story-65-8.md`; the TEA context gate (`pf validate context-story`) hard-blocks RED on exit 2. Fix is `/pf-context create story <id>` (or re-run SM), not auto-authoring it from the TEA seat.

### Make a fail-loud test a TRUE RED via the typed loader, not the generic walk (2026-06-03, story 65-12)
- **What happened:** 65-12 (lore-page world Timeline section) needed an AC1 "malformed legends.yaml → HTTP 500, never a silent timeline-less 200" test. But the lore page ALREADY walks `legends.yaml` through the generic humanization walk (`_file_renders_by_stem`), which renders raw YAML and does NOT validate via the `Legend` model. A malformed-*YAML* fixture (unterminated, like 65-11's map_malformed) might already 500 today via the existing walk → the test would be green-on-arrival, not RED.
- **Why it matters:** A fail-loud contract test only proves anything if it fails for the *new* code path. `Legend` is `extra="forbid"`, so a fixture legend with a **valid-YAML-but-schema-forbidden key** (`bogus_unknown_field: ...`) renders fine through the generic walk (200 today) and only raises when the NEW typed loader (`_load_legends_flexible` → `Legend.model_validate`) runs. That makes the 500 assertion a clean RED that specifically pins the new loader's No-Silent-Fallbacks behaviour (and forbids Dev wrapping the load in a swallowing try/except — python rule #1).
- **How to apply:** When a subsystem already touches a file generically, don't reach for malformed *syntax* to force the loud-fail RED — reach for a payload that only the **new typed/validated path** rejects (extra-key on an `extra="forbid"` model, a missing required field, a wrong-type scalar). Verify via testing-runner that the happy-path fixtures return 200 (no fixture-load 500s) so every other RED failure is a clean contract assertion, not a fixture bug.
- **Section-scoped HTML helper:** reuse the 65-11 map-test shape — a `_section(html)` helper that slices `<section id="X">…</section>` and fails loud if absent. Every assertion scopes to that slice so the *generic* legend/POI rendering elsewhere on the page can't satisfy a section assertion by accident. Route-driven only; no source-text wiring tests (server CLAUDE.md).

### Wiring a "dead" data-only subsystem (epic 82 shape): consumer + OTEL + player field, all one RED file (2026-06-03, story 82-8)
- **What happened:** 82-8 (ADR-021 track 3) wired `WealthTier` gold→label, which had *zero* repo-wide consumers (only the pydantic model + `__init__` export — `grep -rn WealthTier` proves the audit's "data-only" call exactly). The either/or spec ("wire `narrative_weight` OR `WealthTier`") forced a seam choice at RED, since TDD needs concrete failing tests. Chose WealthTier: pure-function core (ideal for the boundary AC), and an existing player surface to extend (`InventoryPayload.gold`/`currency_name` built in `views.party_member_from_character`).
- **Why it matters:** Epic 82 ("Surface the Dark Subsystems") stories all share the shape *find dead seam → add production consumer → emit OTEL → wiring test*. The "done" bar is higher than "code runs": a real consumer AND an OTEL emit AND a player-facing surface, or the wiring doctrine says it isn't live. A pure-unit test alone fails that bar.
- **How to apply:** Decompose the RED into 4 contract layers, each its own test class, **local-importing the not-yet-existing symbol inside each test** so a missing symbol fails *only that AC* (not collection): (1) pure resolver with parametrized boundary/one-above/over-cap/zero/negative + empty→None; (2) the new player-facing protocol field (`InventoryPayload` is `extra="forbid"` → unknown kwarg raises a clean ValidationError RED today, defaults-None pins the no-data case); (3) OTEL span by name via the `otel_capture` fixture (`tests/server/conftest.py:1177`) — `[s for s in otel_capture.get_finished_spans() if s.name == "..."]` with a failure msg listing seen names; (4) behavioral wiring driving the **real** consumer.
- **Views-layer player-facing wiring harness (reusable):** to drive `views.party_member_from_character` for real, copy the `_char(name)` (Character + `CreatureCore`/`Inventory`/`HpPool`) and `_sd(...)` (`_SessionData` with `load_genre_pack(...)`, `MagicMock()` for repository/dungeon_repository/telemetry_sink/orchestrator) builders from `tests/server/test_multiplayer_party_status.py`, then `handler = WebSocketSessionHandler(save_dir=Path("/tmp/sq-test-saves"))` and `sd.snapshot.character_locations = {}` to skip the world-location path. Genre models are mutable + not frozen (`ProgressionConfig` is `extra="forbid"` but assignable) so you can **inject** known data onto the loaded pack (`pack.progression.wealth_tiers = [...]`) — the test owns the boundary data instead of coupling to whatever a real pack authors today.
- **No-Silent-Fallbacks pairing:** always pair the happy-path wiring test with a *negative-invariant* test — no authored data → label `None` AND span does NOT fire. The negative span test is green-on-arrival in RED (no consumer yet), but it's a real invariant that must keep holding through GREEN (a fired span on empty data would be a GM-panel lie). Log the seam choice + any unspecified edge semantics (negative→floor, empty→None) as `minor` Design Deviations so Reviewer knows TEA narrowed the either/or.

### Cross-repo contract test: load the sibling repo's models for real, fail-loud not skip (2026-06-10, story 101-1)
- **What happened:** 101-1 (epic 101 split-brain remediation) needed a test holding the deliberately-duplicated daemon↔server renderer-model seam (`RenderTier` enum + `StageCue` fields/`camera` type). The drift: daemon still had `CARTOGRAPHY` (gone server-side 2026-04-28) + two zimage configs, and `StageCue.camera` was `CameraPreset` enum vs the server's plain `str`. A *snapshot* contract (hardcoding the expected server shape in the daemon test) would only hold the daemon side against a frozen expectation — it would NOT catch the server changing. The real seam needs both live sides compared.
- **Why it matters:** Daemon and server are sibling subrepos under the orchestrator checkout (`oq-N/sidequest-{daemon,server}`). The server's `renderer/models.py` depends only on pydantic + stdlib, so it loads in isolation in the daemon test env **without importing the whole server package** (no server deps needed). That makes a genuine cross-repo comparison cheap. Story 78-3's TEA log warned "a daemon-repo test reaching another repo is fragile and wrong-repo" — but that was a *documentation* obligation (ADR notes) verifiable by review; a wire-contract test inherently MUST compare both sides. Don't let the 78-3 precedent talk you out of a legitimate contract test.
- **How to apply:** Loader pattern — `importlib.util.spec_from_file_location("…_contract", models_path)` + `module_from_spec` + `spec.loader.exec_module(module)`, where `models_path = Path(__file__).resolve().parents[N] / "sidequest-server" / …` with an `os.environ["SIDEQUEST_SERVER_ROOT"]` override (precedent: `telemetry/watcher_bridge.py` uses `SIDEQUEST_SERVER_URL`). Then compare `{t.value for t in DaemonTier} == {t.value for t in module.ServerTier}`, `set(DaemonModel.model_fields) == set(module.ServerModel.model_fields)`, and `DaemonModel.model_fields["f"].annotation == module.ServerModel.model_fields["f"].annotation` (pydantic v2 resolves annotations even under `from __future__ import annotations`). **Fail loud, never skip:** raise `FileNotFoundError`/`ImportError` when the server file is absent — a `pytest.skip` is a silent fallback that stops holding the seam and gives false confidence (the exact defect the epic remediates). In the orchestrator checkout the server is always present, so fail-loud never false-positives. Log the sibling-reach + the no-skip choice as `minor` deviations.
- **Coherent-RED for a deletion refactor:** removing an enum member ripples wider than the model file. `CARTOGRAPHY` lived in 5 production sites — the enum, two `ZIMAGE_*_TIER_CONFIGS` dicts (these reference `RenderTier.CARTOGRAPHY` directly, so a partial removal is an *import-time* AttributeError → the removal is self-forcing), plus string-keyed leftovers in `daemon.IMAGE_TIERS` and the worker `_TIER_TO_R2_KIND` (raw strings, won't crash → silently dead). Add **invariant guard tests** (`IMAGE_TIERS ⊆ {t.value for t in RenderTier}`) that are green now and only flip RED on a *partial* removal — they force Dev to delete the dead routing strings too. And **update, don't leave, the pre-existing tests that encode the old contract** (enum-identity `camera`, `cartography` as a sample render tier) — TEA owns all test changes so Dev touches only source; otherwise the suite can never reach GREEN (contradictory assertions). Find them with `grep -rn` for the symbol/string across BOTH production and tests before writing RED.

### Dead-surface removal touching extra=forbid models + persisted saves needs a migrations.py shim (2026-06-10, story 101-2)
- **What happened:** 101-2 (epic 101) deletes the dead voice-generation surface from the server. Four of the five surfaces are pure deletion (genre voice models, `AudioConfig.creature_voice_presets`, `GenrePack.voice_presets`, `MessageType.VOICE_SIGNAL/VOICE_TEXT` + the `VOICE_TEXT` `TARGETED_KINDS` route). The fifth — `Npc.voice_id` — is the load-bearing one: `Npc` is `model_config={"extra":"forbid"}`, so a pre-removal Postgres save carrying `voice_id` would raise `ValidationError` on load the instant the field is deleted. RED must drive a backward-compat shim, not just an absence assertion.
- **Why it matters:** The shim has a *canonical home* and a copy-paste pattern. `sidequest/game/migrations.py`'s docstring is explicit: "this module is the ONLY place backward-compat shims live" — it runs read-old-write-new on the snapshot dict BEFORE pydantic validation, via `migrate_legacy_snapshot`'s sub-function tuple (`_migrate_s1..s5`). Each sub-function returns OTEL attrs or `None`, and the orchestrator emits one `snapshot.canonicalize` span. The matching extractor allow-list lives in `sidequest/telemetry/spans/persistence.py::_extract_snapshot_canonicalize` ("no defaulted keys"). The exact precedent to clone is S3 (`tests/game/test_party_location_migration.py`): a `_migrate_s{N}_…` strip/seed fn + a `s{N}_…` counter key in the extractor.
- **How to apply:** Drive GREEN toward `_migrate_s6_strip_npc_voice_id` registered in the orchestrator tuple + `s6_voice_id_stripped` added to the extractor allow-list. RED set = (1) `hasattr(migrations, "_migrate_s6_…")` wire test; (2) build a legacy snapshot with `npcs[].voice_id` and assert `"voice_id" not in migrated_npc` — this is the genuine RED driver; (3) OTEL: route-extractor `SimpleNamespace` test + in-memory `InMemorySpanExporter` span-fires test (clone S3's two OTEL tests verbatim). **Gotcha — the "validates under extra=forbid" test is a GREEN-side guard, NOT a RED driver:** today `voice_id` is still a valid `Npc` field, so `Npc.model_validate(dict_with_voice_id)` passes *now* and *after* removal — it never goes RED. Keep it as a regression guard but lean on the `"voice_id" not in migrated` assertion for the actual RED.
- **Scope guards earn their keep on removal stories:** grep the symbol broadly first — `voice` matched `MixerConfig.voice_volume` (audio mixer dial) and `ChassisVoiceSpec` / `register_chassis_voice_section` (ADR-125 chassis voice), neither of which is TTS. Write *retention* tests (`assert "voice_volume" in MixerConfig.model_fields`, `assert hasattr(gm, "ChassisVoiceSpec")`) so an over-eager deletion flips them RED. AC-3 "grep-guard" → reflection (`hasattr`/`model_fields`/enum+dict membership), never `read_text()` (server CLAUDE.md "No Source-Text Wiring Tests"). And own the stale presence-tests: deleted the two `VOICE_*` wire-string tests in `test_enums.py` and flipped `test_message_type_complete_count` 56→54 (TEA touches all tests so Dev touches only source).

### Scene-harness hydrator: extend a block by cloning the 50-2x section idiom + a real-engine wiring proof (2026-06-10, story 90-4)
- **What happened:** 90-4 adds two new fixture blocks to `sidequest/game/scene_harness.py`: character `effort:`/`spellcasting:` → `core.effort`/`core.spellcasting`, and encounter `win_condition:`/`category:`/`actors:` → `StructuredEncounter`. All hydrator tests live in ONE file (`tests/game/test_scene_harness_hydrator.py`, ~2700 lines) sectioned per story (50-18…50-23). Append a new `# ── Story 90-4 ──` banner + a `_write_*_fixture` tmp_path helper; don't start a new file. The hydrator's established contract does all the heavy lifting: pydantic `model_validate` on the nested model + re-wrap `ValidationError` as `FixtureValidationError` (HTTP 422), `is not None` guard so present-but-malformed fails loud, model defaults (`None`/`{}`/Literal default) preserved when the block is omitted.
- **Why it matters:** The load-bearing RED isn't "the field hydrates" — it's "the hydrated seed actually drives the engine." A hydrator can construct a valid-looking `SpellcastingState` whose economy the cast spine rejects, and unit tests would pass while the deterministic proof fails. Two wiring tests close that: feed `core.spellcasting` through the REAL `WwnRulesetModule.resolve_spellcast` (assert `cast=True` + one cast spent), and drive `check_hp_depletion` over hydrated actors/cores (assert `player_victory` with opponent at 0 HP). These are the actual ACs when the story is "prove X live." Mirror the call shapes from `tests/game/ruleset/test_wwn_spellcast.py` (`WwnConfig(attribute_map=...)`, `random.Random(0)`, no-save/no-damage `CastInput` to stay target-free) and `tests/game/test_hp_depletion.py` (`check_hp_depletion(enc, lambda n: cores.get(n))`).
- **How to apply:** RED set per new block = (1) happy-path hydrate asserting exact values; (2) omit → model default (backward-compat, passes NOW — keep as regression guard); (3) loud-fail trio: not-a-mapping/list, `extra=forbid` typo, bad-value-type → `FixtureValidationError` (these fail with "DID NOT RAISE" today because the hydrator silently drops unknown blocks — that IS the RED driver); (4) one real-engine wiring proof; (5) canonical-fixtures-unaffected lock (`combat_brawl_wasteland` et al. stay `spellcasting=None`/`effort={}`/`dial_threshold`). **Deterministic-fixture gotcha:** a content-only proof must seed `prepared` NON-EMPTY — chargen (`seed_wwn_magic`) seeds `prepared==[]` (chosen at rest), so an empty list makes every cast refuse `'<spell>' is not prepared`. Log that divergence as a deviation; Dev must hydrate `prepared` verbatim with no chargen-style truncation. **Effort block shape:** keyed by source (`effort: {high_mage: {max: 3}}`), hydrator derives `EffortPool.source` from the key — matches `core.effort.get(source)` read in `wwn.py`. Flag the keyed-vs-explicit-source choice for Architect SPEC-CHECK.

### Roll-agnostic OTEL wiring assertion for random kit rolls (106-1, 2026-06-13)
- Problem: a production step fires one of N spans depending on a random content roll (e.g. `warrior_kit.armor` is a uniform 1-of-3 pick; leather→`chargen.armor_equipped`, shield/helmet→`chargen.armor_unresolved`). A fixed "AC==13" assertion through the real handler is flaky (~1/3). Seeding the handler-constructed builder's RNG is brittle (couples to slot/roll order).
- Pattern: assert the INVARIANT that holds across all rolls — "exactly one of {span_A, span_B} fired" (the kit always produces exactly one armor item) — then BRANCH-assert the value per outcome (AC=13+equipped on span_A, AC=10+loud-fail on span_B) so it stays non-vacuous. Deterministic pass/fail on the WIRE, immune to the roll and to refactors. Add a pre-condition assert that NO target span fired before the triggering message, to prove the span is caused by THIS step.
- Soundness check first: `grep` that the target spans are emitted ONLY inside the function under test, and that function has exactly ONE production caller. Then span-presence == wire-present is airtight (drop the call → zero spans → fail). This is the server CLAUDE.md "No Source-Text Wiring Tests" — assert the span through the real handler, never grep source.

## Zero-beat WN combat RED tests (108-8, 2026-06-15)
- To reproduce the post-108-3 "cdef.beats == []" state in a test, load the REAL
  wwn pack (heavy_metal) via `tests.integration._wn_round_102_4.load_pack`, seat with
  `seat_wn_combat`, then mutate `cdef.beats = []` on every `win_condition=="hp_depletion"`
  def in `pack.rules.confrontations`. ConfrontationDef is NOT frozen and
  `find_confrontation_def` hands dispatch the SAME object you mutate (mutation sticks).
  Strip AFTER seating (seating still needs beats).
- The single chokepoint is `dispatch/dice.py:407` (`beat = next(b for b in cdef.beats...)`)
  → raises `DiceDispatchError: unknown beat_id 'attack' ... available: []`. The wire
  handler CATCHES this and logs `dice.dispatch_error` (warning) — it does NOT crash — so
  wire tests must assert on the ABSENCE of `wwn.round.resolved`, not on a raised exception.
- WN attack damage source: heavy_metal ships NO `unarmed_damage` floor, so an empty-inventory
  PC's synthesized attack has no weapon dice. Arm the PC with an item dict carrying a
  serialised `damage` key (priority-2 in `damage_roll.resolve_damage_spec_from_beat_and_actor`):
  `{"id":..., "name":..., "category":"weapon", "damage":{"dice":"1d8","bonus":0}}`.
- Canonical WN action id = `"attack"` (UI: CavernActionPanel.tsx + EncounterTab.test.tsx).
  Gotcha: `"attack"` is ALSO an authored native beat (test_genre "Wasteland Brawl"), which is
  exactly why the WN synthesis intercept MUST be `isinstance(ruleset, WithoutNumberRulesetModule)`-gated
  — an unconditional intercept would hijack the native beat. Good cheap native-gate guard:
  dispatch "attack" under native test_genre, assert no `*.native_scaffolding_suppressed` span.

### Dead-read lie-detection in GM-panel projections (story 124-4)
The Inspector `debug_state` projection (`server/rest.py::debug_state`) was riddled with `getattr(obj, "WRONG_NAME", default)` dead reads that silently returned defaults — the exact "convincing output, zero mechanical backing" the OTEL lie-detector principle targets, hiding in a projection rather than narration. Four found in one function: trope `trope_id`→ field is `id`; trope `progression`→ field is `progress` (+`int()` truncation); NPC `core.edge.maximum`→ field is `core.hp` (HpPool, `.max` not `.maximum`) so NPC HP was always 0/0; player inventory hard-coded `items: []`. **Pattern for any State/forensic-tab redesign story: don't trust the projection — for every field the UI renders, open the source model and confirm the read name matches the real attribute.** `extra="ignore"`/`getattr(...,default)` means a typo'd read fails *silently* forever. The synthetic design prototype masks it (its `makeState()` always has rich data), so the bug only shows against a live save.

### Driving a testable extraction from RED (story 124-4)
When the logic you must test is buried inline in an async route handler (DB-coupled, untestable), write the failing test against an **extracted pure function** that doesn't exist yet (`project_session_state_view(snap, *, session_key, last_activity_ts) -> dict`). The ImportError is valid RED; Dev's first green step is lifting the inline block into the function, which is itself the architectural win. This is the repo-blessed "fixture-driven behavior test" over a synthetic `GameSnapshot` — far lighter than persisting a save + hitting the endpoint, and the server CLAUDE.md explicitly prefers it.

### Test the behavioral substance of a visual redesign, not pixels
For a Tufte/visual reskin, brittle style-snapshot assertions (`borderRadius`, exact colors) are low-value and the design README says screenshots are the verification medium. Instead assert the **graphical substance** behaviorally: the OCEAN text-summary becomes a 5-bar `<svg>` glyph (`querySelectorAll('rect').length >= 5`), tropes render in sorted DOM order, weight becomes an inline bar. The current consumer tab renders ZERO `<svg>`, so "an svg with ≥N rects exists" is a clean RED that flips green on implementation. Leave palette/typography fidelity to Reviewer's eyeball-vs-screenshot pass and log it as a test-strategy deviation. Bonus: require an accessible name (aria-label/`<title>`) on the glyph — testable AND a real a11y win over a bare bar chart.

### OCEAN is a 0–10 scale, not 0–1
`OceanProfile` (server `genre/models/ocean.py`) is five floats defaulting to 5.0 (midpoint), clamped — a 0–10 scale. The design prototype used 0–1 decimals. A sparkline normalizing OCEAN must divide by ~10, not treat values as fractions. `ocean_summary` is always `None` on the wire; the live data is the `ocean` dict (keys openness/conscientiousness/extraversion/agreeableness/neuroticism).

### Short-circuit a deep handler at the boundary under test (118-9 auth audit)
To test seat/identity resolution inside `DiceThrowHandler.handle` (which otherwise runs `dispatch_dice_throw` → lore retrieval → a full narration turn), monkeypatch the deep dispatch fn to **capture its kwargs and raise the handler's own typed error** (`DiceDispatchError`, message WITHOUT "active encounter" so the stale-encounter resync branch is skipped). Seat resolution is fully decided by the time `dispatch_dice_throw` is called, so its `rolling_player_id`/`character_name` kwargs ARE the honest assertion surface — no need to mock the entire narration stack. The handler imports the dispatch fn function-locally, so patch it on the SOURCE module (`sidequest.server.dispatch.dice`) — resolved at call time. Same trick works for any handler that funnels through one deep call after the logic you care about. For the spoof OTEL event, patch `watcher_hub.publish_event` (also a function-local import) and assert on `op` containing "spoof" + the inbound/authenticated id fields — decoupled from the exact op spelling but tied to the fate_action-style shape the AC mandates.

### Narrator latency budget — the wire is free, the narrator is gold (sealed-turn batching)

**The optimization target for the turn loop is narrator (LLM) invocations, NOT messages.** A WebSocket/server round-trip costs milliseconds; a narrator call costs *seconds* — the entire 126-9/126-10 latency dig was narrator + router LLM time, never wire time. So the rule is: **spend client↔server chatter like water; hoard narrator calls like gold.**

- **The client owns the roll-adjust loop; the narrator sees no intermediate dice.** A player throws (physics-is-the-roll, ADR-074/148), then adjusts *locally* — invoke (+2) or reroll (re-throw), as many times as they can pay for, spending their own fate points / aspects (all client-known) — and submits ONLY the final result. The server validates the spend on submit (reject loud if unaffordable — No Silent Fallbacks); the narrator never enters this loop. The back-and-forth that makes *solo* play feel good was never the table's business — it's private to one player and their own dice, so it costs nothing and blocks no one.
- **One round, one narration.** The sealed-commit barrier (ADR-036/129) is a *batching engine*: collect every player's intent + final roll, resolve EVERY mechanic server-side (commit → seat NPCs → conditional defend → compare → taken-out → aspects placed), THEN invoke the narrator **once** to render the whole woven exchange as a single cinematic beat. The narrator is never in the mechanical loop. If NPC action-selection needs intelligence, it uses the **cheap tier** (Haiku / heuristic / intent-router), never the expensive cinematic narration.
- **Sealed turns are a special-sauce FEATURE, not a constraint to fight.** Tabletop is serial (initiative: three players watch one act, GM narrates, next acts); we are *simultaneous* — N players commit at once, the engine resolves the whole measure in parallel, and the narrator writes the entire band's bar in one pass. This structurally dissolves the Guitar Solo problem (SOUL): nobody is a silent audience, because everyone's verb resolves into the *same* beat. The barrier "wait" is not dead time — it overlaps every player's local deliberation instead of serializing it.
- **Design test:** if a feature would put the cinematic narrator (Sonnet) *inside* the commit→resolve loop, or round-trip it per-player-per-action, it is mis-designed. Resolve mechanics cheaply on the wire; narrate once at the end. (Source: 2026-06-18 brainstorm w/ Keith — *Fate Core in the sealed-commit turn model*; companion to ADR-148 / ADR-149.)

### Pinning dead-path cleanup in a UI removal story (126-26)
When a story removes a widget/tab/module and lists "clean up the dead path," write RED tests that pin the *cleanup itself*, not just the absence of the rendered tab — otherwise Dev can hide the tab while leaving the registry/union/file behind (half-done). Three deterministic, framework-level checks (sidequest-ui / Vite + vitest):
1. **Registry entry gone:** `expect((WIDGET_REGISTRY as Record<string, WidgetDef|undefined>)["fate"]).toBeUndefined()` and `expect(buildHotkeyMap()["f"]).toBeUndefined()`. (Removing the `WidgetId` union member makes `Record<WidgetId, …>` unable to hold the key — the runtime `undefined` is the shadow of the type cleanup; tsc enforces the rest.)
2. **Source file deleted:** `const files = import.meta.glob("../widgets/*.tsx"); expect(Object.keys(files).some(p => /\/FateWidget\.tsx$/.test(p))).toBe(false)`. Vite statically resolves the glob, so the deleted file drops out of the match set. RED while the file still exists.
3. **Sibling source still lists/omits an entry:** `const src = (await import("@/.../MobileTabView?raw")) as unknown as {default:string}; expect(src.default).not.toMatch(/id:\s*["']fate["']/)`. The `?raw` import gives the file text; an exact-quote regex `["']fate["']` matches `id:"fate"` but NOT `id:"fate-conflict"` (closing quote guards the boundary).
ALWAYS pair removal tests with **preservation guards** (e.g. `fate-conflict` registry entry kept; the consolidated sheet still renders under Character) so a green run can't be achieved by over-deleting a sibling surface — same doctrine as the fate-native-chrome paired suite. And pin the tab-absence with an EXACT accessible name (`getByRole("tab",{name:"Fate"})`) — a `/fate/i` substring also matches "Fate Conflict".

### Testing engine "de-nativization" at the SEATING layer (126-30, Fate; pattern reused for WN epic-108)
"De-nativize X under a bound ruleset" (Bind the Ruleset, ADR-143/144) stories need a RED that pins **the native artifact is gone from the bound path** + a PIN that **native packs keep it** (ruleset-gating, never a blanket removal). Hard-won specifics:
- **Probe the seater empirically BEFORE writing the test.** Seat the real shape via `instantiate_encounter_from_trigger` (wrapper: `tests/_helpers/trigger_encounter.py`) in a `uv run python` one-shot and print the encounter fields + spans for `ruleset='fate'` vs `'dial'`. The bug is usually subtler than the ticket says — for 126-30 the native artifact was NOT a `beats` field (StructuredEncounter has none) but `enc.win_condition=='dial_threshold' + contest=None + live opponent_metric` (the vestigial tension dial). Don't guess the observable; measure it.
- **Fixture vocabulary for a Fate seat:** `pack=SimpleNamespace(rules=RulesConfig(ruleset="fate", fate=FateConfig(skills={...}, refresh=3), confrontations=[cdef]))`; PC `Character`, opponent native `Npc` (no fate_sheet — the seeder attaches one), `GameSnapshot`, `npcs_present=[NpcMention(name=,side="opponent",role=)]`. Mirror `tests/server/dispatch/test_fate_opponent_seeding.py` (Fate pack) + `test_fate_contest.py::test_seating_stamps_contest_state_and_emits_span` (seater call + span capture).
- **Content-validator gotchas building a Fate cdef:** a Fate pack REJECTS `resolution_mode=opposed_check` ("resolves through Contest or a Conflict, never the d20 dial"); `BeatDef(kind=angle)` needs `target_tag`; any non-contest beat needs `stat_check`; and pyright wants `kind=BeatKind.push` (the enum), not the string `"push"` (runtime coerces, pyright doesn't — the gate runs pyright). Use the REAL pack's authored shape as the fixture (e.g. spaghetti_western standoff: `category: pre_combat` + tension dials + size_up/draw beats) so the test reproduces the actual leak.
- **Make the RED assertion HOW-agnostic when the ruling fixes the WHAT, not the marker.** The encounter had no dedicated "Fate-resolved" flag, so assert the negative invariant: `assert not (win_condition=='dial_threshold' and contest is None)` — fails iff left on the bare native dial; passes whether Dev stamps a Fate Contest OR moves the win_condition. File a Delivery-Findings **Question** so Dev/Architect pick the marker deliberately, and note the assertion can be tightened to it in verify. Over-specifying one mechanism falsely-fails a faithful fix.
- **Span capture across a global tracer:** `init_tracer()` (idempotent) → `provider=otel_trace.get_tracer_provider()` → add a FRESH `InMemorySpanExporter` via `SimpleSpanProcessor` IMMEDIATELY before the seat, so `exporter.get_finished_spans()` holds only that seat's spans. Native-seat tell at the seating layer = `npc.edge_published` / `encounter.initiative_rolled`; Fate tell = `fate.opponent.seeded` / `fate.contest.seeded`. (Native BEAT spans don't fire at seating for a `pre_combat` standoff — they're combat-category/payload — so assert the DATA, not a beat span.)
- **Tell Dev to gate the WHOLE native-seeding block, not one branch.** `instantiate_encounter_from_trigger`'s `if cdef.category=="combat":` runs hp_depletion+initiative OR edge-publish with no ruleset check; a partial gate leaves one sub-branch leaking. The hp_depletion sub-branch is hard to RED directly (Fate `ruleset_config()` → FateConfig crashes initiative on a missing DEXTERITY map), so cover it with a Delivery-Findings **Improvement** + the regression pin guarding over-removal.

### De-nativization at the RESOLUTION layer (126-37 — the downstream half of 126-30)
Once SEATING stamps `win_condition == "fate_conflict"` (126-30), the *resolution* dial guards must also de-nativize, and they key on `win_condition`, not on `cdef.ruleset`. There are THREE distinct guards, each with an EXISTING `hp_depletion`/`contest` precedent to clone (Bind-the-Ruleset = REMOVE the native beat, not balance it — assert the dial is frozen + the beat is dropped/skipped, never a "balanced" delta):
1. **`beat_kinds.py::apply_beat`** (the belt — catches ANY caller; the narration loop calls it at `narration_apply.py:~6061`). Precedent: the `dial_suppressed_hp_depletion` block (~625). The doctrine-correct shape is a FULL short-circuit (early return with `skipped_reason`, `deltas=None`, no tags) placed beside the `encounter_resolved`/`neutral_actor`/`withdrawn_actor` returns — STRONGER than hp_depletion's partial dial-skip (which still runs HP/resolution). Unit-test it directly: build `StructuredEncounter(win_condition="fate_conflict")`, call apply_beat with a `strike` BeatDef (no `edge_delta` → no HP channel; Success → own delta `base`), assert metrics stay 0 + suppression event + no `metric_advance`. Cross-ruleset pin: `dial_threshold` still advances + emits `metric_advance`.
2. **`agents/tools/advance_confrontation.py`** (the narrator WRITE tool). Precedent: the `if encounter.win_condition == "hp_depletion":` refusal (~203) → `ToolResult.error(recoverable=True)` + `refused_*` OTEL attr. Clone it for `fate_conflict`. Test via the registered handler (`default_registry._tools["advance_confrontation"]`, builders in `tests/agents/tools/test_advance_confrontation.py`): assert `ERROR_RECOVERABLE` + dial frozen + `tool.confrontation.refused_fate_conflict`. `asyncio_mode=auto` → bare `async def test_`.
3. **`narration_apply.py` dispatch chain (~5502-5955)** (the suspenders — drops the narrator's native beat selections before the legacy loop). Precedent: the `elif cdef.resolution_mode == ResolutionMode.contest:` branch (~5915) that drops + emits `contest_beat_dropped_dial_blocked`. **Key gotcha:** a Fate *conflict* seats with `resolution_mode == beat_selection` (the native DEFAULT, confirmed via `uv run python`), so it falls past the `contest` branch into `else: _legacy_beat_path = True` — the leak. The new guard must key on `enc.win_condition == "fate_conflict"`, not on a resolution_mode. Drive the REAL `_apply_narration_result_to_snapshot` with the `synthetic_two_dial_pack` fixture + an OPPONENT-side `BeatSelection` (opponent beats skip the SOUL "The Test" PC-consent gate, so `from_explicit_action` default False is fine); capture events by monkeypatching `narration_apply._watcher_publish` (patched where USED). Harness to mirror: `tests/server/test_narration_apply_session_wiring.py`.
- **Belt + suspenders are BOTH required — don't collapse #1 into #3.** On the narrator path #3 fires first so apply_beat is never reached, but #1 is the belt for any other caller and must be pinned directly. Log a Delivery-Findings **Improvement** saying so.
- **OTEL marker names: pin concrete, document HOW-agnostic.** Following the 126-30 workflow, assert concrete marker strings (`beat_suppressed_fate_conflict`, `tool.confrontation.refused_fate_conflict`, `conflict_beat_dropped_dial_blocked`) for the wiring test but keep the dial-frozen / beat-not-applied behavioral asserts as the primary RED, and log a deviation inviting Dev to finalize the exact strings (each must stay distinct + `fate_conflict`-gated).
- **Story-context file paths are often approximate on follow-ups** — 126-37's context said `sidequest/server/game/confrontation.py` (no such file). Always `grep -rn "def <fn>"` for the REAL location before writing the test; log it as a **Gap** Delivery Finding for Dev.
- **AC-5 "full pulp_noir e2e, only-Fate-spans" is an sq-playtest concern, not a unit test.** A real Fate-conflict-to-harm-resolution needs a live narrator turn loop (non-hermetic). Cover the OTEL "no native beat spans" requirement at the component level (each guard's suppression/drop event + absence of `metric_advance`/`beat_applied`) and log a `minor` deviation deferring the live-path e2e to sq-playtest.

### Testing inventory gained-lane dedup / apply-path bugs (126-33)
The `gained` inventory lane in `narration_apply.py::_apply_narration_result_to_snapshot` (the `for entry in result.items_gained` block, ~4856-5020) was the ONE lane that appended without an identity check — `recipient_char.core.inventory.items.append(item_dict)` at ~4994. Every sibling lane already matches by case-folded name before mutating: `items_lost` (~5032), `items_discarded` (~5057), `items_consumed` (~5096); the 45-13 container gate (~4876) blocks re-emitted *container* retrievals. So "dedup gained items" is filling the lone gap, and Dev should reuse the existing case-fold idiom.
- **Fixture combo for the gained lane (proven, hermetic):** `snap, base_pack = snapshot_with_pack` (conftest, non-Fate `test_pack` genre — no `fate_sheet` so the ~5002 `promote_gained_item` Fate path stays dormant); `character_named_sam` (non-Fate PC); `pack = copy.deepcopy(base_pack); pack.inventory = InventoryConfig(item_catalog=[]); pack.worlds = {}` → every gained item BARE-MINTS (`narrator:<slug>`), no catalog binding (mirror `test_item_gain_catalog_resolution.py`; deepcopy drops the conftest `worlds={}` stub so re-set it). Drive `_apply_narration_result_to_snapshot(snap, NarrationTurnResult(items_gained=[{"name": ...}]), "Sam", pack=pack, room=room_for(snap))`. `Inventory.items` is `list[dict]` (`creature_core.py:98`), entries carry `id`/`name`/`quantity`(default 1)/`state`.
- **Reproduce the bug via DOUBLE application, not hand-built dups:** apply the same grant twice through the real apply path (like the 45-13 container test grant→re-grant) so both entries come from production code; assert `len(items)==1`. ALSO add a pre-seeded-ledger variant (append a mint-shaped dict first, then grant) to cover the loaded-save path, plus case-insensitive-name and id-match variants (identity is id-first then case-folded name, matching the discard lane).
- **Observability (lie-detector) = a watcher EVENT, not an OTEL span here.** Inventory decisions emit `_watcher_publish("item_gain.*", {...}, component="inventory")` (catalog_resolved/narrator_minted), so the dedup contract is `item_gain.deduped` (component=inventory). Capture synchronously by monkeypatching `narration_apply._watcher_publish` (where USED). On current RED the duplicate grant emits two `item_gain.narrator_minted` + `state_transition` and zero `item_gain.deduped` — that absence is the AC-2 RED.
- **"dedup" bug stories carry a no-op-vs-merge design fork.** A re-narrated duplicate (Oz silver shoes — one unique item mentioned twice) is the container-double-retrieval shape → suppress (quantity UNCHANGED), not count up; incrementing quantity reproduces the same inflation in a different field. But ACs often say "no-op OR merge if quantity-tracked." Pin no-op as the contract in ONE isolated test (`test_dedup_does_not_increment_quantity`), keep the no-duplicate-ENTRY contract in the others (unaffected either way), and file a non-blocking **Question** Delivery Finding so Dev/Architect (Keith call — adjacent to 126-35 item→aspect promotion) can flip that single test if they choose merge. "do not silently drop" is satisfied by the loud dedup event, not by incrementing.

### Testing "operation X must NOT bump a wall-clock timestamp" (126-34, AC#3)
- **Pin the timestamp to a fixed FAR-PAST value, then assert it survives X — never rely on wall-clock deltas.** Two rapid calls can land in the same millisecond, so "read T0, do X, read T1, assert T1==T0" can false-PASS even with the bug live. Instead: create the row, `UPDATE … SET last_played = '2020-01-01T00:00:00+00:00'` (a known epoch-ms via `int(datetime.fromisoformat(...).timestamp()*1000)`), sanity-assert the pin took, run X (the poll), assert the value is STILL the 2020 pin. The buggy path rewrites it to `now()` (~2026, a far larger ms) → unmistakable RED; the fixed read-only path leaves it untouched → GREEN. Deterministic, no `sleep`, no flake.
- **AC#3 root, mapped:** the GM dashboard poll `GET /api/debug/state` (`rest.py::debug_state`) loads every session via `PgSaveRepository.for_slug` → `sessions.ensure_session` (`save_repository.py:108`), whose `INSERT … ON CONFLICT (session_slug) DO UPDATE SET last_played = excluded.last_played` bumps `last_played` (the source of `last_activity_ts` in `forensic.py::list_saves`) on EVERY read. The bump fires even when `load()` later returns `None` (no snapshot) — `for_slug` runs before the load. So a session with a sessions row but no snapshot still gets bumped. Drive the test through the REAL route (`TestClient(create_app(genre_pack_search_paths=[tmp_path], save_dir=tmp_path))`) so the fix is proven end-to-end, not at the `for_slug` seam (the fix likely swaps `for_slug` for a non-bumping load on the read path).
- **Capturing what `publish_event` broadcasts WITHOUT a bound loop or DB (watcher_hub):** monkeypatch the singleton's `publish` — `monkeypatch.setattr(wh.watcher_hub, "publish", lambda ev: events.append(ev))` — then call `wh.publish_event(...)` (the real production entry). Patch where USED (`watcher_hub.watcher_hub.publish`), not a fresh `WatcherHub()`. Neutralize the persistence side-effects first: `wh.bind_event_store(None)` makes `_persist_turn_telemetry` a no-op (sink None → returns), and non-`state_transition`/non-encounter event types skip `_maybe_persist_encounter_row`; `SIDEQUEST_WATCHER_AS_SPANS` is off by default. Set the slug with `wh.bind_session_slug(slug)` and ALWAYS clear it (`bind_session_slug(None)`) in fixture teardown — it's a process-global/ContextVar that leaks across tests. This tests the envelope-construction contract (`session_type`, `session_slug`) through the real publish path — the wiring test for any "tag the broadcast" story.

### Full-App UI wiring test reaches a dock widget via the mobile tab (153-25, sidequest-ui)
For an App-level "WS frame → handler → state → widget renders" test (the faithful AC for a dropped-message bug): model on `src/__tests__/death-banner-wiring.test.tsx` — `jest-websocket-mock` `WS({jsonProtocol:true})`, `<MemoryRouter initialEntries={["/play/<slug>"]}><App/></MemoryRouter>`, mock `/api/games/<slug>` + `/api/genres`, seed `localStorage` `sq:display-name` + `sidequest-history`, then `server.send({type:"SESSION_EVENT",payload:{event:"ready",has_character:true}})` and `findByPlaceholderText(/what do you do/i)` to reach game phase. **Gotcha:** `test-setup.ts` forces the *mobile* breakpoint (matchMedia `max-width:767px`), so GameBoard renders `MobileTabView`, NOT the dockview — dockview never mounts panel content in jsdom. To assert on a dock widget (Map/Items/Knowledge), click its tab: `fireEvent.click(await screen.findByRole("tab", {name:/map/i}))`; the widget mounts and re-renders on `mapData` prop changes (MobileTabView keeps `activeTab` across App re-renders; GameBoard is keyed on genre, stable). Use `document.body.querySelectorAll("rect[data-room-id]")` for SVG selectors (`screen` has no CSS-selector query). RED isolation trick: send the ALREADY-wired sibling frame first (here `MAP_UPDATE → map-overlay`) and assert it renders — that step passing while the new frame's assertions fail proves the harness works and the gap is the missing wiring, not the test.

### Server wire shape ≠ client TS type — test the real (untyped) JSON frame (153-25)
The server `DUNGEON_MAP` payload (`map_emit.py` `DungeonMapLocation`/`DungeonMapPayload`) omits `x`/`y`/`fog_bounds` that the client `ExploredLocation`/`MapState` mark *required*. Don't paper over it with a typed fixture + `x:0,y:0` — that hides the exact shape mismatch the story (AC-4) targets. Send the **exact untyped JSON** through `server.send({type,payload,player_id})` (the WS boundary is untyped, like the real wire), and assert the render still works. You can't assert an erased TS type at runtime — cover "type exists / no `as unknown as` blind cast" behaviorally (real shape renders) + leave the static guarantee to `tsc` + the lang-review #1 reviewer check, and log that split as a deviation.

### Driving SWN (space_opera) hp_depletion combat through the FULL dice dispatch (153-12, 2026-06-22)
**Pattern:** to RED-test a WN-combat resolution bug end-to-end, drive `dispatch_dice_throw` directly with a hand-built space_opera Firefight — model it on `tests/integration/test_opponent_reprisal_e2e.py` (`_make_snapshot`/`_make_encounter`/`_drive_player_shoot`), NOT on `_wn_round_102_4.seat_wn_combat`. **Why:** `seat_wn_combat` goes through `instantiate_encounter_from_trigger`, which rolls P4 initiative and **fails loud** if the PC stat block lacks the SWN DEX-flavor attribute `Reflex` (`make_pc` seeds heavy_metal's STR/DEX/… HM_STATS → `ValueError: player 'X' stat block has no 'Reflex'`). space_opera SWN attributes are **Physique/Reflex/Intellect/Cunning/Resolve/Influence** (classes.yaml); pass those as `character_stats`. **Sealed-vs-immediate fork (dice.py ~705):** `wn_sealed_round` requires `isinstance(ruleset, WithoutNumberRulesetModule) and cdef.win_condition=="hp_depletion" and bool(encounter.initiative)`. With a persisted `initiative` list you hit the production **sealed round** (`run_wn_round` → spans `swn.round.committed`/`.initiative`/`.resolved`); WITHOUT it SWN drops to the **legacy immediate path** (`_apply_committed_player_beat` called directly). Both funnel through the SAME `_resolve_wn_committed_action`, so test BOTH to force the fix into the shared helper. **hp_depletion realism:** park `EncounterMetric.threshold` at the `1_000_000` sentinel so an AC4 "no dial invented" assertion (`metric.current==0 < threshold`) is meaningful. A solo PC (one name in `snapshot.characters`) closes the commit barrier on a single throw. face 20→CritSuccess, face 1→CritFail regardless of the beat DC (game/dice.py) — use the crit extremes to pin a tier-gate without computing the DC.

### The SWN ✦ resolution-beat (Fall Back / Disengage) exit is dead under the WN-native path (153-12, 2026-06-22)
**Bug shape worth a regression guard on any WN-combat work:** `dice._resolve_wn_committed_action` (the ADR-143 native-engine-cut path, used for ALL beats under `WithoutNumberRulesetModule` + `win_condition=hp_depletion`) resolves ONLY via strike→HP + `check_hp_depletion`. It has **no resolution-beat branch**, so a `push`/`retreat` beat (`damage_channel=none`) no-ops: `enc.resolved` stays False, no `encounter.resolved` span, the seated opponent keeps reprising. The native `beat_kinds.apply_beat` DOES end the fight for a resolution beat (line ~1131, "ungated by hp_depletion ON PURPOSE") but that branch is removed from the WN path. space_opera authors TWO such beats: `combat::retreat` (Fall Back) and `ship_combat::disengage`, both `resolution: true`. **Subtle spec point for Dev:** the native branch fires on the UNCONDITIONAL `beat.resolution` flag (any tier), but story 153-12 AC1/AC2 require the WN fix to be **tier-gated** — Success/CritSuccess ends, Fail/CritFail leaves combat live (the ✦ beat has a DC; a failed disengage is a failed skill check). A naive port of the native unconditional behavior passes AC1 but FAILS AC2.

### Behavior/OTEL wiring test for "handler threads room_id into inject()" (153-23, 2026-06-22)
**The gap shape:** 107-2 built the whole per-room creature-binding path (`room_creature_binding.resolve_room_creatures`, `monster_manual_inject.inject(..., room_id=)`, `monster_manual.room_bound` span) and tested it with an EXPLICIT `room_id`, deferring the handler→inject plumbing as a "blocking delivery finding." 153-23 is that finding: `websocket_session_handler._execute_narration_turn` (the `inject(...)` call ~line 843) never passed `room_id`, so the authored entrance `gnaw_swarm` was dead code in production. **Lesson:** when a prior story's tests all pass a param explicitly and the docstring says "the live key is owned by 107-1 / tracked as a delivery finding," the production-wiring test is a *separate* RED story — don't assume green unit coverage means the feature ships.
- **Drive the REAL production turn, never source-text.** Reuse `session_fixture` (gives `(sd, handler)`), stub the collaborators exactly like `tests/server/dispatch/test_monster_manual_inject.py::test_execute_narration_turn_refreshes_stale_monster_manual`: `sd.orchestrator.run_narration_turn = AsyncMock(side_effect=_capture→_make_minimal_narration_turn_result())`, `sd.local_dm = MagicMock(decompose=AsyncMock(return_value=DispatchPackage(...empty...)))`, `handler._validator = MagicMock(submit=AsyncMock(), is_running=MagicMock(return_value=True))`, then `await handler._execute_narration_turn(sd, action, _build_turn_context_for_test(sd))`. `_execute_narration_turn` reads `snapshot = sd.snapshot`.
- **Keep the turn LIGHT but resolve REAL content by GRAFTING onto the fixture's MagicMock pack.** `session_fixture`'s pack is `MagicMock(progression=ProgressionConfig(), drama_thresholds=None, rules=RulesConfig())` — `rules.ruleset="dial"`, `combat_encounters=True` by default, so the per-turn dial tick stays cheap. Don't swap in the real WWN pack (drags the WWN module + heavy progression through the turn). Instead set just `sd.genre_pack.source_dir = real.source_dir` and `sd.genre_pack.effective_bestiary = real.effective_bestiary` (from `load_genre_pack(find_pack_path("caverns_and_claudes"))`), so `resolve_room_creatures` reads the shipped `worlds/beneath_sunden/rooms/entrance.yaml` + real bestiary while the turn machinery stays on dial. For a fully isolated key-resolution test, write a tmp `worlds/<world>/rooms/<id>.yaml` + a `Bestiary([BestiaryEntry(...)])` lambda instead.
- **`ensure_loaded` short-circuit:** set `sd.monster_manual = MonsterManual(genre=, world=)` (empty real Manual) BEFORE the turn so `ensure_loaded` returns on its first line and the opponent comes PURELY from the per-room binding (a MagicMock pack would otherwise blow up `_seed_authored_npcs`). Also set BOTH `sd.world_slug` and `sd.snapshot.world_slug` to the bound world.
- **`region_for()` setup (the key under test):** the binding room id must come from `snapshot.region_for()` (per-PC `pc_regions`), which NEVER falls back to `current_region` (No Silent Fallbacks). `session_fixture` seats `player_seats["player:TestHero"]="TestHero"`, so `snapshot.pc_regions["TestHero"]="entrance"` → `region_for()` returns "entrance". **Decoy test that pins the right key:** set `current_region` and the PC's scene `character_locations` to nodes with NO room file, `pc_regions` to the real bound room → only a region_for-keyed fix places the creature. **Additive guard (passes on develop AND after fix):** seat a 2nd PC in a different region → `region_for()` is None (split party) → assert NO creature placed + NO `room_bound` span (the fix must not fabricate a room id).
- **Spans:** `otel_capture` (SimpleSpanProcessor, synchronous) — `[dict(s.attributes) for s in otel_capture.get_finished_spans() if s.name == SPAN_MONSTER_MANUAL_ROOM_BOUND]` right after the awaited turn; no `sleep(0)` needed (that's only for the watcher_hub async-bridge path).
- **AC3 "arm the encounter" scope trap:** "the engine starts/arms the encounter" against a WWN-bound pack is epic-108/ADR-143 de-nativization, NOT this wiring story (SOUL "Bind the Ruleset, Don't Balance It"). Test AC3 as **Other-readiness** — placed creature is a real hostile (disposition<0, hp>0, threat_level, creature_id from `_creature_patch_from_bestiary_entry`) so the existing combat path can seat it — and log a deviation + delivery finding deferring full `StructuredEncounter` arming. Pre-committing combat on room entry also violates "untaken bait."
- **AC4 with no content surface:** when an AC ("authored treasure → real inventory") has no schema/resolver to place against (`entrance.yaml` has no treasure, `resolve_room_creatures` only does `encounter_creatures`) and the negative half already holds (narrator items already mint `id="narrator:{slug}"`, `narration_apply.py:5167`), DON'T write a vacuous test — log a Gap delivery finding + a deviation and move on.

### Driving `_execute_narration_turn` to the ORCHESTRATOR seam to test a narrator-loop degrade (2026-06-27, 158-41)
- To RED-test the narrator/SDK-loop boundary (e.g. `AnthropicSdkLoopExceeded` from `run_narration_turn`, `websocket_session_handler.py:1075`): use `session_fixture` `(sd, handler)`, then **bypass the two pre-1075 LLM/IO seams** so the turn reaches the orchestrator call deterministically:
  - `monkeypatch.setattr(monster_manual_inject, "ensure_loaded", lambda _sd: None)` — skips the inject block (834-887). Without this you hit the **live develop red at `monster_manual_inject.py:184`**: the `session_fixture` pack's MagicMock `effective_bestiary` unpacks to 0 values → `ValueError: not enough values to unpack (expected 2, got 0)`. (This is WHY `tests/server/test_turn_record_wiring.py` is red in-workspace — pre-existing, not your change. Filed as a delivery finding.)
  - `monkeypatch.setattr(wsh, "execute_intent_router_pre_narrator_pass", AsyncMock(return_value=(None, None)))` — the router pass (1011) is a module-level import (`wsh` line 100) and would otherwise spawn a real Claude client. The factory `intent_router_pass.build_intent_router_for_session` (971) is the documented "tests monkeypatch this" seam, but patching the awaited pass fn at the handler module is the cleaner one-liner.
  - Everything between (seed_tick `ensure_initial_draw` 888, `refresh_turn_context_post_dispatch` 1072) is deterministic — no further stubs needed. `handler._validator = MagicMock(submit=AsyncMock(), is_running=MagicMock(return_value=True))`.
- **The crash itself:** the method-level `try:` at 821 has only a `finally:` (2857) — NO `except`. So a raise from `run_narration_turn` files the degraded `TurnRecord` in the finally and then **re-raises**, escaping to the WS layer → `disconnect_save` (578) → teardown. The "session stays alive" RED is simply `await handler._execute_narration_turn(...)` must NOT raise (a raise IS the teardown trigger). `AnthropicSdkLoopExceeded`/`AnthropicSdkCostCeilingExceeded` are caught NOWHERE in `server/` or `handlers/`.
- **Observability assertion:** capture the GM-panel degrade via `monkeypatch.setattr(wsh, "_watcher_publish", capture)` (signature `(event_type, fields, *, component=, severity=)`), NOT a span exporter — `publish_event` only mints an OTLP span when `SIDEQUEST_WATCHER_AS_SPANS=1`. **Player-message assertion robust to Dev's message-type choice:** `json.dumps([m.model_dump(mode="json") for m in outbound]).lower()` then search the phrase token; check `payload.reconnect_required` falsy across all messages (the crisp "no forced reconnect" = session-alive signal at the message layer).

### A firewall has multiple doors — enumerate the sibling gates (158-34)
When RED-testing a fix that firewalls ONE entry gate, enumerate every *sibling* gate before
declaring the invariant covered. A firewall is routinely bolted onto the gate that produced the
repro and leaves the sibling open — same symptom, different door.

Concrete (dogfight opponent seating, `encounter_lifecycle.py`): the opponent is seated through TWO
doors, both guarded by `not npcs_present`:
1. **location-fallback door** — `_npc_fallback_at_location` grabs a co-located hostile when the
   router named none. #1084 (158-31) firewalled THIS door (`resolution_mode != sealed_letter_lookup`).
2. **router door** — `npcs_present=[NpcMention(...)]` from the intent-router pre-pass (ADR-113).
   #1084 left it OPEN: a router-named `NpcMention(is_creature=True)` skips both seating branches
   (both gated on `not npcs_present`), passes the sealed-letter arity check (`len == 1`), and is
   seated as the enemy *ship* — 158-34's exact ground-creature symptom via the second door. The
   router runs FIRST, so it's the *more* likely real path. RED test:
   `tests/server/dispatch/test_dogfight_router_named_scale.py`.

**Check for sibling-story over-delivery before writing RED.** 158-34's *literal* scope shipped under
sibling **158-31's PR #1084** (one dev followed the whole ADR-153 Plan-1 doc, Tasks 1-5, under one
story). Run the existing tests first (`test_dogfight_seating_scale.py`,
`test_dogfight_default_opponent.py` → GREEN) and `git log --oneline -- <testfile>` to find the
delivering commit. Don't fabricate a duplicate RED; find the genuine remaining gap (here: the router door).

**OTEL span capture in `tests/server/dispatch/`** — there is NO `otel_capture` fixture there (it lives
in `tests/agents/conftest.py`). Replicate as a local fixture: `init_tracer()` →
`otel_trace.get_tracer_provider()` (assert `isinstance(..., TracerProvider)` or spans are NoOp) →
`provider.add_span_processor(SimpleSpanProcessor(InMemorySpanExporter()))`, `yield`, `processor.shutdown()`
in `finally`. The seating lie-detector is the `participant.joined` span `source` attr ∈ {`router_named`,
`location_fallback`, `roster_resolved`, `materialized`, `frame_default`, `friendly_fallback`} (set at
`encounter_lifecycle.py` ~1702/1748/1764/1793, read ~2011). A frame-default *replacement* of a rejected
mention must show `source="frame_default"`, never `router_named` on the rejected name — that's how you
prove a substitution is observable, not silent.
### Expansion-quest premature completion + the silent mint span (2026-06-27, 158-42)
- **The reach_deep premature-completion bug lives ENTIRELY in the observer, not the per-turn handler.** `make_expansion_quest_observer` (`dungeon/expansion_quest.py:405`) does mint (`reconcile_dungeon_quests_into_log`) AND resolve (`resolve_expansion_quests(reached_region_ids={to_region})`) in one call; `_beat_fired` fires `reach_deep` the instant `anchor_region in reached_region_ids`. When the anchor IS the entry region (a 1-region expansion, or one where r0 scored deepest — the real beneath_sunden case), entering it both mints and completes the quest in a single `notify_region_transition`. The websocket per-turn resolve (`websocket_session_handler.py:1623`) passes `reached_region_ids=set()`, so it only handles big_bad/set_piece — reach_deep resolution is observer-only.
- **Test at the observer seam, driven through `notify_region_transition`, NOT at `resolve_expansion_quests` directly.** The existing unit test `test_expansion_quest_resolve.py::test_reach_deep_resolves_on_arrival` ENCODES the buggy behavior (reaching the anchor completes) — but it's correct *in isolation* (reaching the anchor IS the reach_deep beat). The fix is upstream (don't complete on the mint transition / anchor must be a genuinely deeper region), so pin it through the real `seed_expansion_quest` + observer + `notify_region_transition` path. This is robust to BOTH candidate fix locations (seed-time anchor selection vs resolve-time gating). Harness: copy `_otel_in_memory()` + the `_spans_module.tracer = lambda: real_tracer` monkeypatch + `register_frontier_observer`/`unregister_frontier_observer` (try/finally) from `test_expansion_quest_e2e.py`.
- **A single-region expansion is the minimal RED repro AND it PROVES a seed-time-only fix is insufficient.** `_single_region_expansion` (one node, necessarily `_deepest`) → anchor==entry. A pure anchor-selection fix can't help (no deeper region to anchor to), so the single-region test forces a resolve/observer-time gate. `seed_expansion_quest` for `reach_deep` ignores `manifests_by_region` (only the big_bad path reads it) → pass `{}`. Region id format `exp{id:03d}.r0`.
- **HOW-agnostic premature assertion:** assert `quest_log[qid].status != "completed"` (+ `== "active"` for the visible-quest case) after the entry transition, not a specific mechanism. The mint-on-entry IS desired (player must see their active objective; the "never mints" case is sibling 158-43) — so assert present + active, but the load-bearing anti-assertion is "not completed."
- **The OBSERVABILITY gap is the MINT, not the completion.** `dungeon.quest.bound` (seed) and `dungeon.quest.resolved` (resolve) spans already exist and route to the GM panel; the projection that makes a quest player-visible (`reconcile_dungeon_quests_into_log`) emits NOTHING. Per the 126-30/126-37 doctrine, I pinned a concrete contract name `dungeon.quest.minted` (mirrors the bound/resolved family, same SPAN_ROUTES home) for the wiring assertion but logged a Question finding so Dev/Architect can finalize the exact name. Completion-side observability (AC2) is already satisfied by the existing `dungeon.quest.resolved` span — guarded by the two-region descent test, not re-driven.
- **Genuine design fork to flag, not decide:** the exact reach_deep "exit condition" ("the way down past it") is under-specified — "don't resolve on the mint transition" vs "resolve only on a region strictly deeper than the anchor/start" have different blast radius (the latter breaks the blessed `test_expansion_quest_e2e.py` happy path, which completes on reaching the deeper anchor r1). I kept the two-region regression test aligned with that existing e2e contract (complete on reaching the deeper anchor) and filed the precise-semantics fork as a non-blocking Question for Dev.
