## Dev Patterns

<!-- migrated from Claude auto-memory store, 2026-06-24 -->

### Reviving a dormant TurnContext field (snapshot→context bridge) + triaging pre-existing develop failures via stash-compare (2026-06-28, 158-48)
- **The "dormant zone" GREEN fix.** When a `TurnContext` field is declared + consumed (a prompt zone gates on it) but `_build_turn_context` never populates it, the fix is a snapshot→context bridge, NOT new prompt logic. 158-48: `snapshot.pending_resolution_signal` was stamped on a WN kill but never threaded into `TurnContext.pending_resolution_signal`, so the `[ENCOUNTER RESOLVED]` zone was dormant. Fix = a shared `_thread_resolution_signal(turn_context, snapshot)` helper (`session_helpers.py`) called by BOTH context-build seams — `_build_turn_context` (DICE path: the resolution close stamps BEFORE this pre-narrator build, since `dispatch_dice_throw` runs in the handler first) AND `refresh_turn_context_post_dispatch` (free-nav path: a dial/yield close stamps DURING dispatch, after the pre-dispatch build). A one-seam fix leaves the other path broken. Mirror the #1086 cold-seat helper (`_project_encounter_fields`) shape so the two seams can't drift. Identical precedent: story 81-3 `pacing_hint` (`tests/server/test_pacing_hint_turn_context_wiring.py`).
- **One-shot lifecycle = clear at the handler, not read-and-clear at the seam.** The signal is read-once-per-turn. Clear it in `_execute_narration_turn`'s `try/finally` AFTER the orchestrator call (the `finally` runs on the degrade path too — `AnthropicSdkLoopExceeded` is caught and returns, but the finally still fires before the return). Place it BEFORE `state_apply` (~line 1168), which may stamp a FRESH signal (narrator-beat close) that must survive to the next turn. Do NOT read-and-clear inside `_build_turn_context` (loses the signal on retry; breaks the dual-seam). The `ResolutionSignal` docstring documents this ("reads this slot on the next turn and clears it"). Test the clear by driving the real `_execute_narration_turn` via the `session_fixture` harness (`tests/server/test_turn_record_wiring.py` shape) and asserting `sd.snapshot.pending_resolution_signal is None` after — verify it's non-vacuous by neutering the `finally` line and confirming RED.
- **`session_fixture` (`tests/server/conftest.py`) is BROKEN for the full `_execute_narration_turn` path on develop** — its MagicMock pack's `effective_bestiary` returns a non-unpackable mock, so the per-turn monster_manual injection (`monster_manual_inject.py:184`, `world_bestiary, _ = effective_bestiary(...)`) raises `ValueError: not enough values to unpack`. `test_turn_record_wiring.py` (3 tests) is RED on develop because of this. Workaround in your own test: `sd.genre_pack.effective_bestiary = MagicMock(return_value=(None, "genre"))` — `(None, ...)` is a VALID production return (no bestiary), and `purge_foreign_world_creatures(None)` short-circuits cleanly. (`([], ...)` does NOT work — it isn't None, so the purge then hits `[].entries`.)
- **Triage "regressions" with stash-compare before believing the runner.** A broad `testing-runner` sweep flagged 10 "new regressions" (arc-embedding ×13, leak_audit span, `husk_reaped_this_turn` field governance, melee stat_check). ALL failed IDENTICALLY on `git stash`'d committed HEAD (test-only RED commit, no source changes) → pre-existing develop test-debt, NOT caused by my change. The runner's "likely related to your span filter" guess was wrong. Method: `git stash; uv run pytest <flagged tests> -n0 -q; git stash pop` — same counts → pre-existing. Span-count tests also bleed in big combined `-n0` runs (run the suspect file ALONE). `Span.open`'s None-attr filter (the 158-48 yield_side fix) is genuinely safe: OTEL already rejects None attrs (logs "Invalid type NoneType"), so filtering only removes the warning, zero behavior change — proven by the leak_audit/arc spans failing the same with/without it.

### Production-path wiring test for an `emit_event` call site — and proving it actually guards (2026-06-25, 159-3)
- To guard a call inside `emit_event`'s fan-out (e.g. `expand_visibility_for_companions` at emitters.py:658), a test that calls the helper directly is NOT enough (delete the call site, test stays green). Drive the REAL `emit_event` and assert on the per-recipient `projection.filter.decide` OTEL spans. Scaffold (mirror `tests/server/test_merged_mp_emitter_projection.py`):
  - autouse `_pg_isolation(migrated_db, monkeypatch)` fixture (TRUNCATE all tables, set `SIDEQUEST_DATABASE_URL`, `db_pool.close_pool()`); needs `SIDEQUEST_TEST_DATABASE_URL` + `just pg-up`.
  - `repo = _build_pg_repos_for_slug(db_pool.get_pool(), slug=, mode=str(GameMode.MULTIPLAYER), genre_slug=, world_slug=)`; `handler._event_log = EventLog(repo)`; `handler._projection_cache = ProjectionCache(repo)`.
  - `handler._projection_filter = ComposedFilter(rules=load_rules_from_yaml_str("rules: []"), pack_slug=None)` — empty genre rules suffice when the kind is gated structurally in `CoreInvariantStage` (NARRATION_SEGMENT / SECRET_NOTE are).
  - `handler._session_data = _SessionData.__new__(_SessionData)` then set `.snapshot` (a `GameSnapshot` with ≥1 character), `.player_id`, `.genre_slug`, `.world_slug`.
  - `RoomRegistry().get_or_create(...)` → `room.connect(pid, socket_id=...)` + `room.seat(...)` for each player; `room.set_player_identity(...)`; attach `asyncio.Queue` outbound queues.
  - In the test: `monkeypatch.setitem(session_handler._KIND_TO_MESSAGE_CLS, "<KIND>", _FakeMsg)` and `monkeypatch.setattr(views, "status_effects_by_player", lambda _h: {})` to skip delivery-wrapping + status-overlay complexity. Pass a plain dict as `payload_model` (it's `json.dumps`'d). Use `author_player_id=<emitter>` so the emitter is projected too (Track A); recipients = connected − emitter.
  - Assert: `{span.attributes["player_id"]: span.attributes["decision.include"] for span in exporter.get_finished_spans() if span.name == "projection.filter.decide"}` has the expected include per recipient.
- **ALWAYS prove the guard bites:** after the test is green, temporarily disable the production call (`envelope = envelope  # disabled`), run ONLY the new test, confirm it FAILS, then restore. A wiring test that can't fail when the wiring is removed is theater. (This is exactly the gap a reviewer caught here — the original "wiring test" called the helper directly and stayed green with the call site deleted.)

### PII out of telemetry: emit a source/boolean, never the identity value (2026-06-25, 159-3)
- When a new `_watcher_publish`/`publish_event` carries an identity (Cf-Access email, token), drop the raw value — watcher events are broadcast to the GM panel AND persisted (`_persist_turn_telemetry`). Mirror `bind_player_identity` (connect.py): it publishes `{"player_id", "source"}`, never the identity string. For correlation, lean on server-minted `player_id`s (not PII) in a downstream span (`companion.routed_as_pet` carries `pet_player_id`/`owner_player_id`). Bound any user-supplied connect-payload string with `Annotated[str, Field(max_length=N)] | None` (lang-review #11) so it can't bloat the dict/telemetry.

### Without-Number ruleset module wiring checklist — 3 non-obvious touchpoints (2026-05-29, PRs #520/#521)
- Adding a new SWN-family module (`class XwnRulesetModule(SwnRulesetModule)`): module + `XwnConfig(SwnConfig)` + `RulesConfig.xwn` validator + registry entry are obvious. Three plans keep omitting:
- (1) `sidequest/telemetry/spans/__init__.py` re-export — add `from .<x>wn import *  # noqa: F401, F403` (alphabetical), else SPAN_X* constants are unreachable and `tests/telemetry/test_routing_completeness.py` silently SKIPS them.
- (2) `sidequest/server/dispatch/dice.py` downed seam — `resolve_downed` is dead in production unless you broaden BOTH the seam guard (`pack.rules.ruleset == "cwn"` → `in ("cwn","wwn")`, ~line 652) AND `_physical_save_target_for`'s `isinstance(cfg, CwnConfig)` → `isinstance(cfg, (CwnConfig, WwnConfig))` (~line 929). `WwnConfig` extends `SwnConfig` NOT CwnConfig — the isinstance MUST be a tuple.
- (3) OTEL span-assertion tests — mirror `test_cwn_{shock,trauma,downed,system_strain}.py`: InMemorySpanExporter + SimpleSpanProcessor, pass `_tracer=tracer`, assert the LITERAL `<x>wn.*` span name (catches a cwn.*-leak regression).
- Stays cwn-gated until later plans: chargen System-Strain seeding (`game/builder.py`), `adjust_system_strain`/`stabilize_mortal_injury` narrator tools. Ship gunnery/hacking seams stay EXCLUDED for wwn by design.
- Plan-2 add: lifecycle seams (`Session.end_scene`) have NO path to `pack.rules.ruleset` (Session holds only the snapshot; GameSnapshot carries genre/world slug but no ruleset/pack ref). Thread the ruleset slug from `connect.py`'s loaded genre_pack → `SessionRoom.bind_world(ruleset=…)` → `Session.__init__`, gate on `self._ruleset`, resolve via `get_ruleset_module()` with an isinstance fail-loud guard. Prefer bound `Session._ruleset` for lifecycle seams, caller-injected `pack` for dispatch seams (narration_apply/dice_throw have it; yield_action does not). Never load the pack inside a snapshot method via search_paths (silent-fallback trap).

### FATE_STATE.conflict is the shared extension surface for in-conflict UI data (150-2, PR #973)
- The Fate conflict surface (`FateConflictSurface.tsx`) is fed by `FateStatePayload.conflict` (`FateConflictEntry`), built ONLY in `game/ruleset/fate_projection.py::build_fate_state_payload` and broadcast (change-gated) by `server/websocket_handlers/fate_state_emit.py::_maybe_emit_fate_state`. Anything the conflict UI needs that "isn't on the wire" goes HERE — there is one builder + one emitter, no second path. The router/narrator use the *compact* `build_fate_projection` (different fn, flat vocab) — don't confuse them.
- Pattern for adding an in-conflict field: (1) add the typed field to the wire model in `protocol/models.py` (`FateConflictEntry` or `FateConflictParticipant`), empty-default for back-compat + `extra:"forbid"`; (2) populate it in `build_fate_state_payload` — opponents resolve via `snapshot.find_creature_core(actor.name).fate_sheet` (opponents are NPCs, NOT in `snapshot.characters`); a **player** participant's sheet already rides in `payload.characters`, so don't duplicate it on the participant (gate on `actor.side != "player"`); (3) a sheetless non-player actor degrades to an honest empty value — the seated-without-a-sheet invariant is enforced loudly in `decide_opponent_action` (#966), never re-asserted in the projection; (4) emit a `fate.*` OTEL span at the emit site (`SPAN_*` constant in `telemetry/spans/fate.py` + a `SPAN_ROUTES` entry — the routing-completeness lint requires it for SPAN_* constants — + add to `__all__`). Compute span numbers from the WIRE payload so the lie-detector confirms what the client got.
- **#403 (committed-this-exchange flag) lands on this exact surface:** add a per-PC `committed` bool to `FateConflictParticipant` (player-side this time), set it from `encounter.fate_commits` (an actor has a sealed commit this exchange), and the UI gates its proactive tiles / shows `fate-sealed-hint` on it. Reuse this pattern; don't relax the server `dispatch_fate_throw` sealed-commit guard (it's correct).
- ADR-143 doctrine for any Fate "progress/win" surface: the meter is the opponent's **stress fill toward taken-out** (checked stress boxes + taken consequences), NOT the vestigial native `opponent_metric.tension` dial. Project the FateSheet; never read the dial.

### Player-facing character-sheet fields are projected in views.py::party_member_from_character (93-4)
- The single wiring seam between `Character`/session state and the React sheet is `sidequest-server/sidequest/server/views.py::party_member_from_character(handler, sd, character, player_id, player_name)` — it builds `CharacterSheetDetails` (protocol/models.py) which rides PARTY_STATUS. It has `sd.lore_store`, `sd.genre_pack`, the `character`, and identity. Add a new sheet field here, not in a handler — this is the function every PC (requesting + peer MP) flows through.
- Pattern for a new sheet surface: (1) add the typed model + field to `protocol/models.py` (`CharacterSheetDetails`, default_factory for back-compat); (2) compute it in `party_member_from_character` (local imports inside the fn — that's the house style for this fn); (3) mirror the TS type in `sidequest-ui/src/types/payloads.ts` and the field on `CharacterSheetData` in `components/CharacterSheet.tsx`. The 93-2 `creation_answers` → 93-3 render → 93-4 `lore_fragments` chain is the reference trail.
- OTEL for a sheet-projection decision: emit `publish_event(<event>, {...}, component="rag")` via local `from sidequest.telemetry.watcher_hub import publish_event as _watcher_publish` — matches the sibling `character_creation_seed` emit (chargen_mixin.py:1467). Caveat: this fn runs on EVERY PARTY_STATUS, so a naive emit fires per-build (not once) — fine for a lie-detector, gate it if the GM tab gets noisy.

### How to work with Keith
- **Senior architect, 30-year dev, learning Rust via the SideQuest port.** Talk at the architectural abstraction level — pattern names (trait objects, actor model, CQRS, composition), trade-offs, system decomposition. Don't over-explain general programming concepts.
- **Teaching mode on by default.** Explain Rust idioms as they come up, framed against the Python equivalent first (the port is Python→Rust), then Go when Python can't express the concept (traits → Go interfaces, ownership, compile-time guarantees). `#[derive(Serialize)]` ≈ `@dataclass`; traits ≈ Go interfaces, not Python ABCs.
- **Dictation artifacts expected.** "axiom" → "axum", "SERG" → "serde", "playlist" → "playtest", etc. Parse for intent before correcting.
- **Procedural world generation is a 30+ year domain for Keith** — MUSHcode, populinator, conlang markov, townomatic, gotown. SideQuest content systems are mature patterns he's rebuilt before, not novel experiments. Never treat conlang/NPC registry/tropes/POI as new.
- **Velocity calibration: ~20x human dev speed, sustained since Nov 2025.** Divide "human dev" estimates by 5-10x for parallelizable work. There is no "too much for one session."

### Core wiring patterns
- **Wiring means non-test consumers.** Before declaring GREEN, grep for the function/struct in production code paths. Library functions with no consumers are stubs, not features.
- **Wiring means the full pipeline.** "Wire X into Y" = source → intermediate layers → rendered output, not "component accepts props." ACs and tests must cover the full data flow.
- **Wiring means visible in the GM panel.** Every subsystem emit site gets an OTEL watcher event with content the lie detector needs to refute a Claude hallucination (e.g. `room_count`, `hp_delta`, `item_added`). Internal data flow without OTEL is not wired.
- **Combat wiring trace (reference):** IntentRouter → Orchestrator → lib.rs → Protocol → UI CombatOverlay. This is the template for verifying any intent-to-UI pipeline.
- **Debug, don't rebuild.** The code is ~98% done — find the small break. Trace the existing pipeline front-to-back before writing anything new. Dev agents repeatedly rebuild pipelines that already work.
- **Source-level wiring guard tests.** When a runtime integration test is impractical (dispatch_connect has 55 params, dockview needs real DOM sizing, bool CLI defaults), read the source file from the test and assert the wire exists as a substring/pattern within N lines of the emit site. Pattern reference: `map_telemetry_wiring_tests.rs` in sidequest-server, `gameboard-wiring.test.tsx` in sidequest-ui. These catch "someone deleted the wire in a future refactor" at `cargo test` / `vitest run` time, not at playtest time.

### Architectural patterns
- **Rust vs Python split.** If it doesn't operate an LLM, it goes in Rust (game engine, state, protocol, markov chains, name gen). If it runs model inference (Flux, Kokoro, ACE-Step — *not* Claude), it's in the Python daemon. Claude CLI calls go through Rust subprocess.
- **Sidecar tool pattern** (Epic 20): Narrator calls tool → parser validates → `assemble_turn` consumes. Established by `item_acquire`, `merchant_transact`, `lore_mark`. All future narrator-exposed tools follow this pattern.
- **Script tool prompt pattern** (ADR-056): Expose Rust generator binaries to the narrator via skill-style checklists (carrot), not threatening gate language (stick). Clear command table, "when to call" guidance, post-call checklist. Never "You MUST" or "Do NOT" — LLMs bash against hard gates.
- **No server-side re-invocation of the LLM to judge the LLM.** Post-hoc validation lives in OTEL spans ("was the tool used? did output match state?"), not a second LLM round-trip. The user calls this the "God lifting rocks" problem.
- **React `key` as layout reset tool.** Changing `key` on a stateful component forces React to unmount and remount it fresh — cleanest way to reset dockview/three.js/etc. without imperative reset code. Used in `GameBoard key={currentGenre}` to fix the Dockview drift bug.
- **Dedicated protocol messages for structured UI actions.** UI button clicks must send a typed `GameMessage` variant (e.g., `BEAT_SELECTION`, `TACTICAL_ACTION`, `JOURNAL_REQUEST`, `CHARACTER_CREATION`) that carries the exact structured ID. NEVER synthesize natural-language text from button state and send it as `PLAYER_ACTION`. 2026-04-11 incident: commit `05a3dfb` "fixed" beat buttons by synthesizing `"${beat.label} (${beat.stat_check})"` into PLAYER_ACTION and fuzzy-matching it back — violated Zork Problem, no silent fallbacks, no half-wired features, OTEL observability, and "never downgrade to a quick fix." Pipeline: (1) add `GameMessage` variant, (2) add TS protocol type, (3) `send()` the structured message directly, not through `handleSend(text)`, (4) server validates strictly with no fallback, (5) narrator describes outcome AFTER the mechanical state change.
- **Monster Manual NPCs inject into `<game_state>`**, not meta-prompt sections. Format: `"- Name (role, faction) — brief personality, speech quirk"`. Proven across 6+ iterations that Claude reads game_state as world truth and meta-instructions (XML casting calls, `available_characters`, `tool_workflow`) as style inspiration only.
- **Content inheritance: base → genre → world.** Archetypes, NPCs, and most structured content resolve through a three-layer chain like a prototype chain / Python MRO. Base defines shape (no flavor), genre enriches with tone, world adds specific lore. Never flatten these layers — a "sage" in neon_dystopia vs low_fantasy must share structural identity while differing in flavor.

### Anti-patterns to reject at design time
- **Keyword / pattern matching for intent classification is the Zork Problem.** Natural language defeats finite verb sets every time. "I pretend to take the item" → keyword `take` wrongly triggers inventory acquisition. "I grab my backpack" → `grab` wrongly triggers combat. ADR-010 and ADR-032 document this. Intent classification is ALWAYS an LLM call (preferably folded into the narrator's Opus response for zero additional latency), never a regex or keyword heuristic.
- **The regex-fallback reflex is a global anti-pattern, not just intent-specific.** Zawinski: "Some people, when confronted with a problem, think 'I know, I'll use regular expressions.' Now they have two problems." If the system has to *interpret meaning* (fuzzy input, semantic intent, cliche detection, content validation), the model is the retrieval layer — don't regex where you could read. Regex is fine for bounded protocol contracts (port numbers, JSON keys, HTTP verbs) but a category error for semantic problems. Claude Code's own memory system is an example of what goes wrong: keyword-triggered recall produces 10 memory files for 1 canonical rule.
- **No AI judging AI.** Don't design second-LLM validators for narrator output. The first call is already the most semantic reader available.
- **No weasel words in design decisions.** Words like "cleanest," "simplest," "proper approach" are semantically empty. State WHAT the decision is + WHY it's correct (cite the constraint or principle) OR admit it's a workaround and name what the real fix would change.
- **No live LLM calls in test suites.** All tests mock `ClaudeClient`. Live-LLM integration tests belong in `--ignored` suites that run intentionally, not on every `cargo test`. A single accidental live-LLM test in the default suite burns tokens and costs 90+ seconds per run.

### Git / branch patterns
- **Gitflow on every subrepo.** Subrepos (sidequest-api, sidequest-ui, sidequest-content, sidequest-daemon) target `develop`. Only the orchestrator targets `main`. Always check `repos.yaml` before any git op.
- **Branch before editing.** `git checkout -b feat/description` BEFORE touching files, not after. Avoids the "hook-blocked commit → panic → stash scramble" failure mode.
- **Dirty work comparison pattern.** When local uncommitted changes conflict with a pull: commit dirty work to a temp branch, pull clean on the default branch, diff each file local-vs-remote side by side, categorize (identical / remote better / local has extra value / no remote change), then cherry-pick deliberately. Never stash.
- **Additive git ops need no permission** (commit, push non-force, pull --ff-only, checkout existing, checkout -b, fetch, add). **Destructive git ops always ask first** (reset, clean, force-push, branch -D, rebase -i, stash — banned outright).

### Playtest patterns
- **Pingpong cadence: every 2-3 minutes during active playtest.** Read `/Users/keithavery/Projects/sq-playtest-pingpong.md` frequently so new `open` items at the top don't pile up unnoticed. Update to `fixed` immediately when a fix lands — don't batch.
- **Always pull and test on new commits.** When `git log HEAD..origin` shows new commits during playtest: pull → rebuild → restart → test. No "want me to?" prompt.
- **Always rebuild on restart.** Every service restart during playtest is a full rebuild. Compile time is negligible vs. debugging a stale binary.
- **Keep going.** Work through the checklist autonomously during playtest. Don't pause to ask "want me to continue" — Keith wants momentum.
- **Restart in existing tmux panes.** Send Ctrl+C and re-run the command. Don't close/reopen panes — churn annoys Keith and wastes time.
- **Source-grep tests > integration tests for playtest fixes.** Faster to write, faster to run, catches the exact failure class (someone unwired it in a refactor) without needing a full stack harness.

### Spec hierarchy
- **Spec authority order:** Story scope (session file, highest) → story context → epic context → architecture docs / SOUL / rules (lowest). When sources conflict, the session scope wins. Log deviations **at the moment of the decision**, not at phase exit.

## Migrated from auto-memory — working style + system facts (2026-05-26)

> Folded in from the retired Claude auto-memory store.

### How we work
- **Boy-scouting is welcome but bounded.** Opportunistic adjacent cleanups (≤5 lines, ≤2-3 items/story: stale docstrings, unused imports, nearby typos) are fine — "ok as long as it doesn't go exponential." Defer anything needing its own design/grep/test surface as a non-blocking Delivery Finding. Distinguish "noticed while reading" (fix) from "needs analysis" (defer).
- **Just fix mechanical blockers; don't litigate scope.** Merge conflict, stale lock, version bump, env drift → fix it. Keith: "you spend more time proving tiny bugs aren't this scope than you do just fixing it." Only ask first if the fix is genuinely risky (rewrites a subsystem, touches shared infra).
- **Quoted flags/paths/APIs are law.** When Keith's question quotes a specific flag, env var, path, function, or API call, use that string verbatim — don't research alternatives (2026-05-11: he quoted `--tools ""`, Claude detoured to `--disallowedTools "*"`). Deviate only if the quoted thing demonstrably doesn't exist, and say so explicitly.
- **One mechanism per problem.** Replacing one mechanism with another → clean cutover, never "keep the old as fallback / run both for safety / second channel for a subcase." Two parallel systems is how you reach "we don't know what's actually happening" debugging hell. Name uncovered subcases as out-of-scope; if the new mechanism is insufficient day one, delay the cutover, don't hedge it.
- **No fallbacks (hard ban).** A retired/replaced mechanism gets ZERO backstop; "degraded → narrator-only / silent alternative path" is the worst kind (silently re-creates the illusionism bug OTEL exists to kill). On failure of a source-of-truth component, fail LOUD: ERROR span + surfaced to GM panel/player + explicit recoverable retry. Scan your own proposals for "fallback / degraded / backstop / default to / if that fails."
- **Content YAML is comment-free.** Genre-pack content YAML (portrait_manifest, visual_style, history POI, theme) carries no comments — a comment encoding a rule is an unenforced rule that rots; rules belong in validator code. When deleting a comment that encoded a real constraint, surface it for relocation, don't silently drop it.
- **New images go to R2, never git-LFS.** Upload PNG/JPG/WebP to R2 (cdn.slabgorb.com) despite residual `*.png filter=lfs` `.gitattributes` rules. The manifest is the canonical spec; a generated PNG in the working tree means the generator failed to auto-upload — fix the generator, don't commit.
- **Durable retention by default.** No-expiry for any save-referenced artifact (portraits, POIs, world-state, music) — SideQuest runs years-long campaigns; an 18-month-old save must still load its portrait. R2 cost is trivial; only reap with a save-aware sweep. Short CDN Cache-Control (24h) = freshness, not deletion.
- **Player declaration length mirrors the real playgroup.** Keith 2-4 sentences w/ tactical detail; Sebastien short/medium rules-probing; Alex one short line (NEVER a paragraph — load-bearing pacing inclusivity); James length-agnostic; Sonia droppable in ~8 words. Real solo turns ran 34-220 chars. Player gives intent; narrator does prose. Err short.

### System architecture facts
- **Auth is Cloudflare Tunnel + Zero Trust, not app-level.** The origin FastAPI server (:8765) has no login routes/session middleware/token checks by design. Never propose origin-side auth for new routes or raise "no auth gate" as a security concern. DO still consider in-app player-vs-GM role distinctions per route (Zero Trust groups everyone as "authorized player").
- **Image renderer is Z-Image Turbo** (Tongyi-MAI, 6B distilled, ~8 steps), `guidance_scale=0`, IGNORES negative prompts — any "no X" goes in the positive prompt at the end. Renders text from prompts, so lore proper-nouns/dates/quotes bleed in as garbled captions. Read `sidequest-content/PROMPTING_Z_IMAGE.md` before authoring prompts.
- **Live logs are at `~/.sidequest/logs/`** (rotating `sidequest-{daemon,server,client}.log` + timestamped archives). Old `/tmp/sidequest-*.log` is stale (deleted 2026-05-25). The daemon truncates logged prompts to the first ~150 chars (style prefix, not subject body).
- **Editing prompt content needs no daemon restart** — the render path rebuilds the PromptComposer per request via fresh `yaml.safe_load` in `media/catalogs.py` (no cache). Restart only for GPU model swap / re-warm / wedged worker. The real hazard is a slug RENAME (`CatalogMissError`), not prose edits.
- **Genre-pack pydantic models use `extra="forbid"`** (`sidequest/genre/models/`). Adding ANY new field to `theme.yaml` or a sibling validated YAML REQUIRES a paired server-model update in the SAME commit/PR — a content-only field add makes every pack fail `ValidationError` at startup. Scope such stories `--repos content,server`; verify with `load_pack('<pack>')`.
- **`genre_packs/<pack>/images/{poi,creatures}/` is legacy** (pre-bridge); the server POI resolver only reads world-tier `worlds/<world>/assets/poi/<slug>.png`. CARVE-OUT: `images/portraits/` is NOT legacy (renderer writes there, `loader.py:638` reads it — never touch). Tools: `tools/relocate_legacy_pack_images.py`, `tools/audit_poi_slug_mismatches.py`.
- **`/etc/hosts` `player1-4.local` → 127.0.0.1** for two reasons: per-origin cookie isolation between Playwright MP tabs, and dodging SPA-fallback gaps (FastAPI `StaticFiles(html=True)` only serves index.html for dir roots, so `/play/<slug>` 404s on the tunnel). Don't rewrite to Cloudflare IPs.
- **`/reference/...` pages are server-rendered HTML** by `reference_renderer.py` walking pack YAML at request time — NOT a static site / React SPA / build artifact. Design bundles are the visual SPEC the renderer absorbs; redesign targets `reference_renderer.py`.
- **MP is sealed-round / submit-and-wait (ADR-036)** — every seated PC submits each round, narrator resolves together; there is NO single "acting player," so per-turn effects (XP) are party-wide. Resolve the seated set via `snapshot.player_seats.values()`. Storage is per-player (ADR-037). Known port bugs hardcode `snapshot.characters[0]` for XP and narrator-granted inventory.
- **WN sealed-round barrier (102-4) waits on PCs ONLY, never NPC allies.** `ship_combat`/`hp_depletion` on an SWN-family ruleset routes through `server/dispatch/wn_round.py`: each *human* seat seals a Main Action (`seal_wn_commit`), and when the barrier closes (`wn_barrier_closed`) the round walks `encounter.initiative` in descending order — the **opponent acts at its own slot** via `_resolve_opponent_reprisal`, not as a reprisal rider. **Gotcha that deadlocked solo coyote_star (2026-06-10):** story 59-35 auto-seats scene-present FRIENDLY NPCs as `side="player"` EncounterActors (the crew ally), but those allies carry no SWN scores, get **no initiative slot**, and act on narrator beats — they NEVER seal. `wn_waiting_actors` counted them → barrier never closed → `commitment_pending` stuck True, `structured_phase` frozen at Setup forever. **Discriminator: PC ⇔ name in `snapshot.characters`; NPC ally ⇔ in `snapshot.npcs`** (an EncounterActor has no is_pc field). The barrier must exempt allies (`_seated_pc_names` / `wn_barrier_exempt_allies`); it must NOT auto-commit them (contradicts 59-35, and they're not in initiative to walk). Exempt allies are surfaced on the `{slug}.round.committed` span as `exempt_allies` for the GM panel.
- **UI WN seal lock:** `ConfrontationOverlay` must disable the beat grid once the local player commits (else a 2nd click hits the one-Main-Action seal guard → red error alert). Drive it off a local `sealedWaiting` flag set on commit, cleared when the server's `committed_actors` empties (round walk clears the seal ledger on resolution — `test_round_fire_clears_the_commit_ledger_and_payload_key`). Reset with the adjust-state-during-render pattern, NOT a setState-in-effect (eslint `react-hooks/set-state-in-effect`). Solo resolves same-dispatch so it re-enables next frame.
- **Player real names render ONLY in multiplayer** — in SP omit entirely (no em-dash, no "(you)"). Gate on `seated_player_count > 1`. NPCs never show a player name.
- **`pov_swap.py` contract** (post-2026-05-23): narrator writes the anchor PC's NAME for every PC sentence (pronouns reserved for OTHER characters via `narrator_prompts/pov_rules.md`); the rewriter only swaps names + sentence-local reflexives. The antecedent-blind pronoun→"you" Passes 5/6/7 are RETIRED — don't reintroduce.
- **ADR-098 stateless narrator** ships a bounded per-turn prompt (no `--resume`); recent-beat misses are mitigated by story 49-1's `recent_narrative_context` in `AttentionZone.Recency` (2kB/entry, ≤12kB budget). If misses persist, the budgets are too tight — reviving `--resume` is NOT the fix.
- **GM panel / OTEL is a dev/verification tool for Keith**, not player-facing — despite CLAUDE.md's "feature for Sebastien" framing. Sebastien's mechanics-first needs are served via the normal player UI. (Reframed by the DM-customer thesis as "the dev tool IS the product" — not in conflict.)
- **`audio2audio` variations reference the base theme by R2 key** in `ref_audio_input` (ACE-Step is WAV-native but only the OGG persists, to R2). `music_pipeline.generate()` downloads it via `r2_writer.download_pack_asset()` (OTEL `music.ref_audio.fetch`). `scripts/generate_music.py` runs against the LIVE oq-1 checkout with editable daemon imports — restart the daemon after landing daemon code or stale code skips the download.
- **Beneath Sünden** (caverns_and_claudes → contiguous edge-expanding Jaquaysed megadungeon, world `beneath_sunden`): code in `sidequest/dungeon/`; `generate_interior` braid sub-seed is `seed ^ 0x5EED` with a fixed-point collision at seed 24301; generators fail loud on too-small dims. Persistence contract "Seed = Expansion 0": `commit_expansion` persists only `new_nodes`/`new_edges`; the entrance (`expansion_id=0`) must be committed as `Expansion(0,[entrance],[])` first; call `rollback()` on `PersistError` (caller owns the txn).
- **Cookbook-procedural rooms emit `room_type=settlement`** (`room_yaml_emit.py`), not `cavern` — the cavern visual layer is independent (mask blob in `dungeon_map.mask` + runtime `.cavern.png`). `load_room_payload` requires cellular+derived+mask only for `cavern`.
- **Postgres (ADR-115):** 5 task-groups on ONE branch `feat/postgres-substrate` (no merge to develop until TG5). Local PG is `postgresql@18` keg-only (`/opt/homebrew/opt/postgresql@18/bin`). Tests use `SIDEQUEST_TEST_DATABASE_URL`; runtime reads `SIDEQUEST_DATABASE_URL` via `game/db_config.py`. Importer migrates ONE save ad-hoc; MUST normalize all `created_at` to isoformat (SQLite mixes space-format and isoformat).
- **Tactical renderer:** ADR-071 SVG-from-ASCII was abandoned (2026-04-23) for ADR-086 pre-rendered PNG maps via a Python port of maze-maker's Cellular. When touching tactical/Automapper code, prefer the `map_image`/`map_meta` path over `grid:`/SVG; metadata sidecar keeps `floor_mask` for LOS/walkability. (Verify current renderer state — entry is from mid-2026.)
- **Forensics telemetry substrate:** append-only `turn_telemetry` table persists every watcher payload via a sink in `watcher_hub.publish_event`, surfaced by `build_turn_bundle["telemetry"]`. Correctness signal is `conn.in_transaction`. Read path is strictly `?mode=ro`; pre-substrate rounds honestly show "save predates the substrate," never fabricated.

### Scope a "mandatory resource" gate to only-when-needed to cap blast radius (2026-06-01, story 65-8)
- **Context:** 65-8 added an R2-manifest existence gate so the lore page stops emitting broken POI `<img>` for authored-but-not-rendered POIs. AC1 said the manifest "fails loud when absent" (No Silent Fallbacks). Naively making `r2_manifest.json` a hard precondition of `assemble_lore_page` would have 500'd ~15 existing lore-render test files (and every tmp-pack/real-content render lacking a manifest).
- **What worked:** Require the manifest **only when the world actually authors POIs** — `if not authored_slugs: return frozenset()` *before* touching the manifest. POI-less lore pages never consult it, so they can't break. POI-bearing worlds (the only ones that could emit a broken image) still fail loud if their manifest is missing. This honors No-Silent-Fallbacks where it matters without coupling every render to the resource. Result: full server suite 9524 passed, 0 failed.
- **How to apply:** When a new mandatory resource gates a sub-feature, gate on "is the sub-feature even active here?" first. The loud-fail then fires only on the paths that genuinely need the resource. Pin the discovery path so it resolves in BOTH prod and fixture layouts — here `pack_dir.parent.parent / "r2_manifest.json"` maps to `sidequest-content/r2_manifest.json` (prod) and `tests/fixtures/r2_manifest.json` (fixture). Cache the loader with `@lru_cache(maxsize=N)` keyed on the Path for load-once-per-process; unique `tmp_path` per test keeps the cache from bleeding across tests. Fire one OTEL span per render (not per cached read) so the gate stays observable even when the file read was cached — reconciles "load once" with "every render is observable."

### Per-kind custom-beat seam for the N-seat table engine; vessel-scoped Hull reuses rig_pool.* spans (86-6)
- **The table engine (`game/table/engine.py`) hardcoded beat families** (`_POT_ACTIONS`, `cheat`/`read`/`accuse`/`fold`) and `ValueError`d on anything else. To let a new kind own its own verbs WITHOUT the engine learning them: add `TableGame.custom_beat(state, seat, commit, *, rng)` to the ABC (`registry.py`) whose **default raises `ValueError`** (preserves No Silent Fallbacks — poker + an unknown beat still raises), then replace the engine's terminal raise in `_apply_signature_beat` with `game.custom_beat(...)`. A kind (e.g. `war_rig_crew`) overrides it. Backward-compatible: poker/auction never define it, so they keep raising exactly as before. The regression guard (`test_poker_unsupported_beat_still_fails_loud_after_seam`) hinges on the default being a `ValueError`, not `NotImplementedError`.
- **A cooperative crewed vessel rides `resolution_mode: table_resolution` (ADR-129), NOT ADR-077 dogfight.** `TableSeat.private_state` already carries per-seat kind state — stamp `station` there in `deal()`. For a *cooperative* kind, `strength()` returns a uniform constant (0): the win condition is a shared resource (Hull) depleting, not a crew-vs-crew showdown; unit tests stay at one decision point (`max_decision_points` high) so `_showdown` never fires. Did NOT reshape `_showdown`.
- **Vessel-scoped Hull cannot be `RigComposurePool`** (its `character_id` is required+immutable+per-character OTEL-attributed). Build a parallel `WarRigHull(current,max,base_max,vessel_id)` in `game/war_rig_combat.py` that **reuses the `rig_pool.*` span constants** (`SPAN_RIG_POOL_{CREATED,DELTA,ZERO_CROSSING,CRASH_EVENT}`) — route `vessel_id` into the `character_id`/`chassis_id` attr slots the existing extract lambdas read (and also carry an explicit `vessel_id` attr) so the GM panel renders the crewed Hull with zero route changes. On Hull→0, fan 86-2's `resolve_crash_saves()` across **every** occupant (reuse, don't reimplement) + append `injury`+`dismounted` statuses to each, mirroring solo `handle_rig_crash`.
- **`category:` is closed-set** (`{combat, social, pre_combat, movement, hacking}`) but for `table_resolution` confrontations the validator `return self`s at the table branch BEFORE the category check — so the value is unguarded there. Still pick a valid one: a vehicular crew confrontation is `category: movement` (chase precedent), NOT `combat` — `combat` enlists it in the per-pack "exactly one combat confrontation" opponent-seed assertions (road_warrior 86-1's `test_road_warrior_loads_cwn.py::_combat_confrontation`).

### Player-facing legibility descriptors ride the encounter → build_confrontation_payload (73-4)
- To get a per-beat player-facing readout to the overlay: compute a frozen dataclass in `beat_kinds.py` (`describe_beat_impact` — SSoT), have `apply_beat` stamp `asdict(impact)` onto a **per-side** dict field on `StructuredEncounter` (`last_beat_impacts: dict[str, dict[str, Any]]`), and surface the player side in `build_confrontation_payload` as an **additive** key (mirror the `win_condition`/`player_hp` precedent — only add the key when present so legacy/back-compat tests `.get()` → None). `StructuredEncounter` is `extra="forbid"`, so new fields MUST be declared.
- Per-side storage matters: opposed_check calls `apply_beat` once per side in one turn; a singular field is clobbered by the opponent's beat unless the player's beat resolved the encounter first.
- Serialize to a plain dict on the model (not a nested pydantic model) to avoid serialization friction; keep the typed dataclass at the compute boundary only.

### Reference projection chrome rides the ROUTE layer, not the builders (2026-06-09 redesign)
- `build_lore_projection`/`build_rules_projection` stay pure for synthetic-pack unit tests (`pack="p"`, tmp dirs); anything that needs real-pack chrome (`theme`, now `meta`) attaches in `reference_routes.py` after the builder returns. Follow that precedent when the projection grows new top-level keys.
- Chrome gaps follow the PACK_TOC/DEFAULT_TOC doctrine: derive an observable fallback (humanized slug) AND fire an ERROR span (`sidequest.reference.meta_missing`) — never raise for missing prose chrome, never fall back silently. Routes already map ValueError → themed 500 for genuinely broken packs.
- Adding a live pack means adding entries to ALL FOUR chrome constants in `reference_theme.py` (LABELS/BLURBS/EPIGRAPHS/TOC); `tests/server/test_reference_meta.py::test_every_live_pack_has_chrome_entries` now trips on the gap (wry_whimsy was the first).

### Design-bundle handoff URLs (claude.ai/design) are gzip tarballs
- WebFetch on an `api.anthropic.com/v1/design/h/...` link saves a binary `.bin`; `tar -xzf` it. Read README.md + chats/ first (intent lives in the chat), then PORTING.md if present — it maps prototype files to production targets and is usually accurate about server contracts.

### react-refresh/only-export-components forces pure-helper extraction
- sidequest-ui lint errors when a component file also exports functions/constants. Put shared pure predicates in their own module (e.g. `nodeShape.ts` for NodeTree's classify/isCompactNode/slugify) — which is also what lets buildToc share the renderer's exact heading predicate so TOC and headings can't drift.

### Narrowing a pydantic field to `str` at a wire boundary is safe when a downstream model re-validates (2026-06-10, story 101-1)
- **What happened:** 101-1 retyped daemon `StageCue.camera` from `CameraPreset | None` (a `str, Enum`) to plain `str | None` to match the server's permissive wire contract. The worry: does any consumer do `cue.camera.value` or rely on enum identity? `grep -rn '\.camera'` showed consumers pass `cue.camera` straight into `RenderTarget(camera=...)`, and `RenderTarget.camera` stays `CameraPreset`-typed (recipes.py). So pydantic re-validates the str → CameraPreset at the *RenderTarget* boundary: a valid preset string coerces, an invalid one raises there (fail-loud preserved). The `.value` calls were all on `RenderTarget.camera`, never on `StageCue.camera`. Net: the StageCue narrowing required ZERO consumer changes — the permissiveness is intentional (server can send any string), and validation still happens, just one layer in.
- **Why it matters:** "Align this model's field to a looser type" reads scary (sounds like dropping validation), but if a downstream typed model re-validates, you're only moving *where* the strict check fires, not removing it. The contract gets permissive at the wire edge (good — matches the sender) and strict at the consumption edge (good — fail-loud). Confirm this before assuming a type change ripples into consumers.
- **How to apply:** Before a field retype, `grep -rn '\.<field>\b'` across the package, filter to the model in question, and classify each consumer: (a) reads `.value`/enum identity on THIS model → would break, needs work; (b) passes the value into another typed model → safe, that model re-validates; (c) truthiness/None checks → safe. Removing an enum member that other modules reference by direct attribute (`SomeEnum.MEMBER`) is *self-forcing* — those references become import-time `AttributeError`, so partial removal can't slip through (the dict configs in zimage_config.py). Raw-string references to the member's value (routing frozensets, kind-maps) are the silent-dead-code risk — grep the string value too, not just the symbol.

### Removing an extra=forbid field that persisted saves carry → add a migrations.py strip-shim, don't just delete (2026-06-10, story 101-2)
- **What happened:** 101-2 deleted the dead voice surface from the server. Four surfaces were pure deletion (genre voice models, `AudioConfig.creature_voice_presets`, `GenrePack.voice_presets`, `MessageType.VOICE_SIGNAL/VOICE_TEXT` + its `TARGETED_KINDS` route). The fifth — `Npc.voice_id` — needed a save-compat shim because `Npc` is `model_config={"extra":"forbid"}` and pre-removal Postgres saves persisted `voice_id` (always `None`, but written as a key). Deleting the field alone would make every old save raise `ValidationError` on load.
- **Why it matters:** The shim has one canonical home and a copy-paste template. `sidequest/game/migrations.py` runs read-old-write-new on the raw snapshot dict BEFORE pydantic validation, via the `migrate_legacy_snapshot` sub-function tuple. I added `_migrate_s6_strip_npc_voice_id` (mirrors `_migrate_s3_party_location`): iterate `out["npcs"]`, `del npc["voice_id"]` where present, return `{"s6_voice_id_stripped": n}` or `None` (no-op on canonical input — No Silent Fallbacks). The matching OTEL allow-list is `_extract_snapshot_canonicalize` in `sidequest/telemetry/spans/persistence.py` — add the new `s6_*` key there or the GM panel never sees the counter (the extractor has no defaulted keys by design). TEA's RED tests pointed straight at both sites.
- **How to apply:** Deleting a field from an `extra=forbid` pydantic model that round-trips through saves is NEVER a one-line delete. Grep for the field across BOTH the model AND every constructor — here `world_materialization.py` had two `voice_id=None` kwargs that would `TypeError` once the field was gone (RED didn't cover those; the regression sweep did). Sequence: delete field → delete every construction-site kwarg → add the migrations strip-shim + register in the tuple → add the OTEL extractor key → run the full subsystem sweep, not just the story's target tests.
- **Scope discipline on a "remove all X" story:** grep `voice` surfaced look-alikes that are NOT in scope — `MixerConfig.voice_volume` (audio mixer dial) and `ChassisVoiceSpec` (ADR-125 chassis voice). TEA had written retention guards for both; I left them. `AudioEffect` became dead (only the removed voice models referenced it) but isn't in the story's 5-surface list — kept it and logged a non-blocking follow-up finding rather than scope-creep the deletion. Story scope is the authority; flag out-of-scope dead code, don't silently sweep it.

### Extending the scene_harness hydrator with a new fixture block (2026-06-10, story 90-4)
- **What happened:** 90-4 added two save-bearing blocks to `sidequest/game/scene_harness.py`: character `effort:`/`spellcasting:` → `core.effort`/`core.spellcasting`, and encounter `win_condition:`/`category:`/`actors:` → `StructuredEncounter`. The hydrator has a rigid, established idiom (50-18…50-23) — clone it, don't invent: (1) read `raw.get("block")`; (2) `is not None` guard so present-but-empty still validates; (3) not-a-mapping/not-a-list → `raise FixtureValidationError(...)` DIRECTLY (loud); (4) field-level pydantic `ValidationError` → let it propagate so the caller re-wraps it (the singular path wraps `ValidationError`; the multi-PC list path wraps both `ValidationError` and `FixtureValidationError`); (5) omit the block → model default preserved (None/{}/Literal-default) so canonical fixtures are untouched.
- **Why it matters / gotcha:** Where you construct the nested pydantic model decides whether its `ValidationError` gets wrapped. In `_hydrate_character` the new models are built and the surrounding `hydrate_fixture` caller wraps — fine. But in `_hydrate_encounter` the actor list is built BEFORE the `try: return StructuredEncounter(...)` block, so an `EncounterActor(**raw)` with a bad `side` (out-of-Literal) raised an UNWRAPPED `ValidationError` past the module boundary → the "invalid side raises FixtureValidationError" test failed with a raw pydantic error, not the wrapped type. Fix: wrap the per-actor construction in its own `try/except ValidationError → FixtureValidationError`. Rule of thumb: every pydantic construction in this module needs a wrap on its own code path; don't assume the trailing `try` covers earlier constructions.
- **How to apply:** RED→GREEN for a new block = add imports, append to `core_kwargs` (character) or `extra_kwargs` passed via `**` to the model ctor (encounter), then run the file's own suite (`uv run pytest tests/game/test_scene_harness_hydrator.py -n0`). The "DID NOT RAISE" failures are the loud-fail guards — they pass once you add the explicit not-a-mapping/list `raise` + ensure field-level errors wrap. **Deterministic-fixture seed:** seed `prepared` VERBATIM (non-empty) — chargen's `seed_wwn_magic` seeds `prepared==[]` (chosen at rest), so a content-only cast proof needs the spell pre-loaded or `resolve_spellcast` refuses `'<id>' is not prepared`. **Effort shape:** source-keyed mapping (`effort: {high_mage: {max: 3}}`), derive `EffortPool.source` from the key (strip any stray in-value `source`) — matches `core.effort.get(source)` in `wwn.py`. **OTEL:** seeding crunch is a subsystem decision — emit a `_hub.publish_event("wwn.magic_hydrated", {...}, component="magic")` (module-qualified so `_capture_events` intercepts), mirroring the `magic.state_hydrated` emitter; no test asserts it but the OTEL principle mandates it. Verify-phase note: the 7 full-suite failures (namegen corpus thinness, missing orchestrator `docs/api-contract.md`, xdist races in `test_chargen_name_rig_extraction.py` that pass under `-n0`) are pre-existing and unrelated — prove it with `git diff --name-only develop...HEAD` + `grep -l scene_harness <failing files>`.

### Garbled procedural NPC names: the Markov is innocent, the corpus and the Manual cache aren't (2026-06-10, playtest FIXER)
- **What happened:** Playtest flagged `Vesper (version)` + odd names (`Ther Path`, `Deckonone Wainturing`) in the coyote_star roster. Two distinct causes, easy to conflate: (1) **`(version)` is a parenthetical that the current generator CANNOT produce** — `markov.py::train` filters to `category(ch)[0]=="L"` (letters only, char-level), and every culture `word_list` is clean. It's **stale data** in the long-lived on-disk Monster Manual cache (`~/.sidequest/manuals/<genre>_<world>.json`), minted by older code and surviving every reload. (2) The merely *odd* names are real Markov output reading badly because the culture corpora (`sidequest-content/corpus/shared/*.txt`) are **raw Project Gutenberg eBooks** — full English prose — so a lookback-3 char chain reproduces actual English function words (empirically, 3000 samples gave `Administrator That Roducted`, `Legate Seemed Overy`). That's corpus-quality tuning, a separate/larger effort.
- **Why it matters:** Don't "fix the generator" for a junk name without first proving the generator can even emit it — fast empirical loop: build one `build_from_culture` generator per culture (pass `chain_cache={}` so it's not rebuilt per call — rebuilding trains on multi-MB Gutenberg files and hangs), then `gen.generate_person()` ×3000 and grep the output. If clean, the junk is stale cache, not live code.
- **How to apply:** The durable fix for cache-vintage junk is a **boundary guard**, not a generator change: `sanitize_display_name()` in `genre/names/generator.py` strips only bracketed annotations `(…)[…]{…}` (the one thing no `person_pattern` emits), wired at the Monster-Manual injection seam (`server/dispatch/monster_manual_inject.py::_sanitize_patch_names`) so the snapshot is clean regardless of cache age, plus a one-time scrub of the stored `*.json`. **Be conservative — keep commas, quotes, hyphens, digits:** Broken Drift mints `Hush, off Tether` (comma drift-marker) and `Quija 'Salt'` (quoted callsign) ON PURPOSE, and sci-fi designations carry digits (`Unit 7`) — read the culture's `*_namegen.yaml` `person_patterns` before deciding what's "junk," or you'll mangle intentional style. Emit the cleanse count on the existing `monster_manual.injected` span (registry decision = OTEL-worthy).

### Narrator names a mechanical entity that the engine didn't change → it's a v1-feature gap, gate the narrator (2026-06-10, playtest FIXER)
- **What happened:** Dogfight narration claimed a scene tag ("Positional Advantage") was "burned" / flipped to the opponent on a failed beat, but stored `encounter.tags` still had it owned by the player, unconsumed. Root cause: `EncounterTag` is documented **v1** (`game/encounter_tag.py` header: "tags are created, displayed, and persisted but engine does not yet spend leverage. v2 adds consumes_leverage_from"). So the engine *intentionally* doesn't consume/transfer the tag or apply its `leverage` to the to-hit DC — both "symptoms" were by-design v1. The only defect was the narrator getting `Tags: …(leverage=2)` in its `<encounter-live>` block with no constraint, so it fabricated mechanics.
- **Why it matters:** When the lie-detector flags narration claiming a state change the engine didn't make, first check whether the engine is *supposed* to make it (grep the entity's model/docstring for a v1/v2 staging note). If the mechanic is deliberately deferred, the fix is **narrator honesty**, not implementing the mechanic mid-playtest. Modeled the gate on the existing `opposed_check` outcome gate right next to it in `agents/narrator.py` — `TAGS_ARE_ENGINE_STATE`: reference the tag as fiction, never narrate it spent/consumed/transferred/burned/owner-changed; only emit when tags exist (keep the prompt lean).
- **Dogfight two-track gotcha (same session, separate bug):** a `sealed_letter_lookup` + `dial_threshold` dogfight runs TWO parallel energy systems — `beat_kinds.apply_beat`→`describe_beat_impact` stamps `last_beat_impacts` from **beat-kind** deltas (angle beats = tag/Δ0 "by design"), while the **sealed-letter interaction table** (`server/dispatch/sealed_letter.py::_apply_view_deltas`) writes `viewer_energy`/`target_energy` into `actor.per_actor_state`. `last_beat_impacts` only reflects track 1, so an angle beat honestly reads "Δ0/no dial change" while the dials move from track 2 — a legibility gap where you MUST get `encounter_events` to see which track moves the watched dial before fixing; `apply_beat` can't observe the sealed-letter delta.

### CORRECTION — the dogfight dial is narrator-driven, not "track 2"; the legibility bug is a dropped telemetry kind (2026-06-10, playtest FIXER)
- **Supersedes the "two-track" hypothesis in the entry above.** Pulled the real `/api/sessions/<slug>/encounter_events` for the completed dogfight: all 5 `ENCOUNTER_BEAT_APPLIED` rows carry `own_delta:0, opponent_delta:0`, there are **zero `metric_advance` rows**, yet `ENCOUNTER_RESOLVED` reports `final_player_metric:7, final_opponent_metric:25`. So the dial moved with NO engine event. Grepping every `*_metric.current =` / `+=` assignment package-wide: `beat_kinds.apply_beat` (which DOES emit `metric_advance` — absent here, so it never ran for these beats) and **`agents/tools/advance_confrontation.py:247`** — a NARRATOR TOOL. The sealed-letter resolver's `_apply_view_deltas` only writes `per_actor_state.viewer_energy/target_energy`; it NEVER touches `player_metric.current`. There is no wire from the per_actor_state energy track to the dial_threshold metric. The dial is moved **exclusively by the narrator** calling `advance_confrontation` (which is refused on `hp_depletion`/`opposed_check` but NOT on `sealed_letter_lookup`).
- **Why the DRIVER couldn't see it:** `advance_confrontation` DOES emit a full `state_transition` `op="narrator_dial_advance"` (axis/delta/value_before/value_after/reason). But `telemetry/watcher_hub.py::_KIND_BY_OP` had no entry for that op, so `_maybe_persist_encounter_row` hit `kind is None` and **returned without persisting** (live-push to the GM panel only, never written to the `events` table). So `/encounter_events` (ENCOUNTER_% filter) and the GM EncounterTab reload showed beat_applied Δ0 with no dial-advance trail. A "compute delta around beat application" fix would STILL read Δ0 — the dial move is decoupled from the beat.
- **The fix (observability half, shipped):** add `"narrator_dial_advance": "ENCOUNTER_NARRATOR_DIAL_ADVANCE"` to `_KIND_BY_OP` + add that kind to `session_handler.py::_REPLAY_SKIP_KINDS` (the `test_replay_skip_kinds_covers_every_encounter_telemetry_kind` guard enforces the pairing — it goes RED if you add the op-map without the skip entry). Now the dial move persists and is attributable on the timeline. Test: `test_narrator_dial_advance_writes_event_row` in `tests/server/test_encounter_telemetry.py`.
- **What this does NOT fix (escalated to Keith — design call):** (1) the player-facing confrontation PANEL still shows `last_beat_impact` Δ0 next to a moving dial — surfacing the narrator's dial delta as an in-panel impact line is a server-payload + UI change. (2) The architecture question: a `dial_threshold` dogfight whose dial is hand-cranked by the narrator is the SAME free-hand anti-pattern the `opposed_check` guard (RW-2 playtest) was built to stop. The principled fix is engine-owned: wire `per_actor_state` energy → the metric and REFUSE `advance_confrontation` on `sealed_letter` too — but you can't add the refusal alone or the dogfight loses its only dial-mover and never resolves. That's a combat-engine feature (with balance implications), out of FIXER bugfix scope — same call the prior FIXER made.
- **How to apply:** when a dial moves with no `metric_advance` trail, suspect the narrator tool (`advance_confrontation`), not the resolution engine. Confirm by grepping `*_metric.current` assignments and by checking whether the op is in `_KIND_BY_OP` — an unmapped `op` is silently dropped from `events` (reaches the live panel, vanishes from the timeline). New encounter watcher ops need BOTH the `_KIND_BY_OP` entry AND the `_REPLAY_SKIP_KINDS` entry.

### Raw snake_case entity id leaking to a player surface → humanize at the presentation layer, never rewrite the id (2026-06-10, playtest FIXER)
- **What happened:** Confrontation "Them" card showed `unknown_dark_contact` (a runtime-seated opponent slug) verbatim. `actor.name` is the CANONICAL entity id — `EncounterTag.target` and `last_beat_impacts` keys reference it by exact string — so rewriting it on the wire would silently break tag/impact matching. The slug was used as both id and display name (no static literal anywhere; minted at seat-time).
- **Why it matters:** The fix is presentation-only and there was already a precedent in the SAME file: `ConfrontationOverlay.tsx::humanizeSpellId` ("the CONFRONTATION projection carries ids only … labels humanized client-side"). So the consistent move is a sibling `humanizeActorName()` applied at the render sites (the `{opponent.name}` Them-card text + the `ActorChip` title/alt tooltip), leaving `actor.name` the slug everywhere mechanical. Don't reach for a server payload `display_name` field when the established pattern in the consuming component is client-side humanization.
- **Humanize transform gotcha:** capitalize the first letter of each `_`-split segment and **leave the rest untouched** — do NOT `.toLowerCase()` first. A naive title-case lowercases the tail and mangles real names: `Kanga Moana-Teru` → `Kanga Moana-teru` (the post-hyphen T). The split-on-`_` + first-char-upper transform is idempotent on already-formatted names (no underscores → single segment → first char already capital → unchanged), so it safely no-ops on real NPC names while fixing slugs. Pin both cases in the test (slug humanized + real name unchanged).
- **How to apply:** RED test asserts the panel shows the humanized label and `not.toHaveTextContent(slug)`; GREEN adds the helper + applies at every visible render of the name (text + tooltip/alt). UI change → no server restart; Vite HMR / browser reload picks it up.

### "Opening narration splits a sentence mid-paragraph" = the markdown renderer splits on lone `\n`, not a streaming artifact (2026-06-10, playtest FIXER)
- **What happened:** A coyote_star opening sentence rendered across two `<p>` tags mid-clause. Reported as a streaming-accumulator/ADR-133 artifact — it isn't. `sidequest-ui/src/lib/narrativeSegments.ts::markdownToHtml` did `.replace(/\n\n/g,"</p><p>").replace(/\n/g,"</p><p>")` — the SECOND replace turns every remaining lone `\n` into a paragraph break. The openings author prose with YAML `|` **literal** block scalars, which preserve source soft-wrap line breaks as hard `\n`s mid-sentence. So a soft-wrapped sentence became two paragraphs. Systemic across all `|`-authored content (every world), not one field.
- **Why it matters / how to verify the safe fix:** the conventional fix (lone `\n` → space, only `\n{2,}` → paragraph) is a SHARED narration renderer, so the risk is collapsing live-narrator paragraphs IF the narrator uses lone `\n` between paragraphs. Settle it with data, don't guess: query the events table — `SELECT payload_json FROM events WHERE kind='NARRATION' ORDER BY length(payload_json) DESC LIMIT 8` — and check each for `'\n\n' in text` vs lone-`\n`-only. Empirically the Anthropic-SDK narrator separates paragraphs with `\n\n` 8/8, lone-`\n`-only 0/8. So the fix is safe: `\n\n` paragraphs still break correctly; lone `\n` soft-wraps fold to spaces.
- **YAML scalar gotcha for content authors:** `|` (literal) keeps every line break; `>` (folded) turns single line breaks into spaces and blank lines into breaks. openings.yaml mixes both freely. The RENDERER fix is better than converting `|`→`>` per block because it fixes all existing `|` content at once and is robust to future authoring. Don't chase a per-field content edit when the renderer is the common cause.
- **How to apply:** RED test `markdownToHtml` directly (it's exported): lone-`\n` input → one `<p>` with a space, no `</p><p>`; `\n\n` input → two `<p>`s; keep a `**bold**` regression guard. UI change → no server restart (Vite HMR). Run the FULL UI suite after touching a shared renderer (`npx vitest run` from sidequest-ui) — 2069 tests, ~13s.

### Verifying a `draft: true` world's content actually loads — flip draft in a temp copy, run the real loader (2026-06-11, 103-8 GM-lane)
- **What happened:** 103-8 (Seaboard of Saints stocks + dramatic content) ships into a world with `draft: true` in world.yaml so it stays out of player selection until the epic completes. But `_load_single_world` (sidequest-server `genre/loader.py:1132`) returns `None` the instant it reads `config.draft` — so `load_genre_pack` silently omits the world and you CANNOT exercise the real load path (cross-file opening validators, archetype/stock/saint catalog resolution, inventory parse) against it directly. Pack-structure `validate pack` passes but does NOT run the full World assembly.
- **Why it matters:** the strongest content AC verification is "does the whole world assemble without raising `GenreLoadError`" — that single load runs the opening solo/mp-coverage validators, region-ref checks, and validates every stock/saint `granted_mutations` against the MutationCatalog. A draft flag hides all of it behind a `None`.
- **How to apply:** `shutil.copytree` the genre pack to a tempdir, `re.sub(r'(?m)^draft:\s*true\s*$','draft: false', ...)` the target world.yaml copy, then `load_genre_pack(tmp)` and assert the world is in `pack.worlds` with the expected counts (`len(w.openings)`, `len(w.archetypes)`, etc.). NEVER flip draft in the repo — it's intentional gating. For lighter checks the registry loaders take a Path + catalog directly: `load_mutation_catalog(genre/'mutations.yaml')` then `load_stock_registry(world/'stocks.yaml', cat)` / `load_saint_registry(world/'saints.yaml', cat)` — these resolve mutations on load, so a clean return == all granted ids exist in the catalog. Cross-check encounter→bestiary by id-set intersection (37/37 must match).
- **Currency/cliché bans are grep-verifiable but watch for false positives:** the `§11` ban scan for `bottlecap|water.?currency` legitimately hits the explicit "Banned (§11): no bottlecaps" *comment* in inventory.yaml — that's the ban note, not a violation. Same for `scar` (harpoon-scarred leviathan, war "scar tissue", "the Charlestown scar" place) and banned suffixes (`drift` in a `move: "9m drift"` value is not a coined place name). Read each hit's context before flagging.

## WN action-set synthesis (108-8, 2026-06-15)
- The WN combat resolution machinery (108-1's `dice._resolve_wn_committed_action`:
  weapon dice → ablative HP + `wwn.native_scaffolding_suppressed` span) already handles
  ANY `damage_channel=="strike"` beat under a `WithoutNumberRulesetModule` binding. The
  zero-beat (post-108-3) outage was PURELY the two `cdef.beats` lookups rejecting the id
  before resolution: `dice.py:407` and `wn_round.py:374`.
- Fix pattern = mirror item-use synthesis: add `is_wn_action_beat`/`wn_action_beat` to
  `beat_filter.py` (pure id check + transient BeatDef factory), then intercept at BOTH
  seams BEFORE the lookup, gated `isinstance(ruleset, WithoutNumberRulesetModule)`.
  Both seams MUST be patched — dice.py resolves the commit, wn_round.py re-resolves it
  inside the sealed-round walk; fixing only dice.py re-raises at the barrier close.
- Synthesized attack BeatDef carries NO `damage_override` → weapon dice resolve from the
  actor's inventory via `damage_roll.resolve_damage_spec_from_beat_and_actor` priority 2/3
  (or the genre unarmed floor). `attack_bonus`/`combat_skill` default 0 (no class to-hit
  progression on a synthesized action — matches the `wn_attack` narrator tool).
- Gate caveat: "attack" is also an authored NATIVE beat id (test_genre). The isinstance
  gate keeps native ids on the authored-beat lookup, so synthesis only fires under WN.

### GM-panel projection fixes don't need their own OTEL emit (story 124-4)
The State-tab `debug_state` projection is a **read-only GET** that re-shapes an already-loaded `GameSnapshot` for the panel. The OTEL "lie-detector" principle targets subsystem *decisions* (trope tick, combat, inventory mutation) — those emit spans at decision time, and the snapshot already carries their results. A passive projection of that state is the *display*, not a decision, so adding a span to the GET would be noise. Fixing the projection to read the real fields IS the honesty fix; no new emit. (Log this as a deviation so Reviewer doesn't flag a "missing OTEL emit" against the AC's boilerplate "with an OTEL emit" clause.)

### Extract-to-test: lift an inline route projection into a pure helper (story 124-4)
When a RED suite imports a not-yet-existing `project_*` function, the green move is mechanical and high-value: cut the inline `snapshot → dict` block out of the async route into `state_projection.py` as a pure function, then make the route **call it** (the wiring — a pure helper with no production caller is a stub, not a fix; grep confirms `rest.py` imports+calls it). Keep all DB/IO (save enumeration, `repository.load()`) in the route. The extraction is what makes the four dead-read fixes unit-testable against synthetic `GameSnapshot`s.

### React Tufte sparkline: narrow `Record<string, unknown>` with `typeof`, never `||` or `as any` (story 124-4)
The live `ocean` field is typed `Record<string, unknown>`. To draw the 5-bar OCEAN glyph: loop the five Big-Five keys, `if (typeof v !== "number" || Number.isNaN(v)) return null` (graceful "—" when absent — no fabrication), else push. This narrows `unknown`→`number` cleanly (TS rule #1: no `as any`) AND keeps a legitimate `0.0` dimension (TS rule #4: a `||`/`??` fallback would've been a trap had I used `||`). OCEAN is a **0–10** scale (OceanProfile defaults 5.0), so normalize bar height by /10, not as a 0–1 fraction. Give the `<svg>` `role="img"` + `aria-label`/`<title>` with the values — testable AND a real a11y win over a bare bar chart.

### Seat-spoof fix: drive PC resolution from sd.player_id (sole source), never inbound msg.player_id (story 118-9)
Any handler that resolves the acting/rolling PC from `snapshot.player_seats.get(<id>)` keyed on `getattr(msg, "player_id", "") or sd.player_id` is a seat-spoof vector — a client can send a non-empty inbound `player_id` pointing at another seat and act/roll AS that PC. The fix (mirrors `fate_action.py:69-91`, now applied to `dice_throw.py` too): `rolling_player_id = sd.player_id` (the Cf-Access authenticated identity, SOLE source); `inbound_player_id = getattr(msg, "player_id", "") or ""`; if `inbound and inbound != authenticated`, `logger.warning(...)` + `publish_event("state_transition", {field:"session_binding", op:"<channel>_player_id_spoof_rejected", inbound_player_id, authenticated_player_id, recovery:"auth_identity_enforced", source:"<channel>"}, component="session", severity="warning")` and proceed as the authenticated PC. The empty-inbound common case is preserved (no event, resolves to authenticated). `msg.player_id` is a client-controlled OUTPUT annotation, never trusted as inbound identity. Two handlers shared this idiom (fate_action + dice_throw); audit any new player-action handler for it.

### Defend-path authorization rejection is a SECOND spoof class — emit at the dispatch decision point, not the handler except (story 126-13)
The line-200 idiom catches the *transport* spoof (inbound `msg.player_id` != authenticated `sd.player_id`) at the handler. There is a SECOND, distinct spoof on the Fate defend path: a seated PC throwing to answer ANOTHER PC's defend request — `entry.defender != actor_name` inside `dispatch_fate_defense` (`server/dispatch/fate_conflict.py`). It already raised `FateConflictError` but emitted NO watcher event, so the GM-panel lie-detector couldn't see it (no parity with `fate_throw_player_id_spoof_rejected`). Fix: emit `_watcher_publish("state_transition", {field:"encounter", op:"fate_defend_authorization_rejected", request_id, throwing_actor, request_defender, attacker, recovery:"defender_authorization_enforced", source:"fate_defense"}, component="encounter", severity="warning")` at the rejection branch, **before** the `raise`. Two reasons it goes in the dispatch fn, not the handler: (1) `fate_conflict.py` already imports `publish_event as _watcher_publish` and publishes 2 other encounter events — established layer; (2) the handler's `except FateConflictError` catches THREE variants (unknown request_id / already-filled / authorization) generically and can't single out authorization without parsing the exception. Wiring test = spy on the module's `_watcher_publish` alias + drive the real dispatch through the non-defender path + assert the published event (canonical pattern, `test_retrieval_reason_watcher.py`); never grep source. Lesson: "parity with the player_id spoof" doesn't mean "same layer/component" — match the EVENT shape + severity so the panel renders both alike; the emit lives wherever the authorization decision is actually made.

### sanitize_player_text on Optional[str]: guard None (story 118-9)
`sanitize_player_text(None)` returns `""` (its `if not text: return ""` swallows None), NOT None. When sanitizing an optional field at a boundary (e.g. Fate `payload.target`, where None means "passive action, no opponent"), `sanitize_player_text(payload.target)` silently coerces None→"" and breaks downstream None-sentinel logic. Always guard: `sanitize_player_text(x) if x is not None else None`. The seal site already imported `sanitize_player_text`; the fix was three call-site changes (skill was done in 118-8; target+aspect_text in 118-9), target needing the None guard.

### Narrator latency budget — the wire is free, the narrator is gold (sealed-turn batching)

**The optimization target for the turn loop is narrator (LLM) invocations, NOT messages.** A WebSocket/server round-trip costs milliseconds; a narrator call costs *seconds* — the entire 126-9/126-10 latency dig was narrator + router LLM time, never wire time. So the rule is: **spend client↔server chatter like water; hoard narrator calls like gold.**

- **The client owns the roll-adjust loop; the narrator sees no intermediate dice.** A player throws (physics-is-the-roll, ADR-074/148), then adjusts *locally* — invoke (+2) or reroll (re-throw), as many times as they can pay for, spending their own fate points / aspects (all client-known) — and submits ONLY the final result. The server validates the spend on submit (reject loud if unaffordable — No Silent Fallbacks); the narrator never enters this loop. The back-and-forth that makes *solo* play feel good was never the table's business — it's private to one player and their own dice, so it costs nothing and blocks no one.
- **One round, one narration.** The sealed-commit barrier (ADR-036/129) is a *batching engine*: collect every player's intent + final roll, resolve EVERY mechanic server-side (commit → seat NPCs → conditional defend → compare → taken-out → aspects placed), THEN invoke the narrator **once** to render the whole woven exchange as a single cinematic beat. The narrator is never in the mechanical loop. If NPC action-selection needs intelligence, it uses the **cheap tier** (Haiku / heuristic / intent-router), never the expensive cinematic narration.
- **Sealed turns are a special-sauce FEATURE, not a constraint to fight.** Tabletop is serial (initiative: three players watch one act, GM narrates, next acts); we are *simultaneous* — N players commit at once, the engine resolves the whole measure in parallel, and the narrator writes the entire band's bar in one pass. This structurally dissolves the Guitar Solo problem (SOUL): nobody is a silent audience, because everyone's verb resolves into the *same* beat. The barrier "wait" is not dead time — it overlaps every player's local deliberation instead of serializing it.
- **Design test:** if a feature would put the cinematic narrator (Sonnet) *inside* the commit→resolve loop, or round-trip it per-player-per-action, it is mis-designed. Resolve mechanics cheaply on the wire; narrate once at the end. (Source: 2026-06-18 brainstorm w/ Keith — *Fate Core in the sealed-commit turn model*; companion to ADR-148 / ADR-149.)

### Fate "suppress native chrome" = a boolean prop keyed on `fateData != null`, threaded from GameBoard (126-19)
When a story says "hide HP/level/class on a Fate pack, keep it on WN," the established wiring (mirrors the inventory `showCurrency={fateData == null}` gate in `GameBoard.renderWidgetContent`):
1. The ruleset signal is `GameBoard.fateData != null` — NOT a per-PC name match. Do not key suppression off the name-matched `fateSheet` (`fateData.characters.find(c => c.name === sheet.name)`); a roster-name mismatch would leak the chrome (TEA pins this with an explicit mismatch test).
2. Add an optional `suppressNativeChrome?: boolean` (default false) to the leaf component (`CharacterPanel`) and wrap each native element: header `character-level-badge`, header `EdgeBadge` (`character-edge-badge`), body `FolioEdgeTicks` (`character-edge-ticks`), and the party-row class/level/HP secondary `<span>` (`party-member-edge-*`). Default-false keeps every existing CharacterPanel test/caller green with no edits.
3. `CharacterWidget` is a `{...props}` pass-through, so adding the prop to `CharacterPanelProps` is enough — just pass `suppressNativeChrome={fateData != null}` from GameBoard's `character` case.
4. `HpPipScale` (the input-bar HP readout) receives NO ruleset signal — guard it at the call site in the `inputBar` const: `{fateData == null && <HpPipScale .../>}`. (It's in the always-present input region, so it renders regardless of active tab — a GameBoard-integration test can assert `input-hp-scale` absent on Fate without a tab switch.)
This is a pure additive change (one optional prop + two call-site guards): 10/10 story tests green, full UI suite unaffected, eslint + tsc clean. Keep the header subtitle (calling/origin) OUT of scope — it's flavor, not the mechanical class.

### InventoryPanel empty-state is Fate-aware via the same `showCurrency` signal (126-20)
`InventoryPanel` renders an empty-state block (`data-testid="inventory-empty"`) when `data.items.length === 0` — without it the tab is just the "Inventory" header over blank space and reads as broken (Keith, sq-playtest 2026-06-19: "presenting a blank screen is wrong regardless"). It reuses the **existing** `showCurrency` prop as the Fate signal (already the no-economy gate GameBoard passes only for Fate packs — see the "suppress native chrome" entry above): primary line "Nothing in your pockets yet." always; secondary aspects-hint line ("Your signature gear lives in your aspects, not as carried items.") gated on `!showCurrency`. A Fate inventory is *legitimately* empty (gear → FateSheet aspects), so the hint explains *why* rather than implying a load failure. Do NOT add new Fate-detection plumbing for InventoryPanel copy — `showCurrency=false` already means Fate here. Pure additive change; cosmetic copy → no OTEL needed (UI CLAUDE.md exempts cosmetic UI changes).

### Removing a GameBoard dock widget — the full dead-path checklist (126-26)
A "widget" in sidequest-ui touches SIX places; miss one and you get a dangling reference or a tab that renders nothing. To remove widget `X`:
1. `widgetRegistry.ts` — drop `"X"` from the `WidgetId` union AND the `WIDGET_REGISTRY` entry. (Because `WIDGET_REGISTRY: Record<WidgetId, WidgetDef>`, removing the union member forces the entry removal — `tsc` is your backstop. Removing the entry also auto-frees its `hotkey` via `buildHotkeyMap()`.)
2. `GameBoard.tsx` — remove the `availableWidgets.add("X")` gate, the `renderWidgetContent` `case "X":`, and `"X"` from `rightGroupOrder`. Remove the now-unused widget `import`.
3. `MobileTabView.tsx` — remove the `{ id: "X", ... }` TABS entry AND any now-unused lucide icon from the top import (eslint no-unused-vars will catch the icon if you forget).
4. Delete `widgets/XWidget.tsx` (`git rm`).
5. De-stale comments that named the removed widget in files you're already touching (the comment-analyzer only checks the diff, so fix refs in changed files; refs in untouched files aren't flagged).
6. `tsc --noEmit` is the proof the union removal left no dangling `WidgetId` literal anywhere.
**Re-home before you remove.** If the widget hosted anything NOT duplicated elsewhere (here: the non-conflict `FateDiceTray`, fed by `latestFateRoll`), thread that slice into its new home FIRST (here: a new `fateRoll` prop on CharacterPanel, rendered in the Stats tab beside `FateCharacterSheet`, gated by the existing `fateSheet ?` branch so it stays ruleset-gated for free). `CharacterWidget` is a `{...props}` pass-through, so adding the prop to `CharacterPanelProps` + passing it from GameBoard's `character` case is the whole wire. Leave sibling surfaces (`fate-conflict` has its OWN tray) and shared renderers (`FateCharacterSheet`, exported from `FatePanel.tsx`) untouched — deleting the now-unreferenced `FatePanel` *function* is a separate cleanup (it still has tests).

### Proving snapshot slimming actually RAN — plant a decoy non-current room (126-28)
A test that persists room-scoped state and then asserts `prop in json.dumps(_build_state_summary(snap))` is a **false-positive trap**: when `party_location()` returns None (no seated PC in `player_seats`), `apply_snapshot_slimming` SKIPS the room_states projection and passes every room through whole (the safety valve — see dev-gotchas "room_states-scoped state is invisible … unless keyed to party_location()"). So the prop "survives" the summary because slimming never cut anything — the test passes for the wrong reason and would still pass if the projection key were wrong. To pin the REAL projection in a unit test:
1. Seed `snap.player_seats["seat-1"] = "<PC name>"` AND `snap.character_locations["<PC name>"] = ROOM_ID` so `party_location()` resolves in consensus mode (both are required — `character_locations` alone leaves `player_seats` empty → None → skip).
2. Persist the prop into `ROOM_ID`, and ALSO persist a decoy prop into a DIFFERENT room the party is not in.
3. Assert `set(summary["room_states"]) == {ROOM_ID}` (decoy dropped ⇒ projection ran) AND the decoy prop is absent from the projected dump — not just that the real prop is present.
The decoy-dropped assertion is the load-bearing one: presence-of-real-prop alone cannot distinguish "projected correctly" from "projection skipped". (126-28 hardened the 126-18 opening-props suite this way; companion to the dev-gotchas projection-skip entry.)

### Faction/zone content tagging (epic-157 fan-out) — the "*"-vs-region judgment, and per-world shape (2026-06-21, 157-6)
Tagging a multi-region world's pooled content (`bestiary.yaml` / `tropes.yaml` / `seed_tropes.yaml`) with `factions:` for ADR-059 zone-eligibility. The rule: **faction value = the region's cartography `controlled_by` slug** (read `cartography.yaml`; never invent a faction), and the **`"*"` sentinel ONLY for a genuine cross-region thematic spine** — not a lazy default. Items get `[faction_a, faction_b]` when they genuinely belong to two regions (e.g. a feud between two factions, a theme spanning two voyages).
- **Worlds are NOT uniform in shape.** Read each world's content + cartography and tag to its actual fiction:
  - *gulliver / oz / wonderland* (fairytale, region-locked): nearly everything is region-bound; only ~2-3 genuine world-spines are `"*"` (oz: deadly_desert=go-home seal, enchantment_not_kill=lethality doctrine, lost_princess=who-rules mystery; wonderland: nonsense_as_authority=central engine, the_pack_of_cards=the way home). The go-home spine and the world's central thematic engine are the reliable `"*"` candidates.
  - *the_circuit* (shared port city): the OPPOSITE — bestiary is mostly `"*"` (port-city ecology: rats/gulls/feral dogs + Thread entities that "predate every crew"; only the Rocker junkyard guard dog is faction-owned), tropes are mostly `"*"` (circuit-wide event FORMATS: turf race, convoy war, crackdown), and the faction-richness lives in the SEEDS (each reference-stacks on a named faction mechanism). A world's own header/design-note often tells you which shape it is — trust it.
- **The cartography can be COARSE.** wonderland has ~13 regions mapping onto only 3 `controlled_by` banners (the_queens_terror / the_rigged_game / no_one). An item is eligible wherever its faction controls, so tag the banner, not a per-region slug.
- **`extends:` tropes carry factions through `resolve.py::_merge_trope`** (the 157-5 fix). It generalizes: verified oz green_spectacles `[the_wizard]` + lost_princess `["*"]` and wonderland nonsense_as_authority `["*"]` all survive resolution (assert against the RESOLVED `world.tropes`, not the raw file). A trope's file-header comment claiming N extends-tropes can be STALE — only the bodies with an actual `extends:` line count (wonderland's header claims 3, only 1 has it).
- **Verification = mirror `tests/genre/test_157_5_gulliver_no_bleed.py`** per world: `world_is_zoned()==True` (else the proof is vacuous), zones == cartography `controlled_by` set, completeness (every pooled item tagged + value is `"*"` or a real faction — pre-flights the 157-7 load validator), and behavioral no-bleed via `is_eligible(item.factions, {wrong_zone}, zoned=True) is False` / `{own_zone} is True`. Cheap full check before committing: `load_genre_pack(...)` + loop items asserting `it.factions` non-empty and each value in `valid ∪ {"*"}`.
- **YAML edits are surgical hand-edits** (per dev-gotchas): anchor each `factions:` insert on the item's unique `role:`+`tags:` pair (bestiary — `tags:` lines duplicate across creatures) or `- id:`+`name:` pair (tropes/seeds). Match the gulliver inline-comment style and add a per-file header block explaining the scheme. Never round-trip the file.

### "No `as unknown as` blind cast" for a divergent WS wire = type guard + adapter, not loosened shared types (153-25)
When an inbound WS frame's wire shape diverges from the client type the state slot expects (here DUNGEON_MAP omits `x`/`y`/`fog_bounds` that `ExploredLocation`/`MapState` mark required — room graphs have no coordinates, ADR-055), and the AC forbids an `as unknown as MapState` blind cast: implement a small `src/lib/<frame>.ts` with (1) an accurate wire type (`Omit<ExploredLocation,"x"|"y">` reuses the shared type honestly), (2) a runtime guard `isX(p: unknown): p is X` that validates the load-bearing fields and lets the handler narrow `msg.payload` (`Record<string,unknown>`) with NO cast, and (3) an adapter `xToMapState(p): MapState` that fills the inert defaults the renderer never reads. The guard's false branch is your fail-loud drop (`console.warn` + return) — satisfies "No Silent Fallbacks" for free. **Do NOT** just make the shared fields optional to fit the wire: other consumers may read them unguarded (`MapOverlay` does `x1={a.x}` at lines ~250-275), so `x?: number` breaks `tsc` there. Adapt at the boundary, keep the shared type strict.

### Client-side "frame was consumed" marker must put values IN the string, not an object arg (153-25 AC-5)
A telemetry/trace marker asserted by a console-spy test (`args.map(String).join(" ")`) fails if you pass a structured object: `String({rooms:2})` → `"[object Object]"`, so the count/id never appear. Emit `console.info(\`[tag] ... rooms=${n} current=${id}\`)` — values interpolated into the string. Matches the existing App.tsx client-trace convention (`[dice-guard]`, `[scene-harness]`).

### Parallel edges to ONE neighbor are false ambiguity — fix at projection (dedup), not the resolver (153-22, PR pending)
- Dungeon movement bug: `beneath_sunden` materialized three edges `exp002.r2 → exp002.r1`, so `region_projection.project_region` (one `RegionExit` per `graph.edge`) triple-listed `exp002.r1` and `assign_bearings` (keyed by `to_region_id`) collapsed all three onto ONE bearing. The narrator offered "three passages, name a bearing" and none resolved — `movement._resolve`'s descriptor token-overlap saw three tied candidates and returned `ambiguous_descriptor`. **They all lead to the same node, so it's FALSE ambiguity.**
- **Minimal fix is at the projection, not the resolver.** Collapse parallel edges to the same neighbor into ONE exit in `project_region` (prefer visible, then stable kind/bearing). Once the candidate list has one exit per neighbor, the EXISTING descriptor/bearing/deeper paths resolve naturally and a *genuine* distinct-node tie (b vs c) still refuses — `_resolve` needs NO change for the false-ambiguity case. Deduping is smaller and lower-risk than distinct-labeling each parallel edge (which would also force a resolver tie-collapse-by-`to_region_id`, since 3 distinct-bearing edges to `b` still tie on a token like "stairs").
- **Consistency gotcha:** `assign_bearings` ALREADY returns one bearing per neighbor (its dict is keyed by `to_region_id`), and `server/websocket_handlers/map_emit.py` builds the DUNGEON_MAP frame straight from `assign_bearings` — NOT from `project_region`. So if you dedup `project_region` to one-exit-per-neighbor, prose and the map frame agree (both show one `b`). If you'd gone distinct-label instead, you'd have to touch `assign_bearings` too or the prose and map would diverge. Don't change `assign_bearings`.
- **Ordinal/positional descriptors ("first/second/middle/leftmost/last") need their own bridge** — `requested_bearing` only knows compass/vertical words and `_KIND_SYNONYMS` only knows kinds. Add a `_resolve_ordinal(ordered, descriptor)` that runs BEFORE token-overlap (else a shared kind word like "passage" ties every corridor and refuses), filters to the descriptor's kind if named ("the first PASSAGE" = first corridor), orders left-to-right by a bearing rank (west=left), excludes `secret`, and fails loud (no token reinterpret) on an out-of-range position. Return `resolved_via="ordinal"` so the OTEL lie-detector shows the bridge engaged. See [[story_153_22_movement_parallel_edges]].
- **Dedup is logged at `debug` (No Silent Fallbacks)** — three-edges-to-one-node may be a materializer DUP bug, not legit Jaquaysed geometry (ADR-106). The collapse is intentional projection policy, but log it so a recurring duplicate stays visible; the materializer-level root-cause audit was deferred (Dev Delivery Finding).

### Re-introducing an SRD result into the WN-native path WITHOUT a native dial (153-12, 2026-06-22)
Under a WithoutNumber binding + `win_condition=hp_depletion`, `dice._apply_committed_player_beat` routes EVERY beat through `_resolve_wn_committed_action` (the ADR-143 native-engine cut), which resolves ONLY via strike→HP + `check_hp_depletion`. Any beat effect that lived in the native `beat_kinds.apply_beat` (resolution-beat exit, tags, dials) is therefore DEAD on the WN path. To restore ONE such effect faithfully (here: the ✦ resolution-beat / SRD Disengage exit), add the branch to `_resolve_wn_committed_action` — NOT to `beat_kinds.apply_beat` (that path is unreachable under WN) and NOT by reintroducing dials (SOUL "Bind the Ruleset, Don't Balance It"). Pattern: (1) thread the needed signal in — the helper lacked `outcome_tier`, so add the param + pass it at the single call site; (2) gate on the AUTHORED content flag (`getattr(beat,"resolution",False)` — the ✦ marker; both `combat::retreat` and `ship_combat::disengage` carry `resolution: true`), not on native `deltas` (there are none on this path); (3) keep HP-depletion precedence (`if not resolved and not encounter.resolved and ...`); (4) tier-gate per the SRD semantics (a Disengage is a skill check — `outcome_tier in (Success, CritSuccess)` ends, Fail/CritFail does not); (5) set `encounter.outcome="resolution_beat:<id>"` (mirrors native, distinct from `player/opponent_victory`) so the existing `_emit_player_beat_resolution_close` carries it onto the `encounter.resolved` span; (6) emit a focused `wn_resolution_beat_exit` watcher event for the decision (OTEL principle). The fix lands in the SHARED helper, so both the sealed-round (`run_wn_round`) and legacy-immediate dispatch paths inherit it — drive both in tests.


### Wiring a region-keyed subsystem call at _execute_narration_turn (153-23, 2026-06-22)
When a "deferred wiring delivery finding" story lands (a prior story built the whole path but left the production call site passing a default), the fix is often ONE call site. 153-23: `monster_manual_inject.inject(...)` in `websocket_session_handler._execute_narration_turn` (~line 843) needed `room_id`; resolve it with `mm_room_id = snapshot.region_for()` (NO perspective — party-consensus over `pc_regions`; returns None on a split/unseeded party). No call-site None-guard needed: `inject` already gates the binding branch on `if room_id and combat_encounters`, so a None room id reproduces the legacy behavior exactly (strictly additive). **Key correctness point:** use `region_for()` WITHOUT `perspective` — a per-perspective call would resolve the acting PC's region on a split party and wrongly fire the binding, breaking the "split party places nothing" guard. Never reach for `current_region`/`current_location` (No Silent Fallbacks — `region_for` is the only graph-region truth). Add the resolved id to the existing `monster_manual.injected` log so a GM reading server.log sees which authored room bound.

### FIXER forensics: the shared Postgres + `/api/debug/save/...` endpoints are reachable from oq-1 even when the DRIVER's server runs on oq-3 (sq-playtest 2026-06-22)
During a cross-workspace sq-playtest, the host aliases (`player1.local`…) all point to `127.0.0.1` and the DB is a single Postgres per ADR-115 — so from the FIXER workspace you can hit `curl localhost:8765/api/debug/save/<slug>/{snapshot,timeline,turn/N}` AND `psql "$SIDEQUEST_DATABASE_URL"` directly. This turns "the DRIVER says X" into "I read the ground truth myself." Recipe that cracked the blocking combat-never-seats bug: pull both sessions' `/snapshot`, compare the attacked creature's `location`/`region` against the PC's `character_locations` — the SEATED session had them equal ("The Drowned Cavern"), the NEVER-seated one had the Gnaw-Swarm at "Under the Rope" vs PC "The Winding Catacomb". **Caveat:** `intent_router`/seater decision spans are EPHEMERAL — they do NOT land in `turn_telemetry` (only the live WatcherHub/GM-panel stream). `turn_telemetry` carries coarser component rows (validator/genre/persistence/render/mechanical-census). So forensics can prove *outcome* (no ENCOUNTER_STARTED, HP unchanged, an invented mechanic persisted as a Lore footnote) but NOT the router's internal decline reason — that still needs a live GM-panel capture (DRIVER's surface).

### "Combat won't seat" is often a co-location bug, NOT a verb-classifier bug (sq-playtest 2026-06-22, blocking)
A confrontation seats only against a present, co-located Other (ADR-116). The seater/router matches the attack target against creatures whose `location` equals the PC's current scene label. The MM-injects creatures with `location=current_location` so `in_same_zone()` matches (`dispatch/monster_manual_inject._build_active_patches`) — but when the narrator surfaces a creature in prose ("a swarm pools at your feet") whose engine `location` is a STALE/different zone, an attack on it finds no co-located Other and silently never seats — the narrator then free-narrates the whole fight and can fabricate HP. Before touching the IntentRouter prompt or widening combat-verb extraction, **check target co-location in the snapshot** — a blunt "attack X with Y" failing to seat is the tell that it's co-location, not phrasing (widening verbs would not have helped). The fix surface is reconciling the surfaced creature's zone to the PC's current region — overlaps the 153-23 `region_for()` MM-inject threading. [[story_153_23]]

### Hook blocks commit when the Bash call STARTS on develop — checkout must be its own prior call (sq-playtest FIXER, 2026-06-22)
The `pf hooks dispatch PreToolUse` commit-guard evaluates the repo's branch at the moment the Bash tool fires, BEFORE the command runs. So `git checkout -b foo && git commit ...` as one compound command is BLOCKED (the call started on develop) and the checkout never even executes. Sequence as SEPARATE Bash calls: (1) `git checkout -b fix/...`, then (2) `git add … && git commit …`. By call 2 the repo is already on the feature branch and the guard passes. (Re-`cd`/abs-path per call since cwd can drift, but the branch state is in git, not cwd.)

### Graceful-degrade a terminal SDK exception in the narrator turn: catch at the orchestrator call, watcher-event + non-reconnect _error_msg + early return (2026-06-27, 158-41)
`_execute_narration_turn` (`websocket_session_handler.py`) wraps the whole turn in a method-level `try:` whose ONLY clause is a `finally:` (~2857) — no `except`. So any raise from `await sd.orchestrator.run_narration_turn(...)` (~1075) files the degraded `TurnRecord` in the finally and then **propagates**, and the WS layer treats that as fatal → `disconnect_save` → room teardown → reconnect. To make a terminal SDK condition degrade-loudly-but-survive (ADR-006), catch it *right at the call* and **`return` outbound** so the room stays alive:
- `try: result = await sd.orchestrator.run_narration_turn(...) except AnthropicSdkLoopExceeded as exc:` — narrow catch, NOT bare `except`. (`AnthropicSdkLoopExceeded`/`AnthropicSdkCostCeilingExceeded`/`AgentSdkAuthUnavailable` all subclass `AnthropicSdkClientError`<-`LlmClientError`; catch only what you degrade.) Import is `from sidequest.agents.anthropic_sdk_client import AnthropicSdkLoopExceeded`.
- Emit the GM-panel watcher event via the module-level alias `_watcher_publish("narrator.sdk_loop_exhausted", {genre,world,player_id,action_len,turn,reason,recovery}, component="narrator", severity="warning")` — `severity="warning"` is a valid free-string (94 uses); `publish_event` is fire-and-forget (drops silently if no loop/subscribers). This is the lie-detector signal; do NOT silently swallow.
- Player message: `return [_error_msg("…try rephrasing it.", reconnect_required=False, code="…")]`. **`reconnect_required=False` is the load-bearing bit** — it's an ERROR frame the client renders WITHOUT tearing down (the crash path is what forced the reconnect). Simpler + more honest than dressing an engine failure as in-world NARRATION; avoids the heavy `_emit_event` projection/MP path. The early `return` runs the method-level finally (degraded TurnRecord still filed) — `result` stays None there and the finally is None-safe.
- Mirror the in-method `IntentRouterFailure` degrade precedent (~1025) for shape. Do NOT re-route/balance the dogfight here — the fix is generic narrator robustness (SOUL "Bind the Ruleset").
- **Pre-existing broken window (confirmed via baseline A/B, NOT mine):** ~76 server tests that drive `_execute_narration_turn` through `session_fixture` are RED in-workspace, almost all dying at `monster_manual_inject.py:184` `ValueError: not enough values to unpack` (the fixture pack's MagicMock `effective_bestiary` unpacks to 0) plus a few `pregen` bestiary baselines (flickering_reach). Prove your change is clean by `git show HEAD:<file> > <file>` (back up your edit to scratch first — NOT `git stash`/`git checkout --`), run the failing-file set, restore, compare counts. Filed as a delivery finding; out of scope for a p1 narrator-degrade bug.

### Adding a guard to the opponent-seating cascade — filter the input, don't add a branch (2026-06-27, 158-34)
`instantiate_encounter_from_trigger` (`encounter_lifecycle.py` ~1708) seats the Other through a
`if materialized_threat / elif not npcs_present + location-fallback / elif not npcs_present +
frame_default` cascade. **Every `elif` is gated on `not npcs_present`** — so anything already in
`npcs_present` (a router-named `NpcMention`) bypasses all of them and is seated as-is
(`seating_source="router_named"`).
- **To firewall a class of router-named opponent (158-34: a personal-scale `is_creature` mention
  on a ship-scale `sealed_letter_lookup` dogfight), FILTER `npcs_present` BEFORE the cascade** —
  don't add a new `elif`. Emptying the list lets it fall through to the existing `frame_default`
  branch, which already seats a ship Other + emits `participant.joined source="frame_default"`.
  Adding a branch would duplicate that seating logic. The filter is ~6 lines; the branch would be ~20.
- **`npcs_present` is typed bare `list`** and elsewhere holds non-`NpcMention` items (table path uses
  `getattr(n, "name", None) or str(n)`, line ~1607). Use `getattr(m, "is_creature", False)`, not
  `m.is_creature`, to match the file's defensive idiom.
- **OTEL without a new span family:** the seating decision is already observable via the existing
  `participant.joined` `source` attribute (`router_named` → `frame_default`). That flip IS the
  GM-panel lie-detector — reuse it instead of minting a new span. Pair with a `_log.warning` at the
  drop so the rejection isn't silent (No Silent Fallbacks). The test asserts the span `source`, not a
  new span.
- **Scope discipline:** `materialized_threat` (free-string narrator threat, reconciled via
  `_resolve_opponent_from_roster`) is a SEPARATE seating door — out of scope for the npcs_present
  fix; flag it as a sibling door in Delivery Findings rather than guarding it speculatively.
### Fix expansion-quest premature completion at the OBSERVER, plus the silent mint span (2026-06-27, 158-42)
The reach_deep premature-completion bug (a region-anchor quest marked `completed` on entering its anchor, objective unmet) lives in `make_expansion_quest_observer` (`dungeon/expansion_quest.py`), which mints (`reconcile_dungeon_quests_into_log`) then resolves (`resolve_expansion_quests(reached_region_ids={to_region})`) in one call. `_beat_fired` fires `reach_deep` when `anchor_region in reached_region_ids` — so when the anchor IS the entry region (single-region expansion, or one where the entry scored deepest), entering it mints-and-completes in one transition.
- **Gate at the observer, not in `_beat_fired`.** `_beat_fired`/`resolve_expansion_quests` are correct in isolation (reaching the anchor IS the reach_deep beat) and have direct unit tests (`test_expansion_quest_resolve.py`) that must stay green. So add an OPTIONAL `skip_expansion_ids: set[int] | None = None` (default empty → per-turn handshake at `websocket_session_handler.py:1623` and all direct callers unaffected) and have the observer compute "minted this transition" by diffing `snapshot.quest_log` keys around the `reconcile` call (parse `dungeon:expN` via the existing `_DUNGEON_QUEST_PREFIX`). A quest minted this step is skipped from resolution → it completes only on a LATER descent to its anchor. Preserves the blessed `test_expansion_quest_e2e.py` happy path (mint on r0, complete on the deeper anchor r1 — different transitions).
- **Don't reach for "strictly deeper than anchor" (the other fork).** It breaks the e2e/regression "reach the deepest chamber = complete" contract and the observer has no reached-region depth lookup. The cost of the chosen fix: a *genuinely single-region* expansion's quest languishes `active` (no deeper chamber within it) — log that as a Question for Keith, don't silently pick "complete on descending past the expansion" (that's the bigger depth-aware change).
- **The OTEL gap was the MINT, not completion.** `dungeon.quest.bound` (seed) and `dungeon.quest.resolved` (complete) already existed + routed; the projection that makes a quest player-visible emitted nothing. Add `quest_minted_span`/`SPAN_QUEST_MINTED = "dungeon.quest.minted"` to `telemetry/spans/dungeon_quest.py` (mirror the bound/resolved contextmanager + `SPAN_ROUTES[...] = SpanRoute(event_type="state_transition", component="dungeon", ...)`), `__all__` it, and wrap the `if existing is None:` projection block in `reconcile` with it (attrs `expansion_id`/`quest_id`/`signature_kind`). Firing only in the new-projection branch makes it idempotent (no span on re-runs over an already-active quest). Tests monkeypatch `telemetry.spans.tracer` and assert via `InMemorySpanExporter` — `Span.open(tracer_override=None)` falls back to that module tracer, so don't pass a tracer in production code.
