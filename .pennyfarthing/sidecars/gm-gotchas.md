

<!-- migrated from Claude auto-memory store, 2026-06-24 -->

### Check in-flight work before filing a playtest bug (2026-05-25, from Keith)
- Before filing a playtest/QA finding as a standalone "[BUG]", check recent `git log` (all repos) + the active sprint epics to see if it's the player-facing symptom of work Keith is already mid-fix on. If so, frame it as the live end-to-end repro of that story and point at the root cause — don't present it as a naive new discovery.
- Why: Keith was visibly frustrated ("we are TRYING to fix the fucking confrontations") when "[BUG] Confrontations never engage" was filed cold — it was the exact target of epic 59 (IntentRouter). Reporting known-in-progress work as a discovery wastes attention and reads as if you didn't look.
- How: `git log --oneline -- <subsystem files>` + check the sprint board first. Example: "confrontations never engage / Edge never ablates" = the player-facing symptom of the IntentRouter returning markdown-fenced JSON → unparseable → no dispatch; 59-4 retired `begin_confrontation` (ADR-113) so the router is the ONLY path, no fallback.

### Pingpong file has parallel writers — re-read before every Edit (2026-04-26, playtest)
- `/Users/slabgorb/Projects/sq-playtest-pingpong.md` is the live cross-workspace channel between OQ-1 (server-side fixer) and OQ-2 (playtester/GM); both workspaces write concurrently. The Edit tool requires a fresh Read first; if OQ-1 wrote since OQ-2's last Read, the edit is rejected with a stale-read error.
- Re-read the file immediately before every single Edit, even if just read. Use small targeted edits with a unique `old_string` rather than block rewrites. Expect ≥1 stale-read bounce per session: re-read and retry, don't escalate.
- Use `[S2-BUG]`/`[S2-OBS]`/`[S2-UX]` tag prefixes. Screenshots referenced from pingpong live under `/Users/slabgorb/Projects/oq-2/sq-playtest-screenshots/` (Playwright sandbox), not the path the sq-playtest skill text suggests.

### Playtest player actions stay short (2026-05-12, playtest)
- When driving a Playwright playtest as the players, keep submitted actions terse — 1-2 short sentences, sometimes one line. Real players at Keith's table don't write paragraphs.
- Long scripted input is unrepresentative, over-fluffs the narrator's input window with player prose competing with mechanical state, and bypasses the pacing problems we're trying to surface (e.g. Alex's slower-typist concerns).
- Bias toward verbs + concrete intent ("We climb the Track. Halberd up.") — the narrator is the source of prose, the player the source of decision. Scale up only if the user says "improvise like James would" / "rich roleplayer voice." Also don't slow-walk to milk narration quality when the goal is testing a specific subsystem — push to the system under test.

### Playtest re-verify — pull both trees, a restart reloads stale code/content (2026-06-01)
- When re-verifying a just-merged fix, the running server may execute stale code OR stale content even after a restart — a restart reloads disk, it does NOT pull. The oq-2 `sidequest-server` and `sidequest-content` trees lag `origin/develop` independently. Server caches genre packs at startup (ADR-004), so updated content needs a server bounce too.
- 2026-06-01: brief claimed "restarted with pulled code"; measured reality — server 3 commits behind (#568 dial-sweep looked broken), content 8 behind (namegen "still Markov", Gunslinger "No abilities"). All stale-tree artifacts, clean after FF-pull + bounce.
- Apply: measure the running binary. `lsof -nP -iTCP:8765 -sTCP:LISTEN` → PID → `lsof -p <PID> | grep cwd` for the tree; `git rev-list --count HEAD..origin/develop` (after fetch); grep the fix symbol on disk. DRIVER owns the clean FF pull (`git pull --ff-only`); FIXER/Keith owns the bounce. Also `rm ~/.sidequest/manuals/<genre>_<world>.json` before a fresh namegen test (manual cache is per-world).

### audio.yaml `themes:` block shadows `mood_tracks` — purchased tracks never reached (2026-06-14, content fix)
- In `sidequest-server/sidequest/audio/library_backend.py:_resolve_music`, the engine gathers `themes` variations matching `cue.mood` (`theme.mood == cue.mood`) and if ANY exist, picks from them and RETURNS before ever reading `mood_tracks`. So a `themes:` entry for a mood completely shadows that mood's `mood_tracks` pool.
- Consequence: in space_opera the ACE-Step set-1 leitmotifs in `themes:` meant the purchased SciFi/TableTop catalog in `mood_tracks` NEVER played; the only purchased track heard was "3 Dark" (slotted as a theme variation).
- Fix (content-only): delete the `themes:` block → engine falls through to `mood_tracks`. Caveats: (1) any track ONLY referenced in themes must be moved into mood_tracks or it's orphaned; (2) a mood with a theme but no mood_tracks key (space_opera `chase`) falls back to DEFAULT_FALLBACK_MOOD = "exploration" — add an explicit mood_tracks entry. `MoodTrack` has `extra: forbid` and NO weight field, so the only content-side lever to demote a track is to remove it. Verify via load_genre_pack + a `_resolve_music` test (themes==0, each mood resolves to a purchased path).

### portrait_manifest `backdrop_poi` must use the POI explicit `slug`, not the slugified name (2026-06-22, 153-36)
- A `portrait_manifest.yaml` picker's `backdrop_poi:` must reference the POI's explicit `slug:` (from the world's `history.yaml` `points_of_interest[]`), NOT the slugified display name. PlaceCatalog keys on the explicit slug, so a name-derived value (e.g. `the_deep_shaft` for a POI whose slug is `ingurdios_deep_shaft`) misses → daemon raises `CatalogMissError` (`COMPOSE_FAILED`) at render time and the portrait never renders.
- Used to be silent (validator routed a dangling ref to `waiver_warnings`). 153-36 fixed it: `_validate_portrait_manifest` (`sidequest-server/sidequest/cli/validate/pack.py`) now emits a hard `content_error` for a dangling `backdrop_poi` — BUT only when `known_poi_slugs` is non-empty (empty set = POI-collection gap, e.g. unreadable history.yaml; erroring there would false-positive cascade). Validator checks only world `history.yaml` POI slugs, not genre-tier `places.yaml`.
- How to apply: when a picker portrait won't render, FIRST diff its `backdrop_poi` against `PlaceCatalog.load(genre, world)._entries` keys, don't assume a sync gap. Fix = remap to the real slug, then `python scripts/generate_portrait_images.py --genre <g> --world <w>` (no `--force`). Renders use `catalog_compose=True`, so the DAEMON resolves `npc:`/`where:` refs from ITS OWN `SIDEQUEST_GENRE_PACKS` clone — verify that clone's manifest+history match yours (it may run from a different oq-N clone). As of 2026-06-22 zero dangling `backdrop_poi` across all 11 packs (resolved arc: 153-18 evropi, 153-35 long_foundry, 153-36 validator).

### hp_depletion combat authoring traps — inert dials, mechanical-lie deltas, exit-beat `resolution:true` (2026-06-05, 59-26/87-1)
- Fixed under 59-26 / playtest 67-10 (server #551, content #310, ui #309). `win_condition: hp_depletion` (space_opera/SWN, ADR-114) facts: (1) the dials are INERT 1e6 placeholders, the HP channel is authoritative. `encounter_lifecycle.py` synthesizes `MetricDef(threshold=1_000_000)` when combat declares no dial; `beat_kinds.apply_beat` now SUPPRESSES dial-delta application under hp_depletion (`beat_kinds.py:617`) + emits `dial_suppressed_hp_depletion` span.
- CONSEQUENCE (87-1): beat-level `deltas` (e.g. `crit_fail.own: -3`) are ALSO suppressed under hp_depletion (they're dial deltas). So any player-facing `risk:`/`effect:` text naming a dial penalty ("lose 2 momentum", "−3 on a crit fail") is a MECHANICAL LIE — the number never fires (Sebastien/Jade read this math). Systemic across WWN-migrated packs (EH `elemental_burst` `own:-2`; heavy_metal `committed_blow` `own:-3`). Such a beat is strictly dominant over plain `strike`. Cleanup = strip inert `deltas.crit_fail.own` + reword risk to the real consequence.
- (2) UI must read `player_hp`/`opponent_hp`, NOT `player_metric`/`opponent_metric`. Server `ConfrontationPayload` emits `win_condition` + `player_hp`/`opponent_hp {current,max}` (`confrontation.py:244-262`); `ConfrontationOverlay.tsx` renders an HP track when `win_condition === "hp_depletion"`. Rendering the inert dial showed "You 0/1000000" — invisible HP ≠ unwired HP.
- (3) Exit beats need `resolution: true` — the prose `consequence:` string is INERT (engine reads `beat.resolution`, never `consequence`). space_opera `retreat`/`disengage`/`microjump` had `consequence:"...ends"` but no flag → combat unexitable. Systemic authoring trap — audit flee/concede/disengage beats.
- (4) Opponent reprisal damage comes from `ConfrontationDef.opponent_damage`, NOT the borrowed strike beat. `_seed_combat_hp_depletion_to_npcs` seeds the opponent with an empty `Inventory()` → `resolve_damage`→None → enemy hits for 0 HP. Fix (#552/#311): typed `opponent_damage: DamageSpec` read first. Trap: the reprisal e2e MASKED this by hand-arming the opponent in its fixture — drive combat against the REAL seed (weaponless opponent).

### Epic 120 verbatim inventory sweep strands chargen kits (2026-06-15)
- Making WN genre baselines 100% SRD-verbatim by renaming the genre `item_catalog` (`torch`→`wwn_torch`) and moving no-analog items to the WORLD tier STRANDS `equipment_tables.yaml` — chargen class kits still reference old/world ids, so every genre kit dangles even though narrow inventory tests stay green. Smoking-gun tests: `tests/genre/test_cc_class_kits.py::test_cc_kit_items_exist_in_inventory`, `tests/integration/test_106_1_caverns_armor_content.py`.
- Fix pattern (Keith Option-B): genre `equipment_tables.yaml` goes WN-pure; a NEW `worlds/<w>/equipment_tables.yaml` re-adds world items per-slot + grants. Merge via `server.dispatch.equipment_tables_resolve.resolve_equipment_tables(pack, world_slug)` (story 120-4, server PR #888): per-slot APPEND for class_tables/tables, guaranteed_grants append by kit id, rolls_per_slot world-overrides. Production calls it at `connect.py:935`. caverns/beneath_sunden = worked precedent (120-1).
- 120-2 road_warrior CWN (content #468 + server #891): the equipment_tables prediction was WRONG — road_warrior has NO `equipment_tables.yaml`; chargen kits are inline `starting_equipment` in `inventory.yaml`. Fix = repoint `starting_equipment` in BOTH genre and the_circuit inventory.yaml. The REAL cascade = genre-tier VESSEL/RIG tests (Story 86-5): moving rig vessels/mount weapons off genre broke 6 tests reading vessels from genre inventory (`test_road_warrior_vessel_calibration.py`, `test_vessel_full_stat_blocks.py`, `test_the_circuit_rig_playtest_otel.py`) → redirect those loaders to the_circuit world tier. Production unaffected (chargen binds via `bind_rig_pool_from_inventory` over merged `resolve_inventory`). Lesson: grep `tests/**` for genre-tier vessel/`pack.inventory` reads BEFORE any verbatim sweep of a rig pack.

### 121-8 Fate chargen UI is really server,ui,content; every Fate pack must carry the 3 fate_chargen_step scenes (2026-06-17)
- Story 121-8 was filed `repos: ui` but is server,ui,content. Three missing pillars beyond UI: (1) server render payload — `CharacterCreationPayload` had no `fate_*` fields, `builder.py:_render_fate_step_message` emitted only bare input_type (121-7 deferred §7 payload); (2) submission handler — `handlers/character_creation.py` had no `fate_*` branch (hit "Unknown chargen phase"), `record_fate_chargen` had ZERO production callers (121-7 tested it directly, bypassing the handler); (3) content `fate_chargen_step` scenes.
- Wire contract (snake_case, `extra="forbid"`): render `fate_aspect_slots/fate_available_skills/fate_pyramid/fate_apex_rating/fate_current_allocation/fate_ladder_labels/fate_available_stunts/fate_selected_stunts/fate_free_stunts/fate_base_refresh/fate_current_refresh/fate_legal/fate_violations`; submit phases `fate_aspects_confirm/fate_pyramid_confirm/fate_stunts_confirm`. Ladder labels from `fate_resolution.LADDER`. Design: `docs/superpowers/specs/2026-06-16-fate-interactive-chargen-design.md`.
- Recurring content trap: a Fate pack/world whose `char_creation.yaml` lacks the 3 `fate_chargen_step` scenes (aspects/pyramid/stunts) never records FateChargenChoices, so `apply_fate_chargen` never overrides the always-on `seed_chargen_resources` floor (`game/ruleset/fate.py:~68`) → every Fate char gets the genre-default sheet. An un-overridden default, NOT a swallowed fallback. A world's char_creation list REPLACES the genre's wholesale (`loader.py:212`), no merge.
- The 4 Fate packs: pulp_noir, wry_whimsy, spaghetti_western, tea_and_murder. GM authored the 6 missing scripts 2026-06-17. Keith's standing fix ("Both"): content scripts + Dev adds a fail-loud LOAD guard (fate pack missing the 3 steps → `GenreLoadError`) + terminal OTEL span `fate.chargen.derived` vs `fate.chargen.default_fallback`. Merge ordering: content → develop BEFORE the guard. Operator directive: do NOT test against content — make fixtures, validate content (`load_genre_pack`).

### Only pulp_noir's char_creation runs interactive Fate chargen → default-sheet collapse for the other 6 (2026-06-17)
- Interactive Fate chargen (player authors High Concept/Trouble/free aspects + skill pyramid + stunts) runs ONLY via `char_creation.yaml` scenes carrying `mechanical_effects.fate_chargen_step: aspects|pyramid|stunts` → `builder._commit_fate_step` → `apply_fate_aspects/pyramid/stunts` → `game/ruleset/fate_chargen.build_fate_sheet`. Only `pulp_noir/char_creation.yaml` has the 3 scenes. The other 6 OMIT them: wry_whimsy (genre); spaghetti_western/{dust_and_lead,five_points,the_real_mccoy}; tea_and_murder/{glenross,blackthorn_moor}. Verify: `grep -c fate_chargen_step`.
- Without the steps, no FateChargenChoices recorded → sheet falls back to the genre DEFAULT loadout (`rules.yaml` `default_high_concept` + the `gear:` list + fixed skill spread, assembled `game/ruleset/fate.py:97`) → every funnel-chargen char of those packs gets the SAME sheet regardless of archetype. The engine is fine (pulp_noir authors genuinely differentiated sheets); the gap is the chargen SCRIPT. A world's char_creation list REPLACES the genre's wholesale (`loader.py:212`).
- Fix = add the 3 scenes to the 6 scripts (mirror `pulp_noir/char_creation.yaml:250-300`) OR auto-inject in the builder for any Fate pack. Sub-findings: free-text funnel answers reach the NARRATOR (via background/origin_label) but never become aspects/items (`archetype_inferred:false`) — additive color by design; the Character panel never surfaces the Fate sheet (shows native HP/Lv/diamonds + "No stats available" while `core.fate_sheet` is populated).

### A Fate genre pack needs a confrontations: section or conflicts fail-loud and the narrator improvises violence (2026-06-19)
- A Fate-bound genre pack MUST define a top-level `confrontations:` section in its genre `rules.yaml` covering the encounter_types the intent router emits (standoff/combat/negotiation/...) — else EVERY conflict fails loud (`ValueError: unknown encounter_type 'X'`, `dispatch/encounter_lifecycle.py:1219`; "No silent fallbacks" at `agents/subsystems/confrontation.py:25`), `in_conflict=False` persists, and the narrator improvises the fight as pure prose with zero mechanical backing (no 4dF, no stress, no FateSheet). OTEL is the only tell — the prose looks fine.
- Found in the 150-1 pulp_noir/annees_folles cross-confirm: pulp_noir was the ONLY Fate pack with no `confrontations:` section (spaghetti_western/tea_and_murder/wry_whimsy all had one) — likely the true root cause of the parked 150-1 "opposed actions never escalate" symptom (was misattributed to router unreliability). Fixed content PR #479 (`feat/pulp-noir-confrontations`).
- Confrontations are genre-tier (rules.yaml), NOT world-tier. Under ADR-144 native dial fields (player_metric/beat kind/stat_check) are inert — the #964 gate keeps the native overlay off and 4dF owns resolution; they exist only to satisfy the ConfrontationDef/BeatDef schema and seat the encounter_type. `resolution_mode` default = beat_selection → under Fate seats a full Fate conflict. Validate with `load_genre_pack`, not the validator.

### Reusable Fate-world playtest findings from 150-3 five_points (2026-06-20)
- Genre/engine-wide, expect across 150-4..150-9 (spaghetti_western/the_real_mccoy, tea_and_murder ×2, wry_whimsy ×3). The load-bearing split: INSIDE a seated conflict, resolution runs through `encounter.fate_commits` (real 4dF faces stored, e.g. `dice:[0,0,-1,1]` + skill ladder) — VERIFIED GREEN (126-7 in-conflict 4dF, ADR-151 DEFEND barrier, 126-1 harm ablation with stress box checked + core.hp untouched, #942 reconnect hydrate, #945 gear-aspects invokable). OUTSIDE a conflict, player skill checks (Notice/Overcome) get NO engine roll — the narrator FABRICATES the 4dF in prose [MAJOR]. (Dashboard "beats: none / lethality_arbiter 0ms" on conflict turns is EXPECTED — native-beat metrics Fate doesn't use.)
- Other recurring bugs: `chargen_seed_table` is pulp_noir-ONLY → spaghetti_western/tea_and_murder/wry_whimsy get blank Fate pyramid + placeholder-only aspects (126-24 shipped the server seam #965 + pulp_noir content only; hits all 8 non-pulp_noir Fate worlds). Dice render blank — #430 made the face-label font same-origin but the `/dice-cdn` Vite proxy → `cdn.slabgorb.com/dice_assets/Inter-Bold.ttf` returns 403; local `public/fonts/Inter-Bold.ttf` serves 200, unused (`FateDiceTray.tsx:42`; mechanical throw unaffected). Poker (126-2) seats then self-destructs — `instantiate_table_encounter` fires but `deactivated_on_location_change` kills it the same turn. De-nativize confirmed (storied): standoff runs a native tension dial in PARALLEL with the Fate exchange. LOWs: possessive place-ref mints a phantom NPC; historical pre-gens get conlang-mangled names; canonical NPC double-seeded.

### heavy_metal → WWN port (epic 87), 2nd WWN pack, content-only (2026-06-05)
- Bind heavy_metal → wwn ruleset (2nd WWN pack after elemental_harmony). Spec: `docs/superpowers/specs/2026-06-04-heavy-metal-wwn-port-design.md`. Stories: 87-1 (binding+combat foundation, DONE), 87-2 (classes/chargen + magic, 13pt), 87-3 CANCELED (folded into 87-2), 87-4 (sweep+calibration+playtest). Both repos. SCOPE CHANGE (Keith, mid-87-2): "I want actual magic … things replaced with WWN" — real WWN magic ships IN 87-2 (authors `spells_wwn.yaml` faithful PORT of WWN High Magic spells, full caster `wwn_magic` cast tables + starting_prepared, `cast_spell` beat with class_filter, `saving_throws` on every class). Lesson: on a port, never ask "how faithful / reskin" — port WWN verbatim.
- Simpler than road_warrior→CWN: NO net-new engine primitive (EH already proved every wwn seam) — pure content port + standard calibration/OTEL wiring, zero engine changes. Keith decisions: faithful 3-chassis (Warrior/Expert/Mage + Foci), magic_level high, lethality high; RETIRE the bespoke pact/ledger magic (pact_working/debt_collection confrontations + ledger_tracking/pact_cost_attribution custom_rules cut in Story 4; casting → WWN cast_spell).
- Measured engine facts: heavy_metal `rules.yaml` ALREADY declares the six STR/DEX/CON/INT/WIS/CHA (CHA engine-mandatory for Mental save). A complete `wwn.attribute_map` IS required (`_validate_wwn`, `genre/models/rules.py:1163` — all six canonical keys, each value in ability_score_names, `system_strain.max_source` a canonical KEY); heavy_metal's map is canonical→abbreviation (`CONSTITUTION: CON`) — the ONE structural difference from EH (canonical→flavor). REMOVE the `edge_config` block (Edge→ablative HP). Only calibration test to migrate: `test_heavy_metal_pack_loads_with_dual_dial_schema` (`test_pack_load.py:44`, dial_threshold filter); heavy_metal NOT in COMBAT_PACKS/SHIPPED_PACKS. Story-3 trap: once a spell catalog exists, loader requires `saving_throws` on EVERY class (`test_pack_load.py:403`).

### Lore Cast display names are per-world portrait_manifest content (2026-06-04, content #350)
- The lore-page Cast section derives each card heading from the world's `portrait_manifest.yaml` `name:` field. If `name:` is authored as a slug (`white_rabbit`, `the_emperor_of_lilliput`), the heading renders the raw slug.
- The fix is per-world content, not a one-time server fix. Server #630 added a generic `cast_portrait_slug(item)` helper (`reference_presenters.py`) = explicit `id` else `slugify(name)`, reading the raw YAML dict (so `id` is seen despite `PortraitManifestEntry` dropping it via `extra=ignore`). But the heading still comes from `name`. So each world needs `id: <old-slug>` (preserves the R2 portrait key `…/<world>/assets/portraits/<slug>.png`) + `name: <display>` sourced from that world's `npcs.yaml`.
- #348 fixed oz only; wonderland + gulliver got the same pattern in content #350. Any new world authored with `name: <slug>` shows slug Cast headings until you add `id:` + display `name:`. Verify via real `load_genre_pack`, not `validate pack`. Cosmetic — no OTEL; anchors stay slug-based (correct).

### ACE-Step output fields creep back into music params JSON — strip before commit (2026-05-10, PR #204)
- After every music regen, ACE-Step writes output-only fields back into `*_input_params.json`: `audio_path` (machine-local abs path, was hardcoded `/Users/keithavery/...` for months), `timecosts` (timing dict), `retake_seeds`. These are dead data on disk; the daemon (`ace_step_adapter.py:22`) lists them as `_OUTPUT_ONLY_FIELDS` and `prepare_inference_params()` strips them on the way IN, but `scripts/generate_music.py` does NOT post-clean the spec JSON.
- Before PR #204, 307 files across all packs carried them. After running `scripts/generate_music.py`, strip the three fields before commit (rglob `*_input_params.json` under `audio/music`, pop `audio_path`/`timecosts`/`retake_seeds`). Baking the clean into `generate_music.py` is a not-yet-done follow-up.
- Keep `actual_seeds` — it is INPUT despite the name (daemon reads it and renames to `manual_seeds`).

### Monster-manual encounters unwinnable when authored player weapons carry no damage spec (2026-06-12, beneath_sunden playtest)
- Full WWN combat pipeline works (confrontation from generated dungeon room vs Monster-Manual creature, real beat grid, initiative, `dice.throw_resolved`, opponent reprisal, ablative `hp_depletion.resolved`, mortal-injury death) but the player dealt 0 damage all fight (crit included) and died guaranteed.
- Root cause: NO weapon in `caverns_and_claudes/inventory.yaml` carries a `damage`/`damage_dice` field, and there is no WWN unarmed default. The dice resolver (`server/dispatch/dice.py:~1088`) needs a `damage_override`, a weapon-with-damage-spec, OR an unarmed default — none exist for the player → emits `dice.damage_spec_missing ... HP damage skipped (CLAUDE.md no-fabricate)`. Engine is CORRECT (no-fabricate); the gap is content/config. The asymmetry is the tell: Monster-Manual opponents DO carry `damage_override` (mirror path `dice.py:~1781` `opponent_damage_spec_missing`), so every fight is unwinnable guaranteed player death.
- Wiring-trap lesson: when binding/validating a pack for WWN/CWN/SWN `hp_depletion` combat, verify the PLAYER side has a resolvable damage source — author weapon damage specs and/or an engine unarmed/weapon-tag default. A pack can pass load + run encounters and still be 100% lethal-to-player because the player-damage path loudly (OTEL) skips.

### Multiplayer session URLs are deterministic — lobby drop-ins rejoin, not restart (2026-05-10, playtest)
- Session URL pattern: `/play/{YYYY-MM-DD}-{world_slug}-mp`. Two tabs hitting Start Adventure on the same date with the same world + multiplayer settings join the SAME session and inherit earlier-playtest characters; the game then stalls in turn coordination waiting on the now-offline players.
- For a clean playtest: (a) bump the date in the URL, (b) clear the session DB at `~/.sidequest/saves/`, or (c) accept the drop-in to test offline-player handling. Two fresh-looking lobby flows are NOT two fresh sessions.

### `validate pack` is necessary-not-sufficient — only load_genre_pack catches load failures (2026-06-01/06-02)
- `uv run python -m sidequest.cli.validate pack <dir>` (PASS 0/0) is NOT proof a pack loads — it is mostly file-presence + per-file pydantic parse. After validator-green, ALWAYS run `load_genre_pack(Path("<pack-dir>"))` as the wiring gate.
- Loader-only failures found building wry_whimsy/Oz + Wonderland: (1) enum fields validate as existence-only — `visibility_baseline.yaml tone: open` passes but `tone` is Literal['broadcast_heavy','balanced','secret_heavy'] → GenreLoadError. (2) `draft: true` silently SKIPS the world at load (loader.py:924/1377) — validates fine but invisible (WORLDS: []); missing PNGs are warnings not errors, never a reason to draft. (3) world `openings.yaml` uses the unified Opening schema (`triggers.mode` solo|multiplayer|either with ≥1 solo + ≥1 MP eligible; `mp` is INVALID; `first_turn_invitation` must not contain `?`; needs `establishing_narration`/`setting.location_label`/`tone.{register,avoid_at_all_costs}`) — the old archetype/situation shape parses but DROPS narration. NPCs live in `authored_npcs`, not `npcs`. (4) unquoted `: ` mid-phrase in a `list[str]` item silently parses as a one-key dict (yaml.safe_load passes; only the model rejects) — quote it or use `>-`.
- A `draft:true` world masks BOTH its missing-file errors (downgraded to warnings) and its per-file pydantic parse, so the validator says PASS while a file is broken. Smoke recipe: temporarily flip draft→false, load, assert the world is in `p.worlds` and every cartography npc `binding.ref` resolves against `authored_npcs`, restore via trap.

### sq-world-builder skill is stale for AWN-era / historical content (2026-06-11, 103-7)
- `docs/world-builder-skill-rough-edges.md` (orchestrator, committed 2026-06-11) is a living severity-tagged backlog of where the `sq-world-builder` skill (command + agent + `.pennyfarthing/workflows/world-builder/steps/*`) is wrong/silent for post-port / AWN-era content.
- Top blockers: (B-4) "every name through the conlang" is stated absolute and fights historical worlds — real-Earth worlds use curated `names_file:` lists, not Markov (the naming-audit flags real names as slip-throughs). (C-12) `world.yaml draft: true` lifecycle absent — loader silently SKIPs draft worlds so namegen/load_genre_pack can't see them. (C-7/8) Saints/`saints.yaml` + stocks/`stocks.yaml` missing from steps. (A-1/A-2) dead `keithavery`/oq-1 paths + `cargo run sidequest-validate` (Rust validator gone post-ADR-082).
- Apply: before driving a world-builder run on an AWN/historical world, expect to override the conlang-absolutism and hand-author saints/stocks/chargen/draft; consult the doc and append new rough edges as found.

### World-tier confrontations.yaml + faction_agendas.yaml are engine-unwired (2026-06-01, partial 06-06 fix)
- Confrontation mechanics load only from genre `rules.yaml` (`_load_rules_config`). `worlds/<slug>/confrontations.yaml` is dead data for a non-magic pack — its only consumer (`sidequest/magic/confrontations.py` via `server/magic_init.py`) parses a different magic-advancement schema and runs only when magic is active. `worlds/<slug>/faction_agendas.yaml` has ZERO runtime consumers.
- Author confrontations correctly: mechanics go in genre `rules.yaml confrontations` (SOUL "Crunch in Genre, Flavor in World" — it fires); world tier carries FLAVOR only (NPC goals in `npcs.yaml` IS wired ~loader.py:472, tropes/lore/legends). Living-World faction motion = NPC goals in npcs.yaml. Loader DOES merge these world overrides: archetypes, archetype_funnels, visual_style, history, theme, audio, openings, npcs, items/rigs/scenarios/cultures/legends/tropes (~loader.py:973-1030).
- Third instance FIXED 2026-06-06: world `inventory.yaml` WAS dead; epic 94 moved it genre→world for space_opera/heavy_metal/spaghetti_western → blank inventory. Fix shipped: `World.inventory` field, loader loads world inventory (`world_inventory` span), `inventory_resolve.resolve_inventory(pack, world_slug)` world-first REPLACE (not merge), wired into all 4 consumers (chargen loadout, currency view, chargen summary, gained-item catalog). Test gotcha: `resolve_inventory` traverses `pack.worlds` — MagicMock packs need `genre_pack.worlds = {}` re-stubbed AFTER copy.deepcopy.
- Trope corollary: `resolve_trope_inheritance` emits world tropes ONLY (genre tropes are `extends` parent-pool material); a world's tropes.yaml is its whole menu; chapter `tropes:` seeds in history.yaml must use world-tier ids. Lesson: before assigning a world-extension file, confirm it has a Python consumer; a genuine need for mechanically-distinct world confrontations is a loader-wire finding for Dev, not content.

### beneath_sunden combat still NATIVE pre-cutover — Epic 108 is the WWN cutover (2026-06-14, from Keith)
- For WN-bound genres (beneath_sunden on WWN) the native confrontation engine is deprecated (ADR-143 / SOUL "Bind the Ruleset, Don't Balance It"). The WN module must REPLACE native — don't tune/convert/gate native mechanics, remove them. Epic 108 is the cutover.
- Partial: ✅ 108-1 (cut native apply_beat), 108-5 (RP-flavor rider span), 108-6 (WWN dying/down window). 🔲 108-2 (p1) — WN round must seat its Other from the bound roster; bug: narrator fights the bound creature (e.g. Molgrath the Eyeless HP24, creature_id='Thief') but the engine seats an INVENTED opponent (Hold-Dead HP10, creature_id=None) as the 8th npcs entry → two names for one enemy. 🔲 108-3 (p1) — content de-nativize WWN combat defs (remove flat DC 12 beats, the Tie outcome tier, momentum, inert 1e6 dial metrics, the cast-to-hit roll; WWN magic is slot-based with NO cast roll — a "rolled 6 vs DC12 Fail" that burns a slot is native bleed, why the Mage "feels shitty").
- Playtest trap: when combat in a WN world shows Tie tiers, DC-beats, failed-cast rolls, momentum, or inert dials, that is EXPECTED pre-cutover native bleed — evidence for 108-2/108-3, NOT a fresh bug to tune. The finding is structural (native engine present at all); don't analyze native-mechanic VALUES.
- Verified-green WWN content on this build (not cutover-blocked): chargen xp:0, guaranteed Potion of Mending, armor-equip→AC (106-1), light/darkness environment_clock −2, inventory beat-scan (106-4), WWN Mage prepared-spell UI w/ casts N/day.

### Z-Image does not use negative prompting — rejection clauses go in the positive prompt (2026-04, image pipeline)
- The image pipeline renders via Z-Image (mflux, Apple MLX), which does NOT honor negative prompts — its architecture doesn't use classifier-free guidance against a separate negative embedding the way SD/SDXL/Flux-dev do. Any `negative_prompt` the daemon plumbs through is effectively ignored.
- Never recommend "add a negative prompt" for visual polish. Rejection clauses belong at the end of the POSITIVE prompt, phrased "No text, no caption, no title, no watermark, no border…" — the idiom already in `worlds/*/visual_style.yaml` under `positive_suffix`. The `negative_prompt` field may still be populated (daemon plumbs it) but treat it as a dead channel for Z-Image. If the renderer is swapped back to Flux/another CFG model, negatives start mattering again.

### Canonical playtest filesystem paths on this machine (2026-04-30)
- The `sq-playtest` skill was generalized to drop hardcoded `keithavery`/OQ-1/OQ-2 names; it references `../Projects/sq-playtest-pingpong.md`. Concrete paths:
  - Ping-pong file: `/Users/slabgorb/Projects/sq-playtest-pingpong.md`
  - Screenshots dir: `/Users/slabgorb/Projects/sq-playtest-screenshots/` (still used; skill no longer documents it)
  - Archive dir: `/Users/slabgorb/Projects/sq-playtest-archive/`
  - Playtest workspace: `/Users/slabgorb/Projects/oq-2/`; Bugfix workspace: `/Users/slabgorb/Projects/oq-1/`
- Service logs: `~/.sidequest/logs/sidequest-{server,client,daemon}.log` — NOT `/tmp/sidequest-*.log` (retired). Rotated per launch with timestamped `.log.YYYYMMDD-HHMMSS` backups, 30d retention. Current `sidequest-server.log` is appended live even when the running server is oq-1's bare-uvicorn process. Re-tail with `just logs [service]`. Stale code comments still cite `/tmp` — grep `~/.sidequest/logs`.
- oq-2 root is the orchestrator checkout (4 subrepos + cross-repo justfile); `just up/server/client/daemon` work from there. Server is Python (post-ADR-082); direct invocation: `uv run uvicorn sidequest.server.app:create_app --factory --reload --host 127.0.0.1 --port 8765`.

### Exit a running game to the lobby with the in-game 'Leave' button — NOT by navigating to root (2026-06-17, from Keith)
- During a playtest, to drop the current game and pick a new world/PC, click the **'Leave'** control in the game UI; it returns you to the ConnectScreen lobby cleanly. Navigating the browser to `/` does NOT work — the app restores the last active solo session from per-origin localStorage and redirects straight back into `/solo/{slug}`.
- Caveat for a CRASHED board: when the GameBoard has hit its error boundary ("Something went wrong in Game" + [Try again]), there is no 'Leave' button to click, and root-navigation re-loads into the crash. Escape hatch in that case: switch to a different host alias (`player2.local:5173` etc.) — separate origin = empty localStorage = fresh lobby, no active-session redirect. Use 'Leave' for the normal case; host-switch only when the board is already dead.

### Portal-fairytale (wry_whimsy) player-pickers = REAL people from the period the book was written, NOT in-world factions (2026-06-12, from Keith)
- wry_whimsy is PORTAL fantasy: the players are outsiders who fall INTO the fairytale (Dorothy, Alice, Gulliver), not natives. Authoring player_pickers around in-world cultures/factions (oz: munchkin/winkie/quadling/gillikin/emerald; wonderland: card_folk/chesspieces/creature_folk; gulliver: lilliputians/brobdingnagians/laputans/houyhnhnms) is WRONG — Keith: "they are choosing in-world factions which does not work for whimsy."
- Correct model: the picker is WHO YOU WERE in the real world before the portal — an ordinary person from the AUTHOR'S OWN ERA. Period = publication date of the source work: oz=1900 America (Baum), wonderland=1865 Victorian England (Carroll), gulliver=1726 Georgian Britain (Swift). Occupations/dress/names are real-period.
- BUT still render in the WORLD's illustration style (Denslow plate for Oz, Tenniel engraving for Wonderland, etc.) — a real 1900 Kansas farmwife DRAWN as a Denslow plate. World-visual consistency holds; only the SUBJECT changes from native-faction to period-traveler.
- Mechanics: one new world-level real-period culture file per world (kansas_1900 / victorian_english / georgian_british), all pickers point at it; reuse the existing system archetype values; leave NPC entries + in-world culture files untouched (those natives are correct). Same StyleCatalog culture-gate rule applies (file stem must match picker culture slug).
- The in-world-faction model IS correct for NON-portal genres (heavy_metal, spaghetti_western, etc.) where the player IS a native — this exception is portal-fairytale-specific.

### Portrait `--dry-run` does NOT validate the StyleCatalog culture gate — check `cultures/<slug>.yaml` exists per picker culture (2026-06-12)
- `generate_portrait_images.py --dry-run` composes prompts and confirms `backdrop_poi` refs resolve, but it SKIPS catalog composition (render_common.py ~L799: "catalog_compose validation runs AFTER the skip gate"). So a dry-run passes 0-errors while the real render dies with `CatalogMissError: StyleCatalog.culture has no entry for <genre>/<world>/<culture>`. Cost: 18/18 portraits failed on beneath_sunden before I caught it.
- The real gate: the daemon's `StyleCatalog.load` (daemon catalogs.py ~L358) populates culture style ONLY from `genre_packs/<genre>/worlds/<world>/cultures/*.yaml` — keyed by the FILE STEM. Pack-level `cultures.yaml` is NOT read. Every picker `culture:` slug must have a world-level `cultures/<slug>.yaml`. visual_tokens may be empty/absent (defaults to "") — the file just has to EXIST with the matching stem.
- Validate BEFORE rendering with a YAML check (not BSD `sed` — `\s` is literal `s` on macOS and silently mangles): load each portrait_manifest.yaml, for every `type: player_picker` collect `culture`, assert `cultures/<culture>.yaml` exists. Three failure shapes seen across a 21-world fan-out: (1) no world cultures at all (beneath_sunden — fix: author the culture file); (2) wrong slug guess vs real filename (the_circuit: picker `bosozoku` vs file `bōsōzoku.yaml`, `cafe_racers` vs `café_racers.yaml`, `one_percenters` vs `1%ers.yaml` — fix: remap picker `culture:` to the real stem); (3) name-only cultures filed as `<x>_names.yaml` (glenross — fix: remap to the `_names` stem).
- art-director agents reliably guess plausible-but-wrong culture slugs from prose; always cross-check their `culture:` against the actual `cultures/` filenames.

### Report only observed mechanics — never extrapolate the system (2026-06-07, from Keith)
- When filing playtest bugs: quote logs, quote UI, quote authored content text. Do NOT predict adjacent behavior the system was never observed to do (e.g. claiming the attention-based disposition model "predicts NPCs drift cold when ignored" — whether relationships degrade at all is unknown). Keith: "stop making shit up."
- Quoting authored content verbatim (ability text, lore) is always fine — the line is between citing what exists and inventing system dynamics.

### A clean log can be a vacuous pass — confirm the subsystem ENGAGED before flipping verified (2026-06-07)
- blackthorn fix-validation: zero `entity_sync.project_failed` WARNs looked like a #736 pass, but blackthorn seeds ZERO tropes (`active_tropes: []`) — the merge path never ran. Same session: zero watcher crashes (#733) but no dispatches fired; zero discarded text blocks (#734) but the narrator never double-drafted.
- Rule: verification requires (a) the fix's trigger condition observed firing AND (b) the bad outcome absent. Absence of the bad outcome alone proves nothing on a world/scene that can't produce the trigger. Leave status at `fixed` with a DRIVER note instead.
- Corollary: pick the verification world to match the repro (tropes → five_points/evropi; dispatch crash → five_points; double-draft → tool-heavy turns).

### "Needs restart: yes" for the server is usually just a pull (2026-06-07)
- uvicorn runs `--reload`: pulling the server tree triggers a WatchFiles reload that boots a fresh worker (verify via the `WatchFiles detected changes` line + a NEW `Started server process [pid]`). The reload also re-reads packs/corpora from the content tree.
- The inverse does NOT hold: a content-tree pull alone triggers nothing (WatchFiles watches the server tree only) — content-only fixes still need a server-tree touch/pull or manual bounce to re-cache.
- Bonus: every hot-reload is a free ui #350 regression test — watch for the client's silent re-bind (`session.auto_seated` + `slug_connect.replay`, zero `message_rejected_unbound`) and confirm an action narrates without a page reload.

### npc_pool[].invented_from is the identity-fork detector (2026-06-07)
- Post server #738, every culture-namer mint carries its source string. One curl of the snapshot shows what each pool NPC was minted FROM — instantly distinguishing real names ('Varra') from role labels ('Barkeep'), descriptive epithets ('The Heavy Man in Broadcloth' — forked a roster identity), and plural groups ('Green-Grocery Door Men' → one they/them individual). Check this field FIRST when prose and mechanics disagree about who someone is.

### Read the other lane's full notes BEFORE filing a "salvage" PR (2026-06-07)
- Filed #392 to restore #389's pakook cleanup over merged #391 — then found #391's notes said the pakook long stems were a DELIBERATE canon-style exemption ("rookarook reduplication is croak style"), not an oversight. What looked like a missed cleanup was an editorial call. Operator ruled: keep the longer dictionary; #392 closed unmerged.
- Rule: when two lanes touch the same content, a diff alone can't distinguish "missed" from "chose to keep." Read the peer's rationale notes first; if a real disagreement remains, flag it for Keith — conlang/flavor style calls are his to arbitrate, not a PR race to win.

### Corpus expansion: legacy machine-generated stem blocks ARE the funnel pathology (2026-06-07)
- The "Abinthe Mori*" reroll-clone artifact isn't only a corpus-size problem — prior expansion passes left single-stem blocks (lotharian Glo* was 37% of the file, yellow Bal*/Dor* similar) that funnel Markov chains even after the file passes the 1000-word threshold. When expanding a corpus, check `cut -c1-3 | sort | uniq -c | sort -rn | head` and enforce ~1.5% per 3-letter prefix; prune legacy blocks (max-min bigram-distinct survivors), don't just dilute around them.
- The conlang subagent handles this well in parallel (one per culture family), but instruct stem caps explicitly and have it report top-5 prefixes as evidence — and set-diff old vs new (`comm -23 <(git show HEAD:f | sort -u) <(sort -u f)`) to verify "kept all originals" claims; re-sort makes raw diff deletions misleading.
- `python -m sidequest.cli.namegen` takes culture DISPLAY names ("Red Martian"), not slugs — fails loud with the list if wrong.

### pf branch-protection hook evaluates the branch BEFORE the compound command runs (2026-06-07)
- `git checkout -b feat/x && git commit ...` in one Bash call gets BLOCKED on a protected branch — the PreToolUse hook sees `git commit` while HEAD is still develop. Split into two calls: checkout first, commit second.

### Seed/trope YAML: unquoted ": " inside a list-item string silently becomes a dict (2026-06-07)
- Delivery hints written as prose ("The pattern has a shape: one counter, one route...") parse as a one-key mapping and fail SeedTrope's string validation at LOAD, not at write. Hit 4× across 10 seed decks. Pre-flight before committing any authored deck: `grep -n '^    - [^"].*: ' <file>` and quote offenders.
- Always wiring-verify authored pack YAML through the real loader, not just yaml.safe_load: `uv run python -c "from sidequest.genre.loader import load_genre_pack; ..."` from sidequest-server. model_validate on the raw list misses nothing here, but the loader run also proves the file is where the loader looks (seed_tropes.yaml is a STANDALONE file at pack root or worlds/<world>/, NOT a pack.yaml key — the pack.yaml `- seed_tropes` line is just a section list entry).

### World currency lives in a world `inventory.yaml` `currency:` block — and the world file REPLACES the genre inventory WHOLESALE (2026-06-11, story 103-8)
- Currency is NOT a key on `items.yaml`. `WorldItemsCatalog` (server `genre/models/items.py`) is `extra="forbid"` and only accepts named_items/modifier_items/reliquaries/crimson_remnants/consumable_items/implants. Putting `currency:` there fails the load. (An AC that says "currency as items" is loose phrasing — the real surface is the currency block.)
- The currency pattern is the space_opera one: `worlds/<world>/inventory.yaml` with `currency: {name, denominations[], abbreviation, description, secondary}` (model `CurrencyConfig`, extra=forbid). `denominations` accepts a list or a dict; `secondary` accepts a list/dict/string. aureate_span is the reference.
- **THE TRAP:** `server/dispatch/inventory_resolve.resolve_inventory` does `world.inventory **replaces** the genre one — it is NOT merged.` So a currency-only world inventory.yaml silently wipes the genre's `item_catalog` + `starting_equipment` + `starting_gold`, leaving chargen with no loadouts and no gear. To add a world currency you MUST author the FULL inventory surface (currency + item_catalog + starting_equipment per class + starting_gold), world-flavored. Budget for it — "just add coins" is a full-inventory port.

### New `stocks.yaml` entries are NOT auto-wired into chargen — the `char_creation.yaml` stock step lists them separately (2026-06-11, 103-8)
- `stocks.yaml` defines the trait sets and is validated by the loader (every `granted_mutations` id must resolve against the genre `mutations.yaml` or the pack load fails loud — read the catalog FIRST: structure/ sense/ hybrid/ cognition/ pseudo_psychic/ exotic/). But authoring a stock does NOT make it selectable. The chargen stock-selection step is a separate `choices:` list in `worlds/<world>/char_creation.yaml` with `mechanical_effects.stock_id`. Author both, or the roster loads invisibly and a per-stock chargen smoke can only reach the wired few.
- The stock schema has no region/language field — Indigenous-language anchors (e.g. Penobscot `muwin` = bear) live in `description` prose only. The only check is a register read; there's no structured validation.

### Audit sibling stories before authoring — content lands across story lines and out-of-band (2026-06-11, 103-8)
- 103-8 scope listed "bestiary/creatures" but 103-6 had already authored a full `bestiary.yaml`. 103-8's faction-referencing openings depended on "103-7 (backlog)" but the factions were already named in `lore.yaml`'s `factions[]` from the 103-5 world core. Story scopes overlap and the sprint tracker lags the repo (deps marked backlog were merged out-of-band). `ls worlds/<world>/` + grep the existing files before authoring anything — do not trust the story's declared deps or scope boundaries.

### World trope `extends:` must be the SLUGIFIED NAME of a genre `tropes.yaml` trope — NOT a seed-trope id (2026-06-11, 103-8)
- `resolve_trope_inheritance` (server `genre/resolve.py`) builds the parent map from `genre_tropes` keyed by `_slugify(trope.name)`. The valid parents are the genre `tropes.yaml` entries ONLY (mutant_wasteland: `ruin-fever`, `mutation-tide`, `dead-signal`). The `seed_tropes.yaml` deck is a SEPARATE system (SeedTrope model, the trope-deck) and its ids are NOT valid `extends:` targets. A wrong parent fails loud: `GenreMissingParentError: trope 'X' extends 'y' which does not exist`. flickering_reach is the reference (`extends: dead-signal`). If you brief a sub-author to write world tropes, give them the slugified genre-trope names, not the seed-deck ids.

### Validating a `draft: true` world: `load_genre_pack` SKIPS it, so the pack-load smoke proves nothing (2026-06-11, 103-8)
- `_load_single_world` returns None at the `if config.draft:` check (loader.py ~1133) BEFORE loading any of the world's files — so a green `load_genre_pack` run never touched your draft world's stocks/inventory/tropes/openings/char_creation. To actually validate a draft world, force-load it: shim `loader._load_yaml` to flip `WorldConfig.draft` to False, load the mutation catalog + the 3 genre tropes, then call `_load_single_world(world_path, genre_tropes, root, mutations=muts)` directly. That exercises granted-mutation resolution, trope inheritance, inventory schema, and the openings `?`-in-first_turn_invitation contract. (Most worlds under active authoring are draft until the 103-9-style asset gate is met — this applies to nearly every new world.)

### Z-Image is an IMAGE LLM, not a reader of prose — author prompts as trained caption labels, LOOK DON'T TELL (2026-06-11, 103-9)
- **It renders nouns and trained style labels, not literary prose.** Keith's rules, learned the hard way on seaboard_of_saints saint portraits:
  - **No analogies / metaphors.** "candle-amber" → a literal CANDLE in every frame. "sea-foam highlight" / "driftwood" / "harbor" → a lighthouse seascape (lighthouse = the most-trained seashore object; the cliche engine defaults to it). Every concrete noun you name gets rendered as that object — only name things you WANT visible.
  - **No inner-thought / mood narration.** "Quiet wonder over dread", "warm and habitable rather than desolate" — invisible to a diffuser, pure waste. LOOK DON'T TELL: describe only what is literally in the frame.
  - **No "in the manner of" / subtle cultural cues.** "WPA post-office mural warmth" is too subtle — it doesn't resolve. 
  - **DO use real trained style labels.** Z-Image was trained on captions like "Italian Renaissance saint portrait", "Byzantine icon", "Sulpician" (French Catholic holy-card devotional style — exactly the saint-card register), "gold-leaf gilt ground", "egg tempera", "devotional" (confirmed works). These produce the centered devotional figure directly. Art-school subject-matter painters bleed their subjects: "Fitz Henry Lane" / "Hudson River School coastal" drag in marine/coastal scenery → drop them from PORTRAIT suffixes.
- **Architecture for separating subject registers:** `render_common.py:92` — a world may declare `portrait_positive_suffix` (portraits only); POIs/illustrations always use the shared `positive_suffix`. So put devotional-icon labels in `portrait_positive_suffix` and leave maritime/lighthouse nouns in `positive_suffix` where POI landscapes legitimately want them. Don't jam portrait framing into the shared suffix.
- **`register:` and `type:` (saint_icon/npc_major) in portrait_manifest are NOT fed to the prompt** by `generate_portrait_images.py:compose_prompt` (it uses name+role+appearance+culture_aesthetic+element_visual only). Devotional framing therefore has to come from `portrait_positive_suffix`, not the per-entry `register`.

### Beating the devotional-icon Christ-face prior + single-portrait iteration (2026-06-11, 103-9)
- **`Byzantine icon` + `saint` = a Christ-Pantocrator face prior so strong it overrides per-entry face description** — every saint came back the same brown-bearded long-haired Christ. Fix: in `portrait_positive_suffix` use `devotional holy card, gold-leaf gilt ground` and DROP `Byzantine icon` and the word `saint`. The gilt gold-ground look survives on the explicit `gold-leaf gilt ground` token alone; the face prior loosens.
- **Negations don't work and backfire in Z-Image.** `no beard` / `clean-shaven` (as a bare adjective) still rendered a full beard (the model sees "beard"). What worked: aggressive POSITIVE smooth-face tokens leading the description — "a long smooth bare chin and hollow clean-shaven cheeks, only a thin dark mustache on the upper lip." That finally produced a beardless Poe.
- **Composition: lead with the frontal figure, cut the backdrop prose.** "...behind him the Acushnet rides at anchor / Ktaadn's tableland stands in cloud" rendered scene-scale (tiny figure in a landscape). Removing it and leading with the figure pulled them back to portraits. Scene-scale subjects (a figure "tall as a water tower", "the Rock at the tideline") need rewriting to a held attribute + frontal figure or they stay landscapes.
- **`--shard k/n` is round-robin over ALPHABETICALLY-SORTED slugs (index % n == k), NOT manifest order.** So `--shard k/31` (k = the portrait's alphabetical index among the world's portrait slugs) renders exactly ONE portrait in ~3 min — the way to iterate a single face cheaply instead of an 87-min full run. Verify which rendered by mtime, not by assuming order.
- Render-cost reality: full 31-portrait `--force` run = ~87 min serial on the MLX daemon (~168s/image). Iterate single faces with `--shard k/31`, do the full run only once the recipe is locked.

### This machine is slabgorb, not keithavery — pingpong + screenshot paths in the sidecar are stale (2026-06-11)
- Sidecar references `/Users/keithavery/Projects/...` for the ping-pong file and screenshots. On THIS machine those don't exist. Real paths: ping-pong = `/Users/slabgorb/Projects/sq-playtest-pingpong.md` (+ `sq-playtest-pingpong-archive-*.md`), screenshots dir under `/Users/slabgorb/Projects/`. Check the slabgorb path first.
- There are at least THREE content checkouts: oq-1, oq-2, oq-3. The running SERVER's content comes from `SIDEQUEST_GENRE_PACKS` (was pointed at `oq-3/sidequest-content`, since repointed to oq-1). Always check which checkout the server reads (`ps eww <pid> | grep GENRE_PACKS`) before assuming your edits affect the running game — editing oq-1 does nothing if the server reads oq-3.

### Epic-74 / ADR-140 genre→world relocation left content gaps — distinguish over-deletion (restore) from clean-relocation (stale test) (2026-06-14)
- Both Epic 74 (weather/calendar/demographics → world-tier) and ADR-140 (themes/cast/catalog → world-tier) moved flavor OFF the genre root. Some moves were incomplete or over-zealous, leaving red tests that look like "missing content."
- **Weather (real content gap, GM-fixable):** #447 ("remove dead genre-tier files") deleted `genre_packs/tea_and_murder/weather.yaml` but the relocation was never finished — `glenross` had world-tier `calendar.yaml`+`demographics.yaml` but NO `weather.yaml` peer anywhere. Story 74-1's test (`test_genre_flavor_world_tier`) deliberately KEEPS the pack-tier weather.yaml as the canonical schema-valid FIXTURE (`load_pack_weather` unit test + 3 other modules copy it into temp world dirs). Fix = author `worlds/glenross/weather.yaml` (production home) + restore the pack-root fixture #447 over-deleted. Recover deleted content with `git show <removal_commit>^:<path>`.
- **Themes (NOT a content gap — stale test → dev):** #453 cleanly RELOCATED `caverns_and_claudes/themes/` → `worlds/beneath_sunden/themes/` per ADR-140; content EXISTS at world-tier. The failing dungeon tests (`tests/dungeon/test_session_*`, `test_region_projection_*`) pass the genre `pack_dir` to the theme loader → `ThemePaletteMissingError`. Authoring a genre-tier `themes/` would RE-VIOLATE ADR-140 — do not. Route to dev.
- **Chargen None-choices (NOT a content gap — code/test → dev):** `tests/integration/test_cc_chargen_e2e.py` + `test_class_signature_wiring` yield `target_class 'Warrior' not in choices: [None,...]`. Content is present (`caverns_and_claudes` genre root ships classes/char_creation/archetypes + full WWN content from #429/#444). The None-choices are the post-WWN-port chargen flow vs live Postgres, not a missing YAML. Route to dev.
- **Heuristic:** before authoring to "fix" a missing-content test, `git log --oneline --all -- <path>` to see if it was *relocated* (→ content is fine, test is stale → dev) vs *deleted* (→ maybe over-deletion → restore/author). Re-authoring relocated flavor at the genre tier re-commits the exact Crunch-in-World violation the relocation fixed.

### De-nativizing WN combat defs (108-3): encounter_beat_choices is a MIXED pool — strip combat entries, keep DIAL entries (2026-06-15)
- **The trap:** the story said "strip the now-orphaned encounter_beat_choices from each WN class." Read literally that means empty the field — WRONG. `encounter_beat_choices` mixes combat beats (strike/brace/committed_blow/break_contact) AND chase/negotiation DIAL beats (sprint/argue/persuade/dash/vanish...). ADR-143 de-nativizes WN *combat only* — the chase/negotiation DIAL defs stay native. So after stripping the combat beats from rules.yaml, the DIAL beats still exist in the pool. Only the **combat-beat** entries in each class are orphaned. Remove those; KEEP the DIAL entries or you break chase/negotiation for that class.
- **Why it fails loud if you leave stale combat refs:** the loader (`loader.py` `_validate_class_filter_refs`, ~723) validates every `encounter_beat_choices` id against the pool of **all** confrontations' beats. 108-7's asymmetry (line ~753): a WN class with an **empty** list is skipped, but a WN class still carrying **stale** combat-beat refs against the now-empty combat pool **raises PackError**. Empty = OK, stale = loud fail. That's the signal to strip the combat entries specifically, not to empty the field.
- **The two loader gates 108-7 made WN-conditional** (both keyed on `_is_without_number(rules.ruleset)`): `_validate_confrontation_beats` (~702) lets a `category=combat`/`win_condition=hp_depletion` def author ZERO beats and fires the `wn_beat_optional` watcher span; `_validate_class_filter_refs` (~723) lets a WN class carry an empty `encounter_beat_choices`. Non-combat (dial) defs and native packs still fail loud on zero beats.
- **Canonical de-nativized WN combat def shape:** `win_condition: hp_depletion` + `opponent_default_stats` (ALL six abilities + hp/armor_class/dexterity — the WWN defender-save path KeyErrors on a missing ability) + `opponent_damage` DamageSpec (ADR-139 Inv-3: a toothless Other is a loud defect — keep the damage lever). Strip: `resolution_mode: beat_selection`, the whole `beats:` list, `edge_config`, momentum/dial metrics. cast_spell is a beat → it goes too (casting becomes the WN `cast` action supplied by wn_round.py).
- **Name-collision caution across packs:** `yield` is a COMBAT beat in elemental_harmony (the break-contact equiv → strip) but a DIAL/social beat in heavy_metal (→ keep). Pack-isolated, so per-pack removal lists are safe, but never write a cross-pack sed. Build the removal list from each pack's own combat-def beat ids.
- **Verify-target worlds carry no world-tier rules.yaml** — crunch loads from the genre tier for all WWN worlds (beneath_sunden, evropi, long_foundry, barsoom, burning_peace). Every edit is in genre `rules.yaml`; the worlds are where you playtest, not separate edit sites.
- **Still-pending under ADR-143 doctrine:** CWN (road_warrior, neon_dystopia), AWN (mutant_wasteland), SWN (space_opera) packs STILL have `resolution_mode: beat_selection` combat defs — they belong to their own ruleset epics (114-x), explicitly out of 108-3's WWN-only scope. Same de-nativization pattern will apply when those land.
- **Signature abilities (ADR-095) reference combat beats in PROSE** ("on a successful strike beat", "an incoming strike that would reduce your HP to a guard beat"). These are NOT loader-validated (no structured `trigger_beat:` field — pure description text) so they don't break load, but they're now stale flavor pointing at vanished beats, AND how those abilities re-hook to the WN round (attack/move/item-use/cast) is a mechanical question = Keith's crunch lane + partly server-side (108-7/108-8). Flag as a finding; don't silently reword class crunch in a content-strip story.

### Dungeon theme creature_table / loot_table are DECORATIVE (beneath_sunden, 2026-06-24)
- A `themes/*.yaml` `creature_table`/`loot_table` ref does NOT drive runtime
  encounters/loot. Plan 6 (ref resolution) was deferred and never wired. The
  existing 5 themes reference creatures that exist NOWHERE (`blind_cave_eel`,
  `crypt_warden`, `ossuary_crawler`…) and loot that resolves nowhere
  (`silt_pearl`, `grave_silver`) — and nothing breaks, because they're unread.
- **Real runtime creatures** come from the region `look` (necropolis / sunken /
  delvehold) → `cookbook/affinities.yaml` `look_race_affinity` → cookbook RACE
  faction (`cookbook/races/*.yaml`) → world `bestiary.yaml`. **Real loot** comes
  from the cookbook `loot_bias` + SRD inventory. THEME and LOOK are two parallel
  axes: theme = interior generator + narrator register + depth eligibility +
  quest_template; look = creatures. Theme does not pick the race.
- So when authoring a new theme, the creature_table is honesty/future-proofing
  only — use REAL `bestiary.yaml` ids anyway (`hold_skeleton`, `the_seep`,
  `gnaw_swarm`, `wight`, `grave_ghoul`, `otyugh`, …) and put signature
  big-bads in `set_pieces` (prose + a REAL pack trope), not as wandering rows.
- `bestiary.yaml` is genre-truth-gated: `world_register.yaml` allow_types
  [Undead, Aberration, Ooze, Monstrosity, Construct, Giant, Humanoid, Beast],
  deny [Celestial, Fey]; the roster draws from 5 cookbook RACE factions
  (undead/ooze/aberration/goblinoid/vermin) + Humanoid-as-delved-too-deep. The
  `undead` faction filter already grabs `{type: Construct, name_glob: '*animated*'}`
  — animated constructs read as "the Restless" without a new faction.

### Content is VALIDATED, not unit-tested — and a theme's generator must have a cookbook look (beneath_sunden sunless_temple, 2026-06-24)
- **Doctrine (Keith, emphatic):** "WE DO NOT UNIT TEST CONTENT, we VALIDATE IT." The authority on content correctness is the content validator (`sidequest validate` / `just`-driven pack validation), NOT server unit tests. Server materialization tests (`tests/dungeon/test_session_*`, `test_region_projection_*`) may go red when content changes, but they are exercising the engine, not gating the content — fix the content, confirm with the VALIDATOR, and the engine tests follow. Never add a server unit test to "lock" a content invariant.
- **The look↔theme seam (why a theme can be unmaterializable):** a `themes/*.yaml` `interior.algorithm` (built→roomcorridor, organic→cellular, labyrinthine→depthfirst, structured→prim) must be matched by a cookbook LOOK whose `generator_binding` == that algorithm (`_resolve_look_for_theme`, materializer.py). beneath_sunden's `cookbook/looks.yaml` ships only necropolis(depthfirst)/sunken(cellular)/delvehold(prim) — **no roomcorridor look at all** — AND `KNOWN_GENERATOR_BINDINGS` (server `loader.py`) is `{cellular,depthfirst,prim,braid}`, so even authoring a roomcorridor look would fail the cookbook load until a server code change. Net: **"built"/roomcorridor themes are unsupported in beneath_sunden.**
- **Trigger that exposed it (story 158-19):** flattening every theme to `depth_band {0,null}` (random-dungeon eligibility) made `sunless_temple` (built/roomcorridor) shallow-eligible → materializer fail-loud at the look seam, seed-dependently red across session/projection tests. The `sunless_temple.yaml` comment mis-diagnosed it as a roomcorridor <5×5 *dim* issue; the real failure fires earlier, at look resolution, dim-independent.
- **Fix taken (content-only, no code):** re-themed `sunless_temple` built→**labyrinthine** (roomcorridor→depthfirst, params {}, braid_ratio 0.3), binding the existing **necropolis** look. Decisive reason = FACTION, not just map shape: cellular→sunken is ooze-heavy + "drowned/wet" register (wrong for a dry colonnaded temple); depthfirst→necropolis is undead-heavy + "mausoleum-formal, straight lines" (right — matches the temple's own `temple_acolyte_shade`/`altar_horror` creature_table). A theme's look choice picks its monsters; choose the look whose faction fits the fiction.
