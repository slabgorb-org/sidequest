
### Exit a running game to the lobby with the in-game 'Leave' button — NOT by navigating to root (2026-06-17, from Keith)
- During a playtest, to drop the current game and pick a new world/PC, click the **'Leave'** control in the game UI; it returns you to the ConnectScreen lobby cleanly. Navigating the browser to `/` does NOT work — the app restores the last active solo session from per-origin localStorage and redirects straight back into `/solo/{slug}`.
- Caveat for a CRASHED board: when the GameBoard has hit its error boundary ("Something went wrong in Game" + [Try again]), there is no 'Leave' button to click, and root-navigation re-loads into the crash. Escape hatch in that case: switch to a different host alias (`player2.local:5173` etc.) — separate origin = empty localStorage = fresh lobby, no active-session redirect. Use 'Leave' for the normal case; host-switch only when the board is already dead.

### Portal-fairytale (wry_whimsy) player-pickers = REAL people from the period the book was written, NOT in-world factions (2026-06-12, from Keith)
- wry_whimsy is PORTAL fantasy: the players are outsiders who fall INTO the fairytale (Dorothy, Alice, Gulliver), not natives. Authoring player_pickers around in-world cultures/factions (oz: munchkin/winkie/quadling/gillikin/emerald; wonderland: card_folk/chesspieces/creature_folk; gulliver: lilliputians/brobdingnagians/laputans/houyhnhnms) is WRONG — Keith: "they are choosing in-world factions which does not work for whimsy."
- Correct model: the picker is WHO YOU WERE in the real world before the portal — an ordinary person from the AUTHOR'S OWN ERA. Period = publication date of the source work: oz=1900 America (Baum), wonderland=1865 Victorian England (Carroll), gulliver=1726 Georgian Britain (Swift). Occupations/dress/names are real-period.
- BUT still render in the WORLD's illustration style (Denslow plate for Oz, Tenniel engraving for Wonderland, etc.) — a real 1900 Kansas farmwife DRAWN as a Denslow plate. World-visual consistency holds; only the SUBJECT changes from native-faction to period-traveler.
- Mechanics: one new world-level real-period culture file per world (kansas_1900 / victorian_english / georgian_british), all pickers point at it; reuse the existing system archetype values; leave NPC entries + in-world culture files untouched (those natives are correct). Same StyleCatalog culture-gate rule applies (file stem must match picker culture slug).
- The in-world-faction model IS correct for NON-portal genres (heavy_metal, spaghetti_western, etc.) where the player IS a native — this exception is portal-fairytale-specific.

### Portrait `--dry-run` does NOT validate the StyleCatalog culture gate — check `cultures/<slug>.yaml` exists per picker culture (2026-06-12)
- `generate_portrait_images.py --dry-run` composes prompts and confirms `backdrop_poi` refs resolve, but it SKIPS catalog composition (render_common.py ~L799: "catalog_compose validation runs AFTER the skip gate"). So a dry-run passes 0-errors while the real render dies with `CatalogMissError: StyleCatalog.culture has no entry for <genre>/<world>/<culture>`. Cost: 18/18 portraits failed on beneath_sunden before I caught it.
- The real gate: the daemon's `StyleCatalog.load` (daemon catalogs.py ~L358) populates culture style ONLY from `genre_packs/<genre>/worlds/<world>/cultures/*.yaml` — keyed by the FILE STEM. Pack-level `cultures.yaml` is NOT read. Every picker `culture:` slug must have a world-level `cultures/<slug>.yaml`. visual_tokens may be empty/absent (defaults to "") — the file just has to EXIST with the matching stem.
- Validate BEFORE rendering with a YAML check (not BSD `sed` — `\s` is literal `s` on macOS and silently mangles): load each portrait_manifest.yaml, for every `type: player_picker` collect `culture`, assert `cultures/<culture>.yaml` exists. Three failure shapes seen across a 21-world fan-out: (1) no world cultures at all (beneath_sunden — fix: author the culture file); (2) wrong slug guess vs real filename (the_circuit: picker `bosozoku` vs file `bōsōzoku.yaml`, `cafe_racers` vs `café_racers.yaml`, `one_percenters` vs `1%ers.yaml` — fix: remap picker `culture:` to the real stem); (3) name-only cultures filed as `<x>_names.yaml` (glenross — fix: remap to the `_names` stem).
- art-director agents reliably guess plausible-but-wrong culture slugs from prose; always cross-check their `culture:` against the actual `cultures/` filenames.

### Report only observed mechanics — never extrapolate the system (2026-06-07, from Keith)
- When filing playtest bugs: quote logs, quote UI, quote authored content text. Do NOT predict adjacent behavior the system was never observed to do (e.g. claiming the attention-based disposition model "predicts NPCs drift cold when ignored" — whether relationships degrade at all is unknown). Keith: "stop making shit up."
- Quoting authored content verbatim (ability text, lore) is always fine — the line is between citing what exists and inventing system dynamics.

### A clean log can be a vacuous pass — confirm the subsystem ENGAGED before flipping verified (2026-06-07)
- blackthorn fix-validation: zero `entity_sync.project_failed` WARNs looked like a #736 pass, but blackthorn seeds ZERO tropes (`active_tropes: []`) — the merge path never ran. Same session: zero watcher crashes (#733) but no dispatches fired; zero discarded text blocks (#734) but the narrator never double-drafted.
- Rule: verification requires (a) the fix's trigger condition observed firing AND (b) the bad outcome absent. Absence of the bad outcome alone proves nothing on a world/scene that can't produce the trigger. Leave status at `fixed` with a DRIVER note instead.
- Corollary: pick the verification world to match the repro (tropes → five_points/evropi; dispatch crash → five_points; double-draft → tool-heavy turns).

### "Needs restart: yes" for the server is usually just a pull (2026-06-07)
- uvicorn runs `--reload`: pulling the server tree triggers a WatchFiles reload that boots a fresh worker (verify via the `WatchFiles detected changes` line + a NEW `Started server process [pid]`). The reload also re-reads packs/corpora from the content tree.
- The inverse does NOT hold: a content-tree pull alone triggers nothing (WatchFiles watches the server tree only) — content-only fixes still need a server-tree touch/pull or manual bounce to re-cache.
- Bonus: every hot-reload is a free ui #350 regression test — watch for the client's silent re-bind (`session.auto_seated` + `slug_connect.replay`, zero `message_rejected_unbound`) and confirm an action narrates without a page reload.

### npc_pool[].invented_from is the identity-fork detector (2026-06-07)
- Post server #738, every culture-namer mint carries its source string. One curl of the snapshot shows what each pool NPC was minted FROM — instantly distinguishing real names ('Varra') from role labels ('Barkeep'), descriptive epithets ('The Heavy Man in Broadcloth' — forked a roster identity), and plural groups ('Green-Grocery Door Men' → one they/them individual). Check this field FIRST when prose and mechanics disagree about who someone is.

### Read the other lane's full notes BEFORE filing a "salvage" PR (2026-06-07)
- Filed #392 to restore #389's pakook cleanup over merged #391 — then found #391's notes said the pakook long stems were a DELIBERATE canon-style exemption ("rookarook reduplication is croak style"), not an oversight. What looked like a missed cleanup was an editorial call. Operator ruled: keep the longer dictionary; #392 closed unmerged.
- Rule: when two lanes touch the same content, a diff alone can't distinguish "missed" from "chose to keep." Read the peer's rationale notes first; if a real disagreement remains, flag it for Keith — conlang/flavor style calls are his to arbitrate, not a PR race to win.

### Corpus expansion: legacy machine-generated stem blocks ARE the funnel pathology (2026-06-07)
- The "Abinthe Mori*" reroll-clone artifact isn't only a corpus-size problem — prior expansion passes left single-stem blocks (lotharian Glo* was 37% of the file, yellow Bal*/Dor* similar) that funnel Markov chains even after the file passes the 1000-word threshold. When expanding a corpus, check `cut -c1-3 | sort | uniq -c | sort -rn | head` and enforce ~1.5% per 3-letter prefix; prune legacy blocks (max-min bigram-distinct survivors), don't just dilute around them.
- The conlang subagent handles this well in parallel (one per culture family), but instruct stem caps explicitly and have it report top-5 prefixes as evidence — and set-diff old vs new (`comm -23 <(git show HEAD:f | sort -u) <(sort -u f)`) to verify "kept all originals" claims; re-sort makes raw diff deletions misleading.
- `python -m sidequest.cli.namegen` takes culture DISPLAY names ("Red Martian"), not slugs — fails loud with the list if wrong.

### pf branch-protection hook evaluates the branch BEFORE the compound command runs (2026-06-07)
- `git checkout -b feat/x && git commit ...` in one Bash call gets BLOCKED on a protected branch — the PreToolUse hook sees `git commit` while HEAD is still develop. Split into two calls: checkout first, commit second.

### Seed/trope YAML: unquoted ": " inside a list-item string silently becomes a dict (2026-06-07)
- Delivery hints written as prose ("The pattern has a shape: one counter, one route...") parse as a one-key mapping and fail SeedTrope's string validation at LOAD, not at write. Hit 4× across 10 seed decks. Pre-flight before committing any authored deck: `grep -n '^    - [^"].*: ' <file>` and quote offenders.
- Always wiring-verify authored pack YAML through the real loader, not just yaml.safe_load: `uv run python -c "from sidequest.genre.loader import load_genre_pack; ..."` from sidequest-server. model_validate on the raw list misses nothing here, but the loader run also proves the file is where the loader looks (seed_tropes.yaml is a STANDALONE file at pack root or worlds/<world>/, NOT a pack.yaml key — the pack.yaml `- seed_tropes` line is just a section list entry).

### World currency lives in a world `inventory.yaml` `currency:` block — and the world file REPLACES the genre inventory WHOLESALE (2026-06-11, story 103-8)
- Currency is NOT a key on `items.yaml`. `WorldItemsCatalog` (server `genre/models/items.py`) is `extra="forbid"` and only accepts named_items/modifier_items/reliquaries/crimson_remnants/consumable_items/implants. Putting `currency:` there fails the load. (An AC that says "currency as items" is loose phrasing — the real surface is the currency block.)
- The currency pattern is the space_opera one: `worlds/<world>/inventory.yaml` with `currency: {name, denominations[], abbreviation, description, secondary}` (model `CurrencyConfig`, extra=forbid). `denominations` accepts a list or a dict; `secondary` accepts a list/dict/string. aureate_span is the reference.
- **THE TRAP:** `server/dispatch/inventory_resolve.resolve_inventory` does `world.inventory **replaces** the genre one — it is NOT merged.` So a currency-only world inventory.yaml silently wipes the genre's `item_catalog` + `starting_equipment` + `starting_gold`, leaving chargen with no loadouts and no gear. To add a world currency you MUST author the FULL inventory surface (currency + item_catalog + starting_equipment per class + starting_gold), world-flavored. Budget for it — "just add coins" is a full-inventory port.

### New `stocks.yaml` entries are NOT auto-wired into chargen — the `char_creation.yaml` stock step lists them separately (2026-06-11, 103-8)
- `stocks.yaml` defines the trait sets and is validated by the loader (every `granted_mutations` id must resolve against the genre `mutations.yaml` or the pack load fails loud — read the catalog FIRST: structure/ sense/ hybrid/ cognition/ pseudo_psychic/ exotic/). But authoring a stock does NOT make it selectable. The chargen stock-selection step is a separate `choices:` list in `worlds/<world>/char_creation.yaml` with `mechanical_effects.stock_id`. Author both, or the roster loads invisibly and a per-stock chargen smoke can only reach the wired few.
- The stock schema has no region/language field — Indigenous-language anchors (e.g. Penobscot `muwin` = bear) live in `description` prose only. The only check is a register read; there's no structured validation.

### Audit sibling stories before authoring — content lands across story lines and out-of-band (2026-06-11, 103-8)
- 103-8 scope listed "bestiary/creatures" but 103-6 had already authored a full `bestiary.yaml`. 103-8's faction-referencing openings depended on "103-7 (backlog)" but the factions were already named in `lore.yaml`'s `factions[]` from the 103-5 world core. Story scopes overlap and the sprint tracker lags the repo (deps marked backlog were merged out-of-band). `ls worlds/<world>/` + grep the existing files before authoring anything — do not trust the story's declared deps or scope boundaries.

### World trope `extends:` must be the SLUGIFIED NAME of a genre `tropes.yaml` trope — NOT a seed-trope id (2026-06-11, 103-8)
- `resolve_trope_inheritance` (server `genre/resolve.py`) builds the parent map from `genre_tropes` keyed by `_slugify(trope.name)`. The valid parents are the genre `tropes.yaml` entries ONLY (mutant_wasteland: `ruin-fever`, `mutation-tide`, `dead-signal`). The `seed_tropes.yaml` deck is a SEPARATE system (SeedTrope model, the trope-deck) and its ids are NOT valid `extends:` targets. A wrong parent fails loud: `GenreMissingParentError: trope 'X' extends 'y' which does not exist`. flickering_reach is the reference (`extends: dead-signal`). If you brief a sub-author to write world tropes, give them the slugified genre-trope names, not the seed-deck ids.

### Validating a `draft: true` world: `load_genre_pack` SKIPS it, so the pack-load smoke proves nothing (2026-06-11, 103-8)
- `_load_single_world` returns None at the `if config.draft:` check (loader.py ~1133) BEFORE loading any of the world's files — so a green `load_genre_pack` run never touched your draft world's stocks/inventory/tropes/openings/char_creation. To actually validate a draft world, force-load it: shim `loader._load_yaml` to flip `WorldConfig.draft` to False, load the mutation catalog + the 3 genre tropes, then call `_load_single_world(world_path, genre_tropes, root, mutations=muts)` directly. That exercises granted-mutation resolution, trope inheritance, inventory schema, and the openings `?`-in-first_turn_invitation contract. (Most worlds under active authoring are draft until the 103-9-style asset gate is met — this applies to nearly every new world.)

### Z-Image is an IMAGE LLM, not a reader of prose — author prompts as trained caption labels, LOOK DON'T TELL (2026-06-11, 103-9)
- **It renders nouns and trained style labels, not literary prose.** Keith's rules, learned the hard way on seaboard_of_saints saint portraits:
  - **No analogies / metaphors.** "candle-amber" → a literal CANDLE in every frame. "sea-foam highlight" / "driftwood" / "harbor" → a lighthouse seascape (lighthouse = the most-trained seashore object; the cliche engine defaults to it). Every concrete noun you name gets rendered as that object — only name things you WANT visible.
  - **No inner-thought / mood narration.** "Quiet wonder over dread", "warm and habitable rather than desolate" — invisible to a diffuser, pure waste. LOOK DON'T TELL: describe only what is literally in the frame.
  - **No "in the manner of" / subtle cultural cues.** "WPA post-office mural warmth" is too subtle — it doesn't resolve. 
  - **DO use real trained style labels.** Z-Image was trained on captions like "Italian Renaissance saint portrait", "Byzantine icon", "Sulpician" (French Catholic holy-card devotional style — exactly the saint-card register), "gold-leaf gilt ground", "egg tempera", "devotional" (confirmed works). These produce the centered devotional figure directly. Art-school subject-matter painters bleed their subjects: "Fitz Henry Lane" / "Hudson River School coastal" drag in marine/coastal scenery → drop them from PORTRAIT suffixes.
- **Architecture for separating subject registers:** `render_common.py:92` — a world may declare `portrait_positive_suffix` (portraits only); POIs/illustrations always use the shared `positive_suffix`. So put devotional-icon labels in `portrait_positive_suffix` and leave maritime/lighthouse nouns in `positive_suffix` where POI landscapes legitimately want them. Don't jam portrait framing into the shared suffix.
- **`register:` and `type:` (saint_icon/npc_major) in portrait_manifest are NOT fed to the prompt** by `generate_portrait_images.py:compose_prompt` (it uses name+role+appearance+culture_aesthetic+element_visual only). Devotional framing therefore has to come from `portrait_positive_suffix`, not the per-entry `register`.

### Beating the devotional-icon Christ-face prior + single-portrait iteration (2026-06-11, 103-9)
- **`Byzantine icon` + `saint` = a Christ-Pantocrator face prior so strong it overrides per-entry face description** — every saint came back the same brown-bearded long-haired Christ. Fix: in `portrait_positive_suffix` use `devotional holy card, gold-leaf gilt ground` and DROP `Byzantine icon` and the word `saint`. The gilt gold-ground look survives on the explicit `gold-leaf gilt ground` token alone; the face prior loosens.
- **Negations don't work and backfire in Z-Image.** `no beard` / `clean-shaven` (as a bare adjective) still rendered a full beard (the model sees "beard"). What worked: aggressive POSITIVE smooth-face tokens leading the description — "a long smooth bare chin and hollow clean-shaven cheeks, only a thin dark mustache on the upper lip." That finally produced a beardless Poe.
- **Composition: lead with the frontal figure, cut the backdrop prose.** "...behind him the Acushnet rides at anchor / Ktaadn's tableland stands in cloud" rendered scene-scale (tiny figure in a landscape). Removing it and leading with the figure pulled them back to portraits. Scene-scale subjects (a figure "tall as a water tower", "the Rock at the tideline") need rewriting to a held attribute + frontal figure or they stay landscapes.
- **`--shard k/n` is round-robin over ALPHABETICALLY-SORTED slugs (index % n == k), NOT manifest order.** So `--shard k/31` (k = the portrait's alphabetical index among the world's portrait slugs) renders exactly ONE portrait in ~3 min — the way to iterate a single face cheaply instead of an 87-min full run. Verify which rendered by mtime, not by assuming order.
- Render-cost reality: full 31-portrait `--force` run = ~87 min serial on the MLX daemon (~168s/image). Iterate single faces with `--shard k/31`, do the full run only once the recipe is locked.

### This machine is slabgorb, not keithavery — pingpong + screenshot paths in the sidecar are stale (2026-06-11)
- Sidecar references `/Users/keithavery/Projects/...` for the ping-pong file and screenshots. On THIS machine those don't exist. Real paths: ping-pong = `/Users/slabgorb/Projects/sq-playtest-pingpong.md` (+ `sq-playtest-pingpong-archive-*.md`), screenshots dir under `/Users/slabgorb/Projects/`. Check the slabgorb path first.
- There are at least THREE content checkouts: oq-1, oq-2, oq-3. The running SERVER's content comes from `SIDEQUEST_GENRE_PACKS` (was pointed at `oq-3/sidequest-content`, since repointed to oq-1). Always check which checkout the server reads (`ps eww <pid> | grep GENRE_PACKS`) before assuming your edits affect the running game — editing oq-1 does nothing if the server reads oq-3.

### Epic-74 / ADR-140 genre→world relocation left content gaps — distinguish over-deletion (restore) from clean-relocation (stale test) (2026-06-14)
- Both Epic 74 (weather/calendar/demographics → world-tier) and ADR-140 (themes/cast/catalog → world-tier) moved flavor OFF the genre root. Some moves were incomplete or over-zealous, leaving red tests that look like "missing content."
- **Weather (real content gap, GM-fixable):** #447 ("remove dead genre-tier files") deleted `genre_packs/tea_and_murder/weather.yaml` but the relocation was never finished — `glenross` had world-tier `calendar.yaml`+`demographics.yaml` but NO `weather.yaml` peer anywhere. Story 74-1's test (`test_genre_flavor_world_tier`) deliberately KEEPS the pack-tier weather.yaml as the canonical schema-valid FIXTURE (`load_pack_weather` unit test + 3 other modules copy it into temp world dirs). Fix = author `worlds/glenross/weather.yaml` (production home) + restore the pack-root fixture #447 over-deleted. Recover deleted content with `git show <removal_commit>^:<path>`.
- **Themes (NOT a content gap — stale test → dev):** #453 cleanly RELOCATED `caverns_and_claudes/themes/` → `worlds/beneath_sunden/themes/` per ADR-140; content EXISTS at world-tier. The failing dungeon tests (`tests/dungeon/test_session_*`, `test_region_projection_*`) pass the genre `pack_dir` to the theme loader → `ThemePaletteMissingError`. Authoring a genre-tier `themes/` would RE-VIOLATE ADR-140 — do not. Route to dev.
- **Chargen None-choices (NOT a content gap — code/test → dev):** `tests/integration/test_cc_chargen_e2e.py` + `test_class_signature_wiring` yield `target_class 'Warrior' not in choices: [None,...]`. Content is present (`caverns_and_claudes` genre root ships classes/char_creation/archetypes + full WWN content from #429/#444). The None-choices are the post-WWN-port chargen flow vs live Postgres, not a missing YAML. Route to dev.
- **Heuristic:** before authoring to "fix" a missing-content test, `git log --oneline --all -- <path>` to see if it was *relocated* (→ content is fine, test is stale → dev) vs *deleted* (→ maybe over-deletion → restore/author). Re-authoring relocated flavor at the genre tier re-commits the exact Crunch-in-World violation the relocation fixed.

### De-nativizing WN combat defs (108-3): encounter_beat_choices is a MIXED pool — strip combat entries, keep DIAL entries (2026-06-15)
- **The trap:** the story said "strip the now-orphaned encounter_beat_choices from each WN class." Read literally that means empty the field — WRONG. `encounter_beat_choices` mixes combat beats (strike/brace/committed_blow/break_contact) AND chase/negotiation DIAL beats (sprint/argue/persuade/dash/vanish...). ADR-143 de-nativizes WN *combat only* — the chase/negotiation DIAL defs stay native. So after stripping the combat beats from rules.yaml, the DIAL beats still exist in the pool. Only the **combat-beat** entries in each class are orphaned. Remove those; KEEP the DIAL entries or you break chase/negotiation for that class.
- **Why it fails loud if you leave stale combat refs:** the loader (`loader.py` `_validate_class_filter_refs`, ~723) validates every `encounter_beat_choices` id against the pool of **all** confrontations' beats. 108-7's asymmetry (line ~753): a WN class with an **empty** list is skipped, but a WN class still carrying **stale** combat-beat refs against the now-empty combat pool **raises PackError**. Empty = OK, stale = loud fail. That's the signal to strip the combat entries specifically, not to empty the field.
- **The two loader gates 108-7 made WN-conditional** (both keyed on `_is_without_number(rules.ruleset)`): `_validate_confrontation_beats` (~702) lets a `category=combat`/`win_condition=hp_depletion` def author ZERO beats and fires the `wn_beat_optional` watcher span; `_validate_class_filter_refs` (~723) lets a WN class carry an empty `encounter_beat_choices`. Non-combat (dial) defs and native packs still fail loud on zero beats.
- **Canonical de-nativized WN combat def shape:** `win_condition: hp_depletion` + `opponent_default_stats` (ALL six abilities + hp/armor_class/dexterity — the WWN defender-save path KeyErrors on a missing ability) + `opponent_damage` DamageSpec (ADR-139 Inv-3: a toothless Other is a loud defect — keep the damage lever). Strip: `resolution_mode: beat_selection`, the whole `beats:` list, `edge_config`, momentum/dial metrics. cast_spell is a beat → it goes too (casting becomes the WN `cast` action supplied by wn_round.py).
- **Name-collision caution across packs:** `yield` is a COMBAT beat in elemental_harmony (the break-contact equiv → strip) but a DIAL/social beat in heavy_metal (→ keep). Pack-isolated, so per-pack removal lists are safe, but never write a cross-pack sed. Build the removal list from each pack's own combat-def beat ids.
- **Verify-target worlds carry no world-tier rules.yaml** — crunch loads from the genre tier for all WWN worlds (beneath_sunden, evropi, long_foundry, barsoom, burning_peace). Every edit is in genre `rules.yaml`; the worlds are where you playtest, not separate edit sites.
- **Still-pending under ADR-143 doctrine:** CWN (road_warrior, neon_dystopia), AWN (mutant_wasteland), SWN (space_opera) packs STILL have `resolution_mode: beat_selection` combat defs — they belong to their own ruleset epics (114-x), explicitly out of 108-3's WWN-only scope. Same de-nativization pattern will apply when those land.
- **Signature abilities (ADR-095) reference combat beats in PROSE** ("on a successful strike beat", "an incoming strike that would reduce your HP to a guard beat"). These are NOT loader-validated (no structured `trigger_beat:` field — pure description text) so they don't break load, but they're now stale flavor pointing at vanished beats, AND how those abilities re-hook to the WN round (attack/move/item-use/cast) is a mechanical question = Keith's crunch lane + partly server-side (108-7/108-8). Flag as a finding; don't silently reword class crunch in a content-strip story.
