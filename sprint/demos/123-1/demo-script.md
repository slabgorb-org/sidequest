# Demo Script — Story 123-1

## Scene 1: Setup (30 sec)

**Presenter says:** "Problem: Triage and restore the server-test baseline — characterize the ~171 pre-existing full-suite failures surfaced during 114-15 (NONE caused by 114-15; verified by my-surface grep). Step 1 (root-cause, do FIRST): determine whether the dominant bucket — ~174 LlmClientError from build_async_anthropic (tests reach the REAL Anthropic SDK without installing a fake, so the autouse _no_real_anthropic_sdk guard at conftest.py:539 fires) — is a dev-env/stale-tree artifact or a real develop regression: FF-pull both server+content trees, re-run with a clean env (ANTHROPIC_API_KEY unset), and compare. Step 2: fix the snapshot-field governance miss (pending_quest_offers added in 117-3 never registered in ADR-110 session_helpers governance → test_snapshot_field_governance::test_every_snapshot_field_is_categorized; ~one-liner). Step 3: triage tests/integration DB-state failures (needs a migrated PG). OUT OF SCOPE (deliberate, in-flight): the WWN class beat-pool loader PackErrors (class_filter / encounter_beat_choices 'not in pool' on caverns_and_claudes / heavy_metal / elemental_harmony) — epic-108 WWN de-nativization, resolves when 108-3 lands. Deliverable: a categorized baseline-failure manifest committed to the repo + fixes for the non-deliberate buckets + a green-or-explicitly-quarantined suite so future stories get honest CI signal.. Why it matters: a defect was impacting functionality."

**Show:** The issue as users experienced it

## Scene 2: Act 1 (2 min)

**Presenter says:** "We implemented: Triage and restore the server-test baseline — characterize the ~171 pre-existing full-suite failures surfaced during 114-15 (NONE caused by 114-15; verified by my-surface grep). Step 1 (root-cause, do FIRST): determine whether the dominant bucket — ~174 LlmClientError from build_async_anthropic (tests reach the REAL Anthropic SDK without installing a fake, so the autouse _no_real_anthropic_sdk guard at conftest.py:539 fires) — is a dev-env/stale-tree artifact or a real develop regression: FF-pull both server+content trees, re-run with a clean env (ANTHROPIC_API_KEY unset), and compare. Step 2: fix the snapshot-field governance miss (pending_quest_offers added in 117-3 never registered in ADR-110 session_helpers governance → test_snapshot_field_governance::test_every_snapshot_field_is_categorized; ~one-liner). Step 3: triage tests/integration DB-state failures (needs a migrated PG). OUT OF SCOPE (deliberate, in-flight): the WWN class beat-pool loader PackErrors (class_filter / encounter_beat_choices 'not in pool' on caverns_and_claudes / heavy_metal / elemental_harmony) — epic-108 WWN de-nativization, resolves when 108-3 lands. Deliverable: a categorized baseline-failure manifest committed to the repo + fixes for the non-deliberate buckets + a green-or-explicitly-quarantined suite so future stories get honest CI signal.."

**Show:** ## Demo Script — Triage and restore the server-test baseline — characterize the ~171 pre-existing full-suite failures surfaced during 114-15 (NONE caused by 114-15; verified by my-surface grep). Step 1 (root-cause, do FIRST): determine whether the dominant bucket — ~174 LlmClientError from build_async_anthropic (tests reach the REAL Anthropic SDK without installing a fake, so the autouse _no_real_anthropic_sdk guard at conftest.py:539 fires) — is a dev-env/stale-tree artifact or a real develop regression: FF-pull both server+content trees, re-run with a clean env (ANTHROPIC_API_KEY unset), and compare. Step 2: fix the snapshot-field governance miss (pending_quest_offers added in 117-3 never registered in ADR-110 session_helpers governance → test_snapshot_field_governance::test_every_snapshot_field_is_categorized; ~one-liner). Step 3: triage tests/integration DB-state failures (needs a migrated PG). OUT OF SCOPE (deliberate, in-flight): the WWN class beat-pool loader PackErrors (class_filter / encounter_beat_choices 'not in pool' on caverns_and_claudes / heavy_metal / elemental_harmony) — epic-108 WWN de-nativization, resolves when 108-3 lands. Deliverable: a categorized baseline-failure manifest committed to the repo + fixes for the non-deliberate buckets + a green-or-explicitly-quarantined suite so future stories get honest CI signal.

### Scene 1: Setup (30 sec)
**Presenter says:** "Today we're going to show you what we built for Triage and restore the server-test baseline — characterize the ~171 pre-existing full-suite failures surfaced during 114-15 (NONE caused by 114-15; verified by my-surface grep). Step 1 (root-cause, do FIRST): determine whether the dominant bucket — ~174 LlmClientError from build_async_anthropic (tests reach the REAL Anthropic SDK without installing a fake, so the autouse _no_real_anthropic_sdk guard at conftest.py:539 fires) — is a dev-env/stale-tree artifact or a real develop regression: FF-pull both server+content trees, re-run with a clean env (ANTHROPIC_API_KEY unset), and compare. Step 2: fix the snapshot-field governance miss (pending_quest_offers added in 117-3 never registered in ADR-110 session_helpers governance → test_snapshot_field_governance::test_every_snapshot_field_is_categorized; ~one-liner). Step 3: triage tests/integration DB-state failures (needs a migrated PG). OUT OF SCOPE (deliberate, in-flight): the WWN class beat-pool loader PackErrors (class_filter / encounter_beat_choices 'not in pool' on caverns_and_claudes / heavy_metal / elemental_harmony) — epic-108 WWN de-nativization, resolves when 108-3 lands. Deliverable: a categorized baseline-failure manifest committed to the repo + fixes for the non-deliberate buckets + a green-or-explicitly-quarantined suite so future stories get honest CI signal.."
**Show:** The project overview

### Scene 2: Demo (1 min)
**Presenter says:** "Let me show you the changes."
**Show:** The implementation in action

### Scene 3: Closing (30 sec)
**Presenter says:** "That's Triage and restore the server-test baseline — characterize the ~171 pre-existing full-suite failures surfaced during 114-15 (NONE caused by 114-15; verified by my-surface grep). Step 1 (root-cause, do FIRST): determine whether the dominant bucket — ~174 LlmClientError from build_async_anthropic (tests reach the REAL Anthropic SDK without installing a fake, so the autouse _no_real_anthropic_sdk guard at conftest.py:539 fires) — is a dev-env/stale-tree artifact or a real develop regression: FF-pull both server+content trees, re-run with a clean env (ANTHROPIC_API_KEY unset), and compare. Step 2: fix the snapshot-field governance miss (pending_quest_offers added in 117-3 never registered in ADR-110 session_helpers governance → test_snapshot_field_governance::test_every_snapshot_field_is_categorized; ~one-liner). Step 3: triage tests/integration DB-state failures (needs a migrated PG). OUT OF SCOPE (deliberate, in-flight): the WWN class beat-pool loader PackErrors (class_filter / encounter_beat_choices 'not in pool' on caverns_and_claudes / heavy_metal / elemental_harmony) — epic-108 WWN de-nativization, resolves when 108-3 lands. Deliverable: a categorized baseline-failure manifest committed to the repo + fixes for the non-deliberate buckets + a green-or-explicitly-quarantined suite so future stories get honest CI signal. — shipped and verified."

## Scene 3: Act 2 (1 min)

**Presenter says:** "Before: The system exhibited incorrect behavior that affected users.
After: Triage and restore the server-test baseline — characterize the ~171 pre-existing full-suite failures surfaced during 114-15 (NONE caused by 114-15; verified by my-surface grep). Step 1 (root-cause, do FIRST): determine whether the dominant bucket — ~174 LlmClientError from build_async_anthropic (tests reach the REAL Anthropic SDK without installing a fake, so the autouse _no_real_anthropic_sdk guard at conftest.py:539 fires) — is a dev-env/stale-tree artifact or a real develop regression: FF-pull both server+content trees, re-run with a clean env (ANTHROPIC_API_KEY unset), and compare. Step 2: fix the snapshot-field governance miss (pending_quest_offers added in 117-3 never registered in ADR-110 session_helpers governance → test_snapshot_field_governance::test_every_snapshot_field_is_categorized; ~one-liner). Step 3: triage tests/integration DB-state failures (needs a migrated PG). OUT OF SCOPE (deliberate, in-flight): the WWN class beat-pool loader PackErrors (class_filter / encounter_beat_choices 'not in pool' on caverns_and_claudes / heavy_metal / elemental_harmony) — epic-108 WWN de-nativization, resolves when 108-3 lands. Deliverable: a categorized baseline-failure manifest committed to the repo + fixes for the non-deliberate buckets + a green-or-explicitly-quarantined suite so future stories get honest CI signal. — the issue has been resolved and verified with tests."

**Show:** The fix in action, the problem is now resolved

## Scene 4: Closing (30 sec)

**Presenter says:** "The issue is fixed and users can now proceed without problems."

**Show:** The system working correctly after the fix