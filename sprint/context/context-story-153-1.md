# Story 153-1 Context

## Title
[PERF] Slim the IntentRouter DispatchPackage — cut resolved/lethality/you, default visibility, reorder fields, slim prompt

## Metadata
- **Story ID:** 153-1
- **Type:** refactor
- **Points:** 5
- **Priority:** p1
- **Workflow:** tdd
- **Repo:** server
- **Epic:** Intent Router output-slim — cut per-turn latency at the output contract

## Problem
Slim the IntentRouter's DispatchPackage to the fields consumed pre-narration. Root cause (measured 2026-06-20): latency is 0.94-correlated with output_tokens, single-shot, zero-retry — the package accreted output nothing reads. Server-only, no UI, no 2nd LLM call. SPEC: docs/superpowers/specs/2026-06-20-intent-router-output-slim-design.md. PLAN (5 TDD tasks): docs/superpowers/plans/2026-06-20-intent-router-output-slim.md. Belt-and-suspenders safety: death RP runs on the lethality_arbiter HP=0 path (the belt, untouched); we cut only the router's directive-less lethality field (the suspenders).

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria
- resolved[] (Referent list), action_rewrite.you, and the router's lethality[] field are removed from the DispatchPackage schema; Referent class + its protocol/__init__ export removed; LethalityVerdict/LethalityVerdictKind/Reversibility RETAINED (arbiter + genre/models/lethality.py use them).
- Straggler tolerance: a raw package still carrying resolved/lethality/you validates with those keys stripped+logged (not rejected) — extra=forbid must not drop the whole package when Haiku emits a removed field.
- VisibilityTag is omittable on SubsystemDispatch + NarratorDirective, defaulting to visible_to='all'; prompt_redaction + visibility_classifier consumers unaffected.
- lethality_arbiter merge loop reading pd.lethality removed; the HP=0 verdict+paired-directive path is unchanged (death-RP belt intact).
- System prompt drops the referent-resolution step, the per-dispatch visibility mandate, and action_rewrite.you; action_rewrite documented as {named,intent}. confidence_global + per-dispatch confidence gate + all validators preserved.
- Field order: DispatchPackage leads with per_player, PlayerDispatch leads with dispatch (generation-order latency lever).
- VERIFY (gate): latency_diag_82_9 --span-jsonl shows output_tokens p50 roughly halved and the 15-26s latency tail crushed vs the 2026-06-20 baseline (decompose p50 8.6s/p95 18.4s/max 26.6s); a combat scenario shows ZERO new dispatch_engagement.*.mismatch spans + identical dispatch counts. Before/after numbers recorded in the PR.

---
_Generated by `pf context create story 153-1` from the sprint YAML._
