# Story 73-11 Context

## Title
Complete the fixture move — delete duplicate session_handler_factory from tests/server/conftest.py (root conftest is already inherited by tests/server/), eliminating the DRY drift risk where the two byte-identical 208-line copies can silently diverge. Also makes the root copy's 'moved from tests/server/conftest.py' header comment accurate. Flagged in 73-6 review (Reviewer should-fix).

## Metadata
- **Story ID:** 73-11
- **Type:** refactor
- **Points:** 1
- **Priority:** p3
- **Workflow:** trivial
- **Repo:** sidequest-server
- **Epic:** Confrontation Engine Hardening

## Problem
Story 73-6 moved `session_handler_factory` up to the root `tests/conftest.py` so it
would be visible to a wider set of tests, but never deleted the original copy in
`tests/server/conftest.py`. The result is two byte-identical ~208-line copies of the
fixture that can silently diverge — a DRY drift risk. The root copy's header comment
("moved from tests/server/conftest.py ...") is also currently inaccurate, since the
source was never actually removed.

**Verified state (SM, 2026-06-04, sidequest-server develop):**
- `tests/conftest.py:387` — header comment "moved from tests/server/conftest.py to be visible ..."
- `tests/conftest.py:394` — `def session_handler_factory(tmp_path)` (the root copy, inherited by `tests/server/`)
- `tests/server/conftest.py:561` — `def session_handler_factory(tmp_path)` (the duplicate to delete)

pytest conftest inheritance: a fixture defined in `tests/conftest.py` is automatically
available to every test under `tests/server/`, so the per-directory copy is redundant.

## Technical Approach
1. Delete the `session_handler_factory` fixture (and any helpers/imports it solely owns)
   from `tests/server/conftest.py` — the root `tests/conftest.py` copy already covers it
   via conftest inheritance.
2. If the two copies are truly byte-identical, no test should change behavior. If they
   are NOT identical, STOP and surface the divergence as a Delivery Finding before
   deleting — the premise assumes byte-identical copies.
3. Update the root copy's header comment in `tests/conftest.py` so "moved from
   tests/server/conftest.py" is now accurate (the source is genuinely deleted).
4. Remove any imports in `tests/server/conftest.py` that become unused after the deletion.

## Scope
- In scope: delete the duplicate fixture from `tests/server/conftest.py`; correct the
  root header comment; drop now-unused imports.
- Out of scope: editing the root fixture's logic; touching any other fixture; moving
  additional fixtures.

## Acceptance Criteria
1. `tests/server/conftest.py` no longer defines `session_handler_factory`.
2. `tests/conftest.py` retains the single canonical `session_handler_factory`, and its
   header comment accurately reflects that the source has been deleted.
3. The full server test suite passes — every test that used the fixture still resolves it
   via root-conftest inheritance (run with `SIDEQUEST_DATABASE_URL` and
   `SIDEQUEST_GENRE_PACKS` set; see project memory on the env gotchas).
4. No now-unused imports remain in `tests/server/conftest.py`.

---
_Generated by `pf context create story 73-11` from the sprint YAML._
