# Story 126-23 Context

## Title
[BUG] OTEL Inspector Live view not partitioned by session_id — renders the last-emitting session, unusable during concurrent runs

## Metadata
- **Story ID:** 126-23
- **Type:** bug
- **Points:** 3
- **Priority:** p2
- **Workflow:** tdd
- **Repo:** ui,server
- **Epic:** Fate Core playtest follow-ups — annees_folles eval (2026-06-16/17)

## Problem
Source: sq-playtest-pingpong 2026-06-19 [BUG] (SM; Keith: 'the otel dashboard is also totally fucked'). Repro: open #/dashboard while >=2 sessions are active (a playtest + a concurrent headless test test-444686c1). Observed: (1) the Live header jumped to test-444686c1 — a session the operator was NOT driving; (2) Timeline showed TURNS 0 / 'Waiting for first turn...' even though the driven session had taken turns (the live stream only captures spans emitted AFTER the panel connects AND follows whichever session last emitted); (3) the driven session's spans were never viewable in Live. The dashboard must partition the span stream by session_id and render the SELECTED session, not the last-emitting one. Today the sq-playtest skill's Phase-3c caveat says 'trust the by-slug dropdown over Live' — but with concurrent sessions even reaching the right turns is unreliable. This makes the GM-panel lie-detector unusable during concurrent runs (headless tests are frequently concurrent); it blocked live verification of 126-10 router timing. NOT independently tracked before — this is the task. Likely spans server (watcher hub broadcasts all sessions on /ws/watcher) + ui (Dashboard Live should filter to the selected session_id).

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria
- The Inspector Live view partitions the span stream by session_id and renders the operator-SELECTED session, never the last-emitting one.
- With >=2 concurrent sessions, the driven session's turns/spans appear correctly in Live (no cross-session bleed in header or Timeline).
- Update the sq-playtest skill Phase-3c caveat once Live is trustworthy under concurrency.

---
_Generated by `pf context create story 126-23` from the sprint YAML._
