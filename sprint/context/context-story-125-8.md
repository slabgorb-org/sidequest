# Story 125-8 Context

## Title
[TEST DEBT] Restore the WWN/WN-combat test suite after 108-3 de-nativization — ~88 stale tests send stripped combat beats

## Metadata
- **Story ID:** 125-8
- **Type:** chore
- **Points:** 5
- **Priority:** p2
- **Workflow:** tdd
- **Repo:** server
- **Epic:** Tech debt — deferred cleanups & review follow-ups

## Problem
Surfaced during story 126-37 (Fate resolution de-nativization), green phase, 2026-06-20. A full sidequest-server suite run shows ~88 failures + 3 errors across ~34 files, ALL in the WWN/WN-combat area (tests/integration/test_102_4_wn_*, test_106_2_wwn_*, test_108_*, test_wwn_*, test_class_signature_wiring, test_cc_chargen_e2e, test_dice_path_spell_cast_102_2, test_barsoom_cast_beat_live_content, genre/test_heavy_metal_loads_wwn_classes, etc.). Root cause: epic-108's WWN combat de-nativization (content commit 34612bf '108-3 strip native beats', 2026-06-15) removed the native combat beats (strike/brace/break_contact/committed_blow/cast_spell/...) from WWN combat confrontation pools — the WN round (wn_round.py / dice.py 108-8 transient-beat intercept) now supplies the action set (only 'attack' is a recognized WN action beat via is_wn_action_beat). The tests were NOT updated: they still send stripped beat ids, so they fail-loud at either the loader (_validate_class_filter_refs PackError 'references beat id(s) [...] not in pool', loader.py:900) or dispatch (DiceDispatchError 'unknown beat_id committed_blow ... available: []', dice.py:420). These are PRE-EXISTING on origin/develop (the failing code + stale tests predate the 126-37 branch's merge-base e93b4084) — i.e. the WWN-combat suite has been red since ~108-3 landed while epic 108 is in flight (ADR-143/114 'partial', WN-owns-the-round work ongoing). NOT caused by 126-37 (whose 8 tests + 3 fate_conflict-gated guards are all green). One instance was already fixed under 126-37 as a courtesy (tests/genre/test_classes_yaml_loader.py — synthetic classes.yaml hardcoded [strike,brace,break_contact]; changed to [] which the loader exempts for WN classes per 108-7); use it as the reference pattern. WARNING: not all are trivial data swaps — many assert real WN-round behavior (sealed-commit order, reprisal, initiative, shock-kill) and need the test driven through the synthesized WN action set, which may overlap with active epic-108 work; coordinate before sweeping. Recommend its own branch, not a 126-37 follow-on.

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria
- Full sidequest-server suite (uv run pytest) is green: 0 failures, 0 errors in the WWN/WN-combat files (or any remaining red is documented as genuinely-blocked-on-unshipped-epic-108-behavior, with a loud skip + linked story, never a silent xfail).
- Each fixed test exercises the CURRENT de-nativized WN combat path (synthesized WN action beats via is_wn_action_beat / wn_action_beat, or empty encounter_beat_choices for WN classes) — not a trivial assertion-weakening to force green.
- No production code changed unless a genuine wiring gap is found; this is test-debt cleanup. Any production change is logged as a Delivery Finding and re-scoped.
- Coordinated with in-flight epic-108 WN-round work so the fixes don't collide with or pre-empt that story's own test updates.

---
_Generated by `pf context create story 125-8` from the sprint YAML._
