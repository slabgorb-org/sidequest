# Story 158-53 Context

## Title
WWN magic cast does not decrement remaining charges (cinder_lance before=2 after=2)

## Metadata
- **Story ID:** 158-53
- **Type:** bug
- **Points:** 3
- **Priority:** p2
- **Workflow:** tdd
- **Repo:** server
- **Epic:** Playtest sweep follow-ups: WWN combat seating, narrator grounding, roster/map/MP polish

## Problem
WWN magic cast does not spend charges. Evidence: `tests/.../test_wwn_scene_harness_fixture_proof` — a cinder_lance cast shows remaining charges before=2, after=2 (should decrement to 1). The bound-WWN magic cast path resolves the spell narratively but never decrements the remaining-uses/charges counter, so a caster has effectively infinite casts.

WHY IT MATTERS: this is a mechanics-first regression — exactly what Sebastien/Jade (the mechanics-first players) would catch. Magic that never spends breaks resource-management, and the player-facing surface would show an unchanging charge count.

FIX DIRECTION (for pickup): decrement the charge/use counter on a successful WWN cast, and — per the OTEL Observability Principle — emit a watcher span proving the spend (before/after remaining) so the GM panel can verify the decrement fired rather than the narrator improvising the spend. Add a wiring test through the real cast path.

PROVENANCE: pre-existing full-suite failure, surfaced 2026-07-01 while scoping the 160-4 option-C green push. Unrelated to 160-4 (companion-solo-seat); 160-4 lands without it. Diagnosis is from the scoping pass — confirm at pickup.

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria
- A successful WWN cast decrements the spell's remaining charges/uses (cinder_lance before=2 -> after=1)
- The cast-spend emits an OTEL watcher span with before/after remaining so the GM panel verifies the decrement fired
- test_wwn_scene_harness_fixture_proof passes; a wiring test exercises the decrement through the real cast path

---
_Generated by `pf context create story 158-53` from the sprint YAML._
