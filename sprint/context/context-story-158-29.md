# Story 158-29 Context

## Title
Dogfight router→seater→lifecycle — degrade loudly when no engine seats; never wedge the narrator (ADR-153 Plan 2)

## Metadata
- **Story ID:** 158-29
- **Type:** bug
- **Points:** 2
- **Priority:** p1
- **Workflow:** tdd
- **Repo:** server
- **Epic:** Playtest sweep follow-ups: WWN combat seating, narrator grounding, roster/map/MP polish

## Problem
REBUILD per ADR-153 Plan 2 (router→seater→lifecycle contract). Plan 2 is written AFTER a live repro — build Plan 1 (firewall) first, then a /sq-playtest reproduces this. WORK: when a ship-combat verb routes to the dogfight but the engine cannot seat, degrade loudly and observably (OTEL) — never leave the narrator grinding the SDK tool loop. SCOPE: the dogfight dispatch path only. The GENERAL "narrator max_turns must degrade, not crash the session" robustness fix is split out to 158-41 (not dogfight scope) — do not solve it here.

━━ Symptom / why (2026-06-25 coyote_star playtest, develop @aed2d812): a ship-combat verb ('bring guns online, lock a firing solution') produced intent_router.confrontation_verb_unrouted (verb_hits dogfight:lock, dogfight:gun) — the router declined to dispatch the dogfight. With no engine seated the narrator ground the SDK tool loop to max_turns=8 and the turn crashed. NOTE: the old "native dial_threshold dogfight / WN tool-withhold" framing in the original finding is obsolete — ADR-153 deletes the native dial; this story is now purely about the routing→seating→degradation contract. OPEN DISAMBIGUATION (confirm from the watcher stream): did the router fail to emit a ship-combat dispatch key, or dispatch-then-reject?

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria
- Re-scoped under ADR-153 Plan 2 (router->seater->lifecycle contract): dogfight path must degrade loudly, not max_turns-crash. General max_turns-degrade robustness tracked separately. Plan 2 pending live repro.

---
_Generated by `pf context create story 158-29` from the sprint YAML._
