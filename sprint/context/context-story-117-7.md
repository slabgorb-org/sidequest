# Story 117-7 Context

## Title
Render quest-related lore in QuestsPanel — surface the related_lore the server now projects (117-5) so Keith actually SEES the coherent picture. Server-side projection landed in 117-5 (server #876): the QUESTS payload carries related_lore (list of {fact_id, content}) under each quest entry, but the client drops it. Two gaps: (1) sidequest-ui src/types/payloads.ts QuestLogEntry has no related_lore field, so the typed data is invisible; (2) src/components/QuestsPanel.tsx renders only title/status/objective/anchor — needs a 'What I've learned about this job' render block listing the related lore under each quest. UI-only (sidequest-ui, base develop). This is the visible payoff of 117-5; without it the coherence is on the wire but unseen. Player-facing (per CLAUDE.md: legible mechanics in player UI). Wiring test: a QuestsMessage with related_lore renders the lore block (jsdom).

## Metadata
- **Story ID:** 117-7
- **Type:** story
- **Points:** 3
- **Priority:** p1
- **Workflow:** tdd
- **Repo:** ui
- **Epic:** Quest-seed lifecycle — authored hooks mint tracked quests, and a lie-detector that actually catches silent objectives

## Problem
_No description in the sprint YAML — see the story title above and the epic context for scope._

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria
_No acceptance criteria recorded in the sprint YAML — TEA to define during the RED phase._

---
_Generated by `pf context create story 117-7` from the sprint YAML._
