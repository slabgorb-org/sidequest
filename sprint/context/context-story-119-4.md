# Story 119-4 Context

## Title
Depends on whichever migration lands (119-2 or 119-3). Production auth durability + cost/credit observability. 1: long-running server uses an auto-refreshing ant auth login profile on the host (the print-credentials env token is short-lived and not auto-refreshed), construct the client to pick up the refreshing profile not a baked-in token; fail loud on expiry, never silently fall back to PAYG. 2: emit an OTEL span tagging which auth path / billing pool each inference draws (credit vs PAYG overflow) so the cost/GM panel can VERIFY the narrator is on the credit, a Keith/dev observability concern per the CLAUDE.md OTEL principle NOT a player surface. 3: monitor the 200 monthly credit + the pool-3 prepaid overflow buffer (currently 50.52) so mid-month exhaustion is visible before it silently bills PAYG.

## Metadata
- **Story ID:** 119-4
- **Type:** story
- **Points:** 5
- **Priority:** p2
- **Workflow:** tdd
- **Repo:** server
- **Epic:** Narrator inference: move off API-key PAYG onto the free subscription (Agent SDK credit CANCELLED)

## Problem
_No description in the sprint YAML — see the story title above and the epic context for scope._

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria
- Cost-ceiling / runaway-detector recalibration (folded from 119-3): the per-session cost-runaway fingerprint detector + $10 hard-kill ceiling (cost_safety.py, ADR-134) are PAYG-dollar-calibrated, but under 119-3's subscription transport they now meter NOTIONAL CLI cost (one session_id ledger summing narrator + Haiku notional spend). Re-frame them as notional-shape signals so the ceiling neither fires spuriously nor silently never-fires.
- Cache-observability re-home (folded from 119-3 OQ-6): 119-3 left the narrator per-TTL (5m/1h) cache-write split documented-zero (anthropic_sdk_client.py:557-561, cached_input_write_5m/1h_tokens=0; aggregate cache read/write preserved). Surface the real split if the agent SDK exposes it, or document the permanent gap on the cost/GM panel so the 0s are never read as a measured value (No Silent Fallbacks).

---
_Generated by `pf context create story 119-4` from the sprint YAML._
