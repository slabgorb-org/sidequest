# Story 126-11 Context

## Title
[NARRATOR] Drama-aware <length-limit>: scale the verbosity cap to narrative weight (SOUL Cost-Scales-with-Drama). The flat standard cap (8 sentences / 800 chars, orchestrator.py:1588 _build_verbosity_section; static 'BREVITY IS KING' in output_style.md) never widens for a climactic reveal vs a walk to the tavern — it strangles plot beats. Wire a drama/weight signal into the verbosity section so high-weight turns get room and quiet turns stay cheap; OTEL span on the chosen tier. Found in the 2026-06-18 narrator-prompt deep-dive. NOTE: 126-9 (thinking-ON latency regression) is now DONE, so the clean baseline this needs exists.

## Metadata
- **Story ID:** 126-11
- **Type:** story
- **Points:** 3
- **Priority:** p2
- **Workflow:** tdd
- **Repo:** server
- **Epic:** Fate Core playtest follow-ups — annees_folles eval (2026-06-16/17)

## Problem
_No description in the sprint YAML — see the story title above and the epic context for scope._

## Technical Approach

1. **Audit current verbosity control:** Locate and document the flat cap implementation:
   - `orchestrator.py:1588` in `_build_verbosity_section()`
   - Static "BREVITY IS KING" directive in `output_style.md`
   - Current constraint: 8 sentences / 800 characters, applies uniformly to all narration

2. **Wire narrative weight signal into verbosity section:**
   - Extract or derive a "drama/weight" metric from game state (SOUL principle: "Cost Scales with Drama")
   - Define weight tiers (e.g., climactic/confrontation vs. quiet/exploration vs. transition)
   - Scale the verbosity cap per tier (high-weight turns get more room, quiet turns stay cheap)
   - Weight signal sources: confrontation present/type, player action intensity, scene tension, turn count in escalation

3. **Implement scalable caps:**
   - Map weight tiers to cap variants (e.g., tier "climactic" → 12 sentences / 1200 chars; "quiet" → 6 / 600; "normal" → 8 / 800)
   - Apply the chosen tier's cap to the narrator output_style prompt

4. **Add OTEL observability:**
   - Emit a span on the chosen verbosity tier (e.g., `narrator.verbosity_tier` with attributes tier_name, weight_value, cap_sentences, cap_chars)
   - GM panel must be able to see what tier was selected for each turn (lie-detector per OTEL Observability Principle in CLAUDE.md)

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria

1. **Verbosity weight signal integrated:** The narrator's `_build_verbosity_section()` evaluates a drama/weight signal from game state and applies a weight-scaled cap (not a flat 8-sentence / 800-char cap for all turns).

2. **High-weight turns get room, low-weight turns stay cheap:** Climactic/confrontation turns allow longer narration (e.g., 12 sentences / 1200 chars); quiet/exploration turns apply tighter caps (e.g., 6 sentences / 600 chars); normal turns use baseline (8 / 800).

3. **OTEL span on chosen tier:** The narrator emits a `narrator.verbosity_tier` span (or similar) with attributes: tier name, weight value, cap in sentences, cap in characters. The GM panel's OTEL Inspector can verify the tier for each turn (lie-detector test: confirm tier matches the narrative weight of the turn).

4. **Tests cover weight → tier → cap mapping:** Unit/integration tests verify that high-weight game states map to high-cap tiers and low-weight states map to low-cap tiers; the narrator output respects the chosen cap.

5. **Baseline unchanged for non-weighted turns:** Turns where weight cannot be derived or is neutral still default to the baseline 8 / 800 cap (no regressions for existing behavior).

---
_Generated by `pf context create story 126-11` from the sprint YAML._
