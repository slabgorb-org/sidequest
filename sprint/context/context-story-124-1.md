# Story 124-1 Context

## Title
Timing token chart — per-turn cached/fresh split + cache-hit sparkline

## Metadata
- **Story ID:** 124-1
- **Type:** story
- **Points:** 5
- **Priority:** p3
- **Workflow:** tdd
- **Repo:** server,ui
- **Epic:** OTEL Inspector — Tufte visualization follow-ups

## Problem
The Tufte design splits per-turn tokens-in into cached (faded) vs fresh (solid) stacked bars, adds a cache-hit-rate sparkline with cold-start misses flagged in accent, and a 'served from cache' savings summary. BLOCKER at redesign time: cache_read / cache_hit / cold are NOT on the turn_complete event (TurnCompleteFields) — they live on prompt_assembled (PromptFields.cache_usage), which the Prompt tab already surfaces. Wiring options: (a) add a per-turn cache field to turn_complete server-side, or (b) join prompt_assembled cache_usage to turns by turn_number in the dashboard telemetryAdapter. Then extend charts/TokenBarChart.tsx to stack cached+fresh on the shared scale and add the hit-rate track. cache_usage can be null (non-SDK backends) — render only what's present, no fabrication (No Silent Fallbacks).

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria
- turn_complete (or a dashboard-side join) exposes per-turn cache_read / fresh_in / cache_hit / cold
- TokenBarChart renders stacked cached(faded)+fresh(solid) in-bars on the shared honest scale
- cache-hit-rate sparkline with cold-start turns flagged in accent + a cache-savings summary line
- null cache_usage (non-SDK turns) renders honestly with no fabricated estimate

---
_Generated by `pf context create story 124-1` from the sprint YAML._
