# Story 152-3 Context

## Title
[TEST] Restore the chargen class-surface tests (test_cc_chargen_e2e + test_class_signature_wiring) — fix the brittle multi-choice-scene walk + reconcile class_moves to the de-nativized WN action set

## Metadata
- **Story ID:** 152-3
- **Type:** bug
- **Points:** 3
- **Priority:** p2
- **Workflow:** tdd
- **Repo:** server
- **Epic:** WN combat owns the full WWN action set — cast + Total Defense + Fighting Withdrawal via WWN math, not native beats (complete 108-8)

## Problem
TRIAGE (Architect, 2026-06-20; design doc docs/superpowers/specs/2026-06-20-wn-full-action-set-design.md): the chargen [None x6] failures (test_cc_chargen_e2e 4, test_class_signature_wiring 6) are a STALE TEST, NOT a regression. The WWN chargen flow has TWO choice-scenes — the_calling (3 choices, each with mechanical_effects.class_hint Warrior/Expert/Mage) and the_trade (6 background choices, NO class_hint). _build_character's walk (test_class_signature_wiring) naively requires EVERY choice-scene to contain a class_hint==target_class choice, so it dies on the_trade with 'target_class Warrior not in [None,None,None,None,None,None]'. Class selection content is INTACT (char_creation.yaml:23/29/35); the brittle walk simply never adapted to the 120-1 chargen reconciliation that added the_trade. The tests ALSO assert de-nativization-coupled surface (committed_blow in class_moves; Mage class_moves populated from encounter_beat_choices) — under the synthesized WN action set (epic-152) a WN class carries no per-class combat beats (encounter_beat_choices -> [] per 108-7), so the combat menu is the WWN action set (attack/cast/Total Defense/Fighting Withdrawal), not per-class beats. FIX: (1) make the chargen walk pick the class choice on the_calling and a background choice on the_trade (and any future choice-scene) instead of assuming one class scene; (2) reconcile the class_moves / signature assertions to the de-nativized surface the epic-152 stories define. Depends on 152-2 so the action-set surface is settled before asserting it. No production regression — this is test restoration + surface reconciliation.

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria
- The chargen walk in test_class_signature_wiring handles BOTH choice-scenes (class on the_calling, background on the_trade) and reaches confirmation — no [None x6] failure.
- class_moves / signature assertions reflect the de-nativized WN combat surface defined by 152-1/152-2 (universal synthesized action set; WN classes carry no per-class combat beats), not the pre-strip committed_blow/per-class beat model — assertions updated, not deleted.
- test_cc_chargen_e2e and test_class_signature_wiring are green; class selection + the WWN Calling signature abilities (Killing Blow/Veteran's Luck, Read the Ledger, Read the Worked Stone) + pronoun-agnostic prose all still asserted.
- No production code changed for the walk fix; any class_moves surface change needed is owned by 152-1/152-2, not invented here.

---
_Generated by `pf context create story 152-3` from the sprint YAML._
