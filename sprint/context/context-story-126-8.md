# Story 126-8 Context

## Title
[FATE] Player defense 4dF is determinative (physics-is-the-roll) — the DEFEND barrier (ADR-148/149)

## Metadata
- **Story ID:** 126-8
- **Type:** story
- **Points:** 5
- **Priority:** p3
- **Workflow:** tdd
- **Repo:** server,ui
- **Epic:** Fate Core playtest follow-ups — annees_folles eval (2026-06-16/17)

## Problem
The DEFEND half of ADR-148, deliberately deferred from 126-7. Story 126-7 made the player's PROACTIVE Fate 4dF determinative (FATE_THROW: settled tray faces ARE the roll, server resolves from reported faces). 126-8 brings the player's DEFENSE roll into the same physics-is-the-roll model: today _roll_defense / _seat_opponent_commits in sidequest/server/dispatch/fate_conflict.py still roll the defending player's 4dF SERVER-SIDE with the tray as decoration. When a SEATED Fate PC defends against an attack, the player should physically throw the 4dF and the settled faces resolve the defense (no roll_4df on the player defense path). NPC/opponent rolls stay server-side. New client->server FATE_DEFEND_THROW message + ui defense throw tray mirroring 126-7's FateThrowHandler/FateDiceTray. Per ADR-148 line 146 this is 'Story 126-8 / ADR-149' — the no-roll_4df-on-the-player-path invariant extends from proactive to defense. SOUL: bind the ruleset (Fate ladder math unchanged).

## Technical Approach
_Hints for TEA/Dev — refine during RED. Canonical mirror is 126-7's shipped
proactive-throw path; reuse its shape, do not reinvent._

- **Server:** In `sidequest/server/dispatch/fate_conflict.py`, the seated-PC defense path (`_roll_defense` / `_seat_opponent_commits`) must stop calling `roll_4df` for a **seated Fate PC defending**. Instead, the player throws and the settled tray faces are reported and resolved — mirroring how 126-7 resolves `FATE_THROW` from reported faces.
- **Protocol:** add a `client→server FATE_DEFEND_THROW` message (sibling of 126-7's `FATE_THROW`), carrying the settled 4dF faces for the defense.
- **UI:** add a defense throw tray mirroring 126-7's `FateThrowHandler` / `FateDiceTray`, triggered when a seated PC must defend.
- **Boundary (keep server-side):** NPC/opponent defense and all NPC rolls stay server-side (`roll_4df`). Only the **seated PC's own defense** moves to physics-is-the-roll.
- **Invariant (math unchanged):** Fate ladder/shift math is identical — bind the ruleset, don't rebalance it. This story only relocates *who rolls the player's defense die*.

## Scope
- **In scope:** seated-PC defense 4dF becomes determinative; new `FATE_DEFEND_THROW` message; UI defense tray.
- **Out of scope:** NPC/opponent roll path (stays server-side); Fate ladder math; the router-prompt work in 126-10 (oq-1) — do not touch `build_fate_projection` / `intent_router_pass.py`.

## Acceptance Criteria
_TEA to finalize/expand during RED. Starting set derived from the story + ADR-148/149:_

- [ ] When a **seated Fate PC** defends against an attack, NO `roll_4df` is invoked on the player defense path — the result comes from reported tray faces.
- [ ] A `FATE_DEFEND_THROW` client→server message exists and is dispatched; the server resolves the defense from its settled faces (the physics IS the roll, per ADR-074/148).
- [ ] The UI presents a defense throw tray (mirroring `FateDiceTray`) when a seated PC must defend, and reports settled faces back.
- [ ] NPC/opponent defense rolls remain server-side (`roll_4df`) — unchanged.
- [ ] Fate ladder math (shifts, outcomes) is unchanged vs. server-rolled defense for the same faces.
- [ ] A wiring test proves the new message path is reachable from production dispatch (not just unit-tested in isolation).

---
_Generated by `pf context create story 126-8` from the sprint YAML._
