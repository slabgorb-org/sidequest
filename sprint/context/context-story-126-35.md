# Story 126-35 Context

## Title
[FATE] On-play significant items don't promote to an invokable aspect/extra (#945 on-play path; chargen path verified GREEN via 126-21/126-25). A narrator-granted story item (the drifter's surveyor's map) lands in native core.inventory but fate_sheet.aspects/extras is unchanged. KEITH DESIGN CALL pending: either (a) close as model-correct (no equipment economy; ad-hoc item has no authored grants_aspects) + document, or (b) auto-promote narrator-granted significant items to a situation/story aspect or Fate extra under ruleset=='fate'. Surface the decision in the design phase before implementing.

## Metadata
- **Story ID:** 126-35
- **Type:** story
- **Points:** 3
- **Priority:** p3
- **Workflow:** tdd
- **Repo:** server
- **Epic:** Fate Core playtest follow-ups — annees_folles eval (2026-06-16/17)

## Problem
[FATE] On-play significant items don't promote to an invokable aspect (#945 on-play path; chargen path GREEN via 126-21/126-25). A narrator-granted ad-hoc story item (the drifter's surveyor's map) lands in core.inventory but the FateSheet is unchanged because the item matches no authored GearDef.grants_aspects AND the narrator never flags it.

KEITH DESIGN CALL — RESOLVED 2026-06-20 → PATH (c): ACTIVATE THE DORMANT NARRATOR LEVER. Surface 'grants_aspect' to the narrator item-grant tool-contract + prompt so the narrator DELIBERATELY marks an item significant when it grants one; promotion then runs through the existing consumer. REJECTED: (b) engine auto-promotion (invents the equipment economy Fate omits — ADR-144 'a hat is a hat'; Overbaits per Diamonds-and-Coal; needs an unreliable significance heuristic; an aspect is a +2/reroll power faucet) and (a) pure-document (leaves an INVENTED ad-hoc item unpromotable without pre-authoring it as gear).

MEASURED FACTS: FateSheet has only aspects: list[Aspect] — NO 'extras' field (target is an Aspect kind 'situation'). Consumer + promoter already exist and are tested: narration_apply.py:5254 reads entry.get('grants_aspect'); game/ruleset/fate_item_promotion.py::promote_gained_item appends the Aspect; tests/server/test_fate_item_promotion_wiring.py::test_narrator_grants_aspect_on_invented_item covers CONSUMPTION. DORMANT gap (grep: only one server ref to grants_aspect, the consumer): nothing in the narrator tool-schema/prompt tells the narrator it MAY set the field. This is the exact follow-up 126-25 named when it resolved AC4 by SPLIT. Server-only.

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria
- The narrator item-grant tool-contract surfaces an OPTIONAL grants_aspect field (tool-schema + prompt-zone instruction) so the narrator may mark a granted item significant. The instruction is narrator-authored promotion only — it must NOT imply or trigger automatic promotion of every item.
- Full narrator-set WIRING TEST (not just consumption): for a Fate-bound PC, a narrator grant that sets grants_aspect promotes the item to a situation Aspect on the FateSheet that is invokable in a 4dF resolution. Drives the real narrator-set path end-to-end, mirroring the catalog-path wiring test.
- OTEL: fate.item_promoted fires with source=narrator so the GM panel distinguishes the narrator-authored route from the catalog route (source=catalog). The GM panel is the lie detector — the narrator path must be observable.
- Invariant preserved: an ad-hoc item granted WITHOUT grants_aspect does NOT promote (stays inventory flavor). No engine auto-promotion heuristic is added; Fate's no-equipment-economy model (ADR-144) is untouched.

---
_Generated by `pf context create story 126-35` from the sprint YAML._
