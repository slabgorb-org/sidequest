# Story 158-54 Context

## Title
awn.mutation.used OTEL span never fires for mutant_wasteland mutations

## Metadata
- **Story ID:** 158-54
- **Type:** bug
- **Points:** 3
- **Priority:** p2
- **Workflow:** tdd
- **Repo:** server
- **Epic:** Playtest sweep follow-ups: WWN combat seating, narrator grounding, roster/map/MP polish

## Problem
The `awn.mutation.used` OTEL span never fires when a mutant_wasteland (Ashes Without Number) mutation is used. Evidence: `tests/.../test_103_10_seaboard_e2e` and `tests/.../test_102_7_mutant_wasteland_mutations_live` both assert the span and fail — it is not emitted.

WHY IT MATTERS: OTEL is the lie-detector (CLAUDE.md OTEL Observability Principle). A missing subsystem span means we cannot tell whether the mutation effect actually engaged or the narrator is improvising it. Needs investigation into whether (a) only the span is missing (emit it) or (b) the mutation-use path itself is dark (apply the effect AND emit the span).

FIX DIRECTION (for pickup): trace the AWN mutation-use path; confirm whether the mutation effect applies at all; emit `awn.mutation.used` (component + payload identifying the mutation + actor) on the use decision; wire a test through the real path.

PROVENANCE: pre-existing full-suite failure, surfaced 2026-07-01 while scoping the 160-4 option-C green push. Unrelated to 160-4; 160-4 lands without it. Diagnosis from the scoping pass — confirm at pickup.

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria
- awn.mutation.used fires on a mutant_wasteland mutation use, with a payload identifying the mutation + actor
- Confirmed whether the mutation effect actually applies (not just the span); fix whichever is dark
- test_103_10_seaboard_e2e and test_102_7_mutant_wasteland_mutations_live pass via the real path

---
_Generated by `pf context create story 158-54` from the sprint YAML._
