# Story 126-25 Context

## Title
[SERVER] Wire world-tier Fate gear.yaml into rules.fate.gear_catalog so #945 promotes world-specific found-items

## Metadata
- **Story ID:** 126-25
- **Type:** feature
- **Points:** 3
- **Priority:** p2
- **Workflow:** tdd
- **Repo:** server
- **Epic:** Fate Core playtest follow-ups — annees_folles eval (2026-06-16/17)

## Problem
The #945 Fate item-promoter (game/ruleset/fate_item_promotion.py:match_gained_gear) matches a gained item against `_fate_cfg.gear_catalog`, but `gear_catalog` is loaded from the GENRE tier ONLY (loader._load_gear at loader.py:2130 reads `<pack>/gear.yaml` → rules.fate.gear_catalog). World-tier `worlds/<world>/gear.yaml` is NOT merged, so a world-specific Fate GearDef can never be seen by the promoter.

This blocks 126-21 AC2 (Oz silver shoes cannot be promoted to aspects).

## Technical Approach
Load `worlds/<world>/gear.yaml` for the active world and merge its GearDefs into the effective gear_catalog (by-id, world wins — mirror the inventory merge precedent). Keep the genre catalog clean: world artifacts live only at the world tier.

Also resolve the Phase-2 narrator grants_aspect path: either surface it to the narrator tool-contract (activating dormant narrator promotion) or split it to its own story with the gap documented.

## Scope
- In scope: world-tier gear.yaml loading and merging into gear_catalog; Phase-2 narrator grants_aspect path resolution.
- Out of scope: chargen gear defaults (126-24), Oz specific content (126-21).

## Acceptance Criteria
- worlds/<world>/gear.yaml is loaded and its GearDefs merge by-id (world wins) into the effective Fate gear_catalog for the active world; the genre-tier catalog stays unpolluted (a wonderland/gulliver session does NOT see oz_silver_shoes).
- WIRING TEST (full path, not just the merge): active world=oz, player gains the silver shoes → fate.item_promoted span fires with source=catalog → the 'Three Steps Home' aspect appears on the FateSheet and is invokable in a 4dF resolution.
- Match robustness: the placement content's granted item (id/name) resolves to GearDef oz_silver_shoes under match_gained_gear's exact discipline.
- Phase-2 gap resolved (126-21 AC3): either surface grants_aspect to the narrator tool-contract or split to its own story with the gap documented.
- OTEL: the world-tier gear merge is observable (span or log: which world gear ids merged into the catalog).

---
_Generated by manual context creation for 126-25 from the sprint YAML._
