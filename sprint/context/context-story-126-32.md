# Story 126-32 Context

## Title
[BUG] Bind narrated NPCs to seeded/registry entities + fix shuffle_fallback culture/region routing (one root seam, three manifestations). (a) narrated antagonist -> seated Fate opponent identity by exact name (dust_and_lead 'Henry Shaw' not the bestiary 'Western Diamondback'; Defect A seater fix shipped, identity-binding residual open); (b) described canonical figure -> seeded registry NPC (Oz Good Witch of the North, not a hollow 'Amaranth Warmacre' mint, carry its hp/pronouns); (c) Defect B namegen — npc.invented_name_routed shuffle_fallback reroutes established Anglo names to the wrong culture/region (Ndé), key the mint to the current region. Timing change in narration_apply.py (promote narrated NPCs to snapshot.npcs before mint/seating). LARGE/RISKY — proper TDD server story.

## Metadata
- **Story ID:** 126-32
- **Type:** bug
- **Points:** 8
- **Priority:** p2
- **Workflow:** tdd
- **Repo:** server
- **Epic:** Fate Core playtest follow-ups — annees_folles eval (2026-06-16/17)

## Problem
_No description in the sprint YAML — see the story title above and the epic context for scope._

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the
intended behavior._

### SM routing notes (file pointers, not a prescribed fix)
- **Live seam file is `sidequest/server/narration_apply.py`** — NOT `sidequest/game/narration_apply.py`. The story title's `narration_apply.py` reference predates the module's current location; confirm against the `server/` path. (`shuffle_fallback` / `invented_name_routed` symbols live here and in `sidequest/telemetry/spans/npc.py`.)
- Relevant symbols already in that file: `_auto_mint_prose_only_npcs`, `npc_invented_name_routed_span`, the two `snapshot.npcs.append(...)` promote points (~L1485 npc, ~L1638 promoted), and an existing "double-mint: original→mint binding" comment (~L1415) about re-minting at the pool tier — the timing change the title calls for ("promote narrated NPCs to snapshot.npcs BEFORE mint/seating") lands in this region.
- **Manifestation (a)** depends on already-shipped work: "Defect A seater fix shipped, identity-binding residual open." TEA should locate the shipped seater fix first and write the RED test for the *residual* (narrated 'Henry Shaw' must seat as the Fate opponent by exact name, not the bestiary 'Western Diamondback').
- **Manifestation (b)**: described canonical figure (Oz "Good Witch of the North") must bind to the seeded registry NPC and carry its hp/pronouns — not produce a hollow 'Amaranth Warmacre' mint.
- **Manifestation (c)** / Defect B: `npc.invented_name_routed` `shuffle_fallback` reroutes established Anglo names to the wrong culture/region (observed: Ndé). Key the mint to the *current region*. See the divergent-slug-rules hazard when touching name routing.
- Per project doctrine (ADR-031 / OTEL principle): any subsystem-decision fix here MUST emit/retain OTEL watcher spans (`npc_invented_name_routed_span` already exists) so the GM panel can verify the binding fired and Claude isn't improvising the NPC identity.
- This is the Fate-Core annees_folles eval epic (126); related entanglement context: Fate confrontations seat but ride on the native d20/beat surface — relevant to manifestation (a)'s seater path.

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria
_No acceptance criteria recorded in the sprint YAML — TEA to define during the RED phase._

---
_Generated by `pf context create story 126-32` from the sprint YAML._
