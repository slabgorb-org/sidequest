# Story 121-8 Context

## Title
F4a3 — Fate chargen UI (ui; DESIGN-GATED by F4a-design): player-facing Fate creation screens — aspect authoring inputs (high concept + trouble + free), skill-pyramid allocation widget with live legality feedback, stunt picker with refresh readout, the ladder labels; ruleset=='fate'-gated so it never co-renders with the d20/WN chargen screen (paired negative test). Consumes the F4a2 wire contract; server remains the validation authority (No Silent Fallbacks — client mirrors, does not adjudicate). Depends F4a2 (121-7).

## Metadata
- **Story ID:** 121-8
- **Type:** story
- **Points:** 8
- **Priority:** p2
- **Workflow:** tdd
- **Repo:** server,ui (sprint YAML says `ui` only — KNOWN DEVIATION; Architect verified 2026-06-16 that server is required because 121-7 deferred the render payload here)
- **Epic:** F4 — Fate Core content migration (ADR-144)

## Authoritative context

**The full technical approach, extension points, draft ACs, OTEL/wiring plan, and the
repos deviation live in the session file `.session/121-8-session.md` (Technical Context
section).** Read that first. Design source of truth:
`docs/superpowers/specs/2026-06-16-fate-interactive-chargen-design.md` — §7 (UI + wire
contract), §8 (OTEL + wiring), and the "Implementation reconciliation (2026-06-16,
Architect)" addendum at the foot.

## Problem
121-7 (done) shipped the Fate submission contract + the bare `input_type` surface but
deferred the server→UI render payload to this story. 121-8 completes that payload
(server) and builds the three `fate_*` renderers (ui). Server stays the validation
authority; the UI mirrors live legality, never adjudicates.

## Scope
- In scope: server `CharacterCreationPayload` Fate fields + `_render_fate_step_message`
  population; the three UI renderers (`fate_aspects`, `fate_skill_pyramid`, `fate_stunts`);
  the structural ruleset gate + paired-negative test.
- Out of scope: a `ruleset=='fate'` conditional in the renderer (gate is structural —
  §7 lines 270-272); content authoring (121-3/4/5); the Fate conflict engine.

## Acceptance Criteria
_Draft ACs are in the session file; TEA refines them during the RED phase._

---
_Generated by `pf context create story 121-8` from the sprint YAML._
