# Story 151-5 Context

## Title
[NARRATOR] Sidecar cutover II — npcs_present (engine-owned side) + async cosmetic fields (mood, visual_scene, footnotes) (ADR-150 step 4)

## Metadata
- **Story ID:** 151-5
- **Type:** refactor
- **Points:** 5
- **Priority:** p2
- **Workflow:** tdd
- **Repo:** server
- **Epic:** Sidecar Accounting Off the Narrator Hot Path (ADR-150)

## Problem
ADR-150 step 4, **cutover II**. The predecessor 151-4 (cutover I, `done`) moved the
transactional sidecar fields (items×4, gold, companions×2) off the narrator's
`game_patch` hot path to the post-narration Haiku extractor. This story continues that
cutover for the next field group:
- **`npcs_present` (engine-owned side)** — the half of the field the engine derives, not
  the narrator-driven half. TEA to confirm the exact boundary from ADR-150.
- **async cosmetic fields** — `mood`, `visual_scene`, `footnotes`. Non-transactional
  (no game-state mutation), so they move to the extractor as readouts.

Goal per the epic: free Opus narrator attention for prose craft and remove uncached
tokens/turn, one atomic field-group cutover at a time with the lie-detector watching.

## Technical Approach
_Routing pointers from SM — TEA/Dev own the implementation detail and the ACs._

- **Spec of record:** `docs/adr/150-sidecar-accounting-off-narrator-hot-path.md` —
  read step 4 and "Sizing the follow-up" in the Implementation Notes.
- **Pattern to follow:** predecessor **151-4** (cutover I) — `sprint/archive/151-4-session.md`.
  Same shape: atomic field-group cutover to the post-narration extractor, keep the
  catch-loops as the loud net.
- **Extractor scaffold already landed:** 151-2 built the post-narration Haiku sidecar
  extractor (shadow mode + `sidecar_extraction.*` OTEL + lie-detector). This story
  promotes the named fields onto it.
- **OTEL is mandatory** (project principle + epic): each field moved must emit
  `sidecar_extraction.*` watcher spans so the GM panel can verify the field is engaged,
  not improvised.
- **Tests:** fixture-based only — **no live-content coupling** (epic rule).

## Scope
- **In scope:** cutover of `npcs_present` (engine-owned side) + `mood`, `visual_scene`,
  `footnotes` to the post-narration extractor; `sidecar_extraction.*` OTEL for each.
- **Out of scope:** `private_segments` — stays narrator-inline (ADR-105 perception
  firewall, the one irreducible field); that hardening is 151-6. No prose-prompt
  shrink (also 151-6). No playtest validation gate (151-7).

## Acceptance Criteria
_No acceptance criteria recorded in the sprint YAML — **TEA to define during the RED
phase**, derived from ADR-150 step 4 and the 151-4 cutover-I precedent._

---
_Generated by `pf context create story 151-5` from the sprint YAML._
