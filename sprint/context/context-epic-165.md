# Epic 165 Context

## Title
Mapping Track C — tactical mechanics (ADR-096 v2)

## Overview
Spec §3: docs/superpowers/specs/2026-07-08-three-tier-mapping-design.md. Plan: docs/superpowers/plans/2026-07-08-mapping-track-c-tactical-mechanics.md. Pure cell-math library over masks, WN binding enforcement at dispatch_dice_throw, positions in per_actor_state['cell'], turn_telemetry spans, TACTICAL_GRID adjudication echoes + player-visible math, Fate zone projection. Dogfight (ADR-153) untouched. No dependency on Track B.

## Metadata
- **Epic ID:** 165
- **Repo:** server,ui

## Background

### Carryover from 165-1 (C1 pure library — READ before starting any 165-N)
165-1 delivered the pure cell-math library `sidequest/game/tactical/adjudication.py`
(mask parse, Chebyshev, `cells_reachable` flood, `movement_cost`, `adjudicate_move`,
`reach_cells`, `line_of_sight`, `adjudicate_reach`, `aoe_burst`, `aoe_line`). Full
detail in `sprint/archive/165-1-session.md` Delivery Findings + the tea/dev/reviewer
sidecar gotchas (`.pennyfarthing/sidecars/*-gotchas.md`, all tagged 165-1). Load-bearing
carryover for the remaining Track C stories:

1. **The plan doc has bugs in its embedded code — hand-verify before transcribing.**
   `docs/superpowers/plans/2026-07-08-mapping-track-c-tactical-mechanics.md` shipped
   two Task-2 defects (a mask that mislabels floor `(2,2)` as a wall; a `cells_reachable`
   impl that can't satisfy its own difficult-terrain test). Both were fixed in 165-1's
   code but **the plan doc still carries them.** Every sibling copies from this plan —
   hand-evaluate every mask/coordinate assertion and every impl against its own test
   before committing a RED/GREEN. A quick `python3 -c` reimplementing `parse_mask`/`is_floor`
   catches mask bugs in seconds.
2. **`ReachResult.cost` is a SUPERSET of `ReachResult.reachable`** — it includes the
   origin (cost 0) and the immediate over-budget boundary cell. Consumers must read
   `.reachable` for "cells you can stop on," never `cost.keys()`. (Matters most for
   165-4's UI/protocol math display.)
3. **C1's production-reachability WIRING TEST lives in 165-3** (plan Global Constraint
   line 18) — 165-3's ACs MUST include an integration test reaching `game.tactical`
   from a real `dispatch_dice_throw` path, not just more unit tests on `adjudication.py`.
   165-1 has zero production consumers by design; do not treat that as debt until 165-3.
4. **Coverage gaps left by the plan's transcribed tests** (verified-correct code, no
   regression guard): `movement_cost(difficult=…)`, `line_of_sight` endpoint-exclusion
   (`ray[1:-1]`), `aoe_burst(require_los=False)`. Close these when a sibling next extends
   or wires the library (165-2 extends facts; 165-3 wires enforcement).
5. **`adjudicate_reach(mode: str)` is unvalidated** — an unknown mode silently yields the
   "range" noun. Tighten to `Literal["melee","ranged"]` (or validate) when 165-3 consumes
   it in the reach/range gate.

Per-story carryover is also seeded in each `context-story-165-N.md`. If a future
`pf context create` regenerates one of those, this epic section is the durable record.

### Standing epic constraints (from the plan Global Constraints)
- No-grid = no-grid rules: enforcement fires only when both actors carry
  `per_actor_state['cell']`; otherwise resolve as today and emit
  `tactical.enforcement.skipped` (reason `no_grid`) — a scope boundary, not a silent fallback.
- Enforcement lives in the ruleset BINDING (WN / Fate), never in native combat code
  (ADR-117/143). Dogfight (ADR-153) is untouched. Additive protocol only.
- New OTEL spans must call `publish_event` (reach `turn_telemetry`, not just Jaeger) and
  need a `SPAN_ROUTES`/`FLAT_ONLY_SPANS` entry or `test_routing_completeness.py` fails.
- Workflow: `superpowers` tag → run as **spdd** (settled Keith decision, `sm-decisions.md`).

---
_Generated by `pf context create epic 165` from the sprint YAML._
