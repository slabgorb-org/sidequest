# Story 160-1 Context

## Title
Animal companion persona templates — cat/owl/raven/toad/goat CompanionDef YAMLs bound to beneath_sunden, reusing the epic-159 run loop, with a wiring test asserting each parses as a valid role:pet CompanionDef

## Metadata
- **Story ID:** 160-1
- **Type:** story
- **Points:** 2
- **Priority:** p2
- **Workflow:** tdd
- **Repo:** understudy
- **Epic:** Companion playtest follow-up: animal companions

## Problem
_No description in the sprint YAML — see the story title above and the epic context for scope._

## Technical Approach

**Scope:** This is an understudy-only change. Blast radius: `sidequest-understudy` ONLY. Zero server, UI, content, or daemon changes.

**Deliverable:** Five `CompanionDef` YAML files under `sidequest-understudy/src/companion/examples/`:
- `cat_sunden.yaml` — vain/self-important (Donut lineage)
- `owl_sunden.yaml` — pedantic know-it-all, fussy about correctness
- `raven_sunden.yaml` — ominous/cryptic, hoards secrets, gallows humor
- `toad_sunden.yaml` — phlegmatic/unbothered/blunt/food-motivated
- `goat_sunden.yaml` — contrarian/stubborn/headbutts problems/eats everything

**Schema Reference:** Copy the shape of existing `sidequest-understudy/src/companion/examples/donut_sunden.yaml`. The CompanionDef pydantic model is in `sidequest-understudy/src/companion/manifest.py`.

**Each File:**
- `role: pet`
- Bound to `genre: caverns_and_claudes` / `world: beneath_sunden`
- `model: claude_p/sonnet` (NOT anthropic/* — breaks on dev box)
- `game_slug` left as `REPLACE-WITH-HUMANS-ROOM-SLUG` placeholder (Donut lineage)
- Per-species voice register via tuned `axes` in the YAML

**Run Loop:** Reuses epic-159 companion run loop verbatim — NO new run-loop code.

**Mandatory Wiring Test:** Under `sidequest-understudy/tests/companion/`, load all `examples/*_sunden.yaml`, assert each parses as valid `CompanionDef` with `role == pet`, and that `species` and `voice` are non-empty. This is the integration proof the files are loadable defs.

**Design Spec:** See `docs/superpowers/specs/2026-06-27-animal-companions-design.md` (§3). Companion lineage: `docs/superpowers/specs/2026-06-26-companion-chargen-driver-decision.md`.

## Scope
- In scope: the behavior described by the story title.
- Out of scope: unrelated changes.

## Acceptance Criteria
_No acceptance criteria recorded in the sprint YAML — TEA to define during the RED phase._

---
_Generated by `pf context create story 160-1` from the sprint YAML._
