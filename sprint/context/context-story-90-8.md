# Story 90-8 Context

## Title
Route hydration lie-detectors (wwn.magic_hydrated + magic.state_hydrated) into the typed GM-panel Subsystems feed — 90-7 fast-follow #1 (design-bearing). Today scene_harness._hydrate_character emits both via watcher_hub.publish_event, so they reach only the dashboard RAW console: neither event_type is in the UI WatcherEventType union (sidequest-ui/src/types/watcher.ts) NOR in SPAN_ROUTES (sidequest-server/sidequest/telemetry/spans/), so they never appear in the typed Subsystems tab the way wwn.spell.cast / wwn.effort.commit do. DECISION NEEDED: re-emit as a routed OTEL span (Span.open + SpanRoute event_type=state_transition, component=magic — mirrors wwn.py helpers, no UI change) vs add the literal event_types to the UI union. Routed-span path changes 90-7's emit mechanism + its test contract (tests monkeypatch _hub.publish_event). Apply the chosen pattern to BOTH siblings. Ref: 90-7 Reviewer delivery finding; scene_harness.py:371-390.

## Metadata
- **Story ID:** 90-8
- **Type:** chore
- **Points:** 3
- **Priority:** p3
- **Workflow:** tdd
- **Repo:** server
- **Epic:** Ruleset-Module Worlds — Live Combat & Magic Verification Enablement

## Problem
After 90-7 (WWN scene-harness hydrator), two lie-detector OTEL events are emitted (wwn.magic_hydrated and magic.state_hydrated) but they don't appear in the typed GM-panel Subsystems feed. They reach only the dashboard RAW console because:

1. `wwn.magic_hydrated` and `magic.state_hydrated` event_types are not in the UI WatcherEventType union (sidequest-ui/src/types/watcher.ts)
2. They are not in SPAN_ROUTES (sidequest-server/sidequest/telemetry/spans/)
3. As a result, they never appear in the Subsystems tab like other subsystem events (wwn.spell.cast, wwn.effort.commit, etc.)

## Technical Approach
Two viable paths exist (DECISION NEEDED):

**Path 1: Re-emit as routed OTEL spans**
- Use Span.open + SpanRoute with event_type=state_transition, component=magic
- Mirrors the pattern used by wwn.py helpers
- No UI changes required
- Changes 90-7's emit mechanism and test contract (tests monkeypatch _hub.publish_event)

**Path 2: Add literal event_types to the UI union**
- Add wwn.magic_hydrated and magic.state_hydrated to the WatcherEventType union
- Add them to SPAN_ROUTES
- Simpler, more direct routing

**Requirement:** Apply the chosen pattern to BOTH sibling events consistently.

## Scope
- In scope: routing both wwn.magic_hydrated and magic.state_hydrated into the typed GM-panel feed
- In scope: decision between routed OTEL vs UI union approach
- Out of scope: other lie-detector routing improvements

## Acceptance Criteria
_No acceptance criteria recorded in the sprint YAML — TEA to define during the RED phase._

## Reference
- 90-7 Reviewer delivery finding
- scene_harness.py:371-390 (current emit location)
- ADR-031 (Game Watcher — Semantic Telemetry for AI Agent Observability)
- ADR-132 (WatcherHub Infrastructure)

---
_Generated by `pf context create story 90-8` from the sprint YAML._
